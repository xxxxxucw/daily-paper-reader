Title: A Design-Based Minimax Theory for Network Experiments

URL Source: https://arxiv.org/pdf/2608.04909v1

Published Time: Thu, 06 Aug 2026 01:04:44 GMT

Number of Pages: 98

Markdown Content:
# A Design-Based Minimax Theory for Network Experiments 

Vardis Kandiros 1, Christopher Harshaw 1, and Fredrik Sävje 21Columbia University 

> 2

Uppsala University 

August 6, 2026 

Abstract 

Network experiments are used throughout the social and medical sciences to investigate causal effects under the presence of interference. While a large body of work has developed improved statistical procedures, the fundamental limits of statistical estimation in these settings is less well understood. In this paper, we develop and investigate a design-based theory of minimax risk for network experiments under an arbitrary neighborhood interference model. Our notion of minimax risk describes the optimal precision among all statistical procedures for investigating a particular causal effect on the observed interference network. We show that the minimax risk is a function of the corresponding conflict graph, which captures inherent unobservability of estimand-relevant potential outcomes given the observed interference network. Our main contribution is a series of upper and lower bounds on the minimax rate in terms of local and global connectivity properties of the conflict graph. To illustrate their utility, we apply these general results to obtain minimax analyses for two commonly studied effects: the direct treatment effect and global average treatment effect. 

We thank James M. Robins for insightful discussions which helped to shape this work. The authors gratefully acknowledge support from the following agencies: NSF Grant MMS-2316335; Jan Wallander, Tom Hedelius & Tore Browaldh foundations Grant P25-0067; Swedish Research Council Grant 2025-04794. Part of this research was conducted at the Isaac Newton Institute for Mathematical Sciences. arXiv:2608.04909v1 [math.ST] 5 Aug 2026 Table of Contents 

1 Introduction 1

1.1 Illustration of Main Results . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21.2 Related Work . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 4

2 Minimax Theory for Network Experiments 5

2.1 Preliminaries: Model and Estimands . . . . . . . . . . . . . . . . . . . . . . . . . . 52.2 Minimax Risk for Estimating Causal Effects . . . . . . . . . . . . . . . . . . . . . . 72.3 Conflict Graph . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 9

3 Lower Bounds on the Minimax Risk 11 

3.1 Le Cam’s Method for Design-Based Inference . . . . . . . . . . . . . . . . . . . . . 11 3.2 Lower Bound using Global Information . . . . . . . . . . . . . . . . . . . . . . . . . 12 3.3 Lower Bound using Local Information . . . . . . . . . . . . . . . . . . . . . . . . . 14 

4 Upper Bounds on the Minimax Risk 17 

4.1 Unbiased Statistical Procedures . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 17 4.2 Bias-Variance Tradeoff . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 19 

5 Tightness of Minimax Analysis 20 

5.1 Direct Treatment Effect . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21 5.2 Global Average Treatment Effect . . . . . . . . . . . . . . . . . . . . . . . . . . . . 22 

6 Conjectured Computational Hardness 22 7 Conclusion 24 Appendix 28 

i1 Introduction 

Network interference experiments are used in the social and medical sciences to investigate spillover effects and other causal interactions between units. An important problem for the execution of such an experiment is the choice of the experimental design and the causal effect estimator, which we collectively refer to as the statistical procedure . The choice is crucial because it determines how much can be learned from the experiment. An ill-advised choice can introduce both bias and imprecision, often rendering the estimator inconsistent unless the underlying network is very sparse. A growing body of literature has sought to shine light on this question. The typical line of attack is the development of a new statistical procedure for network experiments that can be shown to provide improved rates of convergence under suitable circumstances and restrictions on the underlying interference network. Examples include experimental designs based on cluster randomization (Ugander et al., 2013; Ugander and Yin, 2023; Viviano et al., 2026), independent sets (Karwa and Airoldi, 2018; Jagadeesan, Pillai, and Volfovsky, 2020; Fatemi and Zheleva, 2020), and conflict-resolution schemes (Kandiros et al., 2024). While significant progress has been made with this approach, the literature is stifled by a lack of a theoretical benchmark: what are the statistically optimal rates of estimation for network experiments? The question of rate optimality is not only of theoretical interest, but it also has practical implications. Without an understanding of the fundamental statistical limits of what can be learned from these experiments, both statisticians and practitioners remain unaware of how near or far existing statistical procedures are from being optimal. If it is found that existing procedures are far from the fundamental optimal limit, we should be incentivized to continue working on new procedures, possibly trying novel approaches. In this paper, we develop and investigate a design-based theory of minimax optimality for network experiments. The proposed notion of minimax optimality describes the best precision among all statistical procedures for investigating a particular causal effect τ on an observed interference network G, providing a fundamental statistical limit of the empirical problem at hand. Our notion of minimax optimality has two key features. First, optimality is considered with respect to a single observed interference network G, considering all potential outcomes obeying the restrictions imposed by the network. Second, optimality is with respect to all possible choices of experimental design and estimator without any restrictions. We believe that these two features reflect the challenges faced by practitioners, who are often able to select any statistical procedure after measuring the interference network. Moreover, this fills a gap in the design-based literature; to the best of our knowledge, minimax analyses which account for both experimental design and estimator have not previously been investigated in design-based causal inference. The main contribution of the paper is an analysis of the minimax rate of causal effect estimation under the assumption of arbitrary neighborhood interference (ANI), where the outcome of a unit depends arbitrarily on the treatment of its neighbors. Central to our analysis is the notion of an exposure-level conflict graph, which is closely related to the unit-level conflict graph described by Kandiros et al. (2024). The vertices in the conflict graph H are all potential outcomes relevant for the estimand τ at hand, and an edge is drawn between two vertices if the corresponding potential outcomes cannot be observed simultaneously given the interference network G. We show that the minimax risk of the network experiment problem is in fact a function of the conflict graph H, albeit a complicated one. Thus, the central question becomes: what aspects of the conflict graph determine the minimax rate? Our primary analysis appears in a series of lower and upper bounds on the minimax risk presented 1throughout the paper. We collect these results here, presented in a slightly simplified version. 

Main Result (Informal) . Let G be an interference network, τ be a causal estimand, and H be the corresponding conflict graph. The minimax risk for estimating τ on network G is bounded as 

max  

> (

1

|I (H)| ,

> √

d∗(H)

n

> )

≲ inf  

> D,ˆτ

sup 

> y

E

> [(

ˆτ − τ )2]

≲ min S Q(S, H)

n ,

where the infimum is taken over all possible designs D and estimators ˆτ , and the supremum is taken over all potential outcomes y having bounded second moments and satisfying the network interference assumption. The expectation is taken with respect to the experimental design D.

The lower bounds on minimax risk depend on two quantities describing the conflict graph: its largest independent set I(H) and its critical degree d∗(H), which represent global and local connectivity information of the graph. More precisely, I(H) is the largest subset of vertices for which no pair is adjacent in H and d∗(H) is the largest integer k such that at least √k vertices have degree at least k. The upper bound is the solution to a regularized induced subgraph problem, where for any subset S of vertices, the objective function Q(S, H) = λ(H \ S) + |S| has two terms: the largest eigenvalue of the induced subgraph obtained by removing S and the size of S. This upper bound reflects a bias-variance trade-off and improves over the rate attained by Kandiros et al. (2024). The full analysis considers general moment restrictions, and it uncovers an interplay between the strength of the moment restrictions and degree heterogeneity for the minimax rates, where stronger moment restrictions can accommodate more heterogeneity. The primary technical innovation facilitating this work is the development of techniques to derive statistical lower bounds in a design-based setting. Our approach begins by adopting Le Cam’s method within the design-based framework, where a mixture distribution over potential outcome functions is constructed to certify the statistical lower bound. In the setting of network experiments, several novel challenges arise necessitating the construction of mixture distributions which are both network-specific and design-adaptive. The lower bound techniques we develop may be of independent interest, and may be useful in other high-dimensional network problems. 

1.1 Illustration of Main Results 

To illustrate the utility of our general minimax rate analyses, we apply the main result to two causal estimands previously considered in the literature. In what follows, we denote the minimax rate as R(G, τ ) for a network G and causal effect τ . For simplicity, we here focus on second moment restrictions and present results without considering the aforementioned bias-variance trade-off, effectively setting S = ∅. All formal definitions are described in Section 2 and proofs of the corollaries in this subsection appear in Section D of the appendix. We first consider estimation of the Direct Treatment Effect (DTE). The DTE estimand is the effect of treating a single experimental subject when all other subjects are assigned control treatment, averaged over all subjects. The estimand isolates the effect that treatment has directly on the treated unit, removing any potential spillover and interference effects. The following corollary uses the main results above to establish bounds on the minimax optimal rate of estimating DTE in terms of the underlying interference graph G.2Corollary 1.1. The minimax rate of estimating the Direct Treatment Effect τDTE on a network G

is bounded as 

max  

> (

1

|I (G)| ,

> √

davg (G)

n

> )

≲ R(G, τ DTE ) ≲ λ(G)

n ,

where I(G) is the maximum independent set in G, davg (G) is its average degree, and λ(G) is the largest eigenvalue of its adjacency matrix. 

The bounds are similar to those that appeared in the general main results, but with the underlying interference network G in place of the conflict graph H. This simplification is possible because H is tightly coupled with G for the DTE. The minimax rate is precisely characterized when the upper and lower bounds match; however, there are graphs G for which these bounds do not match. In Section 5, we show that both bounds are tight even within the restricted class of d-regular graphs, in the sense that there exist graphs such that the true minimax rate coincides with the lower bound, and other graphs such that it coincides with the upper bound. In addition, we show that these bounds characterize the minimax risk for random d-regular graphs, up to logarithmic factors. We next consider estimation of the Global Average Treatment Effect (GATE). The GATE estimand is the effect of treating all subjects versus treating no one. The estimand captures both the direct effect of treatment and spillover effects. The following corollary uses the main results above to establish bounds for the GATE in terms of the two-hop network G2, which is obtained by connecting all subjects that are within distance 2 in the original interference network G.

Corollary 1.2. The minimax rate of estimating the Global Average Treatment Effect τGATE on a network G is bounded as √

davg (G2)

n ≲ R(G, τ GATE ) ≲ λ(G2)

n ,

where G2 is the two-hop interference graph, davg (G2) is the average degree of the two-hop graph, and 

λ(G2) is the largest eigenvalue of its adjacency matrix. 

Because the two-hop network G2 is typically much more densely connected than the original network G, the minimax analyses of Corollary 1.2 aligns with the commonly held belief that the DTE is easier to estimate than the GATE. In Section 5, we formalize this intuition by showing that the minimax rate of estimating the DTE is at most that of estimating the GATE for random 

d-regular graphs. The fact that the derived upper and lower bounds on the minimax rates for network experiments do not always match, in the sense that they are the same up to constant terms, means that they do not fully characterize the fundamental limit of the statistical problem. Although the minimax risk is a function of the conflict graph H, this exact relationship depends on the structure of maximal independent sets of H, which are likely too complex to be succinctly described. In Section 6, we formalize this possibility through a set of well-motivated conjectures about the computational hardness of deriving sharper analyses of the minimax risk and discuss their practical consequences. Nevertheless, we believe the bounds in this paper provide an important step towards the understanding of network experiments as a statistical problem. 31.2 Related Work 

We consider experimental design and estimation in network experiments under arbitrary neighborhood interference , allowing the outcome of a unit to depend on the treatments of its neighbors in a known graph. This is an instance of the design-based exposure mapping framework developed by Aronow and Samii (2017), which generalizes the ideas developed by Sobel (2006) and Hudgens and Halloran (2008) to allow for outcomes to depend on arbitrary summaries of the treatment vector. The concept of effective treatments described by Manski (2013) is an alternative formulation. Experimental design under arbitrary neighborhood interference has been considered previously. Ugander et al. (2013) and Ugander and Yin (2023) consider estimation of the Global Average Treatment Effect (GATE) under uniformly bounded outcomes and describe designs that split the graph in clusters used for the assignment of treatment. The work closest to the current paper is Kandiros et al. (2024), who consider arbitrary contrastive effects under bounded second outcome moments and describe an experimental design based on a conflict resolution scheme. Compared to this previous work, our contribution is to derive lower bounds on the minimax rate, which has previously been absent from the literature, and improved upper bounds. We also formalize the study of the minimax rate in the setting of network experiments. Several authors have considered network experiments under restrictions on the potential outcomes beyond those imposed by the neighborhood interference model itself. For example, Toulis and Kao (2013) describe two designs for assigning treatments in graphs under a linear outcome model. A long line of work has explored other types of additional outcome assumptions in network experiments (Sussman and Airoldi, 2017; Fatemi and Zheleva, 2020; Jagadeesan, Pillai, and Volfovsky, 2020; Cai, Zhang, and Airoldi, 2024; Viviano et al., 2026). The purpose of these assumptions is to make potential outcomes that are irrelevant under the neighborhood interference model alone informative of the causal effect of interest, simplifying the experimental design and estimation problems. Our concept of minimax risk and optimality differs from previously considered concepts in the experimental design literature. Previously, analyses have typically fixed the estimator and considered a restricted class of designs (Wu, 1981; Baird et al., 2018; Kallus, 2018; Leung, 2022; Bojinov, Simchi-Levi, and Zhao, 2023; Candogan, Chen, and Niazadeh, 2024). Some analyses instead restrict the design and consider a larger class of estimators (see, e.g., Cortez-Rodriguez, Eichhorn, and Yu, 2023). This type of analysis demonstrates optimality within the restricted class of designs or estimators, but not optimality in a universal sense. We consider the choice of estimator and design simultaneously without restrictions, making the analysis informative of universal optimality. Concepts of optimality not building on minimax performance have also been considered. For example, Basse and Airoldi (2018) consider designs that are optimal with respect to a generative working model, and Karwa and Airoldi (2023) consider admissibility of linear exposure effect estimators under interference. Within the superpopulation framework for causal inference, statistical optimality and minimax analysis have been studied extensively. The data generating process is beyond the researcher’s control here, so optimality is considered only over possible estimators. The predominate focus in this literature is on local minimax risk (van der Vaart, 2000), which considers the best possible asymptotic variance that can be achieved by an asymptotically unbiased estimator. In semiparametric efficiency theory, this is done using the efficient influence curve (Bickel et al., 1993). The approach is local because it considers perturbations of the parameter of interest locally in the parameter space. We instead use a global notion of minimax risk, considering the worst-case performance of a statistical procedure for all potential outcome functions in some class of functions. The global notion is suitable 4to capture the optimal rate of estimating an effect, without consideration for the exact constants involved. A global notion of minimax risk has been considered also in the superpopulation setting to study optimal rates (see, e.g., Robins et al., 2008; Rotnitzky, Smucler, and Robins, 2021; Foster and Syrgkanis, 2023; Hu and Wager, 2023; Kennedy et al., 2024). Within the survey sampling literature, several minimax optimality results for finite populations have been developed (Rinott, 2009). For example, Aggarwal (1959) and Bickel and Lehmann (1981) established that the sample mean is minimax optimal under the constraint that the variance of the outcomes is bounded, and Hodges and Lehmann (1982) showed that a slightly biased version of the sample mean is minimax optimal under the constraint that all outcomes are uniformly bounded. More recently, Aronow and Lopatto (2026) derived lower bounds in a setting with unequal inclusion probabilities under uniformly bounded outcomes. While the underlying structure of the statistical problems are similar, the techniques for establishing minimax optimality in survey sampling are not directly applicable to causal inference problems. The survey sampling techniques rely on classical decision theory arguments based on symmetry of the problem with respect to the unit, allowing the proof to be divided into two parts, establishing the optimal design separately from the optimal estimator (Blackwell and Girshick, 1979; Gabler, 2012). No such symmetry exists in the causal inference setting, and we must consider the choice of design and estimator simultaneously. For this reason, there is no obvious way to apply these approaches to the problems we consider in this paper. The techniques we describe here are developed from first principles and unrelated to those used in the survey sampling literature. 

# 2 Minimax Theory for Network Experiments 

2.1 Preliminaries: Model and Estimands 

We consider an experimental study with n subjects indexed by integers {1, 2, . . . , n } ≜ [n]. The experimenter selects a random intervention z from a set of interventions Ω and the randomized selection mechanism is referred to as the experimental design. Formally, the interventions are a measurable space (Ω , F) and the experimental design is a probability measure D : F → [0 , 1]. We focus on the setting where intervention is a binary treatment to each experimental subject, i.e. 

z = ( z1, z 2, . . . z n) with zi ∈ { 0, 1} so that Ω = {0, 1}n.Each subject i ∈ [n] has an associated potential outcome function yi : Ω → R which describes its responses under each intervention. For notational convenience, we collect these n functions into an entire potential outcome function y : Ω → Rn defined as y(z) = ( y1(z), . . . , y n(z)). In the randomized experiment, the experimenter draws a random intervention Z according to the design 

D and observes the n outcomes y1(Z), . . . , y n(Z). In order to facilitate meaningful inference of causal effects, the experimenter must place further restrictions on the potential outcome functions. A common choice is the arbitrary neighborhood interference (ANI) model, which depends on an observed interference network. We presume that the experimenter has access to an undirected network G = ( V, E ) whose vertices V are experimental subjects with edge set E. For a subject i ∈ [n], we define its neighborhood in the network to be the other subjects to which it is connected: N (i) = {j ∈ [n] : ( i, j ) ∈ E}. For notational convenience, we define the extended neighborhood as ˜N (i) = N (i) ∪ { i}, which is just a unit’s neighbors and itself. The ANI model stipulates that a subject’s potential outcome function can depend arbitrarily on treatments assigned to subjects in its extended neighborhood, but is not affected by treatment assigned to other subjects. 5i iei, 1 ei, 0     

> (a) GATE Exposures
> iiei, 1ei, 0
> (b) DTE Exposures
> Figure 1: An illustration of the relevant exposures that define the global and direct effects. Red and blue correspond to treated and untreated units.

Definition 1. The arbitrary neighborhood interference model M(G) contains all functions y : Ω →

Rn satisfying the following condition: yi(z) = yi(z′) for all subjects i ∈ [n] and interventions z, z ′ ∈ Ωwith zj = z′ 

> j

for all j ∈ ˜N (i). The ANI model is an example of an exposure mapping model (Aronow and Samii, 2017). It will sometimes be convenient to work with the underlying exposure mappings, so we define them now. For each subject i ∈ [n], its exposure set ∆i is defined to contain all subsets of its extended neighborhood, i.e. ∆ i = P( ˜N (i)). The exposure mapping hi : Ω → ∆i maps an intervention z to the subset of extended neighbors which were treated under z, i.e. hi(z) = {j ∈ ˜N (i) : zj = 1 }. In an exposure mapping model, a unit’s potential outcome function is constant across level sets of the exposure mapping, i.e. if hi(z) = hi(z′) then yi(z) = yi(z′). In this way, we can see that the ANI model is an exposure mapping model, where the exposure is the specific configuration of extended neighbors which are treated. In line with the previous literature, we overload the potential outcome notation and write yi(e) to denote the potential outcome that would be observed for unit i under exposure e ∈ ∆i.We consider the class of contrastive causal effects, which are defined as the average over subjects of the contrast between two different potential outcomes. Formally, a contrastive effect τ is a linear functional τ : M(G) → R which is specified by 2 n unit-specific exposures {(ei, 1, e i, 0)}ni=1 and defined as 

τ (y) = 1

n

> n∑
> i=1

yi(ei, 1) − yi(ei, 0) .

To avoid notational clutter, we do not parameterize τ by the choice of exposures. We review two of the most well-studied examples of contrastive effects below: • Global Average Treatment Effect: The global average treatment effect (GATE) is defined by the following two exposures: ei, 1 = ˜N (i), which corresponds to all subjects in the extended neighborhood receiving treatment, and ei, 0 = ∅, which corresponds to all subjects in the extended neighborhood receiving control. The GATE is the effect of treating everyone versus treating no one. This may be relevant for policy makers which intend to roll out treatment to everyone. • Direct Treatment Effect: The direct treatment effect (DTE) is defined by the following two exposures: ei, 1 = {i}, which corresponds to the subject i receiving treatment while all of its neighbors remain untreated, and ei, 0 = ∅, which corresponds to both the subject and its neighbors receiving control. The DTE is the effect of treatment when a subject is the only 6one to get it. This may be more relevant for an individual’s decision if they believe that the crowd will not adopt treatment. See Figure 1 for visual depictions of these two effects and their constituent exposures. Without loss of generality, we only consider estimands for which ei, 1̸ = ei, 0. To see this is without loss of generality, observe that if ei, 1 = ei, 0 then yi(ei, 1) = yi(ei, 0) so that subject i does not contribute to the causal effect and may be removed from consideration. The causal estimand depends on the entire potential outcome function which is unknown to the experimenter even after the experiment is run, a challenge often referred to as the “fundamental problem of causal inference” (Holland, 1986). For this reason, the experimenter must estimate the causal effect from the observed data. Recall that the experimenter draws a random intervention 

Z ∈ Ω according to the design D and observes the n outcomes y1(Z), . . . , y n(Z). Formally, we define an estimator as a measurable function ˆ τ : Ω × Rn → R which takes as input the realized intervention and observed outcomes and produces an estimate of the causal effect. Because the researcher can select both the design D and estimator ˆ τ , we refer to the pair ( D, ˆτ ) jointly as a statistical procedure .When analyzing the precision of a statistical procedure, it is necessary to place restrictions on the magnitude of the potential outcomes, because the precision can be arbitrarily poor if the magnitude of the potential outcomes is sufficiently large. To this end, we impose further moment restrictions on the ANI model, which may be of arbitrary order. For any q ∈ [2 , ∞], define the qth moment restricted ANI model as 

M(G, τ, q ) = 

> {

y ∈ M (G) :  

> (

1

n

> n∑
> i=1

|yi(ei,k )|q)1/q 

≤ 1 for k ∈ { 0, 1}

> }

.

The qth moment restricted model M(G, τ, q ) only contains potential outcome functions in the ANI model M(G) for which the qth moments of the estimand-relevant potential outcomes are bounded. In defining this restricted model, we have made two particular choices: we have assumed that the 

qth moments are bounded by 1, and we have placed no restrictions on the potential outcomes which are not relevant for the estimand. In Appendix A.4, we show that both of these choices are without loss of generality when studying the minimax risk. First, we show that replacing the moment restriction 1 with any fixed constant C > 0 scales the minimax risk by a multiplicative factor of 

C2, so that we may consider C = 1 without loss of generality. Second, we show that the minimax risk is unaffected if we were to place extreme restrictions on the estimand-irrelevant outcomes, so that they were effectively presumed to be known. Because the outcomes that are irrelevant for the estimand provide no information about the causal effect under an ANI model, the extent to which they are restricted is irrelevant for the minimax risk. 

2.2 Minimax Risk for Estimating Causal Effects 

In this paper, we use the mean squared error to measure the precision, or risk, of a statistical procedure. Formally, the mean squared error (MSE) of a design-estimator pair ( D, ˆτ ) for a causal effect τ under potential outcome function y is defined as MSE( D, ˆτ , τ, y ) = E Z∼D 

> [(

ˆτ (Z, y (Z)) − τ (y))2]

.

The MSE depends not only on the design D and estimator ˆ τ , but also the potential outcome function 

y under consideration. Because the underlying potential outcome function y is unknown to the 7experimenter, the MSE of a statistical procedure cannot be evaluated before the experiment is run. In order to evaluate different statistical procedures without exact knowledge of the potential outcome functions, we consider its worst case MSE within a set of potential outcome functions, in our case the moment-restricted interference model. More precisely, the maximum risk of a statistical procedure within a q-th moment restricted ANI model is defined as sup 

> y∈M (G,τ,q )

EZ∼D 

> [(

ˆτ (Z, y (Z)) − τ (y))2]

.

The maximum risk evaluates a design-estimator pair by comparing its worst-case performance among all potential outcome functions in the q-th moment restricted ANI model, y ∈ M (G, τ, q ). Unlike MSE, the maximum risk can, in principle, be evaluated before the experiment is run and can therefore be used as a criterion for selecting a design-estimator pair. We define the minimax risk as the smallest achievable maximum risk when considering all possible choices of design and estimator. Formally, the minimax risk depends on the causal effect τ ,the underlying network G, and the moment restriction q, and is defined as 

R(τ, G, q ) = inf  

> D,ˆτ

R(D, ˆτ , τ, G, q ) = inf  

> D,ˆτ

sup 

> y∈M

EZ∼D 

> [(

ˆτ (Z, y (Z)) − τ (y))2]

.

The minimax risk R(τ, G, q ) characterizes the fundamental statistical limits of the experimental problem at hand: no statistical procedure can attain a maximum risk which is smaller than R(τ, G, q ). In addition to its theoretical motivation, a clear understanding of the minimax MSE can also provide practical guidance. For example, an applied researcher might be satisfied using a statistical procedure whose performance is close to minimax optimal because no other procedure can offer significant improvements. In Section A.1 of the appendix, we show how this notion of minimax risk for ANI models and contrastive effects can be extended to the more general design-based framework of Harshaw, Sävje, and Wang (2022). The minimax risk obeys several basic monotonicity properties: it increases if the moments are made to be less restrictive or if additional edges are added to the graph. These properties follow directly from the fact that these modifications enlarge the restricted ANI model under consideration. For two graphs G, G ′ on the same set of vertices, we write G′ ⊆ G when every edge of G′ is also an edge of G. We present the monotonicity properties below: 

Proposition 2.1. The minimax risk R(G, τ, q ) obeys the following monotonicity properties: 

• Monotonicity in q: If q′ ≤ q, then R(G, τ, q ) ≤ R (G, τ, q ′).

• Monotonicity in G: If G′ ⊆ G, then R(G′, τ, q ) ≤ R (G, τ, q ).

We have defined the minimax risk in terms of deterministic estimators which use only the data from the experiment; however, it is possible to consider randomized statistical procedures which use an additional source of randomness other than the treatment assignment, e.g. a random number generator. In Section A.3 of the appendix, we show that the minimax risk among all procedures, including randomized ones, is the same as the minimax risk among the restricted class of deterministic procedures, mirroring well-known results from classical decision theory (Lehmann and Casella, 1998). Nevertheless, additional randomness may still be beneficial for constructing simpler and computationally efficient statistical procedures. Indeed, the statistical procedures introduced in 8this work are randomized, and their “de-randomizations” appear computationally challenging to implement. Finally, it may be beneficial to draw comparisons between the design-based perspective and the sampling-based perspective, particularly from the viewpoint of semi-parametric inference. In the classical superpopulation setting, one observes independent samples from some distribution 

Pθ, parametrized by a high dimensional nuisance component θ. The goal is to estimate some low dimensional functional ψ(θ) of the nuisance parameter. In design-based inference, one can view the entire potential outcome function y as the high-dimensional nuisance parameter and τ (y) as the low-dimensional quantity of interest. One major distinction between these perspectives arises in the context of experimental design: the semi-parametric theory typically takes the data generating distribution Pθ as given, and this includes the random assignment of treatment. In the design-based framework, the experimental design D is decoupled from the nuisance parameter τ (y) and needs to be chosen by the experimenter. As we will see, this selection of both design and estimator will introduce additional challenges in establishing lower bounds on the minimax risk in the design-based setting. 

2.3 Conflict Graph 

Our analysis of the minimax risk will prominently feature the conflict graph H, originally introduced by Kandiros et al. (2024) for the purpose of constructing statistical procedures. In this paper, we show that the conflict graph is in fact a central quantity when studying statistical complexity of these network experiment problems. At a high level, the conflict graph H encodes the fundamental unobservability between estimand-relevant exposures. Recall the exposures {(ei, 1, e i, 0)}ni=1 which define the contrastive estimand. We say that two exposures ei,k and ej,ℓ are in conflict if there is no intervention under which they can both be observed, i.e. there does not exist z ∈ Ω for which hi(z) = ei,k and hj (z) = ej,ℓ . This happens exactly when the two exposures ei,k and ej,ℓ specify different treatment assignments to at least one of the units in the intersection of extended neighborhoods ˜N (i) ∩ ˜N (j). The conflict graph is formally defined below: 

Definition 2. Given a network G and contrastive effect τ , the corresponding conflict graph 

H = ( VH, E H) contains the estimand-relevant exposures as vertices VH = {(ei, 1, e i, 0)}ni=1 with an edge if two exposures are in conflict. Note that the conflict graph H depends on both the underlying network G and the causal effect of interest τ . In this way, any analysis of the minimax risk in terms of the conflict graph H reflects both G and τ . However, we suppress this dependency to avoid unnecessary notational clutter. We typically consider a generic contrastive effect throughout the paper and make it clear from context when we focus on a specific effect. To better understand how the structure of the conflict graph H depends on both the underlying network G and the causal effect τ , we focus on several examples below: • No Interference: Consider the setting of no-interference, in which case we may see the underlying graph G as containing no edges. Suppose we want to estimate the average treatment effect, where ei, 1 is the exposure that subject i receives treatment and ei, 0 is the exposure that subject i receives the control. In this case, if i̸ = j then there are no conflicts between 

ei,k and ej,ℓ because the individual treatments can be selected separately. On the other hand, 912 345        

> (a) Network G
> e1,0
> e2,0e3,0
> e4,0
> e5,0
> e2,1e3,1
> e1,1
> e5,1
> e4,1
> (b) DTE Conflict Graph
> e1,0
> e2,0e3,0
> e4,0
> e5,0
> e2,1e3,1
> e1,1
> e5,1
> e4,1
> (c) GATE Conflict Graph

Figure 2: An illustration of a network G and conflict graphs for the DTE and GATE. 

there will be a conflict between ei, 1 and ei, 0 because a subject cannot be treated and untreated under any intervention. Thus, the conflict graph H is simply a matching of the 2 n exposures. • Direct Treatment Effect: Recall that ei, 1 is the exposure that subject i is treated and all of its neighbors are untreated while ei, 0 is the exposure that the entire extended neighborhood of subject i receives the control. In this case, there is no edge in the conflict graph H between all-control exposures ei, 0 and ej, 0 since we can observe both exposures by setting all units to control. On the other hand, if at least one of k, ℓ is equal to 1 (i.e. representing the direct exposure), then there is an edge between ei,k and ej,ℓ if and only if i, j are connected in G. In this way, the conflict graph H has the underlying graph G as a subgraph. • Global Average Treatment Effect: Recall that ei, 1 is the exposure that all extended neighbors are treated while ei, 0 is the exposure that no extended neighbors are treated. In this case, there are no edges in the conflict graph H between all-treatment exposures ei, 1 and 

ej, 1 for subjects i and j because they can both be realized by the intervention z = 1. Likewise, there are no edges in the conflict graph H between all-control exposures ei, 0 and ei, 0. Instead, the edges in the conflict graph arise between exposures ei,k and ej,ℓ where k̸ = ℓ and subjects i

and j have common extended neighbors, i.e. ˜N (i) ∩ ˜N (j)̸ = ∅. These exposures are in conflict because both i and j have a common neighbor who would have to be treated and not treated to observe ei,k and ej,ℓ , which is impossible. In this way, the conflict graph H is a bipartite graph that resembles the two hop graph G2 of the underlying graph G.See Figure 2 for an illustration of how for the same underlying graph G, the resulting conflict graph will typically be different for these two effects. As seen in these examples, the edges in the conflict graph encode the fundamental problem of causal inference and its extension to network interference. The conflict graph is a summary of the experimental settings, capturing exactly which pairs of exposures are impossible to realize simultaneously under any design. It abstracts away from other details of the experiment, such as the exact definition of the estimand and the interference structure that gave rise to these conflicts. The following proposition demonstrates that this abstraction retains enough information about the statistical problem to exactly characterize the minimax risk of the corresponding network experiment problem. In what follows, we let H denote the set of all labelled graphs (see Section A.5 in the appendix for a detailed proof). 

Proposition 2.2. Let G be an interference network, τ be a contrastive effect. For each moment parameter q ≥ 2, there exists a function fq : H → R≥0 such that R(G, τ, q ) = fq(H).

10 Proposition 2.2 demonstrates that the minimax risk can be expressed as a function of the conflict graph H. A key aspect of its proof is the translation of the network experiment minimax problem into an equivalent minimax problem defined solely in terms of H through its independence sets. The translation relies on the fact that every experimental design can be identified with a probability measure over independent sets in H. In fact, we show that it suffices to consider probability measures over maximal independent sets in H, corresponding to experimental designs which provide as much information as possible about potential outcomes. The structure of the maximal independent sets in H therefore determines the minimax risk R(G, τ, q ), albeit in a complex way. In particular, the function fq : H → R≥0 in Proposition 2.2 is not explicitly constructed, but rather defined through a minimax problem involving H alone. Our primary analysis in this paper therefore may be understood as providing upper and lower bounds on fq(H) in terms of more accessible graph quantities. The conflict graph presented here is slightly different than that considered in Kandiros et al. (2024). In that paper, the vertices of H were subjects in the experiment whereas in this paper the vertices of H are the estimand-relevant exposures. While the two are related, we prefer to use the definition in this paper as it contains exactly the information to characterize the minimax risk. 

# 3 Lower Bounds on the Minimax Risk 

In this section, we present statistical lower bounds on the minimax risk in terms of the conflict graph. In Section 3.1, we begin by demonstrating how Le Cam’s method can be adapted from classical minimax theory to the design-based setting. Next, we derive two types of statistical lower bounds using global and local information from the conflict graph. These are presented in Sections 3.2 and 3.3, respectively. 

3.1 Le Cam’s Method for Design-Based Inference 

Le Cam’s method is one of the most prominent approaches for obtaining statistical lower bounds on the minimax risk (Yu, 1997; Duchi, 2023; Wu, 2017). The method itself proceeds in two steps: the first step is a reduction from estimation to testing and the second step is the construction of a mixture which quantifies the limits of the testing problem. In this section, we show how the first step can be adapted to the design-based setting. Le Cam’s method requires the construction of probability measures supported on potential outcome functions in the interference model. To facilitate this, let A be the σ-algebra generated by all finite subsets of the restricted model M(G, τ, q ), i.e. the countable-cocountable σ-algebra. A 

distribution over potential outcome functions is a probability measure on ( M(G, τ, q ), A). Let H0, H 1 be two distributions over M(G, τ, q ), supported on submodels M0, M1 ⊆ M (G, τ, q )respectively. In order to quantify the difference between the evaluation of the causal effect on the support of H0 and H1, we define their effect separation as 

d(H0, H 1) = inf 

> y∈M 0,y ′∈M 1

|τ (y) − τ (y′)| .

For any distribution H over potential outcome functions and design D, let Q(D) 

> H

be the pushforward measure of the product measure D ⊗ H under the mapping ( z, y ) 7 → (z, y (z)). In other words, Q(D)

> H

is the distribution of the observed data when the potential outcome function is drawn according to 

H and the intervention is drawn according to D.11 Proposition 3.1. For any network G, causal effect τ , and moment parameter q, the minimax risk is lower bounded as 

R(G, τ, q ) ≥ inf  

> D

sup 

> H0,H 1

d2(H0, H 1)8 ·

> (

1 − dTV 

> (

Q(D) 

> H0

, Q (D)

> H1
> ))

,

where dTV is the total variation distance and the infimum is taken over all designs D.

Proposition 3.1 provides a lower bound that increases with the effect separation between H1 and 

H0, but decreases with their total variation distance. Applying the proposition requires constructing distributions H0 and H1 to trade-off these quantities, which is a challenge shared by all statistical lower bounds of this form. The aspect of Proposition 3.1 that is unique to the design-based setting is the outer infimum over experimental designs D, reflecting the fact that the choice of D is in the researcher’s control. This does not appear in the classical setting, where the data generating process is fixed. This means that when applying Proposition 3.1, we must consider a choice of distributions H0 and H1 for each possible experimental design D. If we were to pick only choice of H0 and H1, it would need to produce good lower bounds for all possible designs to be informative. Otherwise, the distributions 

H0 and H1 must be chosen with respect to the design, which we refer to as a design-adaptive 

construction. Design-adaptivity plays an important role in the development of our local-information lower bounds, whereas our global-information lower bounds are non-adaptive. Very recently, Gao et al. (2026) established statistical lower bounds for misspecification testing within the design-based framework. Their approach may be understood as carrying out the second step of Le Cam’s method. In this work, we focus on minimax optimal rates of estimation and the mixtures we construct are considerably more complex. 

3.2 Lower Bound using Global Information 

Before presenting our lower bound, we must first define several graph-theoretic notions. For the moment, let us consider a generic graph which (in a slight abuse of notation) we denote as G = ( V, E ). A subset of vertices S ⊆ V is said to be independent if no two vertices in S are adjacent in G and the independence number of G is the size of its largest independent set, denoted I(G). Given a subset of vertices T ⊆ V , the T -induced subgraph is GT = ( VT , E T ) where VT = T and the edges 

ET are those edges in the original graph G between vertices in T . We use I(G, T ) to denote the independence number of the induced subgraph GT .The statistical lower bound in this section depends on the induced independence numbers within the conflict graph. These quantities represent global information about the conflict graph in the sense that they depend on its entire topology and cannot be determined from local information such as degree sequences. To gain intuition for these results, observe that each intervention z ∈ Ωreveals a set of exposures S ⊆ VH which, by the definition of conflicts, form an independent set in 

H. Thus, if all independent sets in H are small then each intervention can only provide information on a similarly small number of exposures. The following theorem formalizes this intuition into a precise statistical lower bound: 

Theorem 3.2. Let G be an interference network, τ be a contrastive effect. If n ≥ 3, then for all moment parameters q ≥ 2

R(G, τ, q ) ≥ 1.05 · 10 −5 · sup   

> T⊆VH,|T|≥ 5

1

I(H, T ) · 

> (

|T |

n 

> )2−2
> q

.

12 For a fixed subset of exposures T in the conflict graph H, the lower bound in Theorem 3.2 has two parts: the induced independence number I(H, T ) and the ratio of ( T /n )2−2/q . Roughly speaking, the former arises as an effective sample size while the latter reflects the amount of mass that can be “hidden” depending on the moment q that is being restricted. The intuition behind these quantities is that if the induced independence number is large, we can construct a mixture that hides mass among more potential outcomes. The overall lower bound is obtained by taking the supremum over sufficiently large subsets of exposures T . We have not made an attempt to optimize the constant appearing in Theorem 3.2. In typical applications of Theorem 3.2, it suffices to consider T which are of order Ω( n). In general, it is always possible to set T = VH in which case the lower bound becomes 1 /I(H). In the specific case of the direct effect, the conflict graph H contains G as an induced subgraph of size n,so that by setting T = {ei, 1 : i ∈ [n]}, we obtain that I(H, T ) = I(G). On the other hand, there are cases where the subset T that maximizes the lower bound contains a vanishingly small fraction of vertices in H. For example, consider a conflict graph H that is the union of a clique R of size 

kn and 2 n − kn isolated nodes, where kn = o(n). In this case, the optimal choice is to set T = R

in which case I(H, T ) = 1 and the resulting lower bound is Ω( kn/n ) for q = 2 and Ω(( kn/n )2) for 

q = ∞, both of which can be shown to be tight up to constant factors. In general, the lower bound increases as q decreases. As stated above, the main idea for proving Theorem 3.2 comes from the relationship between independent sets in H and sets of exposures which can be simultaneously observed. In the remainder of this section, we provide a proof sketch together with the necessary ingredients. For a simplified proof sketch, we consider the case where T = VH which yields the weaker Ω(1 /|I (H)|) bound. This simplification highlights the key features of the proof while avoiding some technical complications. For the purpose of notational simplicity (and without loss of generality), we consider the casual estimand τ = (1 /n ) ∑ni=1 {yi(ei, 1) + yi(ei, 0)} where the contrast is now a sum. To apply Le Cam’s method, we will construct distributions H1 and H0 over potential outcome functions in the restricted ANI model. To this end, we consider a class of distributions H(p) on the potential outcome functions which are defined by a single parameter p ∈ (0 , 1). Under H(p), we select a subset S of the 2 n exposures uniformly at random among all subsets of size ⌈2pn ⌉ and assign outcome 1 to exposures in S and outcome 0 to exposures not in S. For a distribution H

over outcome functions and intervention z ∈ Ω, let QH (·| z) be the conditional distribution of the observed outcomes conditioned on the intervention being equal to z. Note that this distribution does not depend on the design D at all, since we are fixing the assignment z. For any z ∈ { 0, 1}n,let Rz be the subset of the 2 n exposures that we get to observe when the treatment assignment is z.We will consider mixtures of the form H0 = H(1 /2) and H1 = H(1 /2 + δ) where the mixture parameter δ > 0 controls the separation between the two distributions. We provide an auxiliary lemma about distinguishing H0 and H1 from the observed outcomes. 

Lemma 3.3. Suppose that n ≥ 3 and fix an intervention z ∈ Ω. If the mixture parameter satisfies 

δ ≤ 0.026 /√|Rz |, then the total variation distance between the conditional observed distributions is bounded as dTV (Q(z) 

> H0

, Q (z) 

> H1

) ≤ 78 .

Lemma 3.3 quantifies how large the mixture parameter δ can be so that the observed outcomes under the two mixtures H0 and H1, conditioned on the intervention z, are hard to distinguish. Together with an application of Le Cam’s method as stated in Proposition 3.1, we are ready to provide a proof sketch of Theorem 3.2. 13 Proof Sketch of Theorem 3.2. Let H0 = H(1 /2) and H1 = H(1 /2 + δ) where δ = 0 .026 /√|I (H)|.We will show that these two distributions satisfy the conditions of Proposition 3.1, which will then imply the desired lower bound. Note that the choice of H0, H 1 is independent of the design D.First, by definition of H0, H 1 we have d(H0, H 1) = 2 δ = 0 .052 /√|I (H)|. Second, by definition of the observed outcome distributions and the law of total probability, we have that for any design D

dTV (Q(D) 

> H0

, Q (D) 

> H1

) = ∑

> z∈{ 0,1}n

D(z) · dTV (QH0 (·| z), Q H1 (·| z)) . (1) Therefore, if we can show that QH0 (·| z), Q H1 (·| z) are close for any value of z, then the same would be true for Q(D) 

> H0

, Q (D) 

> H1

as well, for any design D. For a fixed z, the set Rz is an independent set in H. Indeed, if two exposures in Rz were connected by an edge in H, then by definition of the conflict graph we would not be able to observe both with the same assignment z. Thus, we have that |Rz | ≤ |I (H)|, which implies that 

δ = 0.026 

√|I (H)| ≤ 0.026 

√|Rz | .

Thus, by Lemma 3.3, we have that dTV (QH0 (·| z), Q H1 (·| z)) ≤ 7/8 for all z, which implies that 

dTV (Q(D) 

> H0

, Q (D) 

> H1

) ≤ 7/8 for all designs D by (1). Therefore, using Proposition 3.1, we can lower bound the minimax risk as 

R(G, τ, q, C ) ≥ d(H0, H 1)2

8 · inf 

> D

(

1 − dTV 

(Q(D) 

> H0

, Q (D)

> H1

))

≥ 0.052 2

8 · |I (H)| · (1 − 7/8) = Ω 

( 1

|I (H)|

)

.

3.3 Lower Bound using Local Information 

In this section, we provide a statistical lower bound on the minimax risk in terms of the degrees of the conflict graph H. In contrast to global properties like induced independent sets, the degrees of the H describe local connectivity information. The lower bound presented in this section is complimentary to the one in the previous section in the sense that there are situations where one or the other is more informative. For a vertex ei,k ∈ VH in the conflict graph H, its degree di,k is the number of vertices to which it is connected. For a subset of vertices T , we denote the minimum degree in T as 

dmin (H, T ) = min ei,k ∈T di,k . Finally, we define the generalized harmonic mean degree of vertices in 

T as dharm (H, T, r ) = |T |/{∑  

> ei,k ∈T

1/d ri,k }1/r . Note that for r = 1, the generalized harmonic mean reduces to the usual harmonic mean. The minimax rate will increase as the conflict graph becomes more densely connected and the degrees of the vertices in H provide local information regarding its connectivity. Thus, it seems natural that the the minimax rate can be lower bounded in terms of the degrees. This can be formalized thru the following theorem, whose proof appears in Section B.3 of the appendix. 

Theorem 3.4. Let G be an interference network, τ be a contrastive effect. If n ≥ 5 then for all moment parameters q ≥ 2,

R(G, τ, q ) ≥ 6.6 · 10 −7

n2− 2

> q

sup   

> T⊆VH,|T|≥ 5

min 

(

dmin (H, T ),

√

dharm (H, T, 2 − 2/q ), |T |

)2− 2

> q

.

14 For a fixed subset of vertices T in the conflict graph, the lower bound in Theorem 3.4 involves the minimum of three terms: the minimum degree in T , the generalized harmonic mean degree in T ,and the size of T . In order for the lower bound to be large, there must exist a subset T for which all three of these quantities are large. In particular, the second term indicates that we must choose T

such that its (generalized harmonic) average degree is large. At this point, the first and third terms are in tension: we need to select a large number of vertices T , but we do not want the minimum degree to be small. This means the supremum will typically not be achieved by the entire subset of vertices T = VH because the resulting lower bound will be (at most) the minimum degree in H,which could be quite small, i.e. constant order. There is less tension between these three terms when many exposures have approximately the same degree in the conflict graph. The intuition that the “bulk” of the degrees in the conflict graph governs the minimax risk can be formalized using the critical degree, which is a quantile of the degree distribution. More precisely, we define the degree distribution of H as FH : R → [0 , 1] where 

FH(t) = 12n 

> ∑
> ei,k ∈VH

1[di,k ≤ t] .

From the degree distribution, we can define for any q ≥ 2 the critical degree d∗ 

> q

(H) of H as the largest integer k such that there are at least k 2q−23q−2 nodes in H with degree at least k, i.e. 

d∗ 

> q

(H) = sup 

> {

k ∈ [n] : 1 − FH(k) ≥ k 2q−23q−2

n

> }

.

The supremum can be understood as follows: keep searching for vertices with degree at least k and we stop the first time there are fewer than k 2q−23q−2 of them. In this way, the critical degree describes a way to resolve the trade-off between finding a subset of vertices that is large in size but also has a large (generalized harmonic) mean of degrees, which is a central tension in the lower bound of Theorem 3.4. Note that the critical degree is decreasing as q increases. The following corollary of Theorem 3.4 provides a lower bound based on the critical degree of H,which is the bound mentioned in the Main Result of Section 1. 

Corollary 3.5. Let G be an interference network, τ be a contrastive effect. For all moment parameters q ≥ 2, we have 

R(G, τ, q ) ≥ 6.6 · 10 −7 · (d∗ 

> q

(H)) 4( q−1) 2

> q(3 q−2)

n2− 2

> q

.

The critical degree provides a resolution of the tension between the three terms of Theorem 3.4 by subsets T consisting of nodes with the largest degree. This simplifies the lower bound, but hides the fundamental tension. To understand the implications of Corollary 3.5, it is informative to consider the lower bound for d-regular conflict graphs, for which the critical degree is d∗ 

> q

(H) = d.Under q = 2 moment restrictions, the minimax lower bound in Corollary 3.5 is of rate d1/2/n .Under more restrictive q = ∞ conditions, the lower bound becomes d4/3/n 2. As anticipated from monotonicity of the minimax risk as a function of q, we have that d4/3/n 2 ≤ d1/2/n because d ≤ n.If the average degree in the conflict graph is large, then a standard argument using Markov’s inequality implies that there should be a sufficiently large subset of vertices with sufficiently large 15 degree. Indeed, we can show that the critical degree is always lower bounded by the average degree in the conflict graph (see Proposition B.6 in Section B.3 of the appendix). We now present a proof sketch for Theorem 3.4. For simplicity, we focus on second moment restrictions ( q = 2) and make the simplifying assumption that all degrees in the conflict graph H are lower bounded by d, in which case the lower bound will be √d/n , as realized by T = [ n]. This case captures the essential obstacles for establishing the general claim, while avoiding some technical details of the argument. A notable difference of this proof compared to the global lower bound of Section 3.2 is that the mixture distributions H0 and H1 will now be selected adaptively depending on the experimental design D under consideration. 

Proof Sketch of Theorem 3.4. Assume di ≥ d for all i ∈ VH. For any exposure e = ei,k ∈ V (H), associated with a unit i in the graph G, let N (e) be the set of neighboring exposures in H and let 

Ae be the event that we observe the outcome of i for exposure e, i.e. 

Ae =

> {

z ∈ { 0, 1}n : hi(z) = e

> }

.

Let Rz denote the set of exposures whose outcomes we observe under z. By definition, we have 

|Rz | = ∑ 

> e∈V(H)

1[Ae]( z) .

Consider an arbitrary design D and let p ∈ (0 , 1/2) be a parameter that will depend on D, but will be chosen later. Our construction will be design-adaptive, depending on the size of exposure probabilities. If at least one exposure has large probability, we can exploit that exposure to hide a large causal effect in its conflicting exposures which are unlikely to be observed. If all exposure probabilities are small, then it is unlikely that we will observe many potential outcomes at all, in which case we can hide the causal effect among these outcomes. The parameter p represents what is to be considered small and large exposure probabilities and is chosen at the end to optimize the resulting lower bound. Let us now formally distinguish between these two cases. • Case 1: Suppose there exists an exposure e ∈ V (H) such that D(Ee) ≥ p. In that case, we define H0 and H1 to be supported on potential outcome functions y(0) , y (1) respectively, where 

y(1)  

> i

(ei,k ) =    

> √n
> |N(e)|

if ei,k ∈ N (e)0 otherwise , y(0)  

> i

(ei,k ) = 0 for all ei,k ∈ V (H) .

By construction, we have that 

d(H0, H 1) = 1

n  

> ∑
> e′∈N(e)
> (√

n

|N (e)| − 0

> )

=

> √

|N (e)|

n ≥

> √

dn .

Also, if event Ee occurs, then we get to observe the outcome yi(e). By definition of the conflict graph, this implies that we do not observe any of the outcomes of exposures e′ ∈ N (e). Since 

H0, H 1 only differ in the outcomes of N (e), they are impossible to distinguish under that event. Thus, 

dTV (Q(D) 

> H0

, Q (D) 

> H1

) ≤ 1 − D (Ee) ≤ 1 − p

Therefore, using Proposition 3.1 for this particular design D, we have inf  

> ˆτ

Risk( D, ˆτ ) ≥ d(H0, H 1)2

8 · (1 − dTV (Q(D) 

> H0

, Q (D) 

> H1

)) ≥ dp 

8n . (2) 16 • Case 2: Suppose for all exposures e ∈ V (H) we have that D(Ee) < p . In that case, define the event A that at most 4 pn exposures are observed in total, i.e. 

A = {z ∈ { 0, 1}n : |Rz | ≤ 4pn } ,

By Markov’s inequality, 

D(Ac) = D

> (

|Rz | ≥ 4pn 

> )

≤

E

> [

|Rz |

> ]

4pn =  

> ∑
> e∈V(H)

D(Ee)4pn ≤ p · | V (H)|

4pn = 12 .

Define H0 = H(1 /2) and H1 = H(1 /2 + δ) where δ = 0 .026 /√4pn . Then, if z ∈ A, by definition |Rz | ≤ 4pn . By Lemma 3.3, for any z ∈ A, we have 

dTV (QH0 (·| z), Q H1 (·| z)) ≤ 78 .

The law of total probability then implies that 1 − dTV (Q(D) 

> H0

, Q (D) 

> H1

) ≥ D (A) · (1 − c) ≥ 12

> (

1 − 78

> )

= 116 .

Therefore, using Proposition 3.1 for this particular design D, we get inf  

> ˆτ

Risk( D, ˆτ ) ≥ d(H0, H 1)2

8 · (1 − dTV (Q(D) 

> H0

, Q (D) 

> H1

)) ≥ 48 · 2pn · 116 = Ω  

> (

1

pn 

> )

(3) Since any design D falls in exactly one of the two cases above, the minimax risk is lower bounded by the minimum of (2) and (3). By selecting p = Θ(1 /√d) to optimize this minimum, we get 

R(G, τ, 2) = Ω( √d/n ), as desired. 

# 4 Upper Bounds on the Minimax Risk 

In this section, we present general upper bounds on the minimax risk of network experiment problems. One can view the analysis of previously proposed statistical procedures as implicitly deriving upper bounds on the minimax risk. However, most analyses in the literature are appropriate for providing sufficient conditions for consistency and may not accurately describe the minimax risk itself. Additionally, most works consider only a single moment restriction, usually q = 2 or q = ∞.In contrast, the upper bounds we present in this section are improved (i.e. smaller) and hold for arbitrary moment restrictions. 

4.1 Unbiased Statistical Procedures 

In order to upper bound the minimax risk, we must construct a statistical procedure and analyze its maximum risk. We begin by considering a class of unbiased statistical procedures, which we then refine in the next section thru a bias-variance trade-off. We summarize these upper bounds below in Theorem 4.1 and defer a discussion of the underlying statistical procedure until the end of this section. 17 Theorem 4.1. Let G be an interference network, τ be a contrastive effect. For any moment parameter q ≥ 2, the minimax risk is bounded as 

R(G, τ, q ) ≤ 8.92 · λ(H) 2 

> q

· davg (H)1− 2

> q

n ,

where λ(H) is the largest eigenvalue of the adjacency matrix of H and davg (H) is the average degree of H. Moreover, this bound on the minimax risk is achieved by an unbiased statistical procedure. 

Theorem 4.1 provides upper bounds on the minimax risk which depend on the moment restriction under consideration. Under second moment restrictions ( q = 2), we recover the λ(H)/n bound of Kandiros et al. (2024). For uniformly bounded outcomes ( q = ∞), we obtain an improved rate of davg (H)/n . For intermediate moments q ∈ (2 , ∞), the resulting upper bound interpolates between these two quantities. Like the true minimax risk, our upper bounds are decreasing as the network experiment problem becomes easier, i.e. q increases so the moment condition becomes more restrictive. The difference between davg (H) and λ(H) becomes more pronounced under degree heterogeneity. For example, the largest adjacency eigenvalue always lies between the average and maximum degrees: 

davg (H) ≤ λ(H) ≤ dmax (H). When there is low degree heterogeneity, meaning that all vertices have roughly the same degree, then all three quantities are roughly equal. On the other hand, the quantities can be of different asymptotic orders in the presence of degree heterogeneity. An illustrative example which demonstrates this separation is when H is a star graph, where one “center” node is connected to all remaining “leaf” nodes and and no leaf nodes are connected. Then we would have davg (H) ≈ 2, λ(H) ≈ √n, and dmax (H) ≈ n. Degree heterogeneity is commonly observed in social networks where degree distributions are heavy-tailed, i.e. a small number of people are more popular than the average person. In this case, the degree heterogeneity of the underlying network 

G will induce degree heterogeneity in the conflict graph H and, as a result, the quantities λ(H) and 

davg (H) will have different asymptotic orders. The interplay between moment restrictions and degree heterogeneity in the minimax rate can be understood by considering how much of the total “mass” of the potential outcomes can be concentrated on a few number of subjects. The key idea can be explained as follows: under less restrictive moment conditions, the “mass” of the potential outcomes is allowed to be more concentrated on a few number of subjects, which in turn means that the maximal risk will depend on the smallest exposure probabilities among all subjects. If the conflict graph has heterogeneous degrees, then increasing the probability of observing an exposure with many conflicts is only possible by decreasing other exposure probabilities. On the other hand, under more restrictive moment conditions, the “mass” of potential outcomes cannot be hidden in just a few subjects. This means that it can be advantageous to select an experimental design under which a few exposures are observed with small probability if doing so allows most exposures to be observed with larger probability. Underlying Theorem 4.1 is a new family of experimental designs which are extensions of the Conflict Graph Design of Kandiros et al. (2024) to accommodate general moment restrictions. At a high level, the Conflict Graph Design works by sampling independent auxiliary “desired inclusion” variables and then resolving conflicts according to a particular importance ordering. The distribution of the auxiliary variables and the importance ordering depend on the underlying moment restriction 

q. For the setting of q = 2 considered by Kandiros et al. (2024), auxiliary variables have identical distributions and the importance ordering is given by the largest eigenvector of the adjacency matrix 18 H. For the case of q = ∞, the distribution of each auxiliary variable depends on the exposure’s degree and the importance ordering is simply the degree ordering. Detailed description and analyses of this new family of experimental designs may be found in Section C.1 of the supplement. 

4.2 Bias-Variance Tradeoff 

In this section, we consider bias-variance trade-offs which can further improve the upper bounds on the minimax rate. The key idea is simple: if a few “central” or “high degree” vertices in H cause 

λ(H) or davg (H) to be large, then we can choose to ignore their outcomes when constructing our experimental design and estimator so that the resulting variance is improved at the cost of some additional bias. Similar ideas have been explored in the context of AIPW estimation in network experiments (Zöcklein, 2025). The challenge will be to identify the optimal set S of vertices to ignore. More formally, let S ⊆ VH be a subset of the vertices in the conflict graph. Let H(VH \ S) denote the subgraph of H induced by the vertices in VH \ S. Let τS be the contrastive treatment effect restricted to the exposures in VH \ S, i.e. 

τS = 1

n

> n∑
> i=1
> (

yi(ei, 1)1[ei, 1 /∈ S] − yi(ei, 0)1[ei, 0 /∈ S]

> )

.

If we were to target the estimand τS by running the Conflict Graph Design from Theorem 4.1 on the subgraph H(VH \ S), then our estimator ˆ τ is unbiased for the restricted estimand τS , i.e. E[ˆ τ ] = τS .Thus, its variance and bias for the actual estimand τ can be bounded as Var(ˆ τ ) = O 

> (

λ(H(VH \ S)) 2 

> q

· davg (H(VH \ S)) 1− 2

> q

n

> )

and 

> (

E[ˆ τ ] − τ

> )2

= O 

> ((

|S|

n  

> )2−2
> q)

.

Based on these bounds, we can define the following proxy for the mean squared error of the estimator ˆτ , as it depends on the subset S that is to be removed: 

Qq(S, H) := λ(H(VH \ S)) 2 

> q

· davg (H(VH \ S)) 1− 2 

> q

+ |S|2− 2

> q

n1− 2

> q

.

The best possible MSE using this approach would then be given by the subset S that minimizes 

Qq(S, H). This is the content of the main result of this section (for the proof, see Appendix C.2.1). 

Theorem 4.2. Let G be an interference network, τ be a contrastive effect. For any moment parameter q ≥ 2, the corresponding minimax risk is bounded as 

R(G, τ, q ) ≤ 8.92 · min S⊆VH Qq(S, H)

n .

The upper bound on the minimax rate in Theorem 4.2 is an improvement upon the one presented in Theorem 4.1. To see this, observe that selecting S = ∅ results in the same rate, and indeed the same statistical procedure. Whether the optimal set S∗ = arg min S⊂VH Qq(S, H) offers a substantially different rate depends on the heterogeneity of the conflict graph. If the vertices of the conflict graph all have degree roughly d, then the variance term is O(d) because in this case 

λ(H) ≈ davg (H) ≈ d. In this case, we would need to remove S = Ω( n) vertices before the variance 19 term would become o(d), at which point the squared bias term would already be Ω(1). Thus, the bias-variance trade-off does not improve the minimax rate when H has homogeneous degrees. On the other hand, the bias-variance trade-off offers a large improvement when the conflict graph has significant degree heterogeneity. The reason is that removing a small number of vertices will substantially decrease the variance term whereas the small number of them ensures the increase in bias is minimal. Consider the illustrative example where H is the star graph and let us focus on q = 2. In this case, the unbiased procedure of Theorem 4.1 yields a rate of 1 /√n whereas by removing the center node S = {1}, we have that λ(H(VH \ S)) = 1, so that the resulting rate from Theorem 4.2 is 1 /n . Moreover, the lower bounds establish that 1 /n is indeed the exact minimax-rate for the star graph. Finding the optimal subset S∗ that minimizes Qq(S, H) is a combinatorial optimization problem. The problem is similar to the Partial Vertex Cover (Hochbaum, 1997) which is NP-Hard, so a computationally feasible algorithm to find S∗ is unlikely to exist. However, building upon approximation algorithms for Partial Vertex Cover, we show in the following proposition that there is a polynomial-time algorithm which computes a set S that is within a logarithmic factor of optimal, i.e. Qq(S, H) ≤ log( n) · Qq(S∗, H). This yields a computationally efficient approach to the bias-variance trade-off that incurs only a logarithmic increase in the risk. This is the content of the proposition below and we refer to Section C.3 in the supplement for more details. 

Proposition 4.3. Let G be an interference network, τ be a contrastive effect. For any moment parameter q ≥ 2, there exists a design-estimator pair (D, ˆτ ) that can be implemented in polynomial time whose maximum risk is bounded as 

sup 

> y∈M (G,τ,q )

MSE( D, ˆτ , τ, y ) ≤ O 

> (

(log n)2− 2

> q
> )

· min S⊆V (H) Qq(S, H)

n .

# 5 Tightness of Minimax Analysis 

The minimax risk introduced in Section 2.2 is a function of the underlying network G and the contrastive estimand τ . While we present upper and lower bounds that use different information from the conflict graph H, the bounds themselves do not always match in the usual sense, i.e. they are not equal up to a constant. In this section, we show that the upper and lower bounds are both tight within the class of regular graphs. Moreover, these bounds are nearly matching for a uniformly random regular graph, characterizing the minimax rate in an average-case sense. To demonstrate the tightness of our upper and lower bounds, we consider the class of d-regular 

graphs, where each vertex has exactly d neighbors. We denote the class of d-regular graphs on n

nodes by Gd,n , though we simply write Gd when n is clear from context. Using triangular array asymptotics, we consider the degree parameter d to be possibly growing with n.Restricting our attention to the class of regular graphs is useful because it is parametrized by a single parameter d. This has the benefit of simplifying the presentation of our upper and lower bounds on the minimax rate. On the other hand, the class is rich enough to contain a variety of different graphs. We further focus our attention on the bounds obtained for second moment restrictions, q = 2. Our goal is two fold: we aim to show (1) how the upper and lower bounds depend on d and that (2) they provide tight bounds on the minimax risk within this class Gd,n .20 5.1 Direct Treatment Effect 

We first focus on estimating the Direct Effect τDTE , which was defined in Section 2.1. As discussed there, the conflict graph H for this estimand behaves roughly like the original interference graph 

G. In particular, as we have seen in Corollary 1.1, the following upper and lower bounds on the minimax risk hold for any d-regular graph G:

√d + 1 

n ≲ R(G, τ DTE , 2) ≲ d + 1 

n .

Note that we have removed the independent set I(G) term from the lower bound because it is not governed by d, though it will play a role in our later analysis. When the degree d is constant relative to the number of units n, then the bounds match up to a constant factor. A particular case of interest is the Stable Unit Treatment Value Assumption (SUTVA), where there is no interference and the graph G is empty. In that case, we obtain that the minimax risk of estimating the Average Treatment Effect (ATE) is Θ(1 /n ), which is the classical parametric rate of estimation. When d is growing with n, there is a multiplicative gap of √d between the upper and lower bounds. We will demonstrate that there are families of graphs in Gd for which the minimax rate coincides with the upper bound as well as families for which the minimax rate coincides with the lower bound. The next proposition establishes tightness of the upper bound. 

Proposition 5.1. For any sequence d = {d(n)}∞ 

> n=1

with d = o(n),

sup 

> G∈G d

R(G, τ DTE , 2) ≍ dn .

For the proof, we consider the family of d-chorded cycles, where the maximum independent set is O(d/n ). We then use the global lower bound of Theorem 3.2 to establish the matching lower bound of Ω( d/n ). For further details, see Section E.2 of the supplementary. Next, we show that the lower bound of √d/n is also attainable up to constant factors for a family of graphs in Gd with d = Θ( n2/3). 

Proposition 5.2. There exists a sequence d = {d(n)}∞ 

> n=1

such that d = Θ( n2/3) and 

inf 

> G∈G d

R(G, τ DTE , 2) ≍√dn .

For the proof, we focus on a family of graphs called the Generalized Quadrangles , which are well studied in finite geometry (Tits, 1959; Payne and Thas, 2009). The crucial property of these graphs is the existence of large independent sets that have very well-behaved intersections with each other. Combined with other strong connectivity properties, this allows us to construct an improved design tailored for this graph family that achieves the √d/n estimation rate. More details can be found in Section E.3 of the supplementary. Together, these results imply that both the upper and lower bounds from 5.1 in terms of the degree parameter are tight. Finally, we investigate the behavior of the minimax risk for the “average” graph in Gd, by considering a graph sampled uniformly at random from Gd.

Corollary 5.3. Suppose G is a uniformly random d-regular graph with d = o(n). Then, with probability at least 1 − o(1) over the random choice of G,

dn · log d ≲ R(G, τ DTE , 2) ≲ dn .

21 Corollary 5.3 shows that for the overwhelming majority of d-regular graphs, the minimax risk of estimating the DTE is equal to the upper bound of Corollary 1.1 up to a log d factor. To prove that, we utilize the fact that if d = o(n), then with high probability the largest independent set in 

G has size at most O(n log d/d ) (see e.g. Cooper et al., 2002), together with our global lower bound from Theorem 3.2. See Section E.4 of the supplementary for more details. 

5.2 Global Average Treatment Effect 

When the estimand of interest is the GATE τGATE , the conflict graph H is similar in structure to the two hop graph G2 of the underlying network G, as demonstrated in Section 2.3. In particular, Corollary 1.2 provides the following upper and lower bounds on the minimax risk that hold for any graph G:

> √

davg (G2)

n ≲ R(G, τ GATE , 2) ≲ λ(G2) + 1 

n .

Even when the graph G is d-regular, the quantities 

> √

davg (G2) and λ(G2) are unfortunately not determined by the degree parameter d. The reason is that the size of a two-hop neighborhood can range from d to d2. However, the following theorem shows that when the network G is random 

d-regular, 

> √

davg (G2) and λ(G2) are well-approximated by d and d2 so that there is a multiplicative gap of order d between them: 

Corollary 5.4. Suppose G is a random d-regular graph with d = o(√n). Then, with probability at least 1 − o(1) over the random choice of G,

dn ≲ R(G, τ GATE , 2) ≲ d2

n .

Corollary 5.4 together with Corollary 5.3 provides evidence for the widely held belief that the DTE is easier to estimate than the GATE. To see this, observe that when G is a random d-regular graph, then these two corollaries together yield that R(G, τ DTE ) ≲ d/n whereas R(G, τ GATE ) ≳ d/n .To the best of our knowledge, this provides the first theoretical evidence of the difference between the rates at which different network effects can be estimated. 

# 6 Conjectured Computational Hardness 

In the previous section, we have shown that while the derived upper and lower bounds on the minimax risk do not match in all cases, they are both tight for the class of regular graphs and are in fact matching for random regular graphs. However, this still leaves open the question of whether a simple characterization of the minimax risk is possible. In this section, we present two conjectures against the existence of an efficient algorithm which can approximately compute the minimax risk and discuss their consequences. For the purposes of conciseness, we gloss over certain technical aspects of computational complexity theory. At the heart of our conjectures is an intimate connection between the experimental design problem and computational problems regarding independent sets in graphs. As noted in Section 2.3, the fact that R(G, τ, q ) can be expressed as fq(H) for some function fq relies on the translation of the experimental design problem into a problem regarding distributions over maximal independent 22 sets of H. The central question considered in this section is whether this function fq : H → R≥0 is efficiently computable in any meaningful sense. The computer science literature has established that computational problems involving the independent sets of a graph are provably hard. For example, it is NP-Hard to calculate the size of the largest independent set I(G) of a graph G (Karp, 1972). In fact, even approximating I(G) to within a multiplicative n1−ϵ factor is NP-Hard for all ϵ > 0 (Hastad, 1996; Zuckerman, 2006). This n1−ϵ

inapproximability result presumes that the input graph is a priori unrestricted so that the size of the maximal independent set could range from 1 to n. By focusing specifically on d-regular graphs where the size of the largest independent set is known to lie between n/d and n, the inapproximability can be strengthened further still: approximating I(G) to within a O(d/ log 2 d) multiplicative factor for a d-regular graph G is hard assuming the Unique Games Conjecture (Austrin, Khot, and Safra, 2011). Based on these results and the connection between the minimax rate of network experiment problems and the structure of independent sets in the conflict graph, we present our two conjectures: 

Conjecture 1. Given a network G, contrastive effect τ , and moment parameter q, the minimax risk 

R(τ, G, q ) = fq(H) is NP-Hard to approximate within a multiplicative factor nc for some c ∈ (0 , 1) .

Conjecture 2. Given a d-regular graph G and the direct effect τDTE , the minimax risk R(G, τ DTE , q ) = 

fq(H) is NP-Hard to approximate within a multiplicative factor of d1/2−ϵ for all ϵ > 0.

Conjecture 1 posits that even an approximation of the minimax rate is computationally chal-lenging for general network experiment problems. Conjecture 2 is a strengthening of Conjecture 1 specifically for the direct effect, which states that even determining whether the true minimax rate is close to our derived upper bound d/n or lower bound √d/n is computationally infeasible. As discussed above, our primary evidence for these conjectures is the close relationship between the minimax rate R(G, τ, q ) and the structure of maximal independent sets in the conflict H as well as the general inapproximability results for maximum independent set. Note that the computational hardness of approximating the minimax rate would not preclude an explicit construction of a minimax optimal statistical procedure; however, implementing such a procedure would likely be computationally inefficient, otherwise one could use Monte Carlo simulations of this procedure to approximate the minimax rate. These conjectures posit that the minimax rate does not admit a simple description. If true, these conjectures would have significant consequences for the practical relevance of minimax theory in network experiments. Perhaps the most valuable aspect of minimax theory is to provide a theoretical benchmark that certain statistical procedures should pass, e.g. being minimax rate-optimal. If the minimax rate of a network experiment is not efficiently computable, then the rate optimality of a particular statistical procedure cannot be assessed on an arbitrary network and effect. In this case, we see two possible alternatives. The first is to show rate optimality of statistical procedures within a certain family of graphs in a worst-case or average-case sense. For example, Corollary 5.3 on the rate optimality of Conflict Graph Design for random d-regular graphs may be seen as an example of this type of analysis. The second alternative is to formulate a notion of computationally efficient minimax risk, which restricts the statistical procedure to be implementable in polynomial time. From this perspective, understanding both the optimal computationally efficient statistical procedure and the rate of its corresponding risk would be natural next steps. 23 7 Conclusion 

In this paper, we have studied design-based minimax optimal rates for network experiments. Our upper and lower bounds provide further understanding of how the minimax rate depends on the conflict graph H. Generally speaking, our results show that the minimax rates are governed by its “bulk connectivity”, in the sense that a few well-connected vertices will not affect the rates. Moreover, the analysis suggests an interplay between the strength of moment restrictions and the extent to which small but well-connected subgraphs can affect the minimax rates. We believe that the most interesting open directions posed by this work are further investigation into the computational complexity of network experiments. Is approximately computing the minimax risk R(G, τ, q ) computationally hard? In Section 6, we have provided several well-motivated conjectures that suggest the answer is yes. Progress towards resolving these conjectures in any direction would provide deeper understanding into the fundamental obstructions in constructing optimal statistical procedures. If statistical procedures which attain the minimax risk require exponential computational resources, then what is the best rates achievable by a computationally efficient experimental design and estimator? Questions like these lie in the intersection of statistics, computational complexity, and graph theory. Computational considerations notwithstanding, an exact characterization of the minimax rates in terms of the conflict graph (i.e. a description of 

fq(H)) would be valuable in further understanding the essence of these statistical limits. Doing this would ostensibly require both improved statistical lower bounds and experimental designs, which would be interesting in their own right regardless of computational tractability. 

# References 

Aggarwal, Om P (1959). “Bayes and minimax procedures in sampling from finite and infinite populations–I”. The Annals of Mathematical Statistics , pp. 206–218. Aronow, P.M. and Patrick Lopatto (2026). “Minimax unbiased estimation for finite populations with bounded outcomes”. arXiv preprint arXiv:2605.20572 .Aronow, P.M. and Cyrus Samii (2017). “Estimating average causal effects under general interference, with application to a social network experiment”. The Annals of Applied Statistics 11.4, pp. 1912– 1947. Austrin, Per, Subhash Khot, and Muli Safra (2011). “Inapproximability of vertex cover and inde-pendent set in bounded degree graphs”. Theory of Computing 7.1, pp. 27–43. Baird, Sarah, J. Aislinn Bohren, Craig McIntosh, and Berk Özler (Dec. 2018). “Optimal Design of Experiments in the Presence of Interference”. The Review of Economics and Statistics 100.5, pp. 844–860. Basse, Guillaume W and Edoardo M Airoldi (Dec. 2018). “Model-assisted design of experiments in the presence of network-correlated outcomes”. Biometrika 105.4, pp. 849–858. Bickel, Peter J, Chris AJ Klaassen, Peter J Bickel, Ya’acov Ritov, J Klaassen, Jon A Wellner, and YA’Acov Ritov (1993). Efficient and adaptive estimation for semiparametric models . Vol. 4. Springer. Bickel, Peter J and Erich L Lehmann (1981). “A Minimax Property of the Sample Mean in Finite Populations”. The Annals of Statistics 9.5, pp. 1119–1122. Blackwell, David A and Meyer A Girshick (1979). Theory of games and statistical decisions . Courier Corporation. 24 Bojinov, Iavor, David Simchi-Levi, and Jinglong Zhao (2023). “Design and Analysis of Switchback Experiments”. Management Science 69.7, pp. 3759–3777. Cai, Chencheng, Xu Zhang, and Edoardo Airoldi (2024). “Independent-Set Design of Experiments for Estimating Treatment and Spillover Effects under Network Interference”. The Twelfth International Conference on Learning Representations .Candogan, Ozan, Chen Chen, and Rad Niazadeh (2024). “Correlated Cluster-Based Randomized Experiments: Robust Variance Minimization”. Management Science 70.6, pp. 4069–4086. Clark, Pete L (2013). “Quadratic forms chapter I: Witt’s theory”. University of Georgia 3. Cooper, Colin, Alan Frieze, Bruce Reed, and Oliver Riordan (2002). “Random regular graphs of non-constant degree: Independence and chromatic number”. Combinatorics, Probability and Computing 11.4, pp. 323–341. Cortez-Rodriguez, Mayleen, Matthew Eichhorn, and Christina Lee Yu (2023). “Exploiting neighbor-hood interference with low-order interactions under unit randomized design”. Journal of Causal Inference 11.1, p. 20220051. Duchi, John (2023). “Lecture notes on statistics and information theory”. URL: https://https://web. stanford. edu/class/stats311/lecture-notes. pdf .Fatemi, Zahra and Elena Zheleva (2020). “Network Experiment Design for Estimating Direct Treatment Effects”. KDD Workshop on Mining and Learning with Graphs. Foster, Dylan J and Vasilis Syrgkanis (2023). “Orthogonal statistical learning”. The Annals of Statistics 51.3, pp. 879–908. Frieze, Alan M and TOMASZ Łuczak (1992). “On the independence and chromatic numbers of random regular graphs”. Journal of Combinatorial Theory, Series B 54.1, pp. 123–132. Gabler, Siegfried (2012). Minimax solutions in sampling from finite populations . Vol. 64. Springer Science & Business Media. Gandhi, Rajiv, Samir Khuller, and Aravind Srinivasan (2004). “Approximation algorithms for partial covering problems”. Journal of Algorithms 53.1, pp. 55–84. Gao, Chao, Christopher Harshaw, Fredrik Sävje, and Yitan Wang (2026). “On the Impossibility of Specification Testing of Interference Models Based on Exposure Mappings”. arXiv preprint arXiv:2605.09726 .Godsil, Chris and Gordon F Royle (2013). Algebraic graph theory . Springer Science & Business Media. Harshaw, Christopher, Fredrik Sävje, and Yitan Wang (2022). “A design-based Riesz representation framework for randomized experiments”. arXiv preprint arXiv:2210.08698 .Hastad, Johan (1996). “Clique is hard to approximate within n/sup 1-/spl epsiv”. Proceedings of 37th Conference on Foundations of Computer Science . IEEE, pp. 627–636. Hochbaum, Dorit S (1997). “Approximation algorithms for NP-hard problems”. ACM Sigact News 

28.2, pp. 40–52. Hodges, JL and EL Lehmann (1982). “Minimax estimation in simple random sampling”. Statistics and probability: essays in honor of CR Rao. North-Holland Publishing Company , pp. 325–327. Holland, Paul W (1986). “Statistics and causal inference”. Journal of the American statistical Association 81.396, pp. 945–960. Hu, Yuchen and Stefan Wager (2023). “Off-policy evaluation in partially observed Markov decision processes under sequential ignorability”. The Annals of Statistics 51.4, pp. 1561–1585. Hudgens, Michael G and M Elizabeth Halloran (2008). “Toward causal inference with interference”. 

Journal of the american statistical association 103.482, pp. 832–842. 25 Jagadeesan, Ravi, Natesh S. Pillai, and Alexander Volfovsky (2020). “Designs for estimating the treatment effect in networks with interference”. Annals of Statistics 48.2, pp. 679–712. Kallus, Nathan (2018). “Optimal A Priori Balance in the Design of Controlled Experiments”. Journal of the Royal Statistical Society Series B: Statistical Methodology 80.1, pp. 85–112. Kandiros, Vardis, Charilaos Pipis, Constantinos Daskalakis, and Christopher Harshaw (2024). “The Conflict Graph Design: Estimating causal effects under arbitrary neighborhood interference”. 

arXiv preprint arXiv:2411.10908 .Karp, Richard M (1972). “Reducibility among combinatorial problems”. Complexity of computer computations . Springer, pp. 85–103. Karwa, Vishesh and Edoardo M. Airoldi (2018). “A Systematic Investigation of Classical Causal Inference Strategies under Mis-specification Due to Network Interference”. arXiv:1810.08259v1. arXiv: 1810.08259v1 [stat.ME] .Karwa, Vishesh and Edoardo M Airoldi (2023). “On the admissibility of horvitz-thompson estimator for estimating causal effects under network interference”. arXiv preprint arXiv:2312.01234 .Kennedy, Edward H, Sivaraman Balakrishnan, James M Robins, and Larry Wasserman (2024). “Minimax rates for heterogeneous causal effect estimation”. Annals of statistics 52.2, p. 793. Krivelevich, Michael, Benny Sudakov, Van H Vu, and Nicholas C Wormald (2001). “Random regular graphs of high degree”. Random Structures & Algorithms 18.4, pp. 346–363. Lam, Tsit-Yuen (2005). Introduction to quadratic forms over fields . Vol. 67. American Mathematical Soc. Lehmann, Erich Leo and George Casella (1998). Theory of point estimation . Springer. Leung, Michael P. (2022). “Rate-optimal cluster-randomized designs for spatial interference”. The Annals of Statistics 50.5, pp. 3064–3087. Manski, Charles F (2013). “Identification of treatment response with social interactions”. The Econometrics Journal 16.1, S1–S23. McKay, Brendan D, Nicholas C Wormald, and Beata Wysocka (2004). “Short cycles in random regular graphs”. the electronic journal of combinatorics , R66–R66. Payne, Stanley E and Joseph Adolf Thas (2009). Finite generalized quadrangles . Vol. 9. European Mathematical Society. Rinott, Yosef (2009). “Chapter 41 - Some Decision-Theoretic Aspects of Finite Population Sampling”. 

Handbook of Statistics . Ed. by C.R. Rao. Vol. 29. Handbook of Statistics. Elsevier, pp. 523–558. Robins, James, Lingling Li, Eric Tchetgen, Aad van der Vaart, et al. (2008). “Higher order influence functions and minimax estimation of nonlinear functionals”. Probability and statistics: essays in honor of David A. Freedman . Vol. 2. Institute of Mathematical Statistics, pp. 335–422. Rotnitzky, Andrea, Ezequiel Smucler, and James M Robins (2021). “Characterization of parameters with a mixed bias property”. Biometrika 108.1, pp. 231–238. Saha, Sudip, Abhijin Adiga, B Aditya Prakash, and Anil Kumar S Vullikanti (2015). “Approximation algorithms for reducing the spectral radius to control epidemic spread”. Proceedings of the 2015 SIAM International Conference on Data Mining . SIAM, pp. 568–576. Sobel, Michael E (2006). “What Do Randomized Studies of Housing Mobility Demonstrate?” Journal of the American Statistical Association 101.476, pp. 1398–1407. Sussman, Daniel L. and Edoardo M. Airoldi (2017). “Elements of estimation theory for causal effects in the presence of network interference”. arXiv:1702.03578. arXiv: 1702.03578v1 [stat.ME] .Tits, Jacques (1959). “Sur la trialité et certains groupes qui s’ en déduisent”. Publications Mathé-matiques de l’IHÉS 2, pp. 13–60. 26 Toulis, Panos and Edward Kao (2013). “Estimation of Causal Peer Influence Effects”. Proceedings of the 30th International Conference on Machine Learning . Ed. by Sanjoy Dasgupta and David McAllester. Vol. 28. Proceedings of Machine Learning Research 3. Atlanta, Georgia, USA: PMLR, pp. 1489–1497. Ugander, Johan, Brian Karrer, Lars Backstrom, and Jon Kleinberg (2013). “Graph cluster random-ization: Network exposure to multiple universes”. KDD ’13. ACM, pp. 329–337. Ugander, Johan and Hao Yin (2023). “Randomized graph cluster randomization”. Journal of Causal Inference 11.1, p. 20220014. van der Vaart, Aad W (2000). Asymptotic statistics . Vol. 3. Cambridge university press. Viviano, Davide, Lihua Lei, Guido Imbens, Brian Karrer, Okke Schrijvers, and Liang Shi (2026). 

Causal clustering: design of cluster experiments under network interference . arXiv: 2310.14983 [econ.EM] .Wilf, Herbert S (1967). “The eigenvalues of a graph and its chromatic number”. Journal of the London mathematical Society 1.1, pp. 330–332. Wu, Chien-Fu (1981). “On the Robustness and Efficiency of Some Randomized Designs”. The Annals of Statistics 9.6, pp. 1168–1177. Wu, Yihong (2017). “Lecture notes on information-theoretic methods for high-dimensional statistics”. 

Lecture Notes for ECE598YW (UIUC) 16, p. 15. Yu, Bin (1997). “Assouad, fano, and le cam”. Festschrift for Lucien Le Cam: research papers in probability and statistics . Springer, pp. 423–435. Zöcklein, Gernot (2025). “Regression Adjustment for Randomized Network Experiments”. Master’s thesis. ETH Zurich. Zuckerman, David (2006). “Linear degree extractors and the inapproximability of max clique and chromatic number”. Proceedings of the thirty-eighth annual ACM symposium on Theory of computing , pp. 681–690. 27 Appendix 

# Table of Contents 

A Minimax Theory for Design-Based Inference 29 

A.1 A General Definition of Minimax Risk . . . . . . . . . . . . . . . . . . . . . . . . . 29 A.2 Monotonicity of the Minimax Risk (Proposition 2.1) . . . . . . . . . . . . . . . . . 29 A.3 Randomized Estimators . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 30 A.4 Variations of Outcome Restrictions . . . . . . . . . . . . . . . . . . . . . . . . . . . 32 A.5 Conflict Graph Characterizes Minimax Risk (Proposition 2.2) . . . . . . . . . . . . 35 

B Lower Bounds on the Minimax Risk 41 

B.1 Le Cam’s Method (Proposition 3.1) . . . . . . . . . . . . . . . . . . . . . . . . . . . 41 B.2 Lower Bound using Global Information (Theorem 3.2) . . . . . . . . . . . . . . . . 43 B.3 Lower Bound using Local Information (Theorem 3.4) . . . . . . . . . . . . . . . . . 52 

C Upper Bounds on the Minimax Risk 63 

C.1 Analysis of the Conflict Graph Design (Theorem 4.1) . . . . . . . . . . . . . . . . . 63 C.2 Bias-Variance Tradeoff . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 77 C.3 Polynomial Time Approximation Algorithm (Proposition 4.3) . . . . . . . . . . . . 80 

D Illustrations of Main Results 83 

D.1 Direct Treatment Effect (Corollary 1.1) . . . . . . . . . . . . . . . . . . . . . . . . 83 D.2 Global Average Treatment Effect (Corollary 1.2) . . . . . . . . . . . . . . . . . . . 84 

E Tightness of Bounds 85 

E.1 The Structure of the Conflict Graph . . . . . . . . . . . . . . . . . . . . . . . . . . 85 E.2 Tightness of Upper Bound for Direct Effect (Proposition 5.1) . . . . . . . . . . . . 86 E.3 Tightness of Lower Bound for Direct Effect (Proposition 5.2) . . . . . . . . . . . . 87 E.4 Random Regular Graphs (Corollaries 5.3 and 5.4) . . . . . . . . . . . . . . . . . . 94 

28 A Minimax Theory for Design-Based Inference 

In this section, we present the proofs of our general results involving the minimax risk for estimating arbitrary causal effects. The subsections are organized as follows: • In Section A.1, we present a general definition of minimax risk for arbitrary restrictions on the potential outcome functions. • In Section A.2, we establish various monotonicity properties of the minimax risk. • In Section A.3, we show that additional randomness does not improve estimation. • In Section A.4, we prove that the minimax risk is invariant to some natural restrictions of the potential outcome functions. • Finally, in Section A.5, we prove that the minimax risk is characterized by the conflict graph. 

A.1 A General Definition of Minimax Risk 

In this section, we briefly describe how to extend the definition of minimax optimality to more general design-based causal inference. We focus on the design-based framework proposed by setting introduced by Harshaw, Sävje, and Wang (2022). In the framework of Harshaw, Sävje, and Wang (2022), the researcher will posit, for each subject 

i ∈ [n], a subject-level model space Mi which is a linear subspace of measurable functions. The model spaces serve to encode the structure of the potential outcome functions. The entire model is a subset M ⊆ M 1 × · · · × M n, which may incorporate additional moment restrictions. The causal estimand is a linear functional τ : M → R.Generalizing the definition in Section 2.2, we define the minimax risk of estimating a causal estimand τ over a model M on the potential outcomes as follows: 

R(M, τ ) = inf  

> D,ˆτ

sup 

> y∈M

EZ∼D 

> [

(ˆ τ (Z, y (Z)) − τ (y)) 2] .

When τ is a contrastive effect and M(G) is an ANI model, then the definition of minimax risk above reduces to the one presented in Section 2.2. 

A.2 Monotonicity of the Minimax Risk (Proposition 2.1) 

We first establish a monotonicity result which holds for arbitrary restrictions on the potential outcome functions and for arbitrary estimands. We then use it to prove each of the monotonicity properties in Proposition 2.1. 

Lemma A.1. If M ⊆ M ′ then R(M, τ ) ≤ R (M′, τ ).Proof. Let us fix any estimator ˆ τ and a design P . Then, since M is contained in M′, we have that Risk( P, ˆτ ) = sup 

> y∈M

EZ∼P [(ˆ τ (Z, Y (Z)) − τ (y)) 2] ≤ sup 

> y∈M ′

EZ∼P [(ˆ τ (Z, Y (Z)) − τ (y)) 2] = Risk( P, ˆτ ) .

Taking the infimum over all estimators ˆ τ and designs P on both sides gives the desired result. 29 Now let us prove Proposition 2.1 using the above general monotonicity result. For convenience, we restate it below. 

Proposition 2.1. The minimax risk R(G, τ, q ) obeys the following monotonicity properties: 

• Monotonicity in q: If q′ ≤ q, then R(G, τ, q ) ≤ R (G, τ, q ′).

• Monotonicity in G: If G′ ⊆ G, then R(G′, τ, q ) ≤ R (G, τ, q ).Proof. The proof of each of the monotonicity properties relies on applying Lemma A.1 to the appropriate sets of potential outcome functions. We now show how to do that for each statement separately. 1. Monotonicity in q. This will follow immediately from Lemma A.1 if we could show that 

M(G, q, C ) ⊆ M (G, q ′) for any q′ ≤ q. Consider any tuple of potential outcome functions 

y ∈ M (G, q ). For any k ∈ { 0, 1}, we have that 1

n

> n∑
> i=1

|yi(zi, (k))|q′

= 1

n

> n∑
> i=1

(|yi(zi, (k))|q) q′ 

> q

≤ 

> (

1

n

> n∑
> i=1

|yi(zi, (k))|q) q′

> q

≤ 1 ,

where the first inequality follows from concavity of x 7 → xq′/q and Jensen’s inequality and the final inequality follows from the definition of y ∈ M (G, q ) Therefore, y ∈ M (G, q ′), which implies that M(G, q ) ⊆ M (G, q ′). 2. Monotonicity in G. Finally, for the monotonicity in G, it suffices to establish that if 

G′ ⊆ G, then M(G′, q ) ⊆ M (G, q ), since then Lemma A.1 applies. To prove this, it suffices to show that M(G′) ⊆ M (G), i.e. the potential outcome functions that satisfy ANI with respect to G′ also satisfy ANI with respect to G. But this also follows from the definition of graph inclusion. To distinguish between the two different graphs, we use ˜NH (i) to denote the extended neighborhood of a node i in graph H. In particular, suppose y ∈ M (G′). Then, let 

i ∈ [n] and z, z ′ ∈ { 0, 1}n be two treatment assignments such that for all i the exposure of i

under z and z′ is the same with respect to G, i.e. 

{j ∈ ˜NG(i) : zj = 1 } = {j ∈ ˜NG(i) : z′ 

> j

= 1 } .

Since G′ ⊆ G, we have that ˜NG′ (i) ⊆ ˜NG(i), which implies that 

{j ∈ ˜NG′ (i) : zj = 1 } = {j ∈ ˜NG′ (i) : z′ 

> j

= 1 } .

Since y ∈ M (G′), the last equality implies that yi(z) = yi(z′). Since this holds for all such i

and all z, z ′, we have that y ∈ M (G), which completes the proof. 

A.3 Randomized Estimators 

In Section 2.1, we defined our setup so that the randomness only came from the treatment assignment. However, since the experimenter controls the data collection process, they could also utilize additional randomness in choosing the treatment and in constructing the estimator for the causal estimand of 30 interest. In this section, we show that allowing a statistical procedure to use additional randomness does not improve the minimax risk. In particular, let S be the sample space of the additional randomness and let S be a σ-algebra on S. Since the arguments below work for any choice of S and S, we supress the dependence on them in the following definitions. We define a design with randomness as a probability measure Drand : F ⊗ S → [0 , 1] over the product space of treatment assignments and the additional randomness. Drawing a sample from Drand produces a pair ( Z, X ), where Z ∈ Ω is the treatment assignment that is used by the experimenter and X ∈ S is the additional randomness, which could be used by the estimator. We emphasize again that Drand is the only source of randomness in this problem and it is controlled by the experimenter. Also, the experimenter could correlate the additional randomness with the treatment assignment Z, which implies that Drand is not necessarily a product measure over Ω and 

S. A randomized estimator ˆτrand : Ω × S × Rn → R is a measurable function with respect to the product σ-algebra F ⊗ S ⊗ B (Rn), where B(Rn) is the Borel σ-algebra on Rn. The pair ( Drand , ˆτrand )is referred to as a randomized statistical procedure . In this setting, the experimenter first draws a random pair ( Z, X ) according to the design with randomness Drand , where Z ∈ Ω and X ∈ S. After applying the treatment Z, they observe outcomes Y (Z) and output the estimate ˆ τrand (Z, X, Y (Z)) of the effect τ .In analogy with the definitions in Section A.1, we can define the Randomized Minimax Risk for a given causal effect τ and model M as the minimum risk over all designs with randomness and all randomized estimators, i.e. 

Rrand (M, τ ) = inf 

> Drand ,ˆτrand

sup 

> y∈M

E(Z,X )∼D rand [(ˆ τrand (Z, X, Y (Z)) − τ (y)) 2] .

We now show that the minimax risk of randomized statistical procedures is the same as that of deterministic procedures. In particular, we establish that for any pair of design with randomness and randomized estimator, there exists a deterministic estimator that uses only the treatment part of the randomness and has the same or smaller MSE for all potential outcomes functions. 

Proposition A.2. For any model M and any effect τ , Rrand (M, τ ) = R(M, τ ).Proof. Since deterministic estimators are a special case of randomized estimators, we have by definition that 

Rrand (M, τ ) ≤ R (M, τ ) .

To establish the reverse inequality, let’s fix any randmized experimental design Drand and any randomized estimator ˆ τrand . Since Drand is a probability measure over the product space Ω × S, let’s denote the marginal distribution of Drand over Ω by D. Now, define the estimator ˆ τ : Ω × Rn → R as ˆτ (Z, y (Z)) = E X∼D rand (·| Z)

> [

ˆτrand (Z, X, y (Z)) 

> ]

.

Clearly, ˆ τ is a deterministic estimator that only depends on the treatment assignment Z and the observed outcomes y(Z). Let y ∈ M be a potential outcome function in model M. Then, we have that E(Z,X )∼D rand 

> [(

ˆτrand (Z, X, y (Z)) − τ (y)

> )2]

= E Z∼D 

> [

EX∼D rand (·| Z)

> [

(ˆ τrand (Z, X, y (Z)) − τ (y)) 2]]

31 ≥ EZ∼D 

> [(

EX∼D rand (·| Z)

> [

ˆτrand (Z, X, y (Z)) 

> ]

− τ (y)

> )2]

by using Jensen’s inequality for the convex function x 7 → x2

= E Z∼D 

> [(

ˆτ (Z, y (Z)) − τ (y)

> )2]

.

Therefore, the statistical procedure ( D, ˆτ ) has smaller or equal MSE than the procedure ( Drand , ˆτrand )that operates with extra randomness, for all potential outcome functions y ∈ M . Therefore, taking supremum over all y ∈ M and then infimum over all pairs ( Drand , ˆτrand ), we have 

Rrand (M, τ ) ≥ R (M, τ ) ,

which completes the proof. 

A.4 Variations of Outcome Restrictions 

In this section, we show that the minimax risk remains unaffected under some natural variations of the outcome restrictions introduced in Section 2.1. We start by showing that if we change the scaling of the constraint on the outcomes, the minimax risk scales accordingly. In particular, for an interference network G, effect τ , moment parameter 

q ≥ 2 and positive real number C > 0, we define the following set of potential outcome functions: 

M(G, τ, q, C ) = 

> {

y ∈ M (G) :  

> (

1

n

> n∑
> i=1

|yi(ei,k )|q)1/q 

≤ C for k ∈ { 0, 1}

> }

.

This generalizes the qth moment restricted model M(G, τ, q ) defined in Section 2.1, which is defined with C = 1. Likewise, we can define the minimax risk of estimating the effect τ over this set of potential outcome functions as 

R(G, τ, q, C ) = inf  

> D,ˆτ

sup 

> y∈M (G,τ,q,C )

EZ∼D [(ˆ τ (Z, y (Z)) − τ (y)) 2] .

We now show that changing the bound on the outcomes results in a simple scaling of the minimax risk. Therefore, it is sufficient to consider the case C = 1 without loss of generality. 

Proposition A.3. Let G be an interference network, τ be a contrastive effect and q ≥ 2 be a moment parameter. Then, for any positive real number C > 0, we have that 

R(G, τ, q, C ) = C2 · R (G, τ, q, 1) .

Proof. For any L > 0 and potential outcome function y, we denote by Ly the potential outcome function defined as ( Ly )i(z) = L · yi(z) for all i ∈ [n] and z ∈ { 0, 1}n. Let U, V > 0 be any positive real numbers. Let D be any design and ˆ τ be any estimator. We define the following estimator ˆ τU,V 

as ˆτU,V (Z, y (Z)) = UV · ˆτ (Z, (V /U ) · y(Z)) .

32 In particular, ˆ τU,V scales the observed outcomes by V /U , applies the estimator ˆ τ , and then scales the output by U/V .Now consider an arbitrary potential outcome function y ∈ M (G, τ, q, U ). Then, we have that (V /U ) · y ∈ M (G, τ, q, V ), since for any k ∈ { 0, 1}, we have that  

> (

1

n

> n∑
> i=1

|(( V /U ) · y)i(ei,k )|q)1/q 

= VU 

> (

1

n

> n∑
> i=1

|yi(ei,k )|q)1/q 

≤ VU · U = V .

Also, by linearity we have 

τ (( V /U ) · y) = ( V /U ) · τ (y) .

Therefore, for any y ∈ M (G, τ, q, U ), we have that ED

> [(

ˆτU,V (Z, y (Z)) − τ (y))2]

= E D 

> [(

UV ˆτ (Z, (V /U ) · y(Z)) − UV τ (( V /U ) · y)

> )2]

= U 2

V 2 · ED

> [(

ˆτ (Z, (V /U ) · y(Z)) − τ (( V /U ) · y)

> )2]

≤ U 2

V 2 · sup  

> y∈M (G,τ,q,V )

ED

> [(

ˆτ (Z, y (Z)) − τ (y)

> )2]

,

where in the last step we used the fact that ( V /U )y ∈ M (G, τ, q, V ). Therefore, for any statistical procedure ( D, ˆτ ), we can find another statistical procedure ( D, ˆτU,V ) whose maximal error under 

M(G, τ, q, V ) is upper bounded by the scaled maximal error of ( D, ˆτ ) under M(G, τ, q, U ). It follows that 

R(G, τ, q, U ) ≤ U 2

V 2 · R (G, τ, q, V )We can now apply inequality A.4 twice, one with U = C, V = 1 and one with U = 1 , V = C to obtain the result. We now show that the outcomes of units corresponding to exposures that do not appear in the estimand of interest do not affect the minimax risk. In particular, we show that even if we restrict these outcomes to have value 0, the minimax risk remains the same. Specifically, for any graph G and estimand τ with relevant exposures ei, 1, e i, 0 for i ∈ [n], let 

M0(G, τ ) be the model of outcomes that satisfy ANI with respect to G and have zero outcome in the non-relevant exposures, i.e. 

M0(G, τ ) = 

> {

y ∈ M (G) : yi(e) = 0 , if e̸ = ei, 0, e i, 1, ∀i ∈ [n]

> }

Then, for any graph G, estimand τ and q ≥ 2, we can define the model of potential outcome functions with bounded q moments and zero non-relevant outcomes as follows 

M0(G, τ, q ) = M0(G, τ ) ∩ M (G, τ, q ) .

Analogously to the previous definitions, we can define the minimax risk with respect to outcome functions in M0(G, τ, q ) as follows 

R0(G, τ, q ) = inf  

> D,ˆτ

sup 

> y∈M 0(G,τ,q )

EZ∼D [(ˆ τ (Z, y (Z)) − τ (y)) 2] .

33 In the next proposition, we show that the minimax risk is unaffected by the restrictions in the non-relevant exposures. 

Proposition A.4. For any graph G, estimand τ and q ≥ 2,

R0(G, τ, q ) = R(G, τ, q ) .

Proof. Since M0(G, τ, q ) ⊆ M (G, τ, q ), we trivially have 

R0(G, τ, q ) ≤ R (G, τ, q ) .

We thus focus on showing the reverse inequality. For any outcome function y ∈ M (G), we denote by y0 the corresponding outcome function that agrees with y in the relevant exposures and has 0 outcome everywhere else, i.e. 

y0 

> i

(z) = 

> {

yi(z) if hi(z) ∈ { ei, 0, e i, 1}

0 otherwise .

Clearly, if y ∈ M (G, τ, q ), then y0 ∈ M (G, τ, q ), since the moment constraint is not affected by outcomes of exposures that do not appear in τ . Also, we always have τ (y) = τ (y0), since the estimand doesn’t depend on the outcomes we are zeroing out. Now, let ( D, ˆτ0) be an arbitrary statistical procedure. We will construct an estimator ˆ τ , such that the worst case performance of ( D, ˆτ ) under M(G, τ, q ) is no worse than than of ( D, ˆτ0) under 

M0(G, τ, q ). Indeed, we define ˆ τ in terms of ˆ τ0 as follows ˆτ (z, y (z)) = ˆ τ0(z, y 0(z)) In words, ˆ τ works by zeroing out the non-relevant outcomes and then applying ˆ τ0 to the result. Then, for any y ∈ M (G, τ, q )ED

> [(

ˆτ (Z, y (Z)) − τ (y))2]

= E D

> [(

ˆτ0(Z, y 0(Z)) − τ (y))2]

= E D

> [(

ˆτ0(Z, y 0(Z)) − τ (y0))2]

Taking the supremum over y ∈ M (G, τ, q ) on both sides yields sup 

> y∈M (G,τ,q )

ED

> [(

ˆτ (Z, y (Z)) − τ (y))2]

= sup 

> y∈M (G,τ,q )

ED

> [(

ˆτ0(Z, y 0(Z)) − τ (y0))2]

= sup 

> y∈M 0(G,τ,q )

ED

> [(

ˆτ0(Z, y (Z)) − τ (y))2]

.

The last step follows by the following observation, which follows by the definitions. 

M0(G, τ, q ) = 

> {

y0 : y ∈ M (G, τ, q )

> }

.

Since the above holds for an arbitrary statistical procedure ( D, ˆτ ), we conclude that 

R(G, τ, q ) ≤ R 0(G, τ, q ) .

The result follows. 34 A.5 Conflict Graph Characterizes Minimax Risk (Proposition 2.2) 

In this section, we show that while the conflict graph abstracts away many features of the experimental design problem, it contains the essential information to characterize the minimax risk. More precisely, we will show that the minimax risk is a function of the conflict graph. In the main body, this is formalized as Proposition 2.2, which we reproduce below for completeness. Recall that H is the set of all labeled graphs. 

Proposition 2.2. Let G be an interference network, τ be a contrastive effect. For each moment parameter q ≥ 2, there exists a function fq : H → R≥0 such that R(G, τ, q ) = fq(H).

Our proof strategy will proceed by defining two minimax problems on an arbitrary labeled graph H. In these minimax problems, each of the vertices in H has an associated outcome and the target estimand is the average of the outcomes. A random independent set in H is selected and the outcomes of its vertices is revealed. The minimax problem is to construct both the distribution over independent sets and the estimator. We first consider distributions over all independent sets, and then the restriction to distributions supported on maximal independent sets. Through a series of reductions, we will show that the minimax risk of these graph theoretic problems exactly corresponds to the minimax risk of the original network experiment problem. We begin by formally defining these minimax problems. Consider a labeled graph H = ( VH, E H)with 2 n vertices. Note that we have assumed the number of vertices is even, for simplicity. Consider a function u : VH → R which maps each vertex to a real number and may be identified with a vector 

u ∈ R|VH|. For a moment parameter q ≥ 2, define the qth moment restricted model 

M(H, q ) = {u ∈ RVH : 

> (

1

n

> n∑
> i=1

|u(i)|q)1/q 

≤ 1 and  

> (

1

n

> 2n∑
> i=n+1

|u(i)|q)1/q 

≤ 1} ,

which restricts the qth moment for the first half and second half of the outcomes. Note that the labelling of the graph H is required to give meaning to “first half” and “second half”. The labelling will also be required to define the contrast between two groups of vertices. Formally, we define the 

contrastive functional τ : M(H, q ) → R as 

τ (u) = 1

n

> 2n∑
> i=n+1

u(i) − 1

n

> n∑
> i=1

u(i) .

In these minimax problems, we must select a probability measure over independent sets in H,which we formalize below. Recall that a subset of vertices S ⊆ VH is independent if no pair of vertices i, j ∈ S is adjacent in H. We denote the independent sets of H as I(H). We have that the independent sets form a measureable space ( I(H), P(I(H))), where P(I(H)) is the power set. Let 

D(I(H)) be the set of all probability measures on independent sets I(H). What is observed in these minimax problems is the outcomes for each of the vertices in the (randomly) selected independent set and this is the data which must be used to estimate the average outcome. In order to simplify the formal notation, we introduce an auxiliary variable ∗ which will denote that no outcome has been observed. Given an independent set S ∈ I (H), define the vector uS ∈ RVH where uS (i) = u(i) if i ∈ S and uS (i) = ∗ if i / ∈ S. Thus, the experimenter observes ( S, u S ) ∈ I (H) × (R∗)VH , where R∗ = R ∪ {∗} . An estimator is a measureable function ˆτ : I(H) × (R∗)VH → R. We denote the set of all real-valued measurable functions on I(H) × (R∗)VH

as Γ( I(H)). 35 With this notation in place, we can now define the first minimax problem for labelled graphs. For a graph H and a moment parameter q ≥ 2, define the independent set minimax risk R(H, q ) as 

R(H, q ) = inf  

> D∈ D(I(H)) ˆτ∈Γ( I(H))

sup 

> u∈M (H,q )

ES∼D 

> [

(τ (u) − ˆτ (S, u S )) 2]

.

The independent minimax risk describes the best achievable mean squared error in this graph estimation problem, defined solely in terms of the labelled graph H.The second minimax problem is similar, but considers the restriction to observe only maximal independent sets in H. This restriction is natural—given that we get to observe all outcomes in an independent set, restricting the choice of independent set to be maximal ensures that we observe as much information as possible. Formally, an independent set S ∈ I (H) is maximal if for any 

A ∈ I (H) with S ⊆ A, we have that A = S. In other words, S is not contained in a strictly larger independent set. We denote the set of maximal independent sets in H by MI (H). In an analogous fashion, we denote the set of probability measures on MI (H) by D(MI (H)) and the set of real-valued measurable functions on MI (H) × (R∗)VH as Γ( MI (H)). For a moment parameter 

q ≥ 2, we define the maximal independent set minimax risk as 

RMI (H, q ) = inf  

> D∈ D(MI (H)) ˆτ∈Γ( MI (H))

sup 

> u∈M (H,q )

ES∼D 

> [

(τ (u) − ˆτ (S, u S )) 2]

.

Because this minimax risk considers a restricted class of probability distributions and estimators, it follows directly from the definition that R(H, q ) ≤ R MI (H, q ). In order to show that the conflict graph H characterizes the minimax risk of the original network experiment problem R(G, τ, q ), we will establish a series of reductions between these three minimax problems: 

R(G, τ, q ) ≥ R (H, q ) ≥ R MI (H, q ) ≥ R (G, τ, q ) ,

which will in turn establish equality amongst all three minimax risks. Each inequality will be established in its own lemma. In order to aid in these reductions, we establish some basic propositions which draw the necessary connections between outcomes which are observed under network interventions z ∈ Ω and independent sets in the corresponding conflict graph H. For an intervention z ∈ Ω, define Rz ⊆ VH

to be the subset of exposures which are observed under z, i.e. 

Rz =

> {

ei,k : hi(z) = ei,k 

> }

.

First, we show that each intervention z ∈ Ω corresponds to an independent set in the conflict graph 

H.

Proposition A.5. Fix an interference network G and contrastive estimand τ . For each intervention 

z ∈ Ω, the observed exposures Rz form an independent set in the conflict graph H, i.e. Rz ∈ I (H).Proof. Consider two exposures ei,k , e j,ℓ which are both observed under intervention z ∈ Ω, i.e. 

ei,k , e j,ℓ ∈ Rz . Because they are both observed under z, they cannot be in conflict, by definition. Thus, they are not adjacent in the conflict graph H. This establishes that none of the exposures in 

Rz are adjacent, therefore it is in an independent set. 36 It is worth noting that the reverse direction is not necessarily true, in the following sense: there are independent sets A ∈ I (H) for which there is not intervention z ∈ Ω such that Rz = A. However, the following proposition shows that this reverse direction is true when we restrict A to be a maximal independent set. 

Proposition A.6. Fix an interference network G and contrastive estimand τ . There exists an injective function f : MI (H) → Ω with the following property: each maximal independent set 

A ∈ MI (H) has a corresponding intervention z = f (A) satisfying A = Rz .Proof. Fix a maximal independent set A. We will construct an intervention z ∈ Ω such that Rz = A.Our construction will proceed in an iterative process. Suppose that A has r vertices, which we arbitrarily order as ei1,k 1 , e i2,k 2 . . . e ir ,k r . Initialize an intervention z(0) = (0 , . . . 0), which corresponds to all subject receiving control. For t = 1 . . . r , define the intervention z(t) from z(t−1) by the update: for all j ∈ [n], 

z(t)(j) ←

> {

z(t−1)( j) if j / ∈ ˜N (it)1 if j ∈ ˜N (it) and j ∈ eit,k t

In other words, we iterate over the exposures in A, updating the intervention so that all exposures 

ei1,k 1 . . . e it,k t are realized by intervention z(t). An important aspect in this construction is that if 

z(t)(j) is set to 1 at some iteration t, then it remains set to 1 throughout the process. This is due to the fact that A forms an independent set, and thus all exposures eit,k t ∈ A agree on the treatment assignments of their extended neighborhoods. Once the iterative procedure finishes, we have that all exposures in A are observed simultaneously under intervention z = z(r).The argument above establishes that A ⊆ Rz and requires only that A is an independent set in 

H. In order to establish the reverse containments, i.e. that Rz ⊆ A, we need to invoke maximality of A. But this is now simple: because A ⊆ Rz , the definition of maximality implies that A = Rz .The argument above shows that the function f : MI (H) → Ω with the desired property exists, but it remains to be shown that it is injective. Consider two unique maximal independent set A and 

A′. By maximality of A and A′, there must exist vertices ei,k ∈ A \ A′ and ej,ℓ ∈ A′ \ A which are adjacent and thus in conflict. In this case, the construction above would assign different individual treatments to at least one common neighbor of i and j. It follows that f (A)̸ = f (A′) and thus f is injective. Finally, we need to endow the conflict graph with a labelling scheme so that the moment restrictions and contrastive estimand agree between the network experiment problem and the independent set problems. Given an interference network G and a contrastive effect τ , we will always assign the following labelling to its vertices: exposure ei,k gets the label i + n ∗ k. The following proposition shows that this corresponds to a suitable bijection between the classes M(G, τ, q ) and 

M(H, q ), under which the estimand of interest is also preserved. In a slight abuse of notation, we will always be using the more restricted potential outcome model M0(G, τ, q ) considered in Section A.4, though we suppress this notation for simplicity since the minimax risks are equal. 

Proposition A.7. Fix an interference network G, a contrastive estimand τ , and let H be the correspondingly labelled conflict graph. The function ϕ : M(G, τ, q ) → M (H, q ) given by 

ϕ(y)( i, k ) = yi(ei,k )

is a well-defined bijection satisfying τ (y) = τ (ϕ(y)) .

37 Proof. The proof is essentially a bookkeeping exercise. Both claims follow because the bijection maps the potential outcomes yi(ei, 0) to the first half of vertices labelled 1 . . . n and the potential outcomes yi(ei, 1) to the second half of vertices labelled n + 1 . . . 2n.We are now ready to proceed with the three reductions. Each of these reductions relies on a similar proof strategy, the key structure of which we provide now. Abstractly, we seek to show that the minimax risk for Problem A is greater than that of Problem B. Given a statistical procedure (D, ˆτ ) for Problem A, we construct a statistical procedure ( D∗, ˆτ ∗) for Problem B so that the maximum risk of the second procedure is upper bounded by that of the first, establishing the minimax reduction. Each construction has essentially the following components: • Natural Mapping : We show the existence of a natural mapping f from the sample space in Problem A to the sample space in Problem B. Importantly, the observed information in Problem B completely determines the observed information in the pre-image of f for Problem A. • New Probability Measure : The new probability measure D∗ is defined as the pushforward measure of the original probability measure D under this mapping, i.e. roughly speaking 

D∗(E) = D(f −1(E)). • New Estimator : The new estimator ˆ τ ∗ is defined as the conditional expectation of the original estimator ˆ τ under the pre-image of f , i.e. roughly speaking, ˆ τ ∗(E) = E[ˆ τ (x) | x ∈ f −1(E)]. Because f is such that observed information in Problem B completely determines the observed information in the pre-image of f for Problem A, this is a well-defined estimator for Problem B. • Jensen’s Inequality : An application of Jensen’s inequality shows that the maximum risk of the original procedure ( D, ˆτ ) for Problem A provides an upper bound on the maximum risk of the new procedure ( D∗, ˆτ ∗) for Problem B. Thus, the minimax risk is bounded as desired. We will start with the first reduction, which shows that the minimax risk of the network experiment problem is at most the minimax risk of the independent set problem. 

Lemma A.8. Fix an interference network G, a contrastive estimand τ , and let H be the corre-sponding conflict graph. For every moment parameter q ≥ 2, R(G, τ, q ) ≥ R (H, q ).Proof. Fix a design-estimator pair ( D, ˆτ ) for the network experiment problem, i.e. D ∈ D (Ω) is a probability measure on interventions and ˆ τ ∈ Γ(Ω) is a real-valued measureable function on network outcomes. Our goal is to construct a procedure ( D∗, ˆτ ∗) for the independent set problem, where 

D∗ ∈ D(I(H)) is a probability measure on independent sets and ˆ τ ∗ ∈ Γ( I(H)) is a measureable function on the observations from an independent set. We consider the natural mapping f : Ω → I (H) from network interventions to independent sets given by Proposition A.5; namely, that the intervention z can be mapped to the independent set Rz

of observed exposures in the conflict graph H. Crucially, all interventions z which map to the same independent set Rz provide the same information about the outcomes. Let D∗ be the pushforward measure of D thru f . In particular, the new probability measure assigns the following mass to each independent set: 

D∗(S) = ∑

> z∈Ω

1[f (z) = S] · D (z) = D({z : f (z) = S}) .

38 The new estimator ˆ τ ∗ is defined via taking a conditional expectation of the original estimator ˆ τ

under the original design D where the conditioning event is that {f (z) = S}, i.e. ˆτ ∗(S, u S ) = E Z∼D 

> [

ˆτ (Z, ϕ −1(u)( Z)) | f (z) = S

> ]

,

where ϕ : M(G, τ, q ) → M (H, q ) is the bijection described in Proposition A.7. Note that ˆ τ ∗(S, u S )is in fact a well-defined estimator because the outcomes observed in uS provide enough information to evaluate ϕ−1(u)( z) for each intervention z such that f (z) = S.Finally, let us bound the risk of the new procedure in terms of the old one. Fix functions 

y ∈ M (G, τ, q ) and u ∈ M (H, q ) satisfying ϕ(y) = u. Using Jensen’s inequality, we have that Ez∼D 

> [(

τ (y) − ˆτ (z, y (z)) )2]

≥ Ez∼D 

> [(

τ (y) − E[ˆ τ (z, y (z)) | f (z) = S]

> )2]

= E S∼D ∗

> [(

τ (u) − ˆτ ∗(S, u S )

> )]

,

where the last line used the definition of the new procedures ( D∗, ˆτ ∗) together with the fact that 

τ (y) = τ (u) for u = ϕ(y) (Proposition A.7). Because ϕ is a bijection, we have that the maximum risk of the first procedure ( D, ˆτ ) is an upper bound on the second procedure ( D∗, ˆτ ∗), i.e. sup 

> y∈M (G,τ,q )

Ez∼D [(τ (y) − ˆτ (z, y (z)) )2] ≥ sup 

> u∈M (H,q )

ES∼D ∗ [(τ (u) − ˆτ (S, u S ))2] .

To establish the claim, we consider the infimum over all procedures ( D, ˆτ ) and their mappings to (D∗, ˆτ ∗). We are now ready to proceed to the next reduction. This time, we show that the independent set minimax risk is at least as large as the restricted independent set minimax risk. 

Lemma A.9. Fix a labeled graph H. For any moment parameter q ≥ 2, we have that R(H, q ) ≥RMI (H, q ).Proof. Fix a pair ( D, ˆτ ) for the independent set problem where D ∈ D(I(H)) is a probability measure on independent sets and ˆ τ ∈ Γ( I(H)) is a measureable function of observed outcomes. Our goal is to construct a procedure ( D∗, ˆτ ∗) for maximal independent set problem where D∗ ∈ D(MI (H)) is a probability measure on maximal independent sets and ˆ τ ∗ ∈ Γ( MI (H)) is a measureable function on the observations from maximal independent sets. To this end, consider any mapping f : I(H) → MI (H) that maps independent sets to maximal independent sets and also satisfies the additional inclusion principle: for all independent sets S, we have that S ⊆ f (S). For our purposes, any mapping satisfying the inclusion principle will suffice. Crucially, any such mapping satisfies the following property: the outcomes we observe in a maximal independent set A completely determine all the outcome information we would observed in a subset 

S ⊆ A.Let D∗ be the pushforward measure of D through f . In particular, the new probability measure assigns the following mass to each maximal independent set: 

D ∗ (A) = ∑

> S∈I (H)

1[f (S) = A] · D (S) = D(( {S : f (S) = A}) .

39 The new estimator is defined via taking a conditional expectation of the original estimator ˆ τ

under D where the conditioning event is that {f (S) = A}, i.e. ˆτ ∗(A, u A) = E S∼D 

> [

ˆτ (S, u S ) | f (S) = A

> ]

.

Note that this is indeed a well-defined estimator because the observed outcomes in the larger independent set A provide enough information to evaluate uS for each subset S ⊆ A.Finally, upper bounding the risk of the new procedure in terms of the old one is an application of Jensen’s inequality together with the definition of the newly constructed procedure. For an arbitrary 

u ∈ M (H, q ), we have that ES∼D 

> [

(τ (u) − ˆτ (S, u S )) 2]

≥ ES∼D 

> [(

τ (u) − E[ˆ τ (S, u S ) | f (uS ) = A]

> )2]

= E A∼D ∗

> [(

τ (u) − ˆτ ∗(A, u A)

> )2]

The inequality between minimax risks follows by taking the supremum over outcomes and infimum over procedures. Finally, we arrive at the third reduction. This time, we show that the minimax risk of the network experiment problem is at most the minimax risk of the associated maximal independent set problem. This reduction is more direct and so we do not need to use any conditional expectation nor Jensen’s inequality. 

Lemma A.10. Fix an interference network G, a contrastive estimand τ , and let H be the corre-sponding conflict graph. For every moment parameter q ≥ 2, RMI (H, q ) ≥ R (G, τ, q ).Proof. Fix a procedure ( D, ˆτ ) for the maximal independent set problem where D ∈ D (MI (H)) is a probability measure over maximal independent sets and ˆ τ ∈ Γ( MI (H)) is a real-valued measurable function on maximal independent sets. Our goal is to construct a statistical procedure ( D∗, ˆτ ∗) for the network experimental design problem, where D∗ ∈ D(Ω) is a measure on network interventions and ˆ τ ∗ ∈ Γ(Ω) is a measurable function on the observations. Consider the injective mapping f : MI (H) → Ω. The function has two key properties: first, it is injective so that maximal independent sets are in bijection to a subset of interventions. Second, given a maximal independent set A with intervention f (A) = z, it holds that A is exactly the observed outcomes under intervention z. Thus, the potential outcomes observed under the intervention z

exactly correspond to the vertex outcomes that would be observed under A. This is the crucial property that we need for this final reduction. The new design and estimator are simple: they are essentially reconstructing exactly the procedure ( D, ˆτ ). Fix a network intervention z ∈ Ω. If there exists a maximal independent set A

such that f (A) = z, then assign probability mass D∗(z) = D(A). Otherwise, if no such A exists then D∗(z) = 0. We only need to define the estimator on interventions in the image of f . For these interventions z ∈ f (MI (H)), define the new estimator as ˆτ ∗(z, y (z)) = ˆ τ (A, ϕ (y)A) ,

where A = f (z) and ϕ is the bijection in Proposition A.7. Note that ϕ(y)A can be reconstructed exactly from the information observed in y(z), so that ˆ τ ∗ is indeed a well-defined estimator. 40 Now we show that the risks are equivalent. Consider potential outcome function y ∈ M (G, τ, q )with u = ϕ(y). By the definitions above, we have the equality EA∼D 

> [

(τ (u) − ˆτ (A, u A)) 2]

= E z∼D ∗

> [

(τ (y) − ˆτ (z, y (z))) 2]

,

where we have used that τ (y) = τ (u) from Proposition A.7. Because ϕ is a bijection, the maximum risks of the two procedures are equal: sup 

> u∈M (H,q )

EA∼D 

> [

(τ (u) − ˆτ (u, u A)) 2]

= sup 

> y∈M (G,τ,q )

Ez∼D ∗

> [

(τ (y) − ˆτ (z, y (z))) 2]

.

The final inequality between the minimax risks follows by taking the infimum of both sides and using the mapping from ( D, ˆτ ) 7 → (D∗, ˆτ ∗). After completing these three reductions, we are now ready to prove Proposition 2.2. 

Proof of Proposition 2.2. Fix an interference network G and a constrastive estimand τ and let H

be the resulting conflict graph. Fix a moment parameter q ≥ 2. The lemmas A.8, A.9, and A.10 demonstrate the series of inequalities of the three minimax risks 

R(G, τ, q ) ≥ R (H, q ) ≥ R MI (H, q ) ≥ R (G, τ, q ) ,

which in turn establishes that they are all equal. Thus, we have that the minimax risk of the network experiment R(G, τ, q ) is equal to the minimax risk of the maximum independent set problem 

RMI (H, q ). In this sense, we can take fq = RMI (H, q ). The proof above show that the minimax risk of the network experiment problem can be completely characterized by the conflict graph, albeit through a similarly complex minimax formulation. The reductions themselves show that the structure of the maximal independent sets in H contain the relevant information. 

# B Lower Bounds on the Minimax Risk 

We start by presenting the proof of LeCam’s method in Section B.1. We then continue with the proof of the global lower bound in Section B.2, followed by the proof of the local lower bound in Section B.3. 

B.1 Le Cam’s Method (Proposition 3.1) 

For convenience, we restate the proposition here. 

Proposition 3.1. For any network G, causal effect τ , and moment parameter q, the minimax risk is lower bounded as 

R(G, τ, q ) ≥ inf  

> D

sup 

> H0,H 1

d2(H0, H 1)8 ·

> (

1 − dTV 

> (

Q(D) 

> H0

, Q (D)

> H1
> ))

,

where dTV is the total variation distance and the infimum is taken over all designs D.

41 As we discussed in Section 3.1, the proof proceeds in two steps. In the first step, we argue that if we could solve the estimation problem, we could also test between two different hypotheses about the potential outcome functions. In the second step, we connect the error of the best testing procedure for this problem with the total variation distance between the observed distributions under these two hypotheses. We start by presenting a Lemma regarding the first step. 

Lemma B.1. Let H0, H 1 be two distributions over M(G, τ, q ) supported on finite subsets M0, M 1

respectively. Let 

τ0 = inf 

> y∈M0

τ (y) and τ1 = sup 

> y∈M1

τ (y) .

Assume that τ1 < τ 0. Let (D, ˆτ ) be an arbitrary design-estimator pair for effect τ . Define the test function ψ : {0, 1}n × Rn → { 0, 1} as 

ψ(z, y (z)) = 

> {

0 if ˆτ (z, y (z)) > τ0+τ1

> 2

1 otherwise Then, we have that 

• If y ∈ M0,

D(ψ(Z, y (Z)) = 1) ≤ 4

d(H0, H 1)2 · EZ∼D [(ˆ τ (Z, y (Z)) − τ (y)) 2] .

• If y ∈ M1,

D(ψ(Z, y (Z)) = 0) ≤ 4

d(H0, H 1)2 · EZ∼D [(ˆ τ (Z, y (Z)) − τ (y)) 2] .

Proof. We only prove the first statement, since the second one follows by an analogous argument. First of all, by definition of τ0 and τ1, we have that 

d(H0, H 1) = inf 

> y∈M0,y ′∈M1

|τ (y) − τ (y′)| = τ0 − τ1 .

By definition of ψ, if ψ(z, y (z)) = 1, then ˆτ (z, y (z)) ≤ τ0 + τ1

2 .

For any y ∈ M0, we have that τ (y) ≥ τ0. Therefore, in the event that ψ(z, y (z)) = 1, we have that 

> (

ˆτ (z, y (z)) − τ (y)) ≥ 

> (

τ0 + τ1

2 − τ0

> )2

= d(H0, H 1)2

4 .

Thus, using Markov’s inequality, we have that EZ∼D 

> [

(ˆ τ (Z, y (Z)) − τ (y)) 2]

≥ D (ψ(Z, y (Z)) = 1) · EZ∼D 

> [

(ˆ τ (Z, y (Z)) − τ (y)) 2 | ψ(Z, y (Z)) = 1 

> ]

≥ D (ψ(Z, y (Z)) = 1) · d(H0, H 1)2

4 .

42 Note that in Lemma B.1 the randomness comes only from the treatment assignment Z, while the outcomes are fixed. We are now ready to present the proof of Proposition 3.1, which includes the second step of connecting the testing error with the total variation distance between the observed distributions. 

Proof of Proposition 3.1. We have that 

R(G, τ, q, C ) = inf  

> D,ˆτ

sup 

> y∈M (G,τ,q )

EZ∼D 

[(ˆτ (Z, y (Z)) − τ (y))2]

≥ inf 

> D,ˆτ

( 12 max 

> y∈M0

EZ∼D 

[(ˆτ (Z, y (Z)) − τ (y))2]

+ 12 max 

> y∈M1

EZ∼D 

[(ˆτ (Z, y (Z)) − τ (y))2])

≥ inf 

> D,ˆτ

( 12 Ey∼H0

[

EZ∼D [(ˆ τ (Z, y (Z)) − τ (y)) 2]

]

+ 12 Ey∼H1

[

EZ∼D [(ˆ τ (Z, y (Z)) − τ (y)) 2]

])

≥ d(H0, H 1)2

8 inf 

> D,ˆτ

(

Pr 

> y∼H0,Z ∼D

(

ψ(Z, y (Z)) = 1 

)

+ Pr 

> y∼H1,Z ∼D

(

ψ(Z, y (Z)) = 0 

))

,

where in the last step we used Lemma B.1. Now, define the event E = {(z, y ) : ψ(z, y ) = 1 }.This event depends on the observed data. Recally that Q(D) 

> H0

and Q(D) 

> H1

are the distributions of the observed data under H0 and H1, respectively (see Section 3.1 for the definition). Then, rewriting the last inequality yields 

R(G, τ, q, C ) ≥ d(H0, H 1)2

8 inf 

> D,ˆτ

(

Q(D) 

> H0

(E) + Q(D) 

> H1

(Ec)

)

= d(H0, H 1)2

8 inf 

> D,ˆτ

(

1 −

(

Q(D) 

> H1

(E) − Q(D) 

> H0

(E)

))

≥ d(H0, H 1)2

8 inf 

> D

(

1 − sup 

> A∈F⊗B (Rn)

(

Q(D) 

> H1

(A) − Q(D) 

> H0

(A)

))

where the supremum is taken over all measurable sets A

= d(H0, H 1)2

8 inf 

> D

(

1 − dTV 

(

Q(D) 

> H0

, Q (D)

> H1

))

,

where in the last step we used the definition of the total variation distance. 

B.2 Lower Bound using Global Information (Theorem 3.2) 

Throughout this section, when we refer to distributions over the outcome functions, they will always be supported on a finite number of elements of M(G, τ, q ), as noted in Section 3.1. For a distribution 

H over outcome functions, we use the notation y ∼ H to denote that y is drawn according to H.We recall the notation used in Section 3.2. In particular, the following definition is useful. 

Definition 3. Let p ∈ (0 , 1) and T ⊆ VH. We define the distribution H(p, T ) to be the distribution over outcome functions in M(G, τ, q ) that is sampled in the following way. 43 • First, we sample a uniformly random subset of S ⊆ T , of size ⌊p|T |⌋ .• Then, we set all outcomes for all exposures that are not used to define the effect τ to be 0, i.e. 

yi(z) = 0 for all i ∈ [n] and z ∈ { 0, 1}n such that hi(z) /∈ { ei, 0, e i, 1} .

• For the remaining exposures, we set 

yi(ei,k ) = 

( n 

> |T|

) 1

> q

if ei,k ∈ S and k = 1 

−

( n 

> |T|

) 1

> q

if ei,k ∈ S and k = 0 0 if ei,k /∈ S

In the sequel, we will always assume that p is such that p|T | is an integer, which can always be done by rounding off p.We will denote by M(p, T ) the support of H(p, T ), for any p ∈ (0 , 1) , T ⊆ VH. We start by presenting a few immediate properties of the distribution H(p, T ). 

Lemma B.2. The following are true for all p, r ∈ (0 , 1) , T ⊆ VH, causal effect τ , conflict graph H

and integer q ≥ 2:(i) M(p, T ) ⊆ M (G, τ, q ).(ii) For all y ∈ M (p, T ),

τ (y) = p ·

( |T |

n

)1− 1

> q

.

(iii) 

d(H(p, T ), H (r, T )) = |p − r| · 

( |T |

n

)1− 1

> q

.

Proof. Property (i) follows by noticing that for every y ∈ M (p) we have |yi(ei,k )| ≤ (n/ |T |)1/q for all i ∈ [n] and k ∈ { 0, 1}. Therefore, we have that 1

n

> n

∑

> i=1

(

|yi(ei, 1)|q + |yi(ei, 0)|q)

≤ |T |

n · n

|T | = 1 .

Property (ii) follows by noticing that for every y ∈ M (p, T ), we have that 

τ (y) = 1

n

> n

∑

> i=1

(

yi(ei, 1) + yi(ei, 0)

)

= 1

n

> n

∑

> i=1

(

1[ei, 1 ∈ S] ·

( n

|T |

)1/q 

− 1[ei, 0 ∈ S] ·

(

−

( n

|T |

)1/q )) 

= 1

n · | S| · 

( n

|T |

)1/q 

44 = p · 

> (

|T |

n 

> )1−1
> q

.

Property (iii) follows immediately from the second. For an arbitrary distribution on the outcome functions H and an arbitrary design D, we denote by Q(D) 

> H

the distribution of the observed outcomes and treatment assignment when the potential outcomes are drawn from H and the treatment assignment is drawn from D independently, i.e. the pushforward of the product measure H ⊗ D under the map ( z, y ) 7 → (z, y (z)). Note that the distribution Q(D) 

> H

depends on the design D. Also, for an intervention z ∈ { 0, 1}n, we denote by Q(D) 

> H

(·| z) the conditional distribution of the observed outcomes conditioned on the treatment assignment being equal to z. The next proposition shows that this conditional distribution does not depend on the design D, as long as z has positive probability under D.

Proposition B.3. For any distribution H over the outcome functions, any designs D and any intervention z ∈ { 0, 1}n with D(z) > 0, we have that for any u ∈ Rn,

Q(D) 

> H

(u|z) = Pr 

> y∼H

(y(z) = u) .

Proof. By definition, we have that for any u ∈ Rn,

Q(D) 

> H

(u|z) = Pr( y(Z) = u | Z = z)= Pr( y(Z) = u, Z = z)Pr( Z = z)= Pr y∼H (y(z) = u)D(z)

D(z)using the independence of the sampled y from the sampled Z

= Pr 

> y∼H

(y(z) = u) .

Since the conditional distribution does not depend on the design, we will simply denote it by 

QH (·| z) from now on, i.e. 

QH (u|z) ≜ Pr 

> y∼H

(y(z) = u) .

Property (ii) of Lemma B.2 shows that each distribution H(p, T ) is supported on outcomes that have a fixed value of τ . Therefore, if we could observe all counterfactual outcomes, we could easily distinguish between H(p, T ) and H(q, T ) for any p̸ = q. However, the fundamental problem of causal inference is that we always observe a subset of the outcomes. As defined in Section 3.2, for any z ∈ { 0, 1}n, we denote by Rz the subset of the 2 n exposures that we get to observe when the treatment assignment is z, i.e. 

Rz =

> {

hi(z) : hi(z) ∈ { ei, 0, e i, 1}, i ∈ [n]

> }

.

45 We now state and prove a lemma, which quantifies the tradeoff between separating the two distributions H(p, T ), H (q, T ) and distinguishing between them from the observed outcomes. This is a generalization of Lemma 3.3 that appeared in the main text for the case T = VH.

Lemma B.4. Let z ∈ { 0, 1}n be an intervention, T ⊆ VH an arbitrary subset with |T | ≥ 5 and suppose |Rz ∩ T | ≤ | T |/2. Let H0 = H(1 /2, T ) and H1 = H(1 /2 + δ, T ), where 

δ ≤ 0.026 

√|Rz ∩ T | .

Then, 

dTV (Q(z) 

> H0

, Q (z) 

> H1

) ≤ 78 .

Proof. We will assume that 

δ ≤ C

√|Rz ∩ T |

where C > 0 is a constant that will be determined later according to the constraints. Any point u ∈ Rn that is in the support of Q(z) 

> H0

or Q(z) 

> H1

must have the property that ui ∈{− (n/ |T |)1/q , 0, (n/ |T |)1/q } for all i ∈ [n], since these are all the possible outcomes under both distributions. Let U = y(z) be the random vector of observed outcomes under Q(z) 

> H0

or Q(z) 

> H1

. Let S

be the random set that is sampled under H0 or H1. Then, U is uniquely determined by S ∩ (Rz ∩ T ). Indeed, if we know S ∩ (Rz ∩ T ), then we have 

yi(z) = 



(n/ |T |)1/q if ei, 1 ∈ S ∩ (Rz ∩ T )

−(n/ |T |)1/q if ei, 0 ∈ S ∩ (Rz ∩ T )0 otherwise Hence, U is determined by S ∩(Rz ∩T ). Conversely, if we know U , then we can determine S ∩(Rz ∩T )as follows: 

S ∩ (Rz ∩ T ) = 

{

ei, 1 : i ∈ [n] s.t. yi(z) = ( n/ |T |)1/q }

∪

{

ei, 0 : i ∈ [n] s.t. yi(z) = −(n/ |T |)1/q }

.

Therefore, the distribution of U is the same as the distribution of S ∩ (Rz ∩ T ). This allows us to compute the total variation distance between QH0 (·| z) and QH1 (·| z) as follows: 

dTV (QH0 (·| z), Q H1 (·| z)) = 12

∑ 

> A⊆Rz∩T

∣∣∣Pr 

> H1

(S ∩ (Rz ∩ T ) = A) − Pr 

> H0

(S ∩ (Rz ∩ T ) = A)

∣∣∣

= 12

∑ 

> A⊆Rz∩T

Pr 

> H0

(S ∩ (Rz ∩ T ) = A)

∣∣∣∣∣

Pr H1 (S ∩ (Rz ∩ T ) = A)Pr H0 (S ∩ (Rz ∩ T ) = A) − 1

∣∣∣∣∣ .

Let us now compute these probabilities. For convenience we denote s = |Rz ∩ T | and t = |T |.Suppose |A| = k and we will restrict our attention to k such that |k − s/ 2| ≤ √s/ 2. We will show later that it is enough to restrict to such subsets A.Then, under H0 we have |S| = t/ 2. There are ( t−st/ 2−k

) ways to choose the remaining t/ 2 − k

elements of S that are not in Rz ∩ T , which leads to Pr 

> H0

(S ∩ (Rz ∩ T ) = A) = 

( t−st/ 2−k

)( tt/ 2

) .

46 By assumption, s ≤ t/ 2. This means that t − s ≥ t/ 2, and hence the above probability is positive for all A with |A| ≤ s.On the other hand, under H1 we have |S| = t/ 2 + δ · t = t(1 /2 + δ). There are ( t−st(1 /2+ δ)−k

) ways to choose the remaining t(1 /2 + δ) − k elements of S that are not in Rz ∩ T , which leads to Pr 

> H1

(S ∩ (Rz ∩ T ) = A) = 

( t−st(1 /2+ δ)−k

)( tt(1 /2+ δ)

) .

Note that for this probability to be nonzero, we need t(1 /2 + δ) − k ≤ t − s, since we need to have enough elements outside of Rz ∩ T to choose from. Using the fact that k ≥ s/ 2 − √s/ 2, it is sufficient to have s

2 +

√ s

2 ≤ t

( 12 − δ

)

Also, using the assumption s ≤ t/ 2, it sufficient to have 

t

4 +

√ t

4 ≤ t

( 12 − δ

)

.

Since δ ≤ C, for the above to be satisfied, it is sufficient to have 

C ≤ 14 − 12√t . (4) For the right hand side to be positive, we need t ≥ 5. We will choose C such that (4) holds, so for now we can assume that for all k such that |k − s/ 2| ≤ √s/ 2, both probabilities under H0 and H1

are nonzero. For such a subset A of size k, we have 

∣∣∣∣∣

Pr H1 (S ∩ (Rz ∩ T ) = A)Pr H0 (S ∩ (Rz ∩ T ) = A) − 1

∣∣∣∣∣ =

∣∣∣∣∣( t−st(1 /2+ δ)−k

)( tt(1 /2+ δ)

) ·

( tt/ 2

)( t−st/ 2−k

) − 1

∣∣∣∣∣

=

∣∣∣∣∣∏s−k−1 

> l=0

(t(1 /2 − δ) − l) · ∏k−1 

> l=0

(t(1 /2 + δ) − l)

∏s−k−1 

> l=0

(t/ 2 − l) · ∏k−1 

> l=0

(t/ 2 − l) − 1

∣∣∣∣∣

=

∣∣∣∣∣

> s−k−1

∏

> l=0

(

1 − 2δt t − 2l

)

·

> k−1

∏

> l=0

(

1 + 2δt t − 2l

)

− 1

∣∣∣∣∣

We would like to turn these products into a single exponential factor. To do that, we will utilize the following elementary inequality, which can be proved via taking derivatives. 1 + x = exp( x + b(x)) where |b(x)| ≤ 2x2 for all |x| ≤ 1/2 . (5) To apply this inequality, we need to ensure that the absolute value of each factor in the products is at most 1 /2. For all l ≤ k − 1 we have 

∣∣∣∣∣

2δt t − 2l

∣∣∣∣∣ ≤ 2δt t − 2( k − 1) 

≤ 2δt t − 2( s/ 2 + √s/ 2 − 1) 47 ≤ 2δt t/ 2 − √t + 2 

≤ C 2tt/ 2 − √t + 2 Therefore, we have the following constraint on C.

C ≤ 18 − 14√t + 12t . (6) We will choose C > 0 so that (6) is satisfied. The same exact constraint arises in the case 

l ≤ s − k − 1. Therefore, applying inequality (5) to each factor in the products, we have that 

> s−k−1

∏

> l=0

(

1 − 2δt t − 2l

)

·

> k−1

∏

> l=0

(

1 + 2δt t − 2l

)

= exp (δ1(t) + δ2(t)) ,

where 

δ1(t) = −

> s−k−1

∑

> l=0

2δt t − 2l +

> k−1

∑

> l=0

2δt t − 2lδ2(t) = 

> s−k−1

∑

> l=0

b

( 2δt t − 2l

)

+

> k−1

∑

> l=0

b

( 2δt t − 2l

)

Let us bound |δ1(t)| first. Assume without loss of generality that k ≥ s/ 2, so that k −1 ≥ s−k −1. The other case is symmetric. Then, we have 

|δ1(t)| =

∣∣∣∣∣−

> s−k−1

∑

> l=0

2δt t − 2l +

> k−1

∑

> l=0

2δt t − 2l

∣∣∣∣∣

=

∣∣∣∣∣

> k−1

∑

> l=s−k

2δt t − 2l

∣∣∣∣∣

≤ | 2k − s| · 2δt t − 2( k − 1) 

≤ 2√2sδt t − 2( s + s/ 2 − 1) using k ≤ s/ 2 + √s/ 2

≤ 2√2tt/ 2 − √t + 2 · C . 

We now bound the second error term. 

|δ2(t)| =

∣∣∣∣∣

> s−k−1

∑

> l=0

b

( 2δt t − 2l

)

+

> k−1

∑

> l=0

b

( 2δt t − 2l

)∣∣∣∣∣

48 ≤ 2 · 

> s−k−1∑
> l=0
> (

2δt t − 2l

> )2

+ 2 · 

> k−1∑
> l=0
> (

2δt t − 2l

> )2

≤ 8( s − k)δ2t2 · 1(t − 2( s − k − 1)) 2 + 8 kδ 2t2 · 1(t − 2( k − 1)) 2

≤ 8sδ 2 · t2

(t/ 2 − √t + 2) 2

≤ C2 · 8t2

(t/ 2 − √t + 2) 2 ,

where we have used again the facts k ≤ s/ 2 + √s/ 2 and s ≤ t/ 2. As a result, we have that for any 

k such that |k − s/ 2| ≤ √s,

> ∣∣∣∣∣

Pr H1 (S ∩ (Rz ∩ T ) = A)Pr H0 (S ∩ (Rz ∩ T ) = A) − 1 

> ∣∣∣∣∣

=

> ∣∣∣∣∣

exp 

> (

δ1(t) + δ2(t)

> )

− 1

> ∣∣∣∣∣

≤ exp 

> (

|δ1(t)| + |δ2(t)|

> )

− 1using the fact that |ex − 1| ≤ e|x| − 1 for all x. We would like to establish that 

> ∣∣∣∣∣

Pr H1 (S ∩ (Rz ∩ T ) = A)Pr H0 (S ∩ (Rz ∩ T ) = A) − 1 

> ∣∣∣∣∣

< 12 , (7) which by the previous calculations is implied by the following inequality 

|δ1(t)| + |δ2(t)| < log(3 /2) .

Let us denote 

D(t) = tt/ 2 − √t + 2 .

Then, combining the bounds on |δ1(t)| and |δ2(t)|, it is sufficient to have 2√2D(t)C + 8 D(t)2C2 < log(3 /2) .

Solving this quadratic inequality for C, we have that it is sufficient to have 

C < −√2 + √2 + 8 log(3 /2) 8D(t) . (8) Thus, inequality (7) will hold as long as the three constraints (4), (6) and (8) are satisfied for 

C > 0 and t ≥ 5. Since all the constraints are improving as t increases, it is sufficient to check that they are satisfied for t = 5. Substituting t = 5 into the right hand sides of the three constraints, we get the conditions 

C ≤ 0.026 

C ≤ 0.113 49 C ≤ 0.049 Among these, we choose the most restrictive one, which is C ≤ 0.026. We next show that the contribution of the sets A with |k − s/ 2| ≤ √s to the total variation distance is non-negligible. To do that, it suffices to show that the total probability of such sets under 

H0 is lower bounded by a constant. We can prove this is the case using Chebyshev’s inequality. In particular, we have that 

|A| = ∑ 

> i∈Rz∩T

1[i ∈ S]Let us calculate the expectation and variance of this random variable under H0. By linearity of expectation EH0 [|A|] = ∑ 

> i∈Rz∩T

Pr 

> H0

(i ∈ S) = |Rz ∩ T | · 12 = s

2 ,

using the fact that Pr H0 (i ∈ S) = 1 /2 for all i ∈ Rz ∩ T . We also have Var H0 (|A|) = ∑ 

> i∈Rz∩T

Var H0 (1[i ∈ S]) + ∑ 

> i̸=j∈Rz∩T

Cov H0 (1[i ∈ S], 1[j ∈ S]) = s

4 + s(s − 1) 

( t/ 2( t/ 2 − 1) 

t(t − 1) − 14

)

≤ s

4 .

Therefore, by Chebyshev’s inequality, we have that Pr 

> H0

(∣∣∣|A| − s

2

∣∣∣ ≥

√ s

2

)

≤ Var H0 (|A|)

> s
> 2

≤ 12 .

We are now ready to upper bound the total variation distance. In particular, define the events E, F 

as follows 

E =

{

A :

∣∣∣|A| − s

2

∣∣∣ ≤

√ s

2

}

F =

{

A : Pr 

> H0

(S ∩ (Rz ∩ T ) = A) ≥ Pr 

> H1

(S ∩ (Rz ∩ T ) = A)

}

We have established that Pr H0 (E) ≥ 1/2. This means that Pr H0 (E ∩F ) ≥ 1/4 or Pr H0 (E ∩F c) ≥

1/4. Without loss of generality, let’s assume the first is true, otherwise a similar argument applies. Then, we can write the total variation distance as 

dTV (QH0 (·| z), Q H1 (·| z)) = ∑

> A∈F

Pr 

> H0

(S ∩ (Rz ∩ T ) = A) − Pr 

> H1

(S ∩ (Rz ∩ T ) = A)= ∑

> A∈E∩F

(

Pr 

> H0

(S ∩ (Rz ∩ T ) = A) − Pr 

> H1

(S ∩ (Rz ∩ T ) = A)

)

+ ∑

> A∈Ec∩F

(

Pr 

> H0

(S ∩ (Rz ∩ T ) = A) − Pr 

> H1

(S ∩ (Rz ∩ T ) = A)

)

50 = ∑

> A∈E∩F

Pr 

> H0

(S ∩ (Rz ∩ T ) = A) ·

(

1 − Pr H1 (S ∩ (Rz ∩ T ) = A)Pr H0 (S ∩ (Rz ∩ T ) = A)

)

+ ∑

> A∈Ec∩F

(

Pr 

> H0

(S ∩ (Rz ∩ T ) = A) − Pr 

> H1

(S ∩ (Rz ∩ T ) = A)

)

< 12 · Pr 

> H0

(E ∩ F ) + Pr 

> H0

(Ec ∩ F )= Pr 

> H0

(F ) − 12 · Pr 

> H0

(E ∩ F )

≤ 1 − 12 · 14= 78 .

This concludes the proof. We are now ready to prove Theorem 3.2. 

Proof of Theorem 3.2. Let’s fix an arbitrary subset T ⊆ VH of size t = |T | ≥ 5. Recall that we denote by I(T, H) the largest independent set in the subgraph of the conflict graph H induced by 

T . Assume for now that |I (T, H)| ≤ | T |/2. We define H0 = H(1 /2, T ) and H1 = H(1 /2 + δ, T ) for 

δ = 0 .026 /√|I (T, H)|. By property (i) of Lemma B.2, we have that the support of H0 and H1 is contained in M(G, τ, q ) for any q ≥ 2. Next, we establish that for any z ∈ { 0, 1}n, we have |Rz ∩ T | ≤ |I (T, H)|. It suffices to show that the exposures in Rz ∩ T is an independent set in the induced subgraph H(T ). To see that, note that for any ei,k , e j,l ∈ Rz ∩ T , by definition we have hi(z) = ei,k and hj (z) = ej,l . This means that 

ei,k , e j,l are not connected in the conflict graph H, thus also not connected in the induced subgraph 

H(T ). Thus Rz ∩ T is an independent set of H(T ). Let D be an arbitrary design and lett supp( D) ⊆ { 0, 1}n be the support of D. As we have remarked earlier, all outcomes in the support of H0 and H1 take values in the set 

{− (|T |/n )−1/q , 0, (|T |/n )−1/q } .

Therefore, the observed outcomes y(Z) under H0 or H1 take values in the set 

Un = {− (|T |/n )−1/q , 0, (|T |/n )−1/q }n .

We can write the total variation distance between the observed distributions under H0 and H1 as follows 

dTV (Q(D) 

> H0

, Q (D) 

> H1

) = 12

∑

> z∈{ 0,1}n

∑

> u∈Un

∣∣∣∣∣Pr 

> H1

(Z = z, y (Z) = u) − Pr 

> H0

(Z = z, y (Z) = u)

∣∣∣∣∣

= 12

∑

> z∈supp( D)

D(z) · ∑

> u∈Un

∣∣∣∣∣Pr 

> H1

(y(z) = u|Z = z) − Pr 

> H0

(y(z) = u|Z = z)

∣∣∣∣∣

= ∑

> z∈supp( D)

D(z) · dTV (QH0 (·| z), Q H1 (·| z)) 51 ≤ ∑

> z∈supp( D)

D(z) · 78using Lemma 3.3 and the fact that |Rz ∩ T | ≤ |I (T )| and |I (T )| ≤ | T |/2= 78 .

By property (iii) of Lemma B.2 we have that 

d(H0, H 1) = δ

( |T |

n

)1− 1

> q

= 0.026 

√|I (T )| ·

( |T |

n

)1− 1

> q

.

We will use LeCam’s method (Proposition 3.1) with the same choice of the two distributions H0

and H1 for all designs D. Applying the bound yields 

R(G, τ, q ) ≥ d(H0, H 1)2

8 · inf 

> D

(

1 − dTV (Q(D) 

> H0

, Q (D) 

> H1

)

)

≥ 1.05 · 10 −5 · 1

|I (H)| ·

( |T |

n

)2− 2

> q

.

This proves the desired lower bound for the case |I (T, H)| ≤ | T |/2. We now handle the case 

|I (T, H)| > |T |/2. First of all, by assumption we have that ( ei, 0, e i, 1) ∈ E(H(T )) for all i ∈ [n], which means that any independent set in H contains at most one of the two exposures for each unit. Therefore, we have |Rz | ≤ n. This means that T = VH satisfies |I (T, H)| ≤ | T |/2, so applying the previously obtained bound with T = VH yields 

R(G, τ, q ) ≥ 1.05 · 10 −5 · 22− 2 

> q

· 1

n .

Now, consider an arbitrary subset T ⊆ VH with |I (T, H)| > |T |/2. We have that 1.05 · 10 −5 · 1

|I (T, H)| ·

( |T |

n

)2− 2

> q

≤ 1.05 · 10 −5 · 2

|T | ·

( |T |

n

)2− 2

> q

= 1 .05 · 10 −5 · 2|T |1− 2

> q

n2− 2

> q

≤ 1.05 · 10 −5 · 22− 2 

> q

· 1

n

≤ R (G, τ, q ) ,

where in the last step we used the previously obtained bound for T = VH. Thus, the bound holds for all subsets T ⊆ VH with |T | ≥ 5. The proof is now complete. 

B.3 Lower Bound using Local Information (Theorem 3.4) 

In this section, we present the proof of Theorem 3.4. Let’s recall that for any exposure ei,k ∈ T , we denote by di,k the degree of ei,k in the conflict graph H. We also denote by dmin (T ) the minimum degree of any exposure in T , and by dharm (T, r ) the harmonic mean of the degrees of the exposures in T with parameter r, i.e. 

dmin (T ) = min   

> ei,k ∈T

di,k , dharm (T, r ) = |T |

(∑  

> ei,k ∈T

1/d ri,k 

) 1

> r

.

52 We omit the dependence on H since it is clear from the context. We repeat the statement for completeness. 

Theorem 3.4. Let G be an interference network, τ be a contrastive effect. If n ≥ 5 then for all moment parameters q ≥ 2,

R(G, τ, q ) ≥ 6.6 · 10 −7

n2− 2

> q

sup   

> T⊆VH,|T|≥ 5

min 

(

dmin (H, T ),

√

dharm (H, T, 2 − 2/q ), |T |

)2− 2

> q

.

Before proving Theorem 3.4, we establish an intermediate Lemma that contains the main idea of the argument. It establishes a lower bound on the minimax risk in terms of some tunable parameters 

ci,k .

Lemma B.5. Let G be a graph on n vertices, τ an arbitrary effect and H the conflict graph. Then, for any subset of exposures T ⊆ VH, any integer q ≥ 2, any integers ri,k that satisfy ri,k ≤ di,k for all i ∈ [n], k ∈ { 0, 1} and any positive numbers ci,k ∈ (0 , 1) for i ∈ [n], k ∈ { 0, 1} that satisfy 

∑ 

> ei,k ∈T

ci,k ≥ 1.36 · 10 −3 ,

we have 

R(G, τ, q ) ≥ 2.64 · 10 −6

n2− 2

> q

· min 

(

min   

> ei,k ∈T

ci,k r2− 2 

> q
> i,k

, |T |2− 2

> q

∑  

> ei,k ∈T

ci,k 

)

.

Proof. We define the event Ai,k as the event that i gets exposure ei,k , i.e. 

Ai,k =

{

hi(z) = ei,k 

}

Recall that for each exposure ei,k ∈ T , N (ei,k ) is the set of neighboring exposures of ei,k in 

H. By definition, |N (ei,k )| = di,k . Consider an arbitrary subset Ui,k ⊆ N (ei,k ) of size ri,k for each 

ei,k ∈ T .Let D be an arbitrary design. We distinguish between two cases. 

Case 1: There exists i ∈ [n], k ∈ { 0, 1} with ei,k ∈ T such that D(Ai,k ) ≥ ci,k . We define H0 and 

H1 to be supported on potential outcome functions y(0) , y (1) respectively, where 

y(1)  

> i

(e) = 

( nri,k 

)1/q 

if e = ej, 1 ∈ Ui,k 

−

( nri,k 

)1/q 

if e = ej, 0 ∈ Ui,k 

0 otherwise 

, y(0)  

> i

(e) = 



−

( nri,k 

)1/q 

if e = ej, 1 ∈ Ui,k 

( nri,k 

)1/q 

if e = ej, 0 ∈ Ui,k 

0 otherwise 

.

Let us first verify that these outcome functions satisfy the moment restriction. We have for l ∈ { 0, 1},

> n

∑

> j=1

(∣∣y(l) 

> j

(ej, 1)∣∣q + ∣∣y(l) 

> j

(ej, 0)∣∣q)

= ∑

> e∈Ui,k

(( nri,k 

)1/q )q

= n . 

Therefore, y(0) , y (1) ∈ M (G, q ). Since H0 and H1 are supported on single potential outcome functions, their separation is simply 

d(H0, H 1) = |τ (y(0) ) − τ (y(1) )|

53 = 1

n

∑

> e∈Ui,k

(( nri,k 

)1/q 

−

(

−

( nri,k 

)1/q )) 

= 2 ·

( ri,k 

n

)1− 1

> q

.

Suppose event Ai,k occurs, i.e. we observe the outcome of unit i for exposure ei,k . By definition of the conflict graph, this implies that we do not observe the outcomes of any of the exposures in 

Ui,k . Since H0, H 1 only differ in the outcomes of exposures in Ui,k , they are impossible to distinguish under that event. In other words, for any z ∈ Ai,k , we have QH0 (·| z) = QH1 (·| z) and hence 

dTV (QH0 (·| z), Q H1 (·| z)) = 0 ,

which implies that 

dTV (Q(D) 

> H0

, Q (D) 

> H1

) = ∑

> z∈supp( D)

D(z) · dTV (QH0 (·| z), Q H1 (·| z)) = ∑

> z / ∈Ai,k

D(z) · dTV (QH0 (·| z), Q H1 (·| z)) 

≤ 1 − D (Ai,k ) ≤ 1 − ci,k .

Following the proof of Proposition 3.1 for this particular design D, we have inf  

> ˆτ

Risk( D, ˆτ ) ≥ d2(H0, H 1)8 · (1 − dTV (Q(D) 

> H0

, Q (D) 

> H1

))

≥ 12 ·

( ri,k 

n

)2− 2

> q

· ci,k .

Case 2: For all i ∈ [n], k ∈ { 0, 1} with ei,k ∈ T we have D(Ai,k ) < c i,k . In that case, define the event AT as 

AT =

{

z ∈ { 0, 1}n : ∑ 

> ei,k ∈T

1[Ai,k ]( z) ≤ 2 · ∑ 

> ei,k ∈T

ci,k 

}

=

{

z ∈ { 0, 1}n : |Rz ∩ T | ≤ 2 · ∑ 

> ei,k ∈T

ci,k 

}

.

By Markov’s inequality, we have 

D(AcT ) = D

( ∑ 

> ei,k ∈T

1[Ai,k ] > 2 · ∑ 

> ei,k ∈T

ci,k 

)

≤

E

[∑  

> ei,k ∈T

1[Ai,k ]

]

2 · ∑  

> ei,k ∈T

ci,k 

=

∑  

> ei,k ∈T

D(Ai,k )2 · ∑  

> ei,k ∈T

ci,k 

< 12 .

54 Thus, D(AT ) ≥ 1/2. Now, define H0 = H(1 /2, T ) and H1 = H(1 /2 + δ, T ) where 

δ = 0.026 

√

2 · ∑  

> ei,k ∈T

ci,k 

,

where 0 .026 is the constant from Lemma B.4. Note that for this construction to be well-defined, we need δ < 1/2, which is equivalent to 

∑ 

> ei,k ∈T

ci,k > 2 · 0.026 2 ≈ 1.352 · 10 −3 .

Thus, we choose the constant 1 .36 · 10 −3 in the statement of the lemma to ensure the above inequality is satisfied. By Lemma B.2 

d(H0, H 1) = δ ·

( |T |

n

)1− 1

> q

= 0.026 

√

2 ∑  

> ei,k ∈T

ci,k 

·

( |T |

n

)1− 1

> q

.

For any z ∈ AT , we have 

|Rz ∩ T | ≤ 2 · ∑ 

> ei,k ∈T

ci,k ≤ |T |

2 .

Therefore, applying Lemma B.4, we have that for any z ∈ AT

dTV (QH0 (·| z), Q H1 (·| z)) ≤ 78 .

By the law of total probability, we have 1 − dTV (Q(D) 

> H0

, Q (D) 

> H1

) = ∑

> z∈supp( D)

D(z) · (1 − dTV (QH0 (·| z), Q H1 (·| z))) 

≥ ∑

> z∈AT

D(z) · (1 − dTV (QH0 (·| z), Q H1 (·| z))) 

≥ D (AT ) · (1 − 78 )

≥ 116 .

Therefore, following the proof of Proposition 3.1 for this particular design D, we have inf  

> ˆτ

Risk( D, ˆτ ) ≥ d2(H0, H 1)8 · (1 − dTV (Q(D) 

> H0

, Q (D) 

> H1

))

≥ 0.026 2

28 · ∑  

> ei,k ∈T

ci,k 

·

( |T |

n

)2− 2

> q

≥ 2.64 · 10 −6

∑  

> ei,k ∈T

ci,k 

( |T |

n

)2− 2

> q

.

55 Since any possible design D falls into either Case 1 or Case 2, we can combine the lower bounds from both cases (inequalities B.3 and B.3) to obtain a lower bound on the minimax risk as follows 

R(G, τ, q ) ≥ 2.64 · 10 −6 · min 

(

min   

> ei,k ∈T

ci,k 

( ri,k 

n

)2− 2

> q

, 1

∑  

> ei,k ∈T

ci,k 

( |T |

n

)2− 2 

> q

)

We are now ready to prove Theorem 3.4. The general strategy will be to invoke Lemma B.5 with the appropriate parameters. As we shall see, this choice depends crucially on the properties of each subset T of exposures. 

Proof of Theorem 3.4. We distinguish between various cases based on the properties of the subset 

T .

Case 1: Suppose dmin (T ) < |T |. In that case, it suffices to prove that 

R(G, τ, q ) ≥ 2.64 · 10 −6

n2− 2

> q

· min 

( dmin (T )2− 2

> q

4 , |T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

)

.

Suppose emin ∈ T is an exposure of miminum degree dmin (T ) in T . Set ri,k = di,k for all ei,k ∈ T .Let 

t∗ = 1

n2− 2

> q

· min 

( min ei,k ∈T r2− 2

> q
> i,k

4 , |T |1− 1

> q

√∑   

> ei,k ∈T1
> r2−2
> qi,k

)

ci,k = t∗( nri,k 

)2− 2

> q

We will invoke Lemma B.5 with the above choice of ri,k and ci,k . Let us first check that the ci,k 

satisfy the required assumptions of Lemma B.5. We have 

ci,k ≤ 1

n2− 2

> q

·

( nri,k 

)2− 2

> q

· min ei,k ∈T r2− 2

> q
> i,k

4 ≤ 1 .

Furthermore, we have 

∑ 

> ei,k ∈T

ci,k = t∗ · n2− 2 

> q

· ∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

= min 

( min ei,k ∈T r2− 2

> q
> i,k

4 , |T |1− 1

> q

√∑   

> ei,k ∈T1
> r2−2
> qi,k

)

· ∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

56 = min 

( min ei,k ∈T r2− 2

> q
> i,k

4

∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

, |T |1− 1 

> q

·

√√√√∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

)

From here, we can lower bound the quantity as follows. 

∑ 

> ei,k ∈T

ci,k ≥ 14 ≥ 1.36 · 10 −3 ,

where we lower bounded the sum by the term corresponding to the exposure emin .We can also upper bound the sum as follows. 

∑ 

> ei,k ∈T

ci,k ≤ min ei,k ∈T r2− 2

> q
> i,k

4

∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

≤ 14 · | T | .

Therefore, all assumptions of Lemma B.5 are satisfied. Invoking the lemma, we get 

R(G, τ, q ) ≥ 2.64 · 10 −6

n2− 2

> q

· min 

(

min   

> ei,k ∈T

ci,k r2− 2 

> q
> i,k

, |T |2− 2

> q

∑  

> ei,k ∈T

ci,k 

)

= 2.64 · 10 −6

n2− 2

> q

· min 

(

t∗n2− 2 

> q

, |T |2− 2

> q

t∗n2− 2 

> q

· ∑   

> ei,k ∈T1
> r2−2
> qi,k

)

≥ 2.64 · 10 −6 · min 

(

t∗, |T |1− 1

> q

n2− 2 

> q

·

√∑   

> ei,k ∈T1
> r2−2
> qi,k

)

≥ 2.64 · 10 −6 · t∗ ,

where we used the definition of t∗ in the last two steps. Thus, our claim is complete in that case. 

Case 2: Suppose dmin (T ) ≥ | T |. In that case, it suffices to prove that 

R(G, τ, q ) ≥ 6.6 · 10 −7

n2− 2

> q

min 

(

|T |2− 2 

> q

, |T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

)

. (9) We distinguish between two sub-cases. • Case 2.1: Suppose 

|T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

≤ | T |2− 2 

> q

,

57 which can equivalently written as 

|T |1− 1

> q

√√√√∑ 

> ei,k ∈T

1

d2− 2

> q
> i,k

≥ 1 . (10) In that case, set ri,k = di,k for all ei,k ∈ T and define 

t∗ = 1

n2− 2

> q

min 

( |T |2− 2

> q

4 , |T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

)

ci,k = t∗( ndi,k 

)2− 2

> q

We will show that the ci,k satisfy the assumptions of Lemma B.5. We have 

ci,k ≤ 1

n2− 2

> q

( ndi,k 

)2− 2

> q

· |T |2− 2

> q

4 ≤ dmin (T )2− 2

> q

4d2− 2

> q
> i,k

≤ 1 ,

where we have used the fact that |T | ≤ dmin (T ). Furthermore, 

∑ 

> ei,k ∈T

ci,k = t∗ · n2− 2 

> q

· ∑ 

> ei,k ∈T

1

d2− 2

> q
> i,k

= min 

( |T |2− 2

> q

4 · ∑ 

> ei,k ∈T

1

d2− 2

> q
> i,k

, |T |1− 1 

> q

·

√√√√∑ 

> ei,k ∈T

1

d2− 2

> q
> i,k

)

.

We can lower bound the sum as 

∑ 

> ei,k ∈T

ci,k ≥ 14 ≥ 1.36 · 10 −3 ,

where in the last step we used (10). We can also upper bound the sum as follows. 

∑ 

> ei,k ∈T

ci,k ≤ |T |2− 2

> q

4

∑ 

> ei,k ∈T

1

d2− 2

> q
> i,k

≤ dmin (T )2− 2

> q

4

∑ 

> ei,k ∈T

1

d2− 2

> q
> i,k

≤ 14 · | T | ,

where we used the fact that |T | ≤ dmin (T ) in the last step. Thus, all assumptions of Lemma B.5 are satisfied. Invoking the lemma, we get 

R(G, τ, q ) ≥ 2.64 · 10 −6

n2− 2

> q

· min 

(

min   

> ei,k ∈T

ci,k d2− 2 

> q
> i,k

, |T |2− 2

> q

∑  

> ei,k ∈T

ci,k 

)

58 ≥ 2.64 · 10 −6

n2− 2

> q

· min 

(

t∗n2− 2 

> q

, |T |2− 2

> q

t∗n2− 2 

> q

· ∑   

> ei,k ∈T1
> d2−2
> qi,k

)

using the fact that dmin (T ) ≥ | T |≥ 2.64 · 10 −6 · min 

(

t∗, |T |1− 1

> q

n2− 2 

> q

·

√∑   

> ei,k ∈T1
> d2−2
> qi,k

)

≥ 2.64 · 10 −6 · t∗ ,

where we used the definition of t∗ in the last two steps. Inequality (9) now follows. • Case 2.2: Suppose 

|T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

> |T |2− 2 

> q

,

which can equivalently be written as 

|T |1− 1

> q

√√√√∑ 

> ei,k ∈T

1

d2− 2

> q
> i,k

< 1 .

We will show that we can pick ri,k ≤ di,k with ri,k ≥ | T | for all ei,k ∈ T such that 1 ≤ | T |2− 2

> q

∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

≤ 2 (11) Indeed, we can achieve this by iteratively picking ei,k ∈ T with ri,k > |T | and setting 

ri,k = ri,k − 1 until the left-hand side of (11) is satisfied. We first prove that this process will terminate in a finite number of steps and ri,k ≥ | T | at termination. Indeed, suppose we reach the point where ri,k = |T | for all ei,k ∈ T . Then, we have 

|T |2− 2

> q

∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

= |T |2− 2

> q

∑ 

> ei,k ∈T

1

|T |2− 2

> q

= |T | ≥ 1 .

Therefore, the process will terminate at some point and at termination we have ri,k ≥ | T |

for all ei,k ∈ T . Furthermore, consider a single update step where we pick ei,k ∈ T and set 

ri,k = ri,k − 1. The value of the objective changes by at most 

|T |2− 2 

> q

·

( 1(ri,k − 1) 2− 2

> q

− 1

r2− 2

> q
> i,k

)

≤ 159 since ri,k > |T | by definition of the process. Thus, the objective increases by at most 1 at every update. Therefore, the first time the objective exceeds 1, (11) will be satisfied. From now on, let ri,k be the values obtained at termination of the above process. We set 

t∗ = 1

n2− 2

> q

min 

( min ei,k ∈T r2− 2

> q
> i,k

4 , |T |1− 1

> q

√∑   

> ei,k ∈T1
> r2−2
> qi,k

)

ci,k = t∗( nri,k 

)2− 2

> q

We will show that the ci,k satisfy the assumptions of Lemma B.5. We have 

ci,k ≤ 1

n2− 2

> q

( nri,k 

)2− 2

> q

· min ei,k ∈T r2− 2

> q
> i,k

4 ≤ 1 .

Furthermore, 

∑ 

> ei,k ∈T

ci,k = t∗ · n2− 2 

> q

· ∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

= min 

( min ei,k ∈T r2− 2

> q
> i,k

4 · ∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

, |T |1− 1 

> q

·

√√√√∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

)

≥ min 

(

|T |2− 2 

> q

· ∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

, |T |1− 1 

> q

·

√√√√∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

)

≥ 1.36 · 10 −3 ,

where in the last step we used (11). We can also upper bound the sum as follows. 

∑ 

> ei,k ∈T

ci,k ≤ min ei,k ∈T r2− 2

> q
> i,k

4

∑ 

> ei,k ∈T

1

r2− 2

> q
> i,k

≤ 14 · | T | ,

Thus, all assumptions of Lemma B.5 are satisfied. Invoking the lemma, we get 

R(G, τ, q ) ≥ 2.64 · 10 −6

n2− 2

> q

· min 

(

min   

> ei,k ∈T

ci,k r2− 2 

> q
> i,k

, |T |2− 2

> q

∑  

> ei,k ∈T

ci,k 

)

≥ 2.64 · 10 −6 · min 

(

t∗, |T |1− 1

> q

n2− 2 

> q

·

√∑   

> ei,k ∈T1
> r2−2
> qi,k

)

≥ 2.64 · 10 −6 · t∗ . (12) 60 Now all we have to do is connect t∗ to the desired lower bound of (9). This is because the lower bound is in terms of the original degrees, whereas in t∗ some degrees have been “truncated”. We have 

t∗ = 1

n2− 2

> q

min 

( min ei,k ∈T r2− 2

> q
> i,k

4 , |T |1− 1

> q

√∑   

> ei,k ∈T1
> r2−2
> qi,k

)

≥ 1

n2− 2

> q

min 

( |T |2− 2

> q

4 , |T |1− 1

> q

√∑   

> ei,k ∈T1
> r2−2
> qi,k

)

≥ 1

n2− 2

> q

min 

( |T |2− 2

> q

4 , |T |2− 2

> q

√2

)

where we use (11) = 141

n2− 2

> q

|T |2− 2

> q

≥ 141

n2− 2

> q

min 

(

|T |2− 2 

> q

, |T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

)

Plugging this back to (12), we obtain 

R(G, τ, q ) ≥ 2.64 · 10 −6

4 · 1

n2− 2

> q

min 

(

|T |2− 2 

> q

, |T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

)

,

which establishes the desired lower bound in that case. We have established the desired lower bound in all cases, therefore the proof is complete. We now give the proof of Corollary 3.5, which is restated here for completeness. Recall that in Section 3.3 we defined FH to be the degree distribution function of the conflict graph H. We also defined the critical degree of a conflict graph H with respect to a moment q ≥ 2 as follows 

d∗ 

> q

(H) = sup 

{

d ∈ [n] : 1 − FH(d) ≥ d 2q−23q−2

n

}

.

Corollary 3.5. Let G be an interference network, τ be a contrastive effect. For all moment parameters q ≥ 2, we have 

R(G, τ, q ) ≥ 6.6 · 10 −7 · (d∗ 

> q

(H)) 4( q−1) 2

> q(3 q−2)

n2− 2

> q

.

61 Proof. By definition of d∗ 

> q

(H), there exist a subset of exposures T ⊆ VH with |T | ≥ (d∗ 

> q

(H)) 2( q−1) 3q−2

such that di,k ≥ d∗ 

> q

(H) for all ei,k ∈ T . Therefore, we can invoke Theorem 3.4 with the above choice of T to get 

R(G, τ, q ) ≥ 6.6 · 10 −7

n2− 2

> q

· min 

(

|T |2− 2 

> q

, |T |1− 1

> q

√∑   

> ei,k ∈T1
> d2−2
> qi,k

, d min (T )2− 2

> q

)

≥ 6.6 · 10 −7

n2− 2

> q

· min 

(

|T |2− 2 

> q

, |T |1− 1

> q

√ |T |  

> (d∗
> q(H)) 2−2
> q

, (d∗ 

> q

(H)) 2− 2

> q

)

= 6.6 · 10 −7

n2− 2

> q

· min 

(

|T |2− 2 

> q

, |T | 12 − 1 

> q

· (d∗ 

> q

(H)) 1− 1 

> q

, (d∗ 

> q

(H)) 2− 2

> q

)

≥ 6.6 · 10 −7

n2− 2

> q

· min 

(

(d∗ 

> q

(H)) 2q−23q−2 (2 − 2 

> q)

, (d∗ 

> q

(H)) 2q−23q−2 ( 12 − 1   

> q)+1 −1
> q

, (d∗ 

> q

(H)) 2− 2

> q

)

= 6 .6 · 10 −7 · (d∗ 

> q

(H)) 4( q−1) 2

> q(3 q−2)

n2− 2

> q

.

Finally, we present the proof of Proposition B.6 that lower bounds the critical degree by the average degree of the conflict graph H.

Proposition B.6. Let G be a graph and τ a contrastive effect, with H being the associated conflict graph. We denote by davg (H) the average degree of H. Then, for any q ≥ 2, we have 

d∗ 

> q

(H) ≥ davg (H)4 .

Proof. Let A, B ⊆ VH be the subsets of exposures that have degree at least d∗ 

> q

(H) + 1 and at most 

d∗ 

> q

(H), respectively, i.e. 

A =

{

ei,k ∈ VH : di,k ≥ d∗ 

> q

(H) + 1 

}

B =

{

ei,k ∈ VH : di,k ≤ d∗ 

> q

(H)

}

.

Clearly A and B partition VH. By definition of the critical degree, we have 

|A| ≤ (d∗ 

> q

(H) + 1) 2q−23q−2 .

Thus, we have that 

davg (H) = 

∑  

> ei,k ∈VH

di,k 

|VH|

=

∑  

> ei,k ∈A

di,k + ∑  

> ei,k ∈B

di,k 

|VH|

62 ≤ |A|n + |B|d∗ 

> q

(H)

|VH|≤ 2n(d∗ 

> q

(H) + 1) 2q−23q−2 + 2 nd ∗ 

> q

(H)

|VH|

= ( d∗ 

> q

(H) + 1) 2q−23q−2 + d∗ 

> q

(H)

≤ 2 2q−23q−2 · (d∗ 

> q

(H)) 2q−23q−2 + d∗ 

> q

(H)

≤ 4d∗ 

> q

(H) ,

using the fact that 2 2q−23q−2 ≤ 3 for all q ≥ 2. Rearranging the above inequality, we get the conclusion. 

# C Upper Bounds on the Minimax Risk 

In this section, we present the proofs of our general upper bounds on the minimax risk. We start by presenting the analysis of the generalized Conflict Graph Design in Section C.1 and then continue to the optimal bias-variance tradeoff in Section C.2. 

C.1 Analysis of the Conflict Graph Design (Theorem 4.1) 

In this section, we present the proof of Theorem 4.1, which we restate here for convenience. 

Theorem 4.1. Let G be an interference network, τ be a contrastive effect. For any moment parameter q ≥ 2, the minimax risk is bounded as 

R(G, τ, q ) ≤ 8.92 · λ(H) 2 

> q

· davg (H)1− 2

> q

n ,

where λ(H) is the largest eigenvalue of the adjacency matrix of H and davg (H) is the average degree of H. Moreover, this bound on the minimax risk is achieved by an unbiased statistical procedure. 

For q = 2, this upper bound was established in Kandiros et al. (2024). In particular, they introduced the Conflict Graph Design (CGD) and provided an unbiased estimator which achieves a worst case variance of O(λ(G)/n ). We will follow the general strategy of the Conflict Graph Design, although as we will see there are important obstacles to overcome for general moments. We now provide a roadmap for this section. • In Section C.1.1, we introduce the importance ordering that will be used for our design and establish a crucial property. In Section C.1.2 we describe the design for arbitrary moments q.• In Section C.1.3 we introduce the estimator and prove some basic properties of it. • In Section C.1.4 we bound the covariance terms that arise in the variance of the estimator. • In Section C.1.5 we bound suitable norms of matrices that are related to the covariance matrix of the design. • In Section C.1.6 we present the proof of Theorem 4.1 using all the above ingredients. 63 Throughout this section, we will denote by G the interference graph, τ the effect of interest, H

the conflict graph associated with G and τ . For a node ei,k ∈ V (H), we will denote by N (ei,k ) the set of neighbors of ei,k in H. We denote by di,k = |N (ei,k )| the degree of node ei,k in the conflict graph H. Also, for a unit i ∈ V (G), we denote by ˜N (i) the set consisting of i and its neighbors in G.

C.1.1 Importance Ordering 

An importance ordering is a bijection π : V (H) 7 → [2 n] that “ranks” the vertices in the conflict graph H from the most important to the least important. For an importance ordering π and a node 

ei,k ∈ V (H), we denote by N πb (ei,k ) the set of neighbors of ei,k in H that appear before ei,k in the importance ordering π, i.e. 

N πb (ei,k ) = {ej,l ∈ N (ei,k ) : π(ej,l ) < π (ei,k )} .

In Kandiros et al. (2024), they proposed an importance ordering based on the maximum eigenvector in the graph that ensures every node has a bounded number of more important neighbors. This property was crucial for upper bounding the variance of their design. We will show that there is a different ordering that behaves more favorably for general moments 

q. We first provide pseudocode for the procedure that produces the ordering in Algorithm 1. 

Algorithm 1: Importance Ordering 

Input: Conflict graph H = ( VH, E H) and moment q.

Output: Mapping π : VH → [2 n] representing the importance ordering.  

> 1

S ← VH 

> 2

for ei,k ∈ VH do  

> 3

Set pi,k ← 12λ(H) 2   

> qd1−2
> qi,k
> 4

for c = 1 to 2n do  

> 5

for ei,k ∈ S do  

> 6

wi,k ← ∑   

> ej,l ∈N (ei,k )∩S

pj,l  

> 7

ei∗,k ∗ ← arg min ei,k ∈S wi,k  

> 8

π(ei∗,k ∗ ) = 2 n − c + 1  

> 9

S ← S \ { ei∗,k ∗ }

The ordering π in Algorithm 1 is constructed iteratively, starting from the least important node and ending with the most important node. At each step, S represents the set of nodes that have not been ordered yet. We pick the node whose neighbors in S have the smallest sum of pi,k values and assign it the next available position in the ordering. In the case q = 2, we observe that the ordering of Algorithm 1 is constructed by iteratively selecting the node with smallest degree in the remaining induced graph, placing them in the last available spot in the ordering and removing them from the graph. This ordering of the nodes is used in the classical proof of Wilf’s Theorem (Wilf, 1967), which states that the chromatic number of a graph is upper bounded by the largest eigenvalue of its adjacency matrix. In Kandiros et al. (2024), they use a different ordering based on the leading eigenvector of the adjacency matrix, even 64 though they acknowledge that the ordering based on the smallest induced degree also satisfies the desired property. Since the value of the pi,k variables is different for different values of q, the ordering produced by Algorithm 1 will be different for different values of q. Indeed, the ordering for q = ∞ is determined by the degrees in the original graph, while for q = 2 it is constructed using the degrees in the induced subgraph that is iteratively produced. Thus, the ordering could be very different for different values of q.In the next lemma, we establish the crucial property of the ordering produced by Algorithm 1 that will be used in the analysis of our design later. 

Lemma C.1. Let H be a conflict graph with maximum eigenvalue λ(H). Let q ≥ 2 be an integer. For every node ei,k ∈ V (H) with degree di,k in the conflict graph H, let 

pi,k = 12λ(H) 2 

> q

d1− 2

> q
> i,k

.

Suppose π : VH → [2 n] is the ordering of the vertices in H that is produced by Algorithm 1 with input H and q. Then, for every node ei,k ∈ VH

∑   

> ej,l ∈N πb(ei,k )

pj,l ≤ 1/2 .

Proof. For an arbitrary subset S ⊆ [2 n], we denote by di,k (S) the number of neighbors of ei,k in S,i.e., di,k (S) = |N (ei,k ) ∩ S|. Also, for every node ei,k , we define the quantity 

wi,k (S) = ∑  

> ej,l ∈N (ei,k )∩S

pj,l 

We will show that for every subset S, there exists a node ei,k ∈ S with wi,k (S) ≤ 1/2. It suffices to show that the average value of wi,k (S) over all ei,k ∈ S is at most 1 /2. Indeed, we have 1

|S|

∑ 

> ei,k ∈S

wi,k (S) = 1

|S|

∑ 

> ei,k ∈S

∑  

> ej,l ∈N (ei,k )∩S

pj,l 

= 1

|S|

∑ 

> ei,k ∈S

∑  

> ej,l ∈N (ei,k )∩S

12λ(G)2/q d1−2/q j,l 

= 1

|S|

∑

> ej,l ∈S

∑  

> ei,k ∈N (ej,l )∩S

12λ(G)2/q d1−2/q j,l 

by exhanging the order of summation of i and j

= 1

|S|

∑

> ej,l ∈S

dj,l (S)2λ(G)2/q d1−2/q j,l 

≤ 1

|S|

∑

> ej,l ∈S

dj,l (S)2λ(G)2/q dj,l (S)1−2/q 

65 using the fact that dj,l (S) ≤ dj,l for all S by definition = 1

|S|

∑

> ej,l ∈S

dj,l (S)2/q 

2λ(G)2/q 

= 12λ(G)2/q 

1

|S|

∑

> ej,l ∈S

dj,l (S)2/q 

≤ 12λ(G)2/q 

( 1

|S|

∑

> ej,l ∈S

dj,l (S)

)2/q 

using Jensen’s inequality for the function x 7 → x2/q , which is concave for q ∈ (2 , ∞)

≤ 12λ(G)2/q 

(λ(G))2/q 

using the fact that the average degree of any induced subgraph of G is at most the maximum eigenvalue = 1 /2 .

We now use this property to analyze Algorithm 1. Let ei,k ∈ VH be an arbitrary node. We know that ei,k will be selected exactly once as ej∗,l ∗ in line 7, since this line is executed exactly |VH| times and every time a different node is selected. Let c be the iteration in which ei,k is selected as ej∗,l ∗ .When line 8 is executed in that iteration, we have π(ei,k ) = |VH| − i + 1. Moreover, at that point the set S contains exactly the nodes that are more important than ei,k in the ordering π, since all the less important nodes have already been selected in previous iterations and removed from S.Thus, we have that N πb (ei,k ) = N (ei,k ) ∩ S. Therefore, 

∑   

> ej,l ∈N πb(ei,k )

pj,l = ∑  

> ej,l ∈N (ei,k )∩S

pj,l = wi,k (S) .

By the property we established above, we have 

∑   

> ej,l ∈N πb(ei,k )

pj,l = wi,k (S) = min  

> ej,l ∈S

wj,l (S) ≤ 1

|S|

∑

> ej,l ∈S

wj,l (S) ≤ 1/2 ,

as desired. Since ei,k was an arbitrary node, this establishes the desired property for all nodes in the graph. In the limit q = ∞, Lemma C.1 provides a way of selecting the ordering. However, it turns out that in this special case there is a alternative way of ordering the nodes, which is simply to order them in decreasing order of degree. We present this property as a separate lemma for completeness. 66 Lemma C.2. For every node ei,k ∈ V (H) with degree di,k in the conflict graph H, let 

pi,k = 12di,k 

.

Suppose π : VH → [2 n] is the ordering of the vertices in decreasing value of their degree in H , i.e. 

di,k ≤ dj,l if and only if πi,k ≥ πj,l . Then, for every node ei,k 

∑   

> ej,l ∈N πb(ei,k )

pj,l ≤ 1/2 .

Proof. Let ei,k be an arbitrary node in VH. By definition of the ordering, we have that for every 

ej,l ∈ N πb (ei,k ) it holds that dj,l ≥ di,k . Thus, we have 

∑   

> ej,l ∈N πb(ei,k )

pj,l = ∑   

> ej,l ∈N πb(ei,k )

12dj,l 

≤ ∑   

> ej,l ∈N πb(ei,k )

12di,k 

≤ di,k 

2di,k 

= 1 /2 ,

as desired. 

C.1.2 Conflict Graph Design for General Moment Restrictions 

Since Theorem 4.1 aims to upper bound the minimax risk, it suffices to present a design-estimator pair whose risk is upper bounded by the desired quantity. We start by providing the details of our design for arbitrary moments q, given in Algorithm 2. 

Algorithm 2: Conflict Graph Design (CGD) 

Input: Maximum eigenvalue λ(H), graph G, conflict graph H and moment q.

Output: Random intervention Z ∈ Z = {0, 1}n, variables Ui for all i ∈ V (H).  

> 1

Importance ordering π ← Algorithm 1 with input H and q ; 

> 2

for ei,k ∈ V (H) do  

> 3

pi,k ← 12λ(H) 2  

> qd1−2
> qi,k

; 

> 4

C ← 2 ;  

> 5

Sample desired exposure variables {Ui,k : ei,k ∈ V (H)} independently and identically as 

Ui,k ←

{e with probability pi,k 

> C

∗ with probability 1 − pi,k 

> C

Initialize the intervention vector Z ← 0 ; 

> 6

for ei,k ∈ V (H) do  

> 7

if Ui,k = e and Ur,l = ∗ for all er,l ∈ N πb (ei,k ) then  

> 8

Update intervention vector: set Zj = 1[j ∈ hi(ei,k )] for all j ∈ ˜N (i) ; The design takes as input an importance ordering π, which is simply a total ordering of the vertices in the graph G, the maximum eigenvalue λ(H) of the adjacency matrix of the conflict graph 

H, the graph G itself and the moment q. Note that even though the goal is to generate the random 67 intervention Z ∈ { 0, 1}n, the algorithm also outputs the variables Ui,k for all ei,k ∈ V (H). This is because this additional randomness, which is correlated with the treatment assignment, will be useful for defining the estimator. Thus, this falls under the category of a randomized experimental design , as was discussed in Section 2.2 and defined in Section A.3. The general idea of the design is that every exposure ei,k in the conflict graph is assigned some random variable Ui,k , which determines whether we “desire to be observe” this exposure. As dictated in Line 7, if we desire to observe the outcome under exposure ei,k (i.e. Ui,k = e), and its more important neighbors in the conflict graph are not desired to be observed (i.e. Ur,l = ∗ for all er,l ∈ N πb (ei,k )), then we set the intervention Z in such a way that the exposure ei,k is indeed observed (Line 8). The success of this design relies on the fact that we get to see the outcomes of all exposures with large enough probability. In turn, this depends on the importance ordering π that is chosen. Indeed, if an exposure has many more important neighbors, it is unlikely that it will ever be “allowed” to be observed. We will use the importance ordering produced by Algorithm 1. As we will see, the property established in Lemma C.1 for this ordering will be crucial for showing that every exposure gets observed with large enough probability. 

C.1.3 Effect Estimator 

As we alluded to earlier, our estimator will use the residual randomness of the variables Ui,k . To that end, for every node ei,k ∈ V (H), define the event Ei,k as 

Ei,k =

> {

Ui,k = e and Uj,l = ∗ for all ej,l ∈ N πb (ei,k )

> }

.

This is the event that we desire to observe exposure ei,k and all of its more important neighbors “allow” it to do so. We note that similar events were used in the definition of the estimator by Kandiros et al. (2024). The first observation is that if the event Ei,k occurs, then in the assignment Z that is produced by the design, the outcome of exposure ei,k will be observed for unit i. A similar fact was established as Lemma 3.2 in Kandiros et al. (2024). We give here the statement and proof for completeness. 

Lemma C.3. For every node ei,k ∈ V (H), if the event Ei,k occurs, then if Z ∈ { 0, 1}n is the assignment that is produced by the design of Algorithm 2, hi(Z) = ei,k , i.e. unit i receives the exposure ei,k .Proof. Suppose event Ei,k occurs. Then, by definition, the conditions in line 7 will be satisfied for node ei,k , so that line 8 in Algorithm 2 will be executed for node ei,k . This means that after this execution, the intervention vector Z will be such that Zj = 1[j ∈ hi(ei,k )] for all j ∈ ˜N (i). Thus, after this execution vector Z satisfies hi(Z) = ei,k .We now argue that the values of Z for j ∈ ˜N (i) will not change for the remainder of the algorithm. To see this, note that in order for Z to change, line 15 would have to be executed for some different node er,l ∈ V (H) that comes after ei,k in the importance ordering. If the assignment of Z for some 

j ∈ ˜N (i) changes, this means that j ∈ ˜N (i) ∩ ˜N (r) and 1[j ∈ hi(ei,k )] ̸ = 1[j ∈ hr(er,l )]. By the definition of the conflict graph H, this means that ei,k and er,l are neighbors in H. In order for line 15 to be executed for node er,l , the conditions in line 14 must be met. In particular, we should have Ui,k = ∗, since ei,k is a more important neighbor of er,l in H. However, event Ei,k implies that 68 Ui,k = e, so this is a contradiction. Therefore, the values of Z for j ∈ ˜N (i) will not change, therefore at the end of Algorithm 2 we will have hi(Z) = ei,k , as desired. Lemma C.3 shows that if event Ei,k occurs, then we get to observe the outcome for exposure 

ei,k . Thus, a natural estimator for the effect τ would be to do inverse propensity weighting based on the events Ei,k . The estimator that arises was referred to as the modified Horvitz-Thompson estimator in Kandiros et al. (2024) and is defined as follows. ˆτ = 1

n 

> n∑
> i=1
> (

1[Ei, 1]Pr( Ei, 1) − 1[Ei, 0]Pr( Ei, 0)

> )

· Yi . (13) Note that ˆ τ crucially depends on the residual randomness of the variables Ui,k , since the events Ei,k 

are defined in terms of the Ui,k . Therefore, strictly speaking ˆ τ is a randomized estimator. As in Kandiros et al. (2024), let us first establish that ˆ τ is unbiased for τ .

Lemma C.4. For any y ∈ M (G), the estimator ˆτ is unbiased for τ , i.e. E[ˆ τ ] = τ .Proof. By Lemma C.3 and the fact that y ∈ M (G), we have that if Ei,k occurs, then hi(Z) = ei,k ,so that 

yi(Z) = yi(hi(Z)) = yi(ei,k ) .

Thus, we have E[ˆ τ ] = 1

n 

> (n∑
> i=1

Pr( Ei, 1)Pr( Ei, 1) yi(ei, 1) −

> n∑
> i=1

Pr( Ei, 0)Pr( Ei, 0) yi(ei, 0)

> )

= 1

n 

> (n∑
> i=1

yi(ei, 1) −

> n∑
> i=1

yi(ei, 0)

> )

= τ .

C.1.4 Bounds on Correlation Terms 

We now present bounds on the diagonal and off-diagonal terms of the covariance matrix of ˆ τ . Our proof will follow the same general structure as the proof for q = 2 in Kandiros et al., 2024, but with some important modifications. A key challenge in Kandiros et al., 2024 was to calculate the marginal and joint probabilities of the events E(i,k ). A crucial part of the argument was a linearization step, which used the fact that the importance ordering is such that each i has at most λ(H) neighbors that appear before i in the ordering (the so called more important neighbors ). Here, we will need a generalization of this property, since the probabilities are non-uniform. In particular, we will use the crucial property of the ordering that was presented in Lemma C.1 to justify the linearization that will allow us to calculate the probabilities of the events Ei,k .We start with the bound on the diagonal terms of the covariance matrix of ˆ τ , which amount to lower bounding the probability of the events Ei,k .

Lemma C.5. Let q ≥ 2 be a moment restriction. Suppose we sample the Ui,k , Z variables according to the design in Algorithm 2 with importance ordering π given by Lemma C.1. For every node 

ei,k ∈ V (H) we have that if C ≥ 2, then 

Var  

> (

1[Ei,k ]Pr( Ei,k )

> )

≤ Ce 1

> C

pi,k 

.

69 Proof. We start by noticing that Var 

( 1[Ei,k ]Pr( Ei,k )

)

= 1Pr( Ei,k ) − 1 .

By the definition of the design, we have Pr( E(i,k )) = Pr (Ui,k = e, U j,l = ∗ for all ej,l ∈ N πb (ei,k ))

= Pr( Ui,k = e) ∏   

> ej,l ∈N πb(ei,k )

Pr( Uj,l = ∗)= pi,k 

C

∏   

> ej,l ∈N πb(ei,k )

(

1 − pj,l 

C

)

,

where we used the independence of the variables Ui,k . Now, consider the function f (x) = 1 − x − e−2x

for x ∈ [0 , 1]. We have that 

f ′(x) = −1 + 2 e−2x , f ′′ (x) = −4e−2x

Clearly, f ′′ (x) < 0 for all x ∈ [0 , 1], so that f ′ is decreasing. Also, note that the unique root of f ′ is ln(2) /2 > 1/4. Thus, as long as x ≤ 1/4, we have f ′(x) > 0, which implies that f is increasing in the interval [0 , 1/4]. Since f (0) = 0, it follows that f (x) ≥ 0 for all x ∈ [0 , 1/4]. In particular, we have 1 − x ≥ e−2x for all x ∈ [0 , 1/4]. Now, apply this inequality with x = pj /C . By Lemma C.1, we have for all i ∑  

> j∈N πb(i)

pj ≤ 12This implies that pi,k /C ≤ 1/4 for all i as long as C ≥ 2. Therefore, we can apply the inequality 1 − x ≥ e−2x to each term in the product, yielding 

∏   

> ej,l ∈N πb(ei,k )

(

1 − pj,l 

C

)

≥ ∏   

> ej,l ∈N πb(ei,k )

e−2pj,l /C = e−2 ∑    

> ej,l ∈N πb(ei,k )pj,l /C

≥ e−1/C ,

from which the Lemma follows. Next, we provide bounds on the covariance between two terms corresponding to different exposures. These involve bounding the joint probabilities of events Ei,k and Ej,l .

Lemma C.6. Let q ≥ 2 be a moment restriction. Suppose we sample the Ui,k , Z variables according to the design in Algorithm 2 with importance ordering π given by Lemma C.1 and C ≥ 1. For every pair of nodes ei,k , e j,l ∈ V (H), if d(ei,k , e j,l ) denotes the graph distance between ei,k and ej,l in H,then we have the following: 

• If d(ei,k , e j,l ) = 1 , then 

Cov 

( 1[Ei,k ]Pr( Ei,k ) , 1[Ej,l ]Pr( Ej,l )

)

= −1 .

• If d(ei,k , e j,l ) = 2 , then 

∣∣∣Cov 

( 1[Ei,k ]Pr( Ei,k ) , 1[Ej,l ]Pr( Ej,l )

)∣ ∣∣ ≤ 22C − 1 · ∑   

> er,s ∈N(ei,k )∩N(ej,l )

pr,s .

70 • If d(ei,k , e j,l ) > 2, then 

Cov 

( 1[Ei,k ]Pr( Ei,k ) , 1[Ej,l ]Pr( Ej,l )

)

= 0 .

Proof. By definition, the event Ei,k depends on the values of the variables Ui,k and Uj,l for all 

j ∈ N (ei,k ). Thus, if d(ei,k , e j,l ) > 2, then the events Ei,k and Ej,l depend on disjoint sets of variables, so they are independent, which implies that the covariance is zero. If d(ei,k , e j,l ) = 1, then the events Ei,k and Ej,l are mutually exclusive. Indeed, suppose without loss of generality that 

ei,k appears before ej,l in the ordering, then if Ei,k happens we must have Ui,k = e, but if Ej,l also happens, then we must have Ui,k = ∗, which is a contradiction. Thus, Cov 

( 1[Ei,k ]Pr( Ei,k ) , 1[Ej,l ]Pr( Ej,l )

)

= E 

[ 1[Ei,k ]Pr( Ei,k )

1[Ej,l ]Pr( Ej,l )

]

− 1 = −1 .

Finally, let us examine the case where d(ei,k , e j,l ) = 2. In this case, we can write Cov 

( 1[Ei,k ]Pr( Ei,k ) , 1[Ej,l ]Pr( Ej,l )

)

= E 

[ 1[Ei,k ]Pr( Ei,k )

1[Ej,l ]Pr( Ej,l )

]

− 1= Pr( Ei,k ∩ Ej,l )Pr( Ei,k ) Pr( Ej,l ) − 1= Pr (Ui,k = Uj,l = e1, U r,s = ∗, ∀er,s ∈ N πb (ei,k ) ∪ N πb (ej,l ))

Pr (Ui,k = e1, U r,s = ∗, ∀er,s ∈ N πb (ei,k )) Pr (Uj,l = e1, U r,s = ∗, ∀er,s ∈ N πb (ej,l )) − 1= 1

∏

er,s ∈N πb (ei,k )∩N πb (ej,l )

(

1 − pr,s 

C

) − 1By the inclusion-exclusion principle, we have that 

∏

er,s ∈N πb (ei,k )∩N πb (ej,l )

(

1 − pr,s 

C

)

≥ 1 − ∑

er,s ∈N πb (ei,k )∩N πb (ej,l )

pr,s 

C .

Hence, we have that 1

∏

er,s ∈N πb (ei,k )∩N πb (ej,l )

(

1 − pr,s 

C

) − 1 = 1 − ∏

er,s ∈N πb (ei,k )∩N πb (ej,l )

(

1 − pr,s 

C

)∏

er,s ∈N πb (ei,k )∩N πb (ej,l )

(

1 − pr,s 

C

)

≤

∑

er,s ∈N πb (ei,k )∩N πb (ej,l )

pr,s 

C

1 − ∑

er,s ∈N πb (ei,k )∩N πb (ej,l )

pr,s 

C

≤ 2C

2C − 1

∑

er,s ∈N πb (ei,k )∩N πb (ej,l )

pr,s 

C ,

where in the last step we used the fact that 

∑

er,s ∈N πb (ei,k )∩N πb (ej,l )

pr,s 

C ≤ ∑

er,s ∈N πb (ei,k )

pr,s 

C ≤ 12C ,

71 which follows from Lemma C.1. Finally, we trivially have 

∑     

> er,s ∈N πb(ei,k )∩N πb(ej,l )

pr,s ≤ ∑   

> er,s ∈N (ei,k )∩N (ej,l )

pr,s ,

which concludes the proof. 

C.1.5 Matrix Norm Bounds 

Lemmas C.5 and C.6 suggest that the covariance matrix associated with the estimator ˆ τ can be split into three matrices, one diagonal matrix corresponding to ei,k = ej,l , one corresponding to pairs ei,k , e j,l with d(ei,k , e j,l ) = 1 and one for d(ei,k , e j,l ) = 2. A similar strategy was employed in Kandiros et al. (2024). In particular, we define for all pairs ei,k , e j,l ∈ V (H)

C(i,k ),(j,l ) = Cov 

( 1[Ei,k ]Pr( Ei,k ) , 1[Ej,l ]Pr( Ej,l )

)

.

Then, let us define the matrices T (t) ∈ R2n×2n for t ∈ { 0, 1, 2} as follows: 

T (t)(i,k ),(j,l ) =

{|C(i,k ),(j,l )| if d(ei,k , e j,l ) = t

0 otherwise .

The following Lemmas upper bound the quadratic forms of the matrices T (0) , T (1) , T (2) , evaluated at vectors whose q-th norm is at most 1. These bounds will be crucial for bounding the variance of ˆτ . The proofs are based on careful applications of Hölder’s inequality. For a vector x ∈ R2n, we will sometimes use the double indexing xi,k to refer to the coordinate of x corresponding to node 

ei,k ∈ V (H). 

Lemma C.7. For any vector x ∈ R2n with ∥x∥q ≤ 1, we have 

x⊤T (0) x ≤ 2Ce 1 

> C

· n1− 2 

> q

λ(H) 2 

> q

davg (H)1− 2 

> q

.

Proof. Let m denote the number of edges in the conflict graph H. Using Hölder’s inequality, we have that 

x⊤T (0) x ≤ Ce 1 

> C

· ∑  

> ei,k ∈V(H)

x2

> i,k

1

pi,k 

using Lemma C.5 

≤ Ce 1 

> C

·

( ∑  

> ei,k ∈V(H)

xqi,k 

)2/q ( ∑  

> ei,k ∈V(H)

( 1

pi,k 

) qq−2

)(q−2) /q 

≤ Ce 1 

> C

·

( ∑  

> ei,k ∈V(H)

( 1

pi,k 

) qq−2

)(q−2) /q 

72 by the assumption on x

= Ce 1 

> C

·

( ∑

ei,k ∈V (H)

(2 λ(H)2/q d1−2/q i,k ) qq−2

)(q−2) /q 

= 2 Ce 1 

> C

· λ(H)2/q ( ∑

ei,k ∈V (H)

di,k 

)(q−2) /q 

= 2 Ce 1 

> C

· λ(H)2/q (2 m)1−2/q 

= 2 Ce 1 

> C

· n1− 2 

> q

λ(H) 2 

> q

davg (H)1− 2 

> q

.

Lemma C.8. For any vector x ∈ R2n with ∥x∥q ≤ 1, we have 

x⊤T (1) x ≤ n1− 2 

> q

λ(H) 2 

> q

davg (H)1− 2 

> q

.

Proof. We have that 

x⊤T (1) x = ∑

ei,k ∈VH

xi,k 

∑

ej,l ∈VH

T (1) (i,k ),(j,l )xj,l 

≤ ∑

ei,k ∈VH

|xi,k | ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |≤

( ∑

ei,k ∈VH

|xi,k |q)1/q ( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |

) qq−1 )(q−1) /q 

by Hölder’s inequality 

≤

( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |

) qq−1 )(q−1) /q 

by the assumption on x

≤

( ∑

ei,k ∈VH

(( ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |q/ 2)2/q 

· d1−2/q i,k 

) qq−1 ) q−1

> q

by Hölder’s inequality =

( ∑

ei,k ∈VH

(( ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |q/ 2) 2

> q−1

· d

> q−2
> q−1

i,k 

)) q−1

> q

73 ≤

(( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |q/ 2)2) 1

> q−1

·

( ∑

ei,k ∈VH

(d

> q−2
> q−1

i,k 

) q−1

> q−2

) q−2

> q−1

) q−1

> q

by Hölder’s inequality =

(( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |q/ 2)2) 1

> q−1

·

( ∑

ei,k ∈VH

di,k 

) q−2

> q−1

) q−1

> q

=

( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|T (1) (i,k ),(j,l )|| xj,l |q/ 2)2) 1

> q

· (2 m) q−2

> q

≤

(

λ(H)2 ∑

ei,k ∈VH

(|xj,l |q/ 2)2

) 1

> q

· (2 m) q−2

> q

since |T (1) (i,k ),(j,l )| is the adjacency entry of graph H by Lemma C.6 and using the definition of the maximum eigenvalue property 

≤ λ 2 

> q

(2 m)1− 2

> q

= n1− 2 

> q

λ(H) 2 

> q

davg (H)1− 2 

> q

.

Now, let us handle the third matrix. 

Lemma C.9. For any vector x ∈ R2n with ∥x∥q ≤ 1, we have 

x⊤T (2) x ≤ 42C − 1 · n1− 2 

> q

λ(H) 2 

> q

davg (H)1− 2 

> q

.

Proof. Let AH be the adjacency matrix of the conflict graph H. We have that 

x⊤T (2) x = ∑

ei,k ∈VH

xi,k 

∑

ej,l ∈VH

T (2) (i,k ),(j,l )xj,l 

≤ ∑

ei,k ∈VH

|xi,k | ∑

ej,l ∈VH

|T (2) (i,k ),(j,l )|| xj,l |≤

( ∑

ei,k ∈VH

|xi,k |q)1/q ( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|T (2) (i,k ),(j,l )|| xj,l |

) qq−1 ) q−1

> q

by Hölder’s inequality 

≤ 22C − 12

λ(H)2/q 

( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|xj,l | ∑

er,s ∈N (ei,k )∩N (ej,l )

1

d

> q−2
> q

r,s 

) qq−1

) q−1

> q

74 using the bound on C(i,k ),(j,l ) of Lemma C.6 

≤ 22C − 12

λ(H)2/q 

( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|xj,l ||N (ei,k ) ∩ N (ej,l )| 2

> q

( ∑

er,s ∈N (ei,k )∩N (ej,l )

1

dr,s 

) q−2 

> q

) qq−1

) q−1

> q

again using Hölder’s inequality 

≤ 22C − 12

λ(H)2/q 

( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|xj,l | q 

> 2

|N (ei,k ) ∩ N (ej,l )|

) 2

> q−1

·

( ∑

ej,l ∈VH

∑

er,s ∈N (ei,k )∩N (ej,l )

1

dr,s 

) q−2 

> q−1

) q−1

> q

by another application of Hölder’s inequality 

≤ 22C − 12

λ(H)2/q 

(( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|xj,l | q 

> 2

|N (ei,k ) ∩ N (ej,l )|

)2) 1

> q−1

·

( ∑

ei,k ∈VH

∑

ej,l ∈VH

∑

er,s ∈N (ei,k )∩N (ej,l )

1

dr,s 

) q−2 

> q−1

) q−1

> q

by yet another application of Hölder’s inequality = 4(2 C − 1) λ(H)2/q 

( ∑

ei,k ∈VH

( ∑

ej,l ∈VH

|xj,l | q 

> 2

|N (ei,k ) ∩ N (ej,l )|

)2) 1

> q

·

( ∑

ei,k ∈VH

∑

ej,l ∈VH

∑

er,s ∈N (ei,k )∩N (ej,l )

1

dr,s 

) q−2

> q

≤ 4(2 C − 1) λ(H)2/q 

(

λ(H)2 ∑

ei,k ∈VH

|xi,k |q

) 1

> q

·

( ∑

ei,k ∈VH

∑

ej,l ∈VH

∑

er,s ∈N (ei,k )∩N (ej,l )

1

dr,s 

) q−2

> q

using the definition of the maximum eigenvalue of AH

≤ 4(2 C − 1) λ(H)2/q λ(H) 4

> q

( ∑

ei,k ∈VH

∑

ej,l ∈VH

∑

er,s ∈N (ei,k )∩N (ej,l )

1

dr,s 

) q−2

> q

75 using the constraint for the vector x

= 42C − 1 λ(H)2/q 

( ∑

> er,s ∈VH

1

dr,s 

∑  

> ei,k ∈N(er,s )

∑ 

> ej,l ∈N(er,s )

1

) q−2

> q

= 42C − 1 λ(H)2/q 

( ∑

> er,s ∈VH

d2

> r,s

dr,s 

) q−2

> q

= 42C − 1 λ(H)2/q (2 m)1−2/q 

= 42C − 1 n1− 2 

> q

λ(H) 2 

> q

davg (H)1− 2 

> q

.

C.1.6 Proof of Theorem 4.1 

We are now ready to prove Theorem 4.1, which is a bound on the variance of ˆ τ .

Proof of Theorem 4.1. To prove the upper bound, it suffices to produce a statistical procedure, whose risk is bounded by the appropriate quantity. We will use as our design D the Conflict Graph Design of Algorithm 2 with an appropriate value of C > 0 that we will specify later and as our estimator ˆ τ , the modified Horvitz–Thompson estimator (13). Although the Conflict Graph Design and modified Horvitz–Thompson estimator form a randomized statistical procedure (as defined in Section A.3), its maximal risk provides an upper bound for the minimax risk (Proposition A.2, Section A.3). Our goal is to bound sup 

> y∈M (G,q )

E(Z,U )∼D [(ˆτ (Z, U, y (Z)) − τ (y))2] = sup 

> y∈M (G,q )

Var 

(

ˆτ (Z, U, y (Z)) 

)

.

Let y ∈ M (G, q ). Let us define the vector α ∈ R2n as 

αi,k = 1

n1/q |yi(ei,k )| .

We begin by writing the variance of ˆ τ as a quadratic form of the covariance matrix with vector α,as in Kandiros et al., 2024. In particular, we have Var(ˆ τ ) = Var 

( 1

n

( n∑

> i=1

( 1[Ei, 1]Pr( Ei, 1) yi(ei, 1) − 1[Ei, 0]Pr( Ei, 0) yi(ei, 0)

))) 

= 1

n2

∑ 

> ei,k ∈VH

∑

> ej,l ∈VH

yi(ei,k )yj (ej,l )( −1) i+j Cov 

( 1[Ei,k ]Pr( Ei,k ) , 1[Ej,l ]Pr( Ej,l )

)

≤ 1

n2

∑ 

> ei,k ∈VH

∑

> ej,l ∈VH

|yi(ei,k )|| yj (ej,l )|| C(i,k ),(j,l )|

76 = n 2

> q

n2 α⊤(

T (0) + T (1) + T (2) )

α

Since y ∈ M (G, q ), the q-th norm of the vector α is bounded as 

( n∑

> i=1 1

∑

> k=0

|αi,k |q)1/q 

=

( 1

n

> n

∑

> i=1

|yi(ei, 0)|q + 1

n

> n

∑

> i=1

|yi(ei, 1)|q

)1/q 

≤ 1 ,

using the definition of M(G, q ). Now, applying Lemmas C.7, C.8 and C.9 to bound the three terms in the sum, we have Var(ˆ τ ) ≤ n 2

> q

n2

(

2Ce 1 

> C

+ 1 + 42C − 1

)

λ(H) 2 

> q

(2 m)1− 2

> q

=

(

2Ce 1 

> C

+ 1 + 42C − 1

)

· λ(H) 2 

> q

(2 m/n )1− 2

> q

n

=

(

2Ce 1 

> C

+ 1 + 42C − 1

)

· λ(H) 2 

> q

d1− 2

> q
> avg

n ,

where in the last step we used the fact that davg = 2 m/n . Choosing C = 2 yields the desired bound. 

C.2 Bias-Variance Tradeoff 

In this section, we first establish in Section C.2.1 the improved upper bound of Theorem 4.2. We then show in Section C.3 that we can find a design-estimator pair that can be implemented in polynomial time, whose risk is within multiplicative logarithmic factors from this improved bound. 

C.2.1 Optimal Bias-Variance Tradeoff (Theorem 4.2) 

We first introduce some notation that will be particularly useful in this section. For any subset of nodes S ⊆ VH, we denote by H[VH \ S] = ( VH \ S, E (VH \ S)) the subgraph of H that is induced by the nodes in VH \ S, i.e. for i, j ∈ VH \ S, ( i, j ) ∈ E(VH \ S) if and only if (i, j ) ∈ EH. We denote by λ(H[VH \ S]) the largest eigenvalue of the adjacency matrix of H[VH \ S]. As usual, we will use the notation ˜N (i) for the extended neighborhood of a node i in the original graph G. Also, for convenience, we introduce the following function of a subset S ⊆ V (H) and a moment parameter q.

Qq(S, H) = λ(H[VH \ S]) 2 

> q

· davg (H[VH \ S]) 1− 2 

> q

+ |S|2− 2

> q

n1− 2

> q

.

In this section, we will prove the following theorem. 

Theorem 4.2. Let G be an interference network, τ be a contrastive effect. For any moment parameter q ≥ 2, the corresponding minimax risk is bounded as 

R(G, τ, q ) ≤ 8.92 · min S⊆VH Qq(S, H)

n .

77 Algorithm 3: Biased Conflict Graph Design 

Input: maximum eigenvalue λ(H), graph G, conflict graph H and moment q.

Output: Random intervention Z ∈ Z = {0, 1}n, variables Ui for all i ∈ V (H).  

> 1

S∗ ← arg min S⊆V (H) Qq(S, H) 

> 2

H∗ ← H [VH \ S∗] 

> 3

π∗ ← Ordering on VH \ S∗ produced by Algorithm 1 with input H∗ and q 

> 4

for ei,k ∈ V (H∗) do  

> 5

pi,k ← 12λ(H∗) 2   

> qdi,k (H∗)1−2
> q

; 

> 6

Sample desired exposure variables {Ui,k : ei,k ∈ V (H) \ S∗} independently and identically as 

Ui,k ←

{e with probability pi,k 

> 2

∗ with probability 1 − pi,k  

> 2
> 7

Initialize the intervention vector Z ← 0 

> 8

for ei,k ∈ V (H) \ S∗ do  

> 9

if Ui,k = e and Uj,l = ∗ for all ej,l ∈ N π∗ 

> b

(ei,k ) then  

> 10

Update intervention vector: set Zr = 1[r ∈ hi(ei,k )] for all r ∈ ˜N (i) ; We start by introducing the design that will be used in the proof of Theorem 4.2. We give the pseudocode in Algorithm 3. Algorithm 3 operates similarly to the CGD of Algorithm 2, but with one important difference. In particular, in line 1 we select a particular subset of nodes S∗ in the conflict graph H whose outcomes we will not try to estimate. Then, an ordering π∗ of the nodes in VH \ S∗ is produced by calling the importance ordering procedure from Algorithm 1 on the subgraph H[VH \ S∗] of the original conflict graph. The remaining steps mirror the CGD of Algorithm 2, where we iterate only over the exposures in VH \ S∗ and assign treatment. In order to describe our proposed estimator, we first introduce some definitions for arbitrary subsets of nodes in the conflict graph. In particular, for any subset S ⊆ VH, let πS be the ordering of the nodes in VH \ S produced by the importance ordering procedure from Algorithm 1 with input H[VH \ S] and q (we omit the dependence on q since it is clear from the context). For all 

ei,k ∈ VH \ S, define the events 

E(S) 

> i,k

= {Ui,k = e, U j,l = ∗ for all ej,l ∈ N πS 

> b

(ei,k )} .

Note that we used a similar notation for an analogous event in the original CGD in Section C.1.2, where the event depends on all neighbors of ei,k in the conflict graph. This could be thought of as a special case of the above definition, where S = ∅ and π∅ is the ordering produced by Algorithm 1 on the entire conflict graph H.For any subset S ⊆ VH, we define the following estimator. ˆτS = 1

n

> n

∑

> i=1

( 1[E(S) 

> i, 1

]Pr( E(S) 

> i, 1

) 1[ei, 1 /∈ S] − 1[E(S) 

> i, 0

]Pr( E(S) 

> i, 0

) 1[ei, 0 /∈ S]

)

yi(Z) . (14) 78 All probabilities are calculated with respect to the randomness in the design of Algorithm 3. Note that ˆ τS is a randomized estimator, in the sense defined in Section 2, since it uses the values of the random variables Ui,k in its definition. For an arbitrary subset of exposures S ⊆ VH, denote by τS the average treatment effect defined only on exposures in VH \ S, i.e. 

τS = 1

n

> n

∑

> i=1

(1[ei, 1 /∈ S]yi(ei, 1) − 1[ei, 0 /∈ S]yi(ei, 0)) .

We will use ˆ τS∗ as our estimator for τS∗ , where S∗ is the subset of nodes in the conflict graph that minimizes Q(S, q ), as defined in line 1 of Algorithm 3. Our first proposition establishes that ˆ τS∗ is an unbiased estimator for τS∗ .

Proposition C.10. For any outcome function y ∈ M (G), we have E[ˆ τS∗ ] = τS∗ .Proof. First, observe that if event E(S∗) 

> i,k

holds, then yi(Z) = yi(ei,k ). The argument is essentially identical to that of Lemma C.3 for the original CGD, since Algorithm 3 essentially runs the conflict graph design on the set of nodes VH \ S∗. Therefore, we have E[ˆ τS∗ ] = 1

n

> n

∑

> i=1

( Pr 

(

E(S∗)

> i, 1

)

Pr 

(

E(S∗)

> i, 1

) 1[ei, 1 /∈ S∗]yi(ei, 1) −

Pr 

(

E(S∗)

> i, 0

)

Pr 

(

E(S∗)

> i, 0

) 1[ei, 0 /∈ S∗]yi(ei, 0)

)

= 1

n

> n

∑

> i=1

(

1[ei, 1 /∈ S∗]yi(ei, 1) − 1[ei, 0 /∈ S∗]yi(ei, 0)

)

= τS∗ .

We are now ready to prove Theorem 4.2. 

Proof of Theorem 4.2. We will prove the upper bound on the minimax risk by bounding the risk of a particular design-estimator pair. We will use the Biased Conflict Graph Design of Algorithm 3 as the design D and ˆ τS∗ as the estimator, as defined in equation (14). As observed above, D is a randomized experimental design and ˆ τS∗ is a randomized estimator, since they both use the extra randomness of the variables Ui,k . By Proposition A.2, it suffices to bound the risk of this design-estimator pair to obtain an upper bound on the minimax risk. By Proposition C.10, we have that ˆ τS∗ is an unbiased estimator for τS∗ . Since τS∗ is not necessarily equal to τ , the estimator ˆ τS∗ may be biased for τ . Thus, we can decompose the MSE of ˆτS∗ as a sum of bias and variance terms, as follows. E[(ˆ τS∗ − τ )2] = Var (ˆτS∗

) + ( τS∗ − τ )2 . (15) We will now bound each term separately, starting with the bias term. For any y ∈ M (G, q ), we have (τS∗ − τ )2 =

( 1

n

> n

∑

> i=1

(1[ei, 1 ∈ S∗]yi(ei, 1) − 1[ei, 0 ∈ S∗]yi(ei, 0)) 

)2

≤ 1

n2

( ∑ 

> ei,k ∈S∗

|yi(ei,k )|

)2

79 using triangle inequality 

≤ 1

n2

( ∑ 

> ei,k ∈S∗

|yi(ei,k )|q

) 2

> q

·

( ∑ 

> ei,k ∈S∗

1

) 2( q−1) 

> q

by Hölder’s inequality 

≤ |S∗|2− 2

> q

n2− 2

> q

, (16) using the fact that y ∈ M (G, q ) in the last step. We now focus on the variance term. As we’ve already discussed, after selecting the subset 

S∗, Algorithm 3 proceeds identically to the CGD of Algorithm 2 but only on the subgraph 

H[VH \ S∗]. Moreover, the estimator ˆ τS∗ is essentially the exact analogue of the estimator ˆ τ defined in equation (13) for the original CGD, but only on the exposures in VH \ S∗. Therefore, we can use the analysis of the variance of the original CGD, as given in the proof of Theorem 4.1, to conclude that for any y ∈ M (G, q ), we have Var (ˆτS∗

) ≤ 8.92 · λ(H(VH \ S∗)) 2 

> q

· davg (H[VH \ S∗]) 1− 2

> q

n . (17) Combining the bounds in equations (16) and (17) with the bias-variance decomposition in equation (15), we have that 

R(G, τ, q ) ≤ 8.92 · λ(H(VH \ S∗)) 2 

> q

· davg (H[VH \ S∗]) 1− 2

> q

n + |S∗| 2( q−1) 

> q

n2− 2

> q

= 8 .92 · Qq(S∗, H)

n .

Since S∗ was selected in line 1 to optimize the right hand side of the above inequality, the result follows. 

C.3 Polynomial Time Approximation Algorithm (Proposition 4.3) 

In this section, we prove that there exists a design-estimator pair that run in polynomial time and achieve estimation rate within logarithmic factors from that of Theorem 4.2. The precise statement follows. 

Proposition 4.3. Let G be an interference network, τ be a contrastive effect. For any moment parameter q ≥ 2, there exists a design-estimator pair (D, ˆτ ) that can be implemented in polynomial time whose maximum risk is bounded as 

sup 

> y∈M (G,τ,q )

MSE( D, ˆτ , τ, y ) ≤ O 

(

(log n)2− 2

> q

)

· min S⊆V (H) Qq(S, H)

n .

80 In order to establish Theorem 4.3, it will be useful to introduce a few relevant computational problems. The first one is known as the Partial Vertex Cover problem. 

Definition 4 (Partial Vertex Cover) . The Partial Vertex Cover problem is defined as follows. 

Input: A graph G = ( V, E ) and an integer k.

Output: A minimum-size subset S ⊆ V such that |E(G[V \ S]) | ≤ k.For a graph G and integer k, we denote by OPT pvc (G, k ) the size of the optimal solution to thePartial Vertex Cover problem on input ( G, k ). The second problem is a variant of the first one. We will refer to it as the Partial Eigenvalue Cover problem, although this is not a standard terminology in the literature. 

Definition 5 (Partial Eigenvalue Cover) . The Partial Eigenvalue Cover problem is defined as follows. 

Input: A graph G = ( V, E ) and an integer k.

Output: A minimum-size subset S ⊆ V such that λ(G[V \ S]) ≤ k.For a graph G and integer k, we denote by OPT pec (G, k ) the size of the optimal solution to the Partial Eigenvalue Cover problem on input ( G, k ). We now present two results from the literature, which provide polynomial time approximation algorithms for the above problems. 

Theorem C.11 (Theorem 2.1 in Gandhi, Khuller, and Srinivasan (2004)) . There exists a polynomial time algorithm that takes as input a graph G and integer k and outputs a subset PVC-Approx (G, k ) ⊆

V such that |PVC-Approx (G, k )| ≤ 2 · OPT pvc (G, k ).

Theorem C.12 (Lemma 5.1 in Saha et al. (2015)) . There exists a polynomial time algorithm that takes as input a graph G and integer k and outputs a subset PEC-Approx (G, k ) ⊆ V (G) such that 

|PEC-Approx (G, k )| = O(OPT pec (G, k ) · log n)

and 

λ(G[V \ PEC-Approx (G, k )] ) ≤ 2k . 

We will use the above two algorithms as subroutines in order to find a subset S of vertices in the conflict graph H that approximately minimizes the expression in Theorem 4.2. In particular, we describe the design in Algorithm 4. The only difference between Algorithm 4 and the previously described Algorithm 3 is in the selection of the optimal subset of exposures. While Algorithm 3 searches over all subsets of exposures to minimize Qq(S, H), which takes time exponential in n, Algorithm 4 uses a different procedure that runs in polynomial time. We next establish that the value Qq( ˆS, H) is only logarithmically larger than the optimal value of Qq(S∗, H). 

Lemma C.13. Let S∗ = arg min S⊆V (H) Qq(S, H) as defined in Algorithm 3 and ˆS be the subset selected in line 6 of Algorithm 4. Then, 

Qq( ˆS, H) = O

> (

(log n)2− 2

> q
> )

· Qq(S∗, H) .

81 Algorithm 4: Approximation Design 

Input: Interference network G, conflict graph H and moment q.

Output: Random intervention Z ∈ Z = {0, 1}n, variables Ui,k for all ei,k ∈ V (H).  

> 1

for k1 = 1 to |E(H)| do  

> 2

for k2 = 1 to ⌈λ(H)⌉ do  

> 3

S1 ← PVC-Approx (H, k 1);  

> 4

S2 ← PEC-Approx (H, k 2);  

> 5

Sk1,k 2 ← S1 ∪ S2; 

> 6

ˆS ← arg min k1,k 2 Qq(Sk1,k 2 , H) 

> 7

ˆH ← H [VH \ ˆS] 

> 8

π ˆS ← Ordering produced by Algorithm 1 with input ˆH and q 

> 9

for ei,k ∈ VH \ ˆS do  

> 10

pi,k ← 12λ(H) 2   

> qdi,k ( ˆH)1−2
> q

; 

> 11

Sample desired exposure variables {Ui,k : ei,k ∈ VH \ ˆS} independently and identically as 

Ui,k ←

{e with probability pi,k 

> 2

∗ with probability 1 − pi,k  

> 2
> 12

Initialize the intervention vector Z ← 0 

> 13

for ei,k ∈ VH \ ˆS do  

> 14

if Ui,k = e and Uj,l = ∗ for all ej,l ∈ N π ˆS 

> b

(ei,k ) then  

> 15

Update intervention vector: set Zr = 1[r ∈ hi(ei,k )] for all r ∈ ˜N (i) ; 

Proof. Let k∗ 

> 1

= |E(VH \ S∗)| and k∗ 

> 2

= ⌈λ(H(VH \ S∗)) ⌉. We first note that OPT pvc (H, k ∗ 

> 1

) ≤ | S∗|

and OPT pec (H, k ∗ 

> 2

) ≤ | S∗|. This is because S∗ is a feasible solution to both the Partial Vertex Cover problem with input ( H, k ∗ 

> 1

) and the Partial Eigenvalue Cover problem with input ( H, k ∗ 

> 2

). For convenience, let us denote 

S1 = PVC-Approx (H, k ∗ 

> 1

) and S2 = PEC-Approx (H, k ∗ 

> 2

) .

Then, by Theorem C.11, we have that |S1| ≤ 2 · OPT pvc (H, k ∗ 

> 1

) ≤ 2|S∗|. Also, by Theorem C.12, we have that 

|S2| = O(OPT pec (H, k ∗ 

> 2

) · log n) = O(|S∗| · log n) and ⌈λ(H[VH \ S2]) ⌉ ≤ 2k∗ 

> 2

.

Finally, Sk∗  

> 1,k ∗
> 2

= S1 ∪ S2 has size |Sk∗  

> 1,k ∗
> 2

| ≤ | S1| + |S2| = O(|S∗| · log n). Therefore, at the iteration where k1 = k∗ 

> 1

and k2 = k∗ 

> 2

, we have that 

n1− 2 

> q

· Qq(Sk∗  

> 1,k ∗
> 2

, H) = λ(H(VH \ Sk∗  

> 1,k ∗
> 2

)) 2 

> q

· (2 |E(VH \ Sk∗  

> 1,k ∗
> 2

)|)1− 2 

> q

+ |Sk∗  

> 1,k ∗
> 2

| 2( q−1) 

> q

≤ (2 k∗ 

> 2

) 2 

> q

· (2 k∗ 

> 1

)1− 2 

> q

+ O(|S∗| · log n) 2( q−1) 

> q

= O

(

(log n)2− 2

> q

)

·

(

λ(H(VH \ S∗)) 2 

> q

· | E(VH \ S∗)|1− 2 

> q

+ |S∗| 2( q−1) 

> q

)

82 = O

> (

(log n)2− 2

> q
> )

· n1− 2 

> q

Qq(S∗, H) .

By definition, we have Qq( ˆS, H) ≤ Qq(Sk∗  

> 1,k ∗
> 2

, H), which implies the result. We are now ready to establish Proposition 4.3. 

Proof of Proposition 4.3. As our design D, we will use the design of Algorithm 4 and the estimator ˆτ ˆS , where ˆS is defined in line 6 of Algorithm 4. We first note that Algorithm 4 runs in polynomial time. This is because the total number of iterations in the for loops in the beginning is O(|E(H)| · λ(H)) = O(n3), and each iteration involves running the algorithms of Theorem C.11 and Theorem C.12, which run in polynomial time. The remaining steps of the algorithm also clearly run in polynomial time. After having obtained ˆS, one can also compute the estimator ˆ τ ˆS in polynomial time. Next, we can proceed exactly as in the proof of Theorem 4.2 to show that MMSE (D, ˆτ ˆS , M(G, q )) ≤ 8.92 · Qq( ˆS, H)

n2− 2

> q

.

Finally, by Lemma C.13, we have that 

Qq( ˆS, H) = O

> (

(log n)2− 2

> q
> )

· Qq(S∗, H) ,

where S∗ = arg min S⊆V (H) Qq(S, H) as defined in Algorithm 3. The result follows. 

# D Illustrations of Main Results 

In this section, we present the proofs of two illustrations of the main result for estimating two well-studied causal effects, as presented in Section 1.1. Section D.1 contains the proof for the Direct Treatment Effect, while Section D.2 contains the proof for the Global Average Treatment Effect. 

D.1 Direct Treatment Effect (Corollary 1.1) 

The Direct Treatment Effect (DTE) was formally defined in Section 2.1. We state and prove a more formal version of Corollary 1.1 below. 

Corollary D.1. The minimax rate of estimating the Direct Treatment Effect τDTE on a network G

is bounded as 

max  

> (

1

|I (G)| ,

> √

davg (G)

n

> )

≲ R(G, τ DTE , 2) ≲ λ(G) + 1 

n ,

where I(G) is the maximum independent set in G, davg (G) is its average degree, and λ(G) is the largest eigenvalue of its adjacency matrix. Proof. Let H be the conflict graph corresponding to G and τDTE . Let S = {ei, 1 : i ∈ [n]} be the subset of size n of the ei, 1 exposures. Applying the upper bound of Theorem 4.1 for q = 2, as well 83 as the global lower bound of Theorem 3.2 with T = S and the local lower bound of Corollary 3.5 together with Proposition B.6, we get max  

> (

1

|I (H, S )| ,

> √

davg (H)

n

> )

≲ R(G, τ DTE ) ≲ λ(H)

n (18) We now connect the quantites on the upper and lower bounds with the desired quantities of the interference network G = ( V, E ). As we have discussed in Section 2.3, the conflict graph H for τDTE has the following structure: (ei,k , e j,l ) ∈ EH if and only if ( i, j ) ∈ E and ( k, l )̸ = (0 , 0) or i = j and k̸ = l. Therefore, the induced subgraph H(S) is isomorphic to G, which implies that 

|I (H, S )| = |I (G)| . (19) Also, this means that H has at least as many edges as G, which means that 

davg (H) = 2|EH|

2n ≥ 2|E|

2n = davg (G)2 . (20) Thus, combining (18) together with (19) and (20) yields the lower bound of the statement. For the upper bound, let AH, AG denote the adjacency matrices of H and G, respectively. If we order the nodes in H starting from the n exposures ei, 0 and then moving to the n exposures ei, 1,then by the preceding discussion we can write AH in block form as follows. 

AH =

> (

0 II AG

> )

.

Now, standard matrix bounds imply that 

λ(H) = ∥AH∥op ≤ 2∥I∥op + ∥AG∥op = 2 + λ(G) .

This bound combined with (18) yields the upper bound of the statement. 

D.2 Global Average Treatment Effect (Corollary 1.2) 

The Global Average Treatment Effect (GATE) was formally defined in Section 2.1. We state and prove a more formal version of Corollary 1.2 below. 

Corollary D.2. The minimax rate of estimating the Global Average Treatment Effect τGATE on a network G is bounded as 

> √

davg (G2)

n ≲ R(G, τ GATE , 2) ≲ λ(G2) + 1 

n ,

where G2 is the two-hop interference graph, davg (G2) is the average degree of the two-hop graph, and 

λ(G2) is the largest eigenvalue of its adjacency matrix. 

84 Proof. Let H be the conflict graph corresponding to G and τGATE . Applying the upper bound of Theorem 4.1 for q = 2, as well as the local lower bound of Corollary 3.5 together with Proposition B.6, we get √

davg (H)

n ≲ R(G, τ GATE , 2) ≲ λ(H)

n (21) We now connect the quantites on the upper and lower bounds with the desired quantities of the 2-hop graph G2 = ( V, E (G2)). As we have discussed in Section 2.3, the conflict graph H for τGATE has the following structure: (ei,k , e j,l ) ∈ EH if and only if ( i, j ) ∈ E(G2) and k̸ = l or i = j and k̸ = l. Therefore, for every edge (i, j ) ∈ E(G2) in the 2-hop graph G2, there are two edges ( ei, 1, e j, 0), (ei, 0, e j, 1) ∈ EH. It follows that 

|EH| ≥ 2|E(G2)|, which implies that 

davg (H) = 2|EH|

2n ≥ 4|E(G2)|

2n = davg (G2) .

This inequality combined with (21) establishes the lower bound of the statement. For the upper bound, let AH, AG2 denote the adjacency matrices of H and G2 respectively. Then, if we order the 2 n nodes of H starting from the ei, 0 exposures and then continuing with the 

ei, 1 exposures, we can write AH in block form as 

> (

0 I + AG2

I + AG2 0

> )

.

The identity part comes from the edges ( ei, 0, e i, 1). Therefore, we have 

λ(H) = ∥AH∥op ≤ 2∥I + AG2 ∥op ≤ 2 + 2 λ(G2)The above inequality combined with (21) yields the lower bound in the statement. 

# E Tightness of Bounds 

In this section, we present the proofs of the results in Section 5. We start by providing a convenient description of the conflict graph for direct effect when G is an arbitrary d-regular graph. We then continue by establishing that both the upper and lower bounds for estimating the direct effect in regular graphs are tight. Finally, we show that for a random regular graph, the upper bound is tight up to a log d factor with high probability. 

E.1 The Structure of the Conflict Graph 

We first provide a Lemma that describes the structure of the conflict graph H when the estimand of interest is the Direct Effect. We denote by ei ∈ Rn the vector with 1 in the i-th entry and 0 everywhere else and 0 ∈ Rn be the all 0 vector. Also, for a symmetric matrix A ∈ Rn×n, we denote by ∥A∥2 its spectral norm. Furthermore, for any graph G, we denote by AG its adjacency matrix. 

Lemma E.1. Let G = ( V, E ) be a graph and H = ( VH, E H) be the conflict graph associated with G

and the direct effect τ . Then, we have that 

85 (i) If a node i ∈ G has degree di, then ei, 1 ∈ H has degree 2di + 1 and ei, 0 ∈ H has degree at most 

di + 1 .(ii) Let T = {ei, 1 : i ∈ [n]} be the subset of vertices of H. Then, H[T ] is a graph isometric to G,where the isometry maps ei, 1 to i for each i ∈ [n].Proof. Let us start with the first part. The vertices of H are the 2 n exposures ei, 1 = ei and 

ei, 0 = 0 for i ∈ [n]. For every i ∈ [n], there is an edge ( ei, 1, e i, 0) ∈ EH, since we can not observe both exposures for the same unit. Also, for every i, j ∈ [n] with ( i, j ) ∈ E, we have that (ei, 1, e j, 0), (ei, 0, e j, 1), (ei, 1, e j, 1) ∈ EH. This is because if i receives the ei, 1 exposure, then i is treated, meaning that j has a neighbor that is treated, so j can not receive the ej, 0 or the ej, 1 exposure. Thus, the first property follows. For the second part, it follows from the previous discussion that for any i, j ∈ [n], we have that (ei, 1, e j, 1) ∈ EH if and only if ( i, j ) ∈ E. Therefore, the subgraph of H induced by T is isometric to 

G.

E.2 Tightness of Upper Bound for Direct Effect (Proposition 5.1) 

Proposition 5.1. For any sequence d = {d(n)}∞ 

> n=1

with d = o(n),

sup 

> G∈G d

R(G, τ DTE , 2) ≍ dn .

Proof. Let dn be a sequence of degrees such that dn = o(n) and dnn is even for all n. For a given 

n, we will construct a dn-regular graph Gn on n nodes, which we call the d-regular chorded cycle .We will assume that n is large enough so that dn < n/ 2, which is sufficient since our statement is asymptotic. The k-regular chorded cycle on n nodes Hn,k is defined as follows. We arrange the n

nodes in a cycle, labeled 1 , 2, . . . , n . Two nodes i, j ∈ [n] are connected if and only if |i − j| ≤ k,where the difference is taken modulo n. In other words, each node is connected to the k nodes on each side of it in the cycle. From the definition it follows immediately that Hn,k is 2 k-regular. Let us now define Gn. Since nd n is even, it must be that either n or dn is even. We distinghish between two cases. • If dn is even, we define Gn = Hn,d n/2.• If dn is odd, then n has to be even. We define Gn to be the graph obtained by taking the union of Hn, (dn−1) /2 and a perfect matching on the n nodes. In either case, Gn is dn-regular. We now show that R(Gn, τ, 2) = Θ 

> (

dn/n 

> )

.For the upper bound, we apply Theorem 4.1. Since Gn is dn-regular, by (i) of Lemma E.1 it follows that every node in VH has degree at most 2 dn + 1. Thus, λ(H) ≤ 2dn + 1. Thus, applying Theorem 4.1 with q = 2, we get that 

R(Gn, τ, 2) ≲ 2dn + 1 

n = O 

> (

dn

n

> )

.

For the lower bound, we apply Theorem 3.2 on the subset T = {ei, 1 : i ∈ [n]} of the vertices of H.We will upper bound the size of the largest independent set of H[T ]. By the isometry property (ii) of Lemma E.1, it suffices to bound the largest independent set of G. We will assume for simplicity 86 that n is divided by ⌊dn/2⌋, since the general case can be handled by a standard modification of this argument. We partition [ n] into the subsets {Ti}n/ ⌊dn/2⌋ 

> i=1

, where 

Ti = [ ⌊dn/2⌋(i − 1) , ⌊dn/2⌋i − 1] ∩ [n] .

Clearly Ti are disjoint and [ n] = ∪n/ ⌊dn/2⌋ 

> i=1

Ti. Also, within each Ti all nodes are connected to each other in G, since for any i, j ∈ Ti we have that |i − j| ≤ ⌊ dn/2⌋ ≤ dn. Therefore, any independent set in H[T ] can contain at most one node from each Ti. Since there are at most 

n

⌊ dn 

> 2

⌋ ≤ ndn/2 − 1 = O 

> (

ndn

> )

such subsets, we have that |I (H[T ]) | = |I| = O( ndn ). Thus, applying Theorem 3.2 for the subset T ,we get that 

R(Gn, τ, 2) ≳ 1

|I (H[T ]) | ≳ dn

n .

E.3 Tightness of Lower Bound for Direct Effect (Proposition 5.2) 

For Proposition 5.2, we will use a family of graphs called the Generalized Quadrangle . Thus, we first review some of the properties of these graphs that will be relevant for our purposes. To do that, it will be helpful to introduce some basic concepts from the algebraic theory of quadratic forms. For a detailed introduction to this topic, see Lam (2005). Let s ∈ N be a prime number and 

Fs be the finite field of residues modulo s. Let r ∈ N be a positive integer. For every x ∈ Frs, the corresponding projective point is the set of all scalar multiples of x, i.e. [x] = {y ∈ Frs : y = λx for some λ ∈ Fs} .

The set of all projective points is called the projective space P G (r − 1, s ). Let B : Frs × Frs → Fs

be a bilinear form. Let Q(r − 1, s ) be the set of all projective points [ x] ∈ P G (r − 1, s ) such that 

B(x, x ) = 0. We call Q(r − 1, s ) the quadric associated with B on the vector space Frs. There are three types of quadrics, depending on the dimension r and the Witt index 1 w of the bilinear form B.1. If r is even and w = r/ 2, then Q(r −1, s ) is called a hyperbolic quadric and denoted Q+(r −1, s ). 2. If r is even and w = r/ 2 − 1, then Q(r − 1, s ) is called an elliptic quadric and denoted 

Q−(r − 1, s ). 3. If r is odd and w = ( r − 1) /2, then Q(r − 1, s ) is called a parabolic quadric and denoted 

Q(r − 1, s ). Most of our discussion will focus on the case r = 5. Consider the matrix M ∈ F5×5 

> s

defined as 

M =

> 

0 1 0 0 01 0 0 0 00 0 0 1 00 0 1 0 00 0 0 0 1

> 

.

> 1For the definition of Witt index, see Chapter 1 in Lam (2005).

87 We associate to M the bilinear form B : V × V → Fs defined as B(x, y ) = xT M y for all x, y ∈ V .We will denote by Q(4 , s ) the quadric associated with B on the vector space F5 

> s

. The Witt index of 

B is 2, thus by the above discussion Q(4 , s ) is a parabolic quadric. The points in Q(4 , s ) will be the vertices of our graph. A line L in Q(4 , s ) is a 2-dimensional subspace of V such that B(x, x ) = 0 for all x ∈ L. We say a point [ x] is incident to a line L if x ∈ L. The point graph Γ( Q(4 , s )) associated with Q(4 , s ) is the graph with vertices corresponding to the points in Q(4 , s ), where two points are connected by an edge if and only if they are incident to the same line. We now summarize some basic properties of the graph Γ( Q(4 , s )) related to the number of vertices, edges and spectrum of the adjacency matrix. 

Lemma E.2. Let s ≥ 2 be a prime number. The following properties hold for the graph G =Γ( Q(4 , s )) .(i) The number of nodes is n = ( s + 1)( s2 + 1) .(ii) The graph is d-regular with d = s(s + 1) .(iii) The eigenvalues of the adjacency matrix AG are s(s + 1) , s − 1, −(s + 1) , with multiplicities 

1, s (s + 1) , s 2 respectively. Proof. As established in Section 3.1 of Payne and Thas (2009), G satisfies the general properties of a point-line geometry. From these properties, it follows that G is a d-regular graph with d = s(s + 1) and n = ( s + 1)( s2 + 1) nodes (see e.g. Godsil and Royle, 2013, Lemma 10.8.1). This establishes properties (i) and (ii). Property (iii) is exactly Lemma 10.8.2 of Godsil and Royle (2013). In order for us to construct an improved design that achieves the √d/n rate for this family of graphs, it is important to understand the structure of independent sets in G. We start by defining a 

hyperplane of PG( r, q ), which is a set of the form 

H = {[x] ∈ PG( r, q ) : α⊤x = 0 },

for some α ∈ Frq \ { 0}. Let H be a hyperplane of PG(4 , s ) and I = H ∩ Q(4 , s ). If I is isomorphic to the elliptic quadric Q−(3 , s ), then we call I an ovoid of Q(4 , s ). By definition, I will have Witt index 1, thus it will contain no lines, meaning that I is an independent set in G. By Lemma 3.4.1 in Payne and Thas (2009), ovoids always exist in Q(4 , s ). The following lemma establishes some crucial properties of ovoids that will be used in the construction of our design. 

Lemma E.3. Let G = Γ( Q(4 , s )) and F be the family of ovoids in Q(4 , s ). Then, the following properties hold. (i) Every ovoid has size s2 + 1 .(ii) Every node i belongs in the same number of independent sets in the family {If }f ∈F .(iii) Every pair of nodes i, j for which (i, j ) /∈ E belongs in the same number of independent sets in the family {If }f ∈F .

88 Proof. Property (i) is exactly Lemma 1.8.1 of Payne and Thas (2009). For the second and third properties, we will use Witt’s Extension Theorem, as stated in Theorem 22 of Clark (2013). This Theorem states that any isometry between two subspaces of a quadratic space can be extended to an isometry of the whole space. In particular, let [ x], [y] ∈ Q(4 , s ) be two different vertices of the graph. Consider the one dimensional subspaces V1 = [ x] and V2 = [ y], which are clearly isomorphic. By Witt’s Extension Theorem, there exists an isometry of the whole space that maps 

V1 to V2, thus maps [ x] to [ y]. Since it is an isometry, it preserves the quadratic form, thus all edges between vertices are preserved. This means that ovoids containing [ x] are in 1-1 mapping with ovoids containing [ y], establishing property (ii). Also, let ([ x], [y]) and ([ z], [u]) be two pairs of non-adjacent nodes in G. Consider the two-dimensional subspaces V1 = span([ x], [y]) and V2 = span([ z], [u]). Since [ x], [y] are non-adjacent, we have that B(x, y )̸ = 0 and B(x, x ) = B(x, y ) = 0, thus the restriction of the quadratic form to V1

leads to a hyperbolic quadric. The same is true for V2, thus V1 and V2 are isometric. By Witt’s Extension Theorem, there exists an isometry of the whole space that maps V1 to V2. Since edges are preserved by the isometry, ovoids containing [ x], [y] are in 1-1 mapping with ovoids containing [z], [u], establishing property (iii). We will use the family F of independent sets in the graph G of Lemma E.3 to construct a design that achieves the √d/n rate for this family of graphs. The pseudocode for the design is given in Algorithm 5. 

Algorithm 5: Independent Set Design 

Input: Graph G on n nodes as in Lemma E.2. 

Output: Random intervention Z ∈ { 0, 1}n. 

> 1

Initialize the intervention vector Z ← 0. 

> 2

Select f ∈ F uniformly at random.  

> 3

Sample variables {Ui : i ∈ [n]} independently and identically as 

Ui ←

> {

0 with probability 1 /21 with probability 1 /2

for i ∈ [n] do  

> 4

if i ∈ If then  

> 5

Zi ← Ui 

> 6

else  

> 7

Zi ← 0The design works by selecting a uniformly random independent set from the family {If }f ∈F 

and then treating the nodes in that independent set with probability 1 /2 each, while leaving the rest of the nodes untreated. Therefore, we call this the Independent Set Design . The properties of the family {If }f ∈F will enable a precise characterization of the spectrum of the covariance matrix that arises from this design. Our estimator ˆ τ will be a Horvitz-Thompson type of estimator. For i ∈ [n], k ∈ { 0, 1}, we define the event Ai,k = {i ∈ If , Z i = k}, which corresponds to the event that node i is in the independent set that was selected and receives treatment k. We will use the following estimator for τ

89 ˆτ = 1

n

> n

∑

> i=1

( 1[Ai, 1]Pr( Ai, 1) − 1[Ai, 0]Pr( Ai, 0)

)

Yi . (22) Our next lemma shows that this estimator is unbiased for τ .

Lemma E.4. The estimator ˆτ defined in (22) is an unbiased estimator for τ , i.e. E[ˆ τ ] = τ .Proof. We start by observing that if i ∈ If and Zi = k, then all neighbors of i are in control thus hi(Z) = ei,k and Yi = yi(ei,k ). It also follows by Algorithm 5 that Pr( Ai,k ) > 0 for all 

i ∈ [n], k ∈ { 0, 1}. Therefore, we can write E[ˆ τ ] = E 

[ 1

n

> n

∑

> i=1

( 1[Ai, 1]Pr( Ai, 1) yi(ei, 1) − 1[Ai, 0]Pr( Ai, 0) yi(ei, 0)

)] 

= 1

n

> n

∑

> i=1

( Pr( Ai, 1)Pr( Ai, 1) yi(ei, 1) − Pr( Ai, 0)Pr( Ai, 0) yi(ei, 0)

)

= τ .

We now provide an auxiliary lemma that contains expressions for the variance and covariance terms arising in the analysis of the variance of the estimator ˆ τ .

Lemma E.5. Suppose G is given by Lemma E.2 and Z is generated according to the Independent Set Design of Algorithm 5. Then, for every i, j ∈ [n], k ∈ { 0, 1} we have that 1. 

Var 

( 1[Ai,k ]Pr( Ai,k )

)

= s . 

2. If i̸ = j

Cov 

( 1[Ai,k ]Pr( Ai,k ) , 1[Aj,k ]Pr( Aj,k )

)

=

{ 1 

> s

if (i, j ) /∈ E

−1 if (i, j ) ∈ E .

Proof. We start with the first part. We have that Var 

( 1[Ai,k ]Pr( Ai,k )

)

= 1Pr( Ai,k ) − 1Thus, it suffices to compute Pr( Ai,k ). Since the subset If is selected uniformly at random from the family {If }f ∈F , independently of the random variables {Ui}ni=1 , we have that Pr( Ai,k ) = Pr( i ∈ If , Z i = k)= Pr( i ∈ If ) Pr( Zi = k|i ∈ If )= Pr( i ∈ If ) Pr( Ui = k)= 12

|{ f : i ∈ If }| |F| .

90 Thus, it suffices to compute |{ f : i ∈ If }| . By property (ii) of Lemma E.3, every node belongs in the same number of independent sets in the family {If }f ∈F , thus this set has the same size M for all i ∈ [n]. We have that 

M n =

> n

∑

> i=1

|{ f : i ∈ If }| 

= ∑ 

> f∈F

|If |

by exhanging the order of summation = |F| (s2 + 1) ,

where in the last step we used property (i) of Lemma E.3. Thus, M = |F| (s2 + 1) /n . Therefore, we have that Pr( Ai,k ) = 12

|{ f : i ∈ If }| |F| 

= 12

|F| (s2 + 1) /n 

|F| 

= 12

s2 + 1 

n

= 12

s2 + 1 (s + 1)( s2 + 1) = 12( s + 1) ,

where we used property (i) of Lemma E.2 in the last step. Thus, we have that Var 

( 1[Ai,k ]Pr( Ai,k )

)

= 1Pr( Ai,k ) − 1 = 2 s + 1 For the second part, we have that Cov 

( 1[Ai,k ]Pr( Ai,k ) , 1[Aj,k ]Pr( Aj,k )

)

= Pr( Ai,k ∩ Aj,k )Pr( Ai,k ) Pr( Aj,k ) − 1Thus, it suffices to compute Pr( Ai,k ∩ Aj,k ). If ( i, j ) ∈ E, then i and j can not be in the same independent set, thus Pr( Ai,k ∩ Aj,k ) = 0. This proves the first case. If ( i, j ) /∈ E, then by property (iii) of Lemma E.3 every pair of non-adjacent nodes belongs in the same number of independent sets in the family {If }f ∈F . Let L be this number. Following the same strategy as before, we sum over all non-adjacent pairs of nodes and get that 

L ·

(( n

2

)

− nd 

2

)

= ∑ 

> i,j :( i,j )/∈E

|{ f : i, j ∈ If }| 

= ∑ 

> f∈F

(|If |

2

)

91 by exhanging the order of summation = |F| (s2 + 1) s2

2 ,

where in the last step we used property (i) of Lemma E.3. Thus, we have that Pr( Ai,k ∩ Aj,k ) = Pr( i, j ∈ If , Z i = k, Z j = k)= Pr( i, j ∈ If ) Pr( Zi = k, Z j = k|i, j ∈ If )= Pr( i, j ∈ If ) Pr( Ui = k, U j = k)= 14

L

|F| 

= 14

> (s2+1) s2
> 2

(n

> 2

) − nd 

> 2

= 14(s2 + 1) s2

n(n − 1 − d)= 14(s2 + 1) s2

(s + 1)( s2 + 1)(( s + 1)( s2 + 1) − 1 − s(s + 1)) = 14

s2

(s + 1) s3

= 14s(s + 1) .

Therefore, Cov 

( 1[Ai,k ]Pr( Ai,k ) , 1[Aj,k ]Pr( Aj,k )

)

= Pr( Ai,k ∩ Aj,k )Pr( Ai,k ) Pr( Aj,k ) − 1=

> 14s(s+1) 14( s+1) 2

− 1= (s + 1) 2

s(s + 1) − 1= s + 1 

s − 1= 1

s .

We are now ready to prove Proposition 5.2, which we also restate here for convenience. 

Proposition 5.2. There exists a sequence d = {d(n)}∞ 

> n=1

such that d = Θ( n2/3) and 

inf 

> G∈G d

R(G, τ DTE , 2) ≍√dn .

92 Proof. Assume for simplicity that there is a prime number s such that n = ( s + 1)( s2 + 1) and 

d = s(s + 1). We will show in the end how to extend to arbitrary values of n and d by adding isolated nodes to the graph. Let G be the graph on n nodes with the properties of Lemma E.2. Let us start from the lower bound. Since G is d-regular, by (i) of Lemma E.1 it follows that every node in the conflict graph H has degree at least d. Therefore, d∗(H) ≥ d. Thus, applying the lower bound of Corollary 1.1 with q = 2, we get that 

R(G, τ, 2) ≳

√dn .

Now, let us establish the upper bound. This involves finding a statistical procedure, i.e. a pair of design P and estimator ˆ τ , whose risk is O(√d/n ). We will use the Independent Set Design of Algorithm 5 as our design P . Below, when we write probabilities, it will always be with respect to the randomness of the Independent Set Design. Our estimator ˆ τ will be the one defined in (22). As established in Lemma E.4, this estimator is unbiased for τ . Therefore, it suffices to upper bound the variance of ˆ τ . To simplify analysis, we break down the estimator into the estimators for the two exposures, i.e. we write ˆ τ = ˆ τ1 − ˆτ0, where ˆτ1 = 1

n

> n∑
> i=1

1[Ai, 1]Pr( Ai, 1) yi(ei, 1)ˆτ0 = 1

n

> n∑
> i=1

1[Ai, 0]Pr( Ai, 0) yi(ei, 0)We have that Var(ˆ τ ) = Var(ˆ τ1 − ˆτ0)= Var(ˆ τ1) + Var(ˆ τ0) − 2 Cov(ˆ τ1, ˆτ0)

≤ Var(ˆ τ1) + Var(ˆ τ0) + 2 

> √

Var(ˆ τ1) Var(ˆ τ0)

≤ 2 Var(ˆ τ1) + 2 Var(ˆ τ0) ,

where we used the Cauchy-Schwarz and AM-GM inequalities. Therefore, it suffices to upper bound Var(ˆ τ1) and Var(ˆ τ0). We focus on ˆ τ1, since the analysis for ˆ τ0 is identical. We have Var(ˆ τ1) = 1

n2 Var  

> (n∑
> i=1

1[i ∈ If , Z i = 1] Pr( i ∈ If , Z i = 1) yi(ei, 1)

> )

= 1

n2

> n∑
> i=1
> n∑
> j=1

Cov( 1[i ∈ If , Z i = 1] , 1[j ∈ If , Z j = 1]) Pr( i ∈ If , Z i = 1) Pr( j ∈ If , Z j = 1) yi(ei, 1)yj (ej, 1) .

Define the vector α ∈ Rn as αi = yi(ei, 1)/√n for i ∈ [n]. Then, the preceding variance can be written as a quadratic form Var(ˆ τ1) = 1

n α⊤Cα ,

where C ∈ Rn×n is the matrix with entries 

Ci,j = Cov( 1[Ai, 1], 1[Aj, 1]) Pr( Ai, 1) Pr( Aj, 1) .

93 Since y ∈ M (G, τ, 2), we have that 

∥α∥2 = 1

n

> n∑
> i=1

yi(ei, 1)2 ≤ 1 .

It follows that Var(ˆ τ1) ≤ 1

n ∥C∥2 . (23) We have already calculated all entries of C in Lemma E.5. In particular, if I ∈ Rn×n is the identity matrix, 1 ∈ Rn is the all 1 vector and AG is the adjacency matrix of G, then we can write 

C = 2( s + 1) I + 1

s (11 ⊤ − I − AG) − AG

=

> (

2( s + 2) − 1

s

> )

I −

> (

1 + 1

s

> )

AG + 1

s 11 ⊤ .

The matrix 11 ⊤/s has one eigenvalue equal to n/s with eigenvector 1 and n − 1 eigenvalues equal to 0. The matrix AG has leading eigenvalue equal to d with eigenvector 1. Thus, the absolute value of the eigenvalue corresponding to the eigenvector 1 for matrix 11 ⊤/s − (1 + 1 /s )AG is equal to ∣∣∣∣∣

ns −

> (

1 + 1

s

> )

d 

> ∣∣∣∣∣

=

> ∣∣∣∣∣

(s + 1)( s2 + 1) 

s −

> (

1 + 1

s

> )

s(s + 1)  

> ∣∣∣∣∣

= s2 − 1

s = O(s) .

By property (iii) of Lemma E.2, the remaining eigenvalues of AG are s − 1 and −(s + 1), which are both O(s) in absolute value. Thus, we have established that 

> ∥∥∥∥∥

1

s 11 ⊤ −

> (

1 + 1

s

> )

AG

> ∥∥∥∥∥2

= O(s) .

We also trivially have that ∥∥∥∥∥(

2( s + 2) − 1

s

> )

I

> ∥∥∥∥∥2

= O(s) .

Thus, by the triangle inequality we have that ∥C∥2 = O(s). Therefore, by (23) we concude that Var(ˆ τ1) = O(s/n ). By the same argument, we also have that Var(ˆ τ0) = O(s/n ). Thus, we have that Var(ˆ τ ) = O(s/n ). Since s = Θ( √d), we have that Var(ˆ τ ) = O(√d/n ), which establishes the upper bound. 

E.4 Random Regular Graphs (Corollaries 5.3 and 5.4) 

A random d-regular graph G is a graph chosen uniformly at random from the set of all d-regular graphs on n vertices Gn,d . We denote this as G ∼ U (Gn,d ). In this section, we identify the minimax rate for random d-regular graphs up to a logarithmic factor. We will use the following fact about independent sets in d-regular graphs. In this section, we identify the minimax rate for random 

d-regular graphs up to a logarithmic factor. We will use the following fact about independent sets in random d-regular graphs. Recall that for a graph G, we denote by I(G) the largest independent set in G.94 Theorem E.6. Suppose G ∼ U (Gn,d ). There exist constants c1, c 2 > 0 such that if c1 ≤ d < n − c2n/ log n, then with probability 1 − o(1) as n → ∞ |I (G)| = O 

> (

n log dd

> )

.

Proof. Frieze and Łuczak (1992) showed that there exists a constant c1 > 0 such that if d ≥ c1 and 

d = o(n1/3), then for G ∼ U (Gn,d ), with probability 1 − o(1) as n → ∞ |I (G)| = O 

> (

2n log dd

> )

.

Furthermore, from the discussion after Theorem 5.2 in Krivelevich et al. (2001), it follows there exists a constant c2 > 0 such that for any α < 1/2, if nα < d < n − c2n/ log n, then with probability 1 − o(1) 

|I (G)| = O 

> (

n log dd

> )

.

We can thus choose any α < 1/3 and combine these two results to get the desired conclusion. We are now ready to prove Corollary 5.3, which we restate here for convenience. 

Corollary 5.3. Suppose G is a uniformly random d-regular graph with d = o(n). Then, with probability at least 1 − o(1) over the random choice of G,

dn · log d ≲ R(G, τ DTE , 2) ≲ dn .

Proof. The upper bound follows from Corollary 1.1. For the lower bound, we can apply Theorem E.6 to get that with probability 1 − o(1) as n → ∞ |I (G)| = O 

> (

n log dd

> )

.

We can then apply the global lower bound of Theorem 3.2 to get that with probability 1 − o(1) as 

n → ∞ 

R(Gn,d ) = Ω  

> (

log dd

> )

,

which completes the proof. Finally, we present the proof of Corollary 5.4 for the global effect, which we restate here for convenience. 

Corollary 5.4. Suppose G is a random d-regular graph with d = o(√n). Then, with probability at least 1 − o(1) over the random choice of G,

dn ≲ R(G, τ GATE , 2) ≲ d2

n .

95 Proof. Corollary 1.2 yields that for any graph G,

> √

davg (G2)

n ≲ R(G, τ GATE , 2) ≲ λ(G2) + 1 

n .

If G is a d-regular graph, then every node has exactly d neighbors, and each of those neighbors has at most d − 1 other distinct neighbors. Therefore, every node in G2 has degree at most d(d − 1) + d = d2.It follows that λ(G2) ≤ d2 for every d-regular graph G, from which the upper bound follows. For the lower bound, let Xr denote the number of walks of length r in G. Then, if di(G2) denotes the degree of node i in G2, we have that 

> n∑
> i=1

di(G2) = nd 2 − 6X3 − 4X4 . (24) This is because each triangle of nodes a, b, c contributes two “invalid” paths of lenght 2 for each of the three vertices. For example, for vertex a, the paths a → b → c and a → c → b are invalid, since they end up in a 1-hop neibhbor of a. Similarly, each 4-cycle of nodes a, b, c, d contributes one invalid path of length 2 for every vertex. For example, for vertex a, the paths a → b → c and 

a → d → c both end up in the same vertex, so one of them is considered invalid. Using Theorem 3(a) from McKay, Wormald, and Wysocka (2004), we have the following bounds on the expected number of triangles and 4-cycles in a random d-regular graph G, which hold as long as d = o(√n). E[ X3] = d3

6 (1 + o(1)) and E[ X4] = d4

8 (1 + o(1)) .

By Markov’s inequality and the fact that d = o(n), if follows that X3 = op(nd 2) and X4 = op(nd 2). Therefore, by equation (24), we have that 

davg (G2) = 1

n

> n∑
> i=1

di(G2) = d2(1 + op(1)) ,

from which the lower bound follows. 96