---
title: "Generated outcomes as generated regressors: Equivalences in recursive causal estimation"
title_zh: 生成结果作为生成回归变量：递归因果估计中的等价性
authors: "Wisse Rutgers, Rahul Singh"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.29009v1"
tags: ["query:two-sided"]
score: 9.0
evidence: 递归因果估计中治疗效应的等价关系
tldr: 递归因果估计中，时变处理效应、替代识别效应和中介效应均可表示为递归回归，其中前一步的预测值成为下一步的生成结果。本文比较了递归插件、递归平衡权重和递归双稳健三种估计量，发现当每阶段使用普通最小二乘法（OLS）拟合时，三者无论模型是否正确指定，在任意有限样本下数值完全等价。对于Ridge惩罚的平衡权重，双稳健估计表现为逐阶段混合OLS与惩罚回归的反向递归，且OLS权重随阶段数几何衰减。这表明横截面中偏差校正使估计量趋近OLS的直觉在多阶段下逐渐失效。此外，针对一般凸惩罚，推导了每个阶段的恒等式。
source: arxiv
selection_source: fresh_fetch
motivation: 递归回归中生成结果作为回归量时，不同估计量（如插件、平衡权重、双稳健）的行为和等价性关系尚不明确，需要系统研究。
method: 形式化比较三种递归因果估计量，分析在每阶段使用OLS或Ridge惩罚时它们的有限样本与渐近关系，并推导一般凸惩罚下的阶段性恒等式。
result: OLS下三种估计量任意有限样本完全等价；Ridge惩罚下双稳健估计是OLS与惩罚回归的几何加权递归，权重随阶段数指数衰减。
conclusion: 多阶段递归估计中横截面偏差校正直觉不再适用，一般凸惩罚下的恒等式为因果推断提供了统一框架。
---

## 摘要
时变处理效应、替代标识的处理效应和中介效应都可以写成递归回归的形式，其中每个回归的预测值成为下一个回归的生成结果。我们研究标准因果估计量在这种设定下的行为。形式上，我们比较了递归代入估计量、递归平衡权重估计量和递归双稳健估计量。当每一阶段都使用普通最小二乘法（OLS）拟合时，无论模型是否正确指定，这三个递归估计量在任何有限样本中都一致。因此，通过递归回归生成结果进行估计在数值上等价于通过递归平衡生成回归变量进行估计。在平衡权重使用岭惩罚的情况下，双稳健估计量是各阶段有惩罚和OLS回归混合的反向递归。递归OLS回归的权重随着时间期数几何衰减。因此，来自截面设置的直觉——偏差修正将估计量推向OLS——随着时间期数的增加而越来越不适用。对于一般的凸惩罚，我们在每个阶段推导出一个恒等式。

## Abstract
Time-varying treatment effects, surrogate-identified treatment effects, and mediation effects can all be written as recursive regressions, in which each regression's predicted values become generated outcomes for the next regression. We study how standard causal estimators behave in this setting. Formally, we compare the recursive plug-in, recursive balancing weight, and recursive doubly robust estimators. When every stage is fitted by ordinary least squares (OLS), the three recursive estimators coincide in any finite sample, whether or not the models are correctly specified. As such, estimation by recursively regressing generated outcomes is numerically equivalent to estimation by recursively balancing generated regressors. Under ridge penalisation for the balancing weights, the doubly robust estimator is a backward recursion of stage-wise blends of penalised and OLS regressions. The weight on the recursive OLS regression decays geometrically in the number of time periods. Therefore, the intuition from the cross-sectional setting, where the bias correction moves the estimator towards OLS, applies less and less as the number of time periods increases. For general convex penalties, we derive an identity at each stage.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，以 Markdown 形式生成一份详细的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：这篇论文研究的是在**递归因果估计**的框架下，几种标准估计量（递归插件、递归平衡权重、递归双稳健）之间的数值等价关系。这个框架广泛应用于时变处理效应、替代标识（Surrogacy）和中介效应（Mediation）等重要的因果推断问题中。
- **研究动机**：在横截面（只有一期）的因果推断中，已有研究表明某些回归估计量、平衡权重估计量和双稳健估计量之间存在数值等价，且双稳健估计的偏差校正可理解为向普通最小二乘法（OLS）的收缩（即欠平滑）。作者的核心问题是将这一结论推广到**多阶段递归**设定下时，这些等价性和解释是否仍然成立？
- **整体含义**：研究发现，在更复杂的多阶段递归设定下，原有的等价性仍然成立，但关键性的解释——“偏差校正即向OLS收缩”——却随着时间阶段数的增加而逐渐失效。这为理解多阶段因果推断中正则化的作用提供了新的视角，并指出了横截面结果在纵向数据中应用时的局限性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明即可）

- **核心思想**：论文采用代数方法，在**线性估计框架**下分析了递归因果参数。它假设每阶段的先验函数（outcome regression）和 Riesz 表示函数（用于平衡权重）都是同一组基函数（dictionary）的线性组合，这适用于许多非参数估计器（如级数、Lasso、核方法）。
- **关键技术细节**：
    1.  **递归函数类**：形式化定义了递归因果参数，该参数通过后向的条件期望递归（g-computation公式）和前向的Riesz表示递归（逆概率加权公式）来识别。双稳健估计则通过结合两者来构建正交矩。
    2.  **线性估计框架**：设定了每阶段的基函数、由基函数生成的Gram矩阵和交叉矩矩阵，以及定义在线性系数上的凸惩罚函数。
    3.  **三种估计量**：
        - **递归插件估计**（`θ̂^P`）：通过后向递归回归每阶段的生成结果（即在给定历史数据下对Y或前阶段预测值的条件期望）来计算。
        - **递归平衡权重估计**（`θ̂^Q`）：通过前向递归估计每阶段的Riesz表示函数（作为权重），最终实现对结果的加权。
        - **递归双稳健估计**（`θ̂^DR`）：结合上述两者，在正交矩估计方程中同时使用插件和权重进行偏差校正。

- **核心算法/公式推导路径**：
    1.  **无正则化（OLS）**：论文证明在每阶段均使用OLS的情况下，所有三种估计量在任何有限样本下完全等价。具体证明是：每阶段的偏差校正项（`̂η_t' * ̂G_t * 残差`）在OLS下因为正交性自动为零，并且所有中间的双线性形式都等于同一个值。
    2.  **Ridge正则化**：当对平衡权重使用Ridge惩罚时，双稳健估计量可被表示为一个递归的OLSa，其系数（`β̂^{Aug}`）是由每阶段的Ridge收缩矩阵（`A_t`）混合了基础Outcome回归系数（`β̂_{gen}`）和本阶段的OLS系数（`β̂_{OLS}`）反向递归计算而来。关键发现是，最终目标中纯OLS插件的权重是各阶段收缩因子（`a_t`）的乘积，随阶段数（`T`）**几何衰减**（如标量对角情况，权重为 `∏ a_t`）。
    3.  **一般凸惩罚**：由于无法得到封闭解，论文推导出了一个恒等式（Telescoping identity，公式43），将双稳健估计量分解为 OLS 估计量加上各阶段 Riesz 残差（`c_t`）与Outcome回归系数偏差（`β̂_{gen} - β̂_{OLS`）的内积之和。这揭示了双稳健估计纠正偏差的迭代结构。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集与场景**：这篇论文是纯**理论分析**，没有使用任何真实或模拟数据集进行数值实验。它通过三个理论示例来阐述其通用框架的适用性：
    1.  时变处理效应（T=2）
    2.  替代标识分析（Surrogacy, T=2）
    3.  中介效应分析（Mediation, T=2）
    以及一个非递归的基准示例（单期，T=1）。
- **Benchmark**：论文的理论基准是横截面（T=1）情况下已有的结论（Bruns-Smith et al., 2025）。
- **对比方法**：论文在理论上对比了**递归插件估计**、**递归平衡权重估计**和**递归双稳健估计**在无正则化（OLS）、Ridge正则化和一般凸正则化下的代数结构。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- **明确指出**：作为一篇纯理论推导和证明的论文，本文**没有使用任何计算实验**，因此**完全不涉及任何关于GPU型号、数量或训练时长的描述和讨论**。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

- **实验数量**：论文**没有进行任何数值或模拟实验**，所有结论均基于代数推导和数学证明。
- **充分性判断**：对于其研究目标——**揭示估计量之间的代数结构和等价关系**——论文的证明是详尽、严格且自洽的。通过覆盖OLS、Ridge和一般凸惩罚三种情况，并推导出不同正则化下的关键恒等式和收缩路径，其理论分析是充分的。它不需要依赖于实验数据来证明其代数结论，因此可以认为是客观的。没有实验也意味着不存在不公平的比较基准问题。

### 6. 论文的主要结论与发现

1.  **等价性**：当所有阶段都使用OLS时，递归插件、递归平衡权重和递归双稳健估计量在任意有限样本下**完全等价**。用递归回归生成结果进行估计等价于用递归平衡生成回归器进行估计。
2.  **Ridge惩罚下的结构**：当平衡权重使用Ridge惩罚时，双稳健估计量等于一个由各阶段混合（`A_t`）了基础回归和OLS回归的系数反向递归计算得来的“增压”插件估计量。
3.  **衰减效应**：在Ridge惩罚下，最终双稳健估计量中纯OLS插件的权重随阶段数（`T`）**几何衰减**。这意味着随着时间阶段的增加，双稳健估计量越来越不像（欠平滑的）OLS，而更像有偏的正则化模型。
4.  **与横截面结论的对比**：论文的核心阴性结论是：来自横截面（T=1）的直觉——“偏差校正使估计量向OLS收缩（欠平滑）” ——**不适用于多阶段递归设定**。阶段数`T`是理解正则化作用的关键，`T`越大，该直觉越不成立。
5.  **一般凸惩罚下的结构**：推导出了通用恒等式（公式43），将双稳健估计量分解为OLS估计量加上一系列各阶段残差与系数偏差的内积和，这揭示了其偏差纠正的迭代和耦合本质。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论建树上的一致性**：成功地将横截面因果推断中关于估计量等价和偏差校正的重要结论推广到了更复杂、应用更广泛的递归因果模型中，建立了统一的理论框架。
- **深刻的洞察和反直觉发现**：清晰揭示了“阶段数”这一在横截面中不存在的维度如何改变了双稳健估计的行为模式，并指出“欠平滑”直觉的局限性，这是一个新颖且深刻的洞见。
- **严谨的代数方法**：通过代数推导而非渐近分析来研究有限样本行为，提供了精确且无概率误差的结论。这种方法在理论计量经济学和统计学中具有很高的价值。
- **覆盖广泛的惩罚形式**：不仅分析了无惩罚和Ridge惩罚，还通过推导一般凸惩罚下的恒等式，为处理更复杂的正则化技术（如Lasso）提供了一个统一的分析起点。
- **清晰的定位与**：详细阐述了其工作与前人工作（Bruns-Smith et al., 2025, Chernozhukov et al., 2022b等）之间的关联，使其贡献点变得清晰明确。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺乏实证验证**：作为一篇纯理论论文，它没有提供任何模拟或实证研究来支撑其理论发现，这是一个主要的局限。读者无法直观地看到理论结论在实际数据或模拟场景下的具体表现和收敛速度。
- **基础函数线性假设**：虽然该假设涵盖了多种非参数方法（级数、核方法），但仍有一些现代流行的复杂模型（如某些深度神经网络、无需特征映射的随机森林）不直接遵守这一线性形式。结论的普适性受到限制。
- **理论分析的简化**：分析假设了有限维或无限维特征空间下的理想条件（如Gram矩阵可逆）。在高维（变量数多于观测数）或近奇异的实际数据中，依赖Moore-Penrose伪逆的扩展虽然在附录中讨论，但其数值表现和稳定性可能需要进一步验证。
- **应用层面的缺失**：论文没有提供关于如何在实际应用中**选择正则化参数**的指导。尽管揭示了其几何衰减的本质，但并未给出选择`δ_t`或`λ_t`的实用准则，这降低了其直接应用于实证研究的门槛。
- **结论的通用性边界**：论文的主要负向结论（“欠平滑”直觉不适用）是在特定假设下证明的。在更复杂（例如，混合人群、非二进制处理、存在不可忽略的丢失数据）的场景下，这个结论是否完全成立，论文没有探讨。

（完）
