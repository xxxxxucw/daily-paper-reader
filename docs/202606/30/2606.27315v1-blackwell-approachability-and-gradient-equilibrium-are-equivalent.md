---
title: Blackwell Approachability and Gradient Equilibrium are Equivalent
title_zh: Blackwell可接近性与梯度均衡等价
authors: "Brian W. Lee, Nika Haghtalab, Michael I. Jordan, Ryan J. Tibshirani"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27315v1"
tags: ["query:cp"]
score: 9.0
evidence: 证明布莱克威尔可接近性与梯度均衡等价，并应用于在线共形预测
tldr: 梯度均衡（GEQ）是最近提出的在线优化框架，泛化了离线优化中的一阶平稳性，并抽象了在线共形预测等问题。本文证明GEQ与Blackwell可逼近性在算法意义上是等价的，即两者可以互相归约且渐近误差率不变。结合已知等价关系，GEQ也与遗憾最小化和标定等价。该归约高效，可将乐观性和强自适应性等精细保证从遗憾最小化迁移到GEQ。此外，论文还识别了GEQ的必要充分条件，并建立了无约束和有约束决策集下不同GEQ概念之间的归约。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27315v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1106, \"height\": 396, \"label\": \"Figure\"}]"
motivation: 揭示GEQ与已有在线学习框架（如遗憾最小化）之间的精确关系，明确其在线学习全景中的位置。
method: 通过构造双向黑箱归约证明GEQ与Blackwell可逼近性等价，并利用已有等价链连接其他框架。
result: 证明了GEQ与Blackwell可逼近性、遗憾最小化、标定等框架在算法上等价，且归约高效无渐近损失。
conclusion: GEQ并非孤立概念，而是在线学习核心等价类的一员，其误差率与遗憾可互相转化。
---

## 摘要
梯度均衡（GEQ）是最近引入的一种在线优化框架，它将一阶平稳性从离线优化推广开来，并抽象了在线共形预测等问题。虽然GEQ与已知的在线学习框架（即遗憾最小化）存在有趣的相似之处，但先前的研究表明，GEQ误差和遗憾是不可比较的目标，这使得我们无法准确理解GEQ如何融入更广泛的在线学习格局。在本文中，我们证明GEQ在算法意义上等价于Blackwell可接近性。也就是说，Blackwell可接近性问题总是可以通过查询黑箱GEQ预言机来求解，且预言机的错误率没有渐近损失，反之亦然。结合可接近性、遗憾最小化和校准之间的已知等价关系，这些结果表明GEQ也与这些框架等价。我们的归约是高效的，可用于将精细化保证（如乐观性和强自适应性）从遗憾最小化转移到GEQ。在此过程中，我们还识别了GEQ的必要和充分条件，并建立了无约束和有约束决策集下不同GEQ概念之间的归约。

## Abstract
Gradient equilibrium (GEQ) is a recently introduced online optimization framework that generalizes first-order stationarity from offline optimization and abstracts problems like online conformal prediction. While GEQ has curious similarities with known online learning frameworks, namely regret minimization, prior work has shown that GEQ error and regret are incomparable objectives, leaving open a precise understanding of how GEQ fits into the broader online learning landscape. In this work, we show that GEQ is equivalent to Blackwell approachability in the algorithmic sense. That is, a Blackwell approachability problem can always be solved using queries to a black-box GEQ oracle, with no asymptotic loss in the oracle's error rate, and vice versa. Taken together with known equivalences between approachability, regret minimization, and calibration, these results imply that GEQ is equivalent to these frameworks, as well. Our reductions are efficient and can be used to transfer refined guarantees, such as optimism and strong adaptivity, from regret minimization to GEQ. Along the way, we also identify necessary and sufficient conditions for GEQ, and establish reductions between different notions of GEQ with unconstrained and constrained decision sets.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

梯度均衡（Gradient Equilibrium, GEQ）是Angelopoulos等人（2025）提出的在线优化新框架，旨在将离线优化中的一阶平稳性（梯度为零）推广到在线场景，并已用于抽象在线共形预测等问题。然而，GEQ与经典的遗憾最小化（Regret Minimization, RM）之间关系一直不明确：先前工作表明两者误差不可比较，且适用的损失类（修复性与凸性）也不可比较。本论文的核心问题是：**GEQ在在线学习全景中究竟处于什么位置？** 它的核心贡献是证明GEQ与Blackwell可接近性（Blackwell Approachability, BA）在算法意义上是等价的，从而通过已知等价链（BA↔RM↔校准），GEQ也与RM和校准等价。这意味着虽然GEQ的语义不同，但其算法能力与经典框架完全相同。

### 方法论：核心思想、关键技术细节、算法流程

论文构造了双向的黑箱预言机归约，核心思想基于一个简单观察：GEQ误差可写成点到原点的距离，而距离可通过支撑函数表示为最大值形式，从而自然对应BA问题。

**1. GEQ归约为BA（Section 3）**  
- 将GEQ问题（Θ=ℝᵈ，向量场类G）映射为BA问题：行动集A=ℝᵈ，自然集B=G，支付函数f(θ,g)=−g(θ)，目标集S={0}。  
- 证明GEQ的修复性条件蕴含Blackwell条件（Lemma 2），并构造**近似半空间预言机**：用递增函数α(t)将BA返回的方向缩放后作为决策θₜ=α(t)·uₜ/‖uₜ‖₂。由于修复性仅在θₜ范数≥h时保证方向正确，该方法最多在⌈α⁻¹(h)⌉轮内发生错误，其余轮次满足⟨uₜ,−gₜ(θₜ)⟩≤0。  
- 使用鲁棒BA预言机（允许有限错误轮次），可保持与原BA预言机相同的渐近误差率（Theorem 3）。特别地，当BA用Blackwell算法时，可恢复OGD的误差界。

**2. BA归约为GEQ（Section 4）**  
- 核心技巧：将BA问题（目标集S为锥）转化为GEQ问题，决策集Θ=S°（对偶锥），向量场为g_b(θ)=−f(O_H(θ),b)。  
- 证明该GEQ满足(0,0)-修复性（Lemma 4），且GEQ预言机保证存在法向量序列，其时间均值位于目标集内，从而隐式地使真实支付时间均值逼近目标集（Lemma 5）。  
- 为解决有约束GEQ问题，论文进一步给出从有约束GEQ到无约束GEQ的归约（Section 4.2）：通过添加投影残差项构造新向量场，使无约束GEQ算法可求解原问题（Lemma 7, Lemma 8）。  
- 结合锥提升技巧（Abernethy et al. 2011），可将非锥目标集问题转化为锥形式，最终得到BA→GEQ(ℝᵈ)的完整归约（Theorem 9）。

**3. 应用（Section 5）**  
- 通过组合BA↔RM归约，导出GEQ→RM和RM→GEQ的算法。  
- GEQ→RM（Corollary 10）：将RM预言机直接用于选择方向，缩放后输出决策，得到GEQ误差受RM误差加常数项控制。  
- 乐观性和强自适应性迁移（Corollary 11, 12）：注入乐观RM算法或强自适应RM算法，即可得到具有相应精细保证的GEQ算法。  
- RM→GEQ（Corollary 13）：用无约束GEQ预言机（如步长常数OGD）可在不预设L和T的情况下实现最优遗憾，无需调参。

### 实验设计

论文为纯理论工作，**未包含任何数值实验或数据集实验**。所有结论以定理、引理和推论形式呈现，并通过数学证明支撑。

### 资源与算力

文中未提及任何计算资源（GPU、训练时长等），亦无实验部分需要算力。

### 实验数量与充分性

无实验。理论论证是完整的：每个归约都给出了明确的构造和误差界分析，且与已有等价结果（Abernethy et al. 2011, Perchet 2013）结合，形成了闭环等价链。在理论层面是充分的。

### 主要结论与发现

1. GEQ与Blackwell可接近性在算法意义上等价：两者可以双向黑箱归约，且渐近误差率保持不变。  
2. 因此，GEQ与遗憾最小化、校准等经典在线学习框架也等价（通过已有等价链）。  
3. GEQ的**必要充分条件**是Blackwell条件（而非仅修复性），这首次被明确指出。  
4. 有约束GEQ可归约为无约束GEQ，从而简化算法设计。  
5. 通过等价性，可将乐观遗憾界、强自适应遗憾界等精细保证迁移到GEQ，也可用简单GEQ算法（如步长常数OGD）实现无需调参的遗憾最小化。

### 优点

- **理论贡献深刻**：清晰地将GEQ纳入在线学习核心等价类，消除了其“孤立”地位。  
- **归约高效通用**：黑箱归约不假设具体算法，且渐近最优。  
- **技术灵活**：通过鲁棒BA预言机处理近似半空间预言机的有限错误，并用锥提升技巧处理非锥目标集，展示了通用设计模式。  
- **应用意义**：直接给出如何将现有RM算法的先进保证（乐观、强自适应）转移到GEQ，以及如何用GEQ算法简化RM实现（免调参）。

### 不足与局限

- **纯理论，缺乏实验验证**：未在具体任务（如在线共形预测）上验证归约的实际效果或比较不同算法的实证表现。  
- **假设较强**：要求向量场Lipschitz有界且满足(ℎ,0)-修复性，而Blackwell条件虽更弱，但未给出显式可验证的充分条件（修复性仍是实际主要条件）。  
- **锥提升导致维度增加**：非锥目标集需提升到ℝᵈ⁺¹，可能带来常数或线性维度代价，但理论上仅是多项式增加，未讨论维度过高时的实际效率。  
- **应用部分仅给出推论**：未实现具体算法并在标准数据集上测试，实际性能未知。  
- **忽略非凸/非修复性情形**：GEQ原本也可用于非凸损失（修复性不要求凸性），但归约至BA/RA时是否覆盖所有非凸情况？文中未深入讨论。

（完）
