Title: Causal Variational Deep Embedding: A Family of Interventional Generators for Confounded Images

URL Source: https://arxiv.org/pdf/2606.21806v1

Published Time: Tue, 23 Jun 2026 01:39:15 GMT

Number of Pages: 24

Markdown Content:
# Causal Variational Deep Embedding: A Family of Interventional Generators for Confounded Images 

Jingyuan Chen 

Department of EECS Syracuse University 

jchen357@syr.edu 

Kangrui Ruan 

AWS AI Lab 

kr2910@columbia.edu 

Junzhe Zhang 

Department of EECS Syracuse University 

jzhan403@syr.edu 

# Abstract 

Deep generative models reproduce the observational distribution of their training data, inheriting any spurious associations it contains. A common source is an unobserved confounder that shapes both an attribute the user wants to control at sampling time and an attribute expected to vary in response. Existing causal generative approaches resolve the resulting ambiguity by imposing structural assumptions strong enough to single out one interventional distribution; in image domains, such assumptions are rarely warranted, and the data is generally consistent with a set of distinct causal mechanisms—a feasible region of interventional distributions. We propose C AU VADE (Causal Variational Deep Embedding), built on a canonical augmented SCM in which the unobserved confounder collapses, without loss of generality, into a discrete latent cluster of bounded support while continuous variation is absorbed into independent noises. We prove that this canonical class is dense, in both observational and interventional Wasserstein distance, in the class of augmented SCMs compatible with a given causal diagram, and instantiate it as a mixture variational autoencoder whose cluster variable plays the role of the canonical confounder. An entropy regularizer with weight γ on the cluster posterior then traces a family of candidate causal effects that fit the observational data to comparable likelihood while spanning the feasible region. Experiments on image data benchmarks show that C AU VADE produces diverse interventional samples and improves FID against an unconfounded reference. 

# 1 Introduction 

Deep generative models have become the standard tool for synthesizing image data [ 17 , 6, 9]. Frame-works such as VAEs, GANs, diffusion models, and structured variants like β-VAE and Variational Deep Embedding [ 8, 12 ] aim to reproduce the distribution of a training corpus. This is well-matched when the training set faithfully represents the deployment world, but poorly matched when the training distribution carries spurious correlations the user would like the generator not to inherit—a setting we call deconfounded image generation .To make this concrete, suppose a corpus pairs a digit attribute X with a background-color attribute 

Y that are correlated only because some hidden factor U —a writer, a recording session, a lighting condition—influenced both during data collection. We call such an unobserved U a confounder .From data alone, the model cannot tell what fraction of the digit–color dependence is genuine and what fraction is an artifact of U . If the user instead asks what the color distribution would look like if the digit were set to 1, the trained generator simply replays the confounded conditional. We write this counterfactual query as Px(Y ), the distribution of Y when an external operation fixes X=x, as opposed to the observational P (Y | x) [30 ]. Standard generative training implicitly assumes no such confounder exists [33, 28]; when this fails, samples approximate P (Y | x), not Px(Y ).     

> Preprint.
> arXiv:2606.21806v1 [cs.LG] 19 Jun 2026 (a) Observational (b) Interventional (c) VAE (d) DDPM (e) GAN (f) ANCM

Figure 2: Samples from (a) confounded and (b) unconfounded Color-MNIST; and generated by conditional (c) VAE, (d) DDPM, (e) GAN, and (f) ANCM. 

Figure 1: Estimated 

P (Y =1 | do( X=1)) 

on confounded Color-MNIST. Dashed lines indicate Manski’s bound [ 22 ] identified from observational data. 

Example 1 (Confounded Color-MNIST). Consider a binary Color-MNIST in which digit X ∈ {0, 1} and background color Y ∈{green , blue } are linked through a binary confounder U in the train-ing set (Fig. 2a), but are independent in the unconfounded distribution we would like the model to support at sampling time (Fig. 2b). We train a vanilla VAE, a denoising diffusion model, a GAN, and the Aug-mented Neural Causal Model (ANCM) [ 28 ]—the last specifically de-signed for counterfactual image editing—on the confounded data. As shown in Figs. 2c–2f, none recovers the unconfounded distribution; each reproduces the training-set association between digit 1 and green back-grounds. Fig. 1 quantifies the failure: VAE, GAN, and DDPM estimate 

P (Y =1 | do (X=1)) near the confounded P (Y =1 | X=1) . ANCM stays closer to the ground truth but commits to a single point within the sharp Manski bound [ 22 ]—one of many plausible causal explanations the data cannot distinguish, and in general, the wrong one. ■

The challenge revealed by Example 1 is one of identifiability : whether the data contains enough information to pin down the answer. With U

hidden, it does not—the training distribution P (X, Y ) is consistent with many distinct mechanisms, each predicting a different Px(Y ). Existing causal generative approaches [ 33 , 28 ] resolve the ambiguity by adding structural assumptions—specific functional forms, noise independence, auxiliary supervision—strong enough to single out one mechanism. When such assumptions hold, this is the right move; in image domains with hidden confounders they rarely do, and a model returning a single Px(Y ) is 

overconfident , having silently picked one element of a set the data cannot distinguish. A more honest target is partial identification : rather than committing to a single Px(Y ), characterize the feasible region —the set of interventional distributions consistent with the observed data and whatever structural assumptions are defensible [ 1 , 22 , 4 ]. In discrete tabular domains, this region admits sharp Manski bounds derivable from a tractable polynomial program [ 41 , 1]; recent work uses generative models as flexible function approximators inside such programs [ 37 , 38 , 26 ]. None of this transfers directly to images: the observed object is high-dimensional, the hidden confounder interacts with rich visual structure, and the feasible region must be navigated by something other than a polynomial program. The natural goal is then to traverse this region rather than bound it analytically—producing a family of generators (e.g., indexed by γ) whose interventional distributions span the set of causal explanations compatible with observations (Manski bound in Fig. 1). This paper proposes a generative framework for partial identification in deconfounded image gen-eration. Our contributions are threefold. (1) Canonical augmented SCM. We introduce a class of structural models in which the unobserved confounder is a discrete latent of bounded support and continuous variation is carried by independent exogenous noises. We prove that this class is dense, in both observational and interventional Wasserstein distance, in the class of all augmented SCMs com-patible with a given causal diagram. (2) CAU VADE . We instantiate this class as a Gaussian-mixture VAE whose cluster variable plays the role of the canonical confounder. Combined with an entropy regularizer of weight γ on the cluster posterior, C AU VADE traces across a sweep of γ a family of generators that fit P (X, I ) to comparable likelihood while spanning the feasible region of Px(I).

(3) Experiments on Color-MNIST, CelebA, and MIMIC-CXR-JPG [ 19 , 13 ] show that C AU VADE produces diverse interventional distributions covering the feasible region and improves FID against an unconfounded reference. Proofs and implementation details are deferred to Appendices A–G. 21.1 Preliminaries   

> X
> Z
> Y
> I
> (a) cM
> X
> Z
> Y
> I
> (b) cMx

Figure 3: (a) An ASCM with treatment X,pre-treatment Z, post-treatment attribute Y ,and image I. (b) Submodel induced by 

do( X=x), in which the structural equation for X is replaced by the constant X ← x.We use capital letters to denote variables ( X), small letters for their values ( x), and ΩX for their domains. For a set X, |X| denotes its cardinality. The probabil-ity distribution over variables X is denoted by P (X),and we write P (x) as shorthand for P (X = x).A structural causal model (SCM) [ 30 , 2] is a triple 

M = ⟨V , U , F⟩ describing how a set of observed variables V = {X, Y, Z } is generated. Throughout, 

X denotes a treatment , Y an outcome , and Z a covari-ate . The set U collects unobserved exogenous vari-ables, drawn from an exogenous distribution P (U ).The structural equations F = {fX , f Y , f Z } spec-ify how each observed variable is generated: Z ←

fZ (U ), X ← fX (Z, U ), and Y ← fY (X, Z, U ).Sampling U ∼ P (U ) and applying these equations induces the observational distribution 

P (X, Y, Z ) ≜ P (X, Y, Z ; M), which is what the learner observes in the data. An intervention do( X=x) models the operation of externally setting X to a constant x, overriding whatever value fX would have produced. Formally, do( X=x) replaces fX in M with the constant assignment X ← x, yielding a submodel Mx. Sampling U ∼ P (U ) in Mx induces the inter-ventional distribution Px (Y ) ≜ P (Yx; M), where Yx(u) ≜ YMx (u) is the potential outcome of 

Y when X is set to x. The interventional distribution differs from the observational conditional 

P (Y, Z | X=x) exactly when some component of U confounds X with Y or Z. The SCM machin-ery just introduced operates on abstract semantic variables and says nothing about pixels; Sec. 2 closes this gap, specializes the variable roles to the image-domain setting, and develops the canonical class of structural models on which C AU VADE is built. 

# 2 Canonical Causal Models for Image Generation 

This section develops the structural foundation of C AU VADE. We first extend the SCM of Sec. 1.1 to a model that generates images, specializing the variable roles and deriving observational and interventional image distributions. We then introduce the canonical augmented SCM (Sec. 2.1) along with a density theorem justifying our restriction to this class—the scaffold on which our partial-identification mechanism (Sec. 3) operates. 

Augmented SCMs for Images. The SCM of Sec. 1.1 generates semantic variables (X, Y, Z ) but says nothing about pixels. The Augmented Structural Causal Model (ASCM) of [ 28 ] closes this gap by extending M with an image variable I, an exogenous noise EI independent of U , and a generation mechanism I ← fI (X, Y, Z, EI ), where EI accounts for visual variation—pose, texture, lighting, style—not encoded in the semantic variables. Throughout, we assume: • (A1) Bounded, continuous image support. ΩI is bounded and fI is continuous in all arguments. • (A2) Finite-measure treatment domain. ΩX has finite Lebesgue measure (treatments may be categorical or lie in a bounded continuous range). • (A3) Semantic invertibility of fI . There exists a continuous map h with (X, Y, Z ) = h(I).(A1)–(A2) reflect that images are finite-resolution objects produced by smooth rendering processes and that practical treatments lie in bounded sets; (A3) is the standard nonlinear-ICA assumption [ 10 ,15, 20], i.e., X, Y , Z are visible properties of I.In the image-domain reading, (X, Y, Z ) specializes as follows: X is the semantic attribute the user wishes to manipulate at sampling time (e.g., the digit), Z is a pre-treatment attribute that may influence X (e.g., a writer’s identity), and Y is a post-treatment attribute that responds to X (e.g., the background color). Fig. 3a shows the resulting graph; by convention, exogenous variables U

are not drawn explicitly, and a dashed bidirected edge X ↔ Y denotes the presence of unobserved confounders affecting X and Y simultaneously. The ASCM unifies observational and interventional image distributions. Sampling U ∼ P (U )

and EI ∼ P (EI ) and propagating through the structural equations gives the observational image 

3distribution P (I) ≜ P (I; cM). Applying the intervention do( X=x) on the treatment attribute X

replaces the structural equation for X with constant assignment X ← x and severs the edges incident on X. This operation yields the submodel cMx in Fig. 3b and the interventional image distribution 

Px(I) ≜ P (Ix; cM) = P  fI (x, Y x, Z x, EI ), (1) which differs from P (I | X=x) exactly when U confounds X with Y or Z. Standard generative training learns P (I | X=x), while a user reasoning about manipulation of X wants Px(I). When 

Px(I) is not point-identifiable, our object of interest is the set (i.e., a feasible region) of ASCMs compatible with P (X, I ), each inducing a possibly distinct Px(I).

Labeling assumption. Prior causal-image work [ 28 , 37 , 38 ] assumes the full vector (X, Y, Z ) is labeled. We require only that X be fully labeled; Y and Z may be partially labeled or fully unlabeled, with any unlabeled components observed only through I and recovered during learning. This matches realistic image-domain settings: the attribute the user wishes to control is typically the most readily annotated, while affected attributes (color, hair, lung opacity) are often too numerous or costly to label exhaustively, though partial annotations are sometimes available. 

2.1 Canonical ASCMs 

The set of ASCMs compatible with P (X, I ) is generally infinite-dimensional: U has unknown dimen-sion, support, and joint distribution, and fX , f Y , f Z may be arbitrary measurable functions. Searching this set directly is intractable; we show that for capturing both observational and interventional image distributions, it suffices to consider a far simpler class. 

Definition 2.1 (Canonical ASCM) . A canonical ASCM (CASCM) is an ASCM in which: (i) the un-observed confounder is a single discrete variable U ∈ { 1, . . . , d } drawn from P (U ); (ii) the semantic variables are generated by continuous structural functions Z ← fZ (U, EZ ), X ← fX (Z, U, EX ),

Y ← fY (X, Z, U, EY ) with mutually independent noises EZ , EX , EY ; (iii) the image is produced by an invertible mechanism I ← fI (X, Y, Z, EI ) satisfying (A1)–(A3). The discrete U collapses what could be an arbitrarily complex unobserved process into a categori-cal variable; the independent noises EZ , EX , EY , EI carry whatever continuous variability remains. The next theorem shows that this canonical restriction is without loss of generality for modeling observational and interventional image distributions. 

Theorem 2.2 (Augmented Causal Approximation Property) . Fix the causal diagram in Fig. 3a and assume (A1)–(A3). For every ϵ > 0 and every ASCM cM compatible with this diagram, there exists a CASCM bN such that: 

W1

 P (I, X ; cM), P (I, X ; bN ) < ϵ, (2) 

Z

> ΩX

W1

 Px(I; cM), P x(I; bN ) dx < ϵ, (3) 

where W1 is the 1-Wasserstein distance. That is, the class of CASCMs is dense in the class of ASCMs compatible with Fig. 3a under joint W1 approximation of the observational law (Eq. 2) and integrated 

W1 approximation of the interventional law (Eq. 3). 

The proof (App. D.1) builds on the canonical-confounder argument of [ 41 ] for discrete tabular settings; the new content is the lift to image domains, where push-forward stability of W1 under uniformly continuous maps translates W1 approximation over (X, Y, Z ) into W1 approximation of P (I) and Px(I). A practical consequence is that the cardinality d of U becomes the sole nontrivial hyperparameter governing the model class: larger d admits richer confounding patterns, while the structural functions and noise distributions are absorbed into standard neural-network parameterizations. Thm. 2.2 thus licenses our modeling strategy: rather than searching the unbounded space of ASCMs compatible with P (X, I ), we restrict attention to the canonical class, knowing any compatible behavior is matched to arbitrary precision by some CASCM. What remains is to fit this class and enumerate the candidate generators it admits. 

# 3 Causal Variational Deep Embedding 

Thm. 2.2 reduces the search over ASCMs compatible with the diagram to a search over CASCMs, in which the unobserved confounder is a discrete variable of bounded support. What remains is to 4(i) choose a parametric family that realizes this canonical class, (ii) fit it from confounded images in which only X is labeled, and (iii) navigate the feasible region within it. C AU VADE addresses these needs by combining a Gaussian-mixture variational autoencoder with a single scalar regularizer.  

> C
> Z
> XY
> I
> (a) Decoder
> C
> Z
> I
> (b) Encoder

Figure 4: CAU VADE: (a) decoder of (4) ,where the discrete cluster C instantiates the canonical confounder of Def. 2.1 and the con-tinuous Z bundles the pre-treatment attribute with independent noises; (b) encoder network 

qϕ(Z, C | I) = qϕ(Z | I) qϕ(C | Z).CAU VADE is itself an instance of CASCM. The starting point is Variational Deep Embedding (VaDE) [ 12 ], which models the latent space of a VAE as a Gaussian mixture indexed by a discrete cluster 

C ∈ { 1, . . . , K }. The cluster variable in VaDE plays the role of the canonical confounder U in Def. 2.1: both are unsupervised, discrete, of bounded support, and index a finite mixture of generative processes. We therefore identify C with U throughout, treating the VaDE cluster as the parametric instantiation of the canonical confounder. The continuous latent collects the remaining exogenous structure of Def. 2.1 into a single bundle, Z = ( Z, EZ , EX , EY , EI ), where 

Z is the pre-treatment attribute and EZ , EX , EY , EI

are the independent noises driving the four CASCM mechanisms; structural functions fZ , f X , f Y , f I are then realized by neural decoders. C AU VADE spec-ifies the generative process 

C ∼ P (C), Z | C ∼ N (μc, σ 2 

> c

I), X | Z, C ∼ pθ (X | Z, C ),Y | X, Z, C ∼ pθ (Y | X, Z, C ), I | X, Y, Z, C ∼ pθ (I | X, Y, Z, C ), (4) where P (C) is a learned categorical prior, (μc, σ 2 

> c

) are cluster-specific Gaussian parameters, and the conditionals over X, Y , and I are amortized by neural decoders θ. Fig. 4(a) shows the corresponding graph: C and Z jointly play the role of the unobserved exogenous structure of the CASCM, X and Y

are the user-controlled and post-treatment semantic attributes, and I is the rendered image. Note that 

Z and the noises do not receive separate latent variables in C AU VADE: they are bundled into Z and recovered by the decoder along with Y as unlabeled semantic factors of I.

Intervention. The CASCM submodel induced by do( X=x) replaces the structural equation for 

X by the constant assignment X ← x and severs the edges incoming to X, while leaving the joint distribution of the unobserved exogenous structure—here (C, Z)—unchanged. Translated to C AU VADE, sampling under intervention proceeds by drawing (C, Z) from their joint prior 

P (C) pθ (Z | C), fixing X = x, and propagating through the decoders for Y and I. This yields the parametric interventional image distribution 

P θx (I) = 

> K

X

> c=1

P (c)

Z

pθ (z | c)

Z

pθ (y | x, z, c ) pθ (I | x, y, z, c ) dy d z. (5) Crucially, (C, Z) are drawn from their joint prior, not their posterior given x: this is what distinguishes 

P θx (I) from the observational conditional P θ (I | X=x) and what makes C AU VADE a model of intervention rather than of selection. 

3.1 Augmented Variational Lower Bound 

We fit C AU VADE from a confounded dataset D = {(xi, I i)}Ni=1 in which only X is labeled. Following VaDE, we approximate the posterior p(Z, C | I, X ) by a factorized variational family 

qϕ(z, c | I) = qϕ(z | I) qϕ(c | z) (Fig. 4(b)); note that X is not used by the encoder, since at sampling time the user supplies X via intervention rather than conditioning. The standard ELBO derivation (App. B) yields 

LELBO (θ, ϕ ) = Eqϕ

log pθ (I | X, Z, C )| {z }

> Lrec

− DKL 

 qϕ(Z, C | I) ∥ pθ (Z | C) P (C)| {z }

> LKL

. (6) Two issues remain. First, since X is the only labeled semantic variable, nothing in LELBO ties the latent representation to the observed treatment: the decoder is free to ignore X and reconstruct I

from (Z, C ) alone, in which case the interventional distribution (5) is degenerate in x. Second, even 5with treatment-aware decoding, maximizing LELBO selects a single θ per local optimum and gives no mechanism for tracing the feasible region. We address both issues with two additional terms. 

Treatment-consistency term. We anchor X in the latent representation with a discriminative head: 

LX (ϕ, ψ ) = Eqϕ

− log pψ (X | Z, C ), (7) where pψ is an auxiliary classifier on top of the encoder. At the structural level, LX enforces that the canonical-confounder–exogenous-noise pair (C, Z) contains the information needed to predict 

X, which is exactly what the structural equation for X in the CASCM requires. It introduces no assumption beyond the labeling already in D; Y and the components of Z remain unlabeled. 1

Cluster-entropy regularizer. The interventional law (5) depends on the dispersion of P (C) and, through training, on the dispersion of qϕ(C | I). Two limiting regimes illustrate the dependence. If 

qϕ(C | I) collapses to a one-hot assignment for every image, the cluster carries no shared structure across images: the canonical confounder degenerates and C AU VADE reduces to a deterministic VAE whose interventional law P θx (I) tracks the observational conditional P θ (I | X=x). If, conversely, 

qϕ(C | I) is uniform for every image, the cluster carries no image-specific information; sampling under intervention then averages the decoder over the full prior P (C), decoupling I from X except through the explicit decoder dependence and pulling P θx (I) toward a confounder-marginalized law. Between these extremes lies a continuum, each setting compatible with the observational distribution to comparable likelihood, each implying a distinct interventional law. We control this continuum directly with an entropy regularizer: 

Lent (ϕ) = EP (I)Hqϕ(C | I) = − EP (I)

> K

X

> c=1

qϕ(c | I) log qϕ(c | I) ∈ [0 , log K]. (8) Combining all terms gives the C AU VADE objective 

L(θ, ϕ, ψ ; γ) = α Lrec + β LKL + λ LX + γ Lent , (9) with α, β, λ > 0 fixed across runs and γ ≥ 0 swept over a range. The reconstruction term ties the model to the observational image distribution, the KL term regularizes the latent space, LX keeps the treatment label informative for the latents, and Lent controls the dispersion of the cluster posterior, and through it the interventional law in (5). 

Training and sampling. For each value of γ in a fixed grid Γ = {γ1, . . . , γ M }, we train a separate CAU VADE model by minimizing (9) with stochastic gradient descent on minibatches from D.Following VaDE [ 12 ], we reparametrize Z and marginalize the discrete cluster C analytically : each loss term is computed in closed form as PKc=1 qϕ(c | z) [ · ], so gradients flow through qϕ(c | z)

without sampling C. The encoder qϕ, decoders pθ , and classifier head pψ are jointly optimized; the GMM parameters (P (C), μ c, σ 2 

> c

) are initialized by a short pretraining phase on the unregularized ELBO and then fine-tuned end-to-end. The number of clusters K is selected once per dataset and held fixed across the sweep; we use K moderately larger than the cardinality of any plausible discrete confounder, since extra clusters are absorbed by the prior P (C). At sampling time, given a target 

x ∈ ΩX , we draw c ∼ P (C), z ∼ pθ (Z | c), y ∼ pθ (Y | x, z, c ), and I ∼ pθ (I | x, y, z, c ), yielding samples from P θx (I) in (5). Pseudocode is in Appendix C. 

3.2 From the γ-Sweep to the Feasible Region 

The combination of Thm. 2.2 and the γ-sweep is what makes C AU VADE a partial-identification method [ 22 ] tracing the feasible region of candidate CASCMs compatible with the confounded observations, rather than a single biased estimator. The density theorem guarantees that any ASCM compatible with the diagram is approximated, in observational and interventional Wasserstein distance, by some CASCM. C AU VADE parametrizes the canonical confounder by the cluster C

and the residual exogenous structure by Z. Distinct values of γ select distinct optima of (9) , each corresponding to a CASCM with a different cluster posterior over confounder probabilities and causal mechanisms, and—via (5) —a different interventional law P θx (I). Because the canonical confounder is finite-dimensional and (8) is bounded above by log K, the sweep γ ∈ [0 , ∞) explores a bounded family of solutions rather than wandering an unbounded model space.    

> 1Labeled subsets of Yor components of Z, when available, are readily incorporated by adding analogous discriminative heads with no change to the rest of the objective.

6The next two results make this connection precise. The first shows that varying γ continuously parametrizes a curve of optima distinguished by their average cluster entropy; the second shows that, in the limit of expressive decoders and large K, the family of curves traced out by the sweep covers the feasible region of parametrization for interventional distribution. 

Lemma 3.1 (Entropy–interventional sensitivity) . Fix the diagram in Fig. 3a and assume (A1)–(A3). Let θ⋆ denote a global optimum of the unregularized objective L( · ; γ=0) with observational law 

P ⋆(X, I ). For every η ∈ (0 , log K] there exists γ(η) ≥ 0 and an associated optimum (θ(η), ϕ (η)) 

of (9) at γ = γ(η) such that 

EP (I) Hqϕ(η)(C | I) = η, W1

 P θ(η)(X, I ), P ⋆(X, I ) ≤ δFθ ,

where δFθ → 0 as the decoder family Fθ becomes universal in the space of continuous mechanisms compatible with (A1)–(A3). Moreover, the map η 7 → P θ(η) 

> x

(I) is continuous in the W1 topology, and its image is non-singleton whenever the set of ASCMs compatible with the diagram and with 

P ⋆(X, I ) induces more than one interventional law Px(I).

Lem. 3.1 (proof in App. D.2) tells us that the γ-sweep is well-behaved: every attainable entropy level 

η is realized by some optimum, that optimum continues to fit the observational law as the decoder grows expressive, and the resulting interventional law varies continuously and—when the feasible region is non-trivial—non-trivially with η. The sweep thus traces a continuous curve through the recovered family rather than jumping between disconnected modes. Lem. 3.1 alone, however, does not guarantee that this curve covers the full feasible region. The next result rules this out: with sufficiently many clusters and an expressive decoder, the family of CASCMs reachable by C AU VADE matches the family of ASCMs compatible with the diagram and the observational law. 

Proposition 3.2 (Feasible-region coverage) . Fix the diagram in Fig. 3a and assume (A1)–(A3). Let 

P ⋆(X, I ) denote the true data-generating law, and define 

PASCM  

> x

(δ) ≜

n

Px(I; cM) : cM compatible with Fig. 3a , W 1

 P (X, I ; cM), P ⋆(X, I ) ≤ δ

o

as the set of interventional image distributions realizable by any ASCM whose observational law lies within W1-distance δ of P ⋆, and analogously 

P CAU VADE  

> x

(δ; K, Fθ ) ≜

n

P θx (I) : θ ∈ F θ , W 1

 P θ (X, I ), P ⋆(X, I ) ≤ δ

o

as the set realizable by a K-cluster CAU VADE configuration with decoder family Fθ . If Fθ is dense in the space of continuous mechanisms compatible with (A1)–(A3), then for every δ > 0,

[

> K≥1

P CAU VADE  

> x

(δ; K, Fθ ) = PASCM  

> x

(δ),

where (·) denotes closure in the W1 topology on probability measures over ΩI .

Prop. 3.2 (proof in App. D.3) is a direct consequence of Thm. 2.2 together with the universality of the C AU VADE decoder family: any interventional law a compatible ASCM can produce is matched, up to arbitrarily small Wasserstein error, by some C AU VADE configuration with sufficiently many clusters and an expressive enough decoder. The role of γ is then to select elements of this set: by Lem. 3.1, sweeping γ over [0 , ∞) moves the cluster posterior between collapsed and uniform regimes and traces a continuous curve through the recovered family. The empirical result, reported in Sec. 4, is exactly this: a parametric trace through the feasible region in which all generators fit the training data to comparable likelihood while realizing visibly different interventional distributions. 

# 4 Experiments 

We evaluate C AU VADE on three datasets: synthetic Confounded Color-MNIST, CelebA [ 19 ], and MIMIC-CXR-JPG [ 13 , 14 , 5]. In each, an unobserved confounder U jointly governs the treatment 

X and post-treatment attribute Y , inducing a spurious correlation that standard generative models inherit. We compare C AU VADE against a vanilla VAE and the ANCM [ 28 ], a causally-enhanced VAE for counterfactual generation; all three share the same autoencoder backbone, so any difference in interventional behavior is attributable to the causal mechanism rather than the generative family. 7Table 1: Observational P (Y = 1 | X) versus interventional P (Y = 1 | do (X)) on Confounded Color-MNIST and CelebA. Baselines collapse to a single estimate, while C AU VADE traces the feasible region characterized by Manski’s bound as γ varies.                                                                                 

> P(Y= 1 |X)P(Y= 1 |do (X)) VAE ANCM Feasible Region[22] CAU VADE Lower Upper γ= 0 γ= 1 γ= 10 γ= 100
> C-MNIST X= 0 0.609 0.400 0.707 ±0.008 0.580 ±0.010 0.28 0.82 0.570 0.540 0.493 0.716
> X= 1 0.778 0.600 0.707 ±0.016 0.619 ±0.021 0.42 0.88 0.639 0.556 0.544 0.834 CelebA X= 0 0.462 0.330 0.456 ±0.013 0.487 ±0.015 0.255 0.725 0.422 0.571 0.476 0.395
> X= 1 0.308 0.463 0.302 ±0.024 0.481 ±0.016 0.143 0.673 0.437 0.506 0.480 0.385

(a) (b) (c) (d) (e) (f) 

Figure 5: C AU VADE samples on Confounded Color-MNIST under X = 1 at (a) γ = 1 , (b) γ = 2 ,(c) γ = 10 , (d) γ = 20 , (e) γ = 50 , and (f) γ = 100 . Larger γ shifts the digit–color association, revealing distinct causal mechanisms consistent with the same observational distribution. We assess each by how well its estimated P (Y | do (X)) aligns with the feasible region partially identifiable from observational data, characterized in the discrete setting by Manski’s bound [ 22 ]. Both baselines collapse to a single point estimate: VAE recovers the confounded P (Y | X), and ANCM concentrates at one location inside the feasible region. Sweeping γ in C AU VADE instead traces a continuous curve spanning Manski’s bound, exposing a diverse set of causal explanations consistent with the observational distribution. C AU VADE also preserves generation quality, with samples visually aligned with the unconfounded ground truth on CelebA and lower FID than both baselines on MIMIC-CXR-JPG. We defer to Appendix E for more details on the experimental setup. 

(a) Color-MNIST (b) Confounded CelebA 

Figure 6: Estimated P (Y | do (X)) on Confounded Color-MNIST and CelebA. VAE and ANCM each col-lapse to a single point; C AU VADE traces the feasible region (shaded, Manski bound [22]) as γ is swept. 

Confounded Color-MNIST. We construct a confounded variant of Color-MNIST with digits X ∈ { 0, 1} and background col-ors Y ∈ { green , blue } (encoded as 0, 1), introducing a binary confounder U that jointly governs both. Samples from P (I |

X = 1) and the unconfounded ground truth PX=1 (I) are shown in Figs. 2a–2b (X = 0 in App. E). Following Exam-ple 1, we train VAE and ANCM on the confounded images across 5 seeds and re-port their estimated Px(Y ) in Table 1. The vanilla VAE collapses to the confounded 

P (Y | X), while ANCM stays within the feasible region but at a single point. Apply-ing C AU VADE with a γ-sweep systematically shifts P (Y | do (X)) across the feasible region: the interventional images in Fig. 5 exhibit a diverse set of digit–color mechanisms, and Fig. 6a shows that γ drives the estimates across a wider portion of the feasible region than either baseline. 

Confounded CelebA. On CelebA (202,599 images), we isolate the causal effect of “Young” ( X)on “Heavy Makeup” ( Y ) with “Attractive” ( U ) as the unobserved confounder. Figs. 7a–7b show confounded and unconfounded samples for X = 1 (full population in Appendix E): the dataset contains disproportionately many attractive seniors who wear heavy makeup. VAE and ANCM trained on the confounded faces (3 seeds) collapse to a single point within the feasible region (Fig. 6b). The γ-sweep instead expands coverage of P (Y | do (X)) across Manski’s bound (Fig. 6b, Table 1). Beyond satisfying these constraints, samples under γ ∈ { 1, 10 } (Figs. 7f–7g) visually align with the unconfounded baseline despite training only on confounded data, demonstrating that C AU VADE disentangles causal mechanisms from spurious influences in image spaces. 8(a) (b) (c) (d)    

> (e) (f) (g) (h)

Figure 7: (a) Confounded CelebA samples P (I | X = 1) ; (b) unconfounded ground truth P (I |

do( X=1)) ; (c) VAE; (d) ANCM; (e) C AU VADE at γ = 0 ; (f) C AU VADE at γ = 1 ; (g) C AU VADE at γ = 10 ; (h) C AU VADE at γ = 100 . Blue circles mark images with Y = 1 (Heavy Makeup). Table 2: FID measured against unconfounded MIMIC-CXR-JPG. Lower is better.      

> VAE ANCM CAU VADE FID 354.97 407.73 284 .57

Figure 8: Confounded MIMIC-CXR-JPG samples: X = 0 (top row, no Pneumonia) and X = 1 (bottom row, Pneumonia). 

Confounded MIMIC-CXR-JPG. We evaluate C AU -VADE on MIMIC-CXR-JPG [ 13 , 14 , 5] (377,110 radiographs; Fig. 8), modeling the causal effect of Pneumonia ( X) on Lung Opacity ( Y ) with patient lying position ( U ) as an unobserved confounder: supine (AP) views are acquired primarily on bedrid-den, acutely ill patients more likely to have pneumo-nia, and the supine geometry itself broadens cardiac silhouette and increases apparent opacity, so gen-erators trained on the raw correlation overstate the pneumonia–opacity link. We construct a position-balanced reference cohort and report FID between samples drawn under do( X=x) and this reference (Table 2, averaged across the γ-sweep). C AU VADE achieves 284 .57 , an absolute improvement of 70 .4 over VAE ( 354 .97 ) and 123 .16 over ANCM ( 407 .73 ). Both baselines fail under hidden confounding, but in opposite ways: VAE inherits the position-induced bias directly from the training distribution, while ANCM commits to a single causal explanation that, with no auxiliary supervision available, lands further from the unconfounded reference than even the uncorrected VAE. C AU VADE avoids both failure modes by producing a family of corrected generators whose average remains faithful to the position-balanced reference. 

# 5 Conclusion 

We studied deconfounded image generation under a hidden confounder, where the data does not point-identify the interventional distribution and a generator that returns a single Px(I) silently picks one element of a set the data cannot distinguish. The right object to model in this regime is the feasible region itself. We made this practical in two steps: showing that the class of augmented SCMs compatible with a given diagram is densely approximated in W1 by canonical augmented SCMs with a discrete bounded-support confounder, and instantiating this class as a Gaussian-mixture VAE in which an entropy regularizer on the cluster posterior, swept over its weight γ, traces a family of generators that fit the training data to comparable likelihood while inducing distinct interventional distributions. Across Color-MNIST, CelebA, and MIMIC-CXR-JPG, the sweep recovers a curve through the feasible region that VAE and ANCM collapse to a single point inside, and yields a tighter average FID against an unconfounded reference. 9References 

[1] A. Balke and J. Pearl. Bounds on treatment effects from studies with imperfect compliance. 

Journal of the American Statistical Association , 92(439):1171–1176, 1997. [2] E. Bareinboim, J. D. Correa, D. Ibeling, and T. Icard. On Pearl’s hierarchy and the foundations of causal inference. In H. Geffner, R. Dechter, and J. Y. Halpern, editors, Probabilistic and Causal Inference: The Works of Judea Pearl , ACM Books, pages 507–556. Association for Computing Machinery, New York, NY, USA, 2022. [3] G. Duarte, N. Finkelstein, D. Knox, J. Mummolo, and I. Shpitser. An automated approach to causal inference in discrete settings. Journal of the American Statistical Association ,119(547):1778–1793, 2024. [4] C. E. Frangakis and D. B. Rubin. Principal stratification in causal inference. Biometrics ,58(1):21–29, 2002. [5] A. L. Goldberger, L. A. N. Amaral, L. Glass, J. M. Hausdorff, P. C. Ivanov, R. G. Mark, J. E. Mietus, G. B. Moody, C.-K. Peng, and H. E. Stanley. PhysioBank, PhysioToolkit, and PhysioNet: Components of a new research resource for complex physiologic signals. 

Circulation , 101(23):e215–e220, June 2000. PMID: 10851218. [6] I. J. Goodfellow, J. Pouget-Abadie, M. Mirza, B. Xu, D. Warde-Farley, S. Ozair, A. Courville, and Y. Bengio. Generative adversarial networks. arXiv preprint arXiv:1406.2661 , 2014. [7] J. Hartford, G. Lewis, K. Leyton-Brown, and M. Taddy. Deep IV: A flexible approach for counterfactual prediction. In Proceedings of the 34th International Conference on Machine Learning (ICML) , volume 70 of Proceedings of Machine Learning Research , pages 1414–1423. PMLR, 2017. [8] I. Higgins, L. Matthey, A. Pal, C. Burgess, X. Glorot, M. Botvinick, S. Mohamed, and A. Ler-chner. β-VAE: Learning basic visual concepts with a constrained variational framework. In 

International Conference on Learning Representations (ICLR) , 2017. [9] J. Ho, A. Jain, and P. Abbeel. Denoising diffusion probabilistic models. arXiv preprint arXiv:2006.11239 , 2020. [10] A. Hyvärinen and P. Pajunen. Nonlinear independent component analysis: Existence and uniqueness results. Neural Networks , 12(3):429–439, 1999. [11] A. Javaloy, P. Sánchez-Martín, and I. Valera. Causal normalizing flows: From theory to practice. In Advances in Neural Information Processing Systems 36 (NeurIPS 2023) , pages 58833–58864, 2023. [12] Z. Jiang, Y. Zheng, H. Tan, B. Tang, and H. Zhou. Variational deep embedding: An unsupervised and generative approach to clustering. In Proceedings of the 26th International Joint Conference on Artificial Intelligence (IJCAI) , pages 1965–1972, 2017. [13] A. E. W. Johnson, T. J. Pollard, N. R. Greenbaum, M. P. Lungren, C.-y. Deng, Y. Peng, Z. Lu, R. G. Mark, S. J. Berkowitz, and S. Horng. MIMIC-CXR-JPG — chest radiographs with structured labels. PhysioNet , March 2024. Version 2.1.0. [14] A. E. W. Johnson, T. J. Pollard, N. R. Greenbaum, M. P. Lungren, C. ying Deng, Y. Peng, Z. Lu, R. G. Mark, S. J. Berkowitz, and S. Horng. MIMIC-CXR-JPG, a large publicly available database of labeled chest radiographs, 2019. [15] I. Khemakhem, D. P. Kingma, R. P. Monti, and A. Hyvärinen. Variational autoencoders and nonlinear ICA: A unifying framework. In Proceedings of the Twenty Third International Conference on Artificial Intelligence and Statistics (AISTATS) , volume 108 of Proceedings of Machine Learning Research , pages 2207–2217. PMLR, 2020. [16] I. Khemakhem, R. Monti, R. Leech, and A. Hyvärinen. Causal autoregressive flows. In 

Proceedings of the 24th International Conference on Artificial Intelligence and Statistics (AISTATS) , volume 130 of Proceedings of Machine Learning Research , pages 3520–3528. PMLR, 2021. 10 [17] D. P. Kingma and M. Welling. Auto-encoding variational Bayes. In 2nd International Confer-ence on Learning Representations (ICLR) , 2014. [18] M. Kocaoglu, C. Snyder, A. G. Dimakis, and S. Vishwanath. CausalGAN: Learning causal implicit generative models with adversarial training. In International Conference on Learning Representations (ICLR) , 2018. [19] Z. Liu, P. Luo, X. Wang, and X. Tang. Deep learning face attributes in the wild. In Proceedings of the IEEE International Conference on Computer Vision (ICCV) , December 2015. [20] F. Locatello, S. Bauer, M. Lucic, G. Rätsch, S. Gelly, B. Schölkopf, and O. Bachem. Chal-lenging common assumptions in the unsupervised learning of disentangled representations. In 

Proceedings of the 36th International Conference on Machine Learning (ICML) , volume 97 of 

Proceedings of Machine Learning Research , pages 4114–4124. PMLR, 2019. [21] I. Loshchilov and F. Hutter. Decoupled weight decay regularization, 2019. [22] C. F. Manski. Nonparametric bounds on treatment effects. The American Economic Review ,80(2):319–323, 1990. [23] C. F. Manski. Monotone treatment response. Econometrica , 65(6):1311–1334, 1997. [24] W. Miao, Z. Geng, and E. J. Tchetgen Tchetgen. Identifying causal effects with proxy variables of an unmeasured confounder. Biometrika , 105(4):987–993, 2018. [25] M. Monteiro, F. De Sousa Ribeiro, N. Pawlowski, D. C. Castro, and B. Glocker. Measuring axiomatic soundness of counterfactual image models. In The Eleventh International Conference on Learning Representations (ICLR) , 2023. [26] A. Nasr-Esfahany, M. Alizadeh, and D. Shah. Counterfactual identifiability of bijective causal models. In Proceedings of the 40th International Conference on Machine Learning (ICML) ,volume 202 of Proceedings of Machine Learning Research , pages 25733–25754. PMLR, 2023. [27] K. Padh, J. Zeitler, D. Watson, M. Kusner, R. Silva, and N. Kilbertus. Stochastic causal programming for bounding treatment effects. In Proceedings of the Second Conference on Causal Learning and Reasoning (CLeaR) , volume 213 of Proceedings of Machine Learning Research , pages 142–176. PMLR, 2023. [28] Y. Pan and E. Bareinboim. Counterfactual image editing. In Proceedings of the 41st International Conference on Machine Learning (ICML) , volume 235 of Proceedings of Machine Learning Research , pages 39087–39101. PMLR, 2024. [29] N. Pawlowski, D. C. Castro, and B. Glocker. Deep structural causal models for tractable counterfactual inference. In Advances in Neural Information Processing Systems (NeurIPS) ,volume 33, pages 857–869, 2020. [30] J. Pearl. Causality: Models, Reasoning, and Inference . Cambridge University Press, New York, 2000. [31] P. R. Rosenbaum. Observational Studies . Springer, New York, 2 edition, 2002. [32] P. Sanchez and S. A. Tsaftaris. Diffusion causal models for counterfactual estimation. In 

Proceedings of the First Conference on Causal Learning and Reasoning (CLeaR) , volume 177 of Proceedings of Machine Learning Research , pages 647–668. PMLR, 2022. [33] B. Schölkopf, F. Locatello, S. Bauer, N. R. Ke, N. Kalchbrenner, A. Goyal, and Y. Bengio. Toward causal representation learning. Proceedings of the IEEE , 109(5):612–634, 2021. [34] E. J. Tchetgen Tchetgen, A. Ying, Y. Cui, X. Shi, and W. Miao. An introduction to proximal causal inference. Statistical Science , 39(3):375–390, 2024. [35] T. J. VanderWeele and P. Ding. Sensitivity analysis in observational research: Introducing the E-value. Annals of Internal Medicine , 167(4):268–274, 2017. 11 [36] C. Villani. Optimal Transport: Old and New , volume 338 of Grundlehren der mathematischen Wissenschaften . Springer, Berlin, Heidelberg, 2009. [37] K. Xia, K.-Z. Lee, Y. Bengio, and E. Bareinboim. The causal-neural connection: Expressiveness, learnability, and inference. In Advances in Neural Information Processing Systems 34 (NeurIPS 2021) , 2021. [38] K. Xia, Y. Pan, and E. Bareinboim. Neural causal models for counterfactual identification and estimation. In The Eleventh International Conference on Learning Representations (ICLR) ,2023. [39] L. Xu, Y. Chen, S. Srinivasan, N. de Freitas, A. Doucet, and A. Gretton. Learning deep features in instrumental variable regression. In International Conference on Learning Representations (ICLR) , 2021. [40] M. Yang, F. Liu, Z. Chen, X. Shen, J. Hao, and J. Wang. CausalVAE: Disentangled representation learning via neural structural causal models. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 9593–9602, 2021. [41] J. Zhang, J. Tian, and E. Bareinboim. Partial counterfactual identification from observational and experimental data. In Proceedings of the 39th International Conference on Machine Learning (ICML) , volume 162 of Proceedings of Machine Learning Research , pages 26548–26558. PMLR, 2022. 

# A Related Work 

Causal generative models. A growing body of work injects causal structure into deep generative models. CausalGAN [ 18 ] and CausalVAE [ 40 ] learn generators whose latent variables follow a user-supplied DAG, enabling do-style interventions on disentangled factors. DiffSCM [ 32 ] and counterfactual diffusion variants [ 25 ] extend this idea to denoising-diffusion backbones, performing counterfactual edits via abduction–action–prediction in latent diffusion space. Normalizing-flow SCMs [ 11 , 16 ] parametrize the structural equations as invertible flows so that exogenous noise can be inferred and counterfactuals computed in closed form. All of these methods assume the SCM is identifiable from the data—typically requiring labeled intermediate factors, parametric noise restrictions, or a known causal ordering—and return a single Px(I) per query. C AU VADE drops these identifiability assumptions and returns a family. 

Causal representation learning. A complementary line seeks latent variables aligned with causal generative factors [ 33 , 15 , 20 ], typically under structural assumptions—auxiliary supervision, specific noise models, or intervention data—sufficient for point identification of the latent factors. C AU VADE differs in two ways: it targets the interventional pixel law Px(I) rather than the latent factors themselves, and it embraces non-identifiability rather than imposing assumptions to remove it. 

Augmented Neural Causal Models. Augmented SCMs and their neural realizations [ 28 , 37 , 38 ]extend the SCM formalism to images and conditional generation, providing the structural scaffold we build on in Sec. 2. These methods assume the full vector of semantic factors is labeled and that the resulting model is identifiable from the joint distribution; given those assumptions, they fit a single SCM and return a single Px(I) per query. Both assumptions fail in our setting, where only the treatment X is observed and a hidden confounder leaves Px(I) partially identified. C AU VADE drops both assumptions and, rather than committing to a single SCM consistent with the data, returns a family of CASCMs spanning the feasible region. 

Counterfactual image editing. Methods such as DeepSCM [ 29 ] and counterfactual variants of diffusion and autoregressive flows [ 32 , 25 , 16 ] produce image-level counterfactuals by editing along disentangled or label-aligned axes. Like ANCM, these approaches commit to a single counterfactual per query and rely on accurate attribute labels or a known causal ordering to anchor the causal axes. CAU VADE produces an ensemble of counterfactual generators rather than a single edit, and recovers the relevant axes unsupervised through the cluster posterior. 12 Causal inference under hidden confounding. A separate body of work targets the same underlying problem—hidden confounding in observational data—but at the level of treatment effects rather than image generation. Sensitivity analysis [ 31 , 35 ], instrumental-variable methods [ 7 , 39 ], and proxy-variable approaches [ 34 , 24 ] either bound the unobserved bias or impose auxiliary variables that point-identify the effect. These methods return scalar effect estimates or interval bounds, not generative samples; C AU VADE can be read as a generative analogue of sensitivity analysis, with γ

playing a role analogous to a sensitivity parameter and samples replacing scalar bounds. 

Partial identification. Partial identification has a long tradition in econometrics and statistics [ 1 ,23 , 4, 41 , 3], almost exclusively in tabular domains. The canonical-confounder construction we extend originates here: [ 41 ] establish that for discrete observables, the unobserved confounder can be replaced by a categorical latent of bounded support without loss of generality, reducing partial identification to a polynomial program. 

Neural partial identification. A more recent thread uses generative models as function approxima-tors for partially-identified causal effects [ 37 , 38 , 26 , 27 ], typically through a min–max objective that searches the feasible region by alternately maximizing and minimizing the target functional subject to data-fit constraints. These methods operate over discrete tabular domains. C AU VADE is, to our knowledge, the first to extend this program to images, replacing the explicit min–max with an entropy regularizer whose weight γ traces the feasible region—a parametric procedure better matched to the smoothness of pixel-level distributions than the discrete saddle-point search used in tabular work. 

# B Derivation of the ELBO 

This appendix derives (6) . The notation matches Sec. 3: the generative model is (4) , the variational family factorizes as qϕ(z, c | I) = qϕ(z | I) qϕ(c | z) (Fig. 4(b)), and the data is a confounded set 

D = {(xi, I i)}Ni=1 in which only X is labeled. Recall that Z = ( Z, EZ , EX , EY , EI ) bundles the CASCM pre-treatment attribute with the four independent exogenous noises, and C is the discrete cluster identified with the canonical confounder U .The training target is the conditional log-evidence log pθ (I | X), in which the unobserved post-treatment attribute Y is marginalized inside the image likelihood and X enters as a conditioning variable. We adopt the shorthand 

pθ (I | X, Z, C ) ≜

Z

pθ (Y | X, Z, C ) pθ (I | X, Y, Z, C ) dY, (10) which in practice is evaluated by Monte Carlo with a single sample y ∼ pθ (Y | X, z, c ) propagated through the image decoder. We first derive the bound. Let rθ (z, c ) ≜ pθ (z | c) P (c) denote the joint latent prior implied by (4) .By the standard variational argument [17], for any encoder qϕ(z, c | I) and any X,

log pθ (I | X) = log 

Z KX

> c=1

pθ (I | X, z, c ) rθ (z, c ) dz

= log Eqϕ(z,c |I)

 pθ (I | X, z, c ) rθ (z, c )

qϕ(z, c | I)



≥ Eqϕ(z,c |I)



log pθ (I | X, z, c ) + log rθ (z, c )

qϕ(z, c | I)



= Eqϕ

log pθ (I | X, Z, C ) − DKL 

 qϕ(Z, C | I) ∥ rθ (Z, C ).

The inequality is Jensen’s, applied to the concave log ; equality holds when qϕ(z, c | I) = pθ (z, c |

I, X ). Substituting rθ (z, c ) = pθ (z | c) P (c) yields (6). 13 The KL term decomposes in closed form. Applying the chain rule of KL divergence with the encoder factorization qϕ(z, c | I) = qϕ(z | I) qϕ(c | z) and the prior factorization rθ (z, c ) = P (c) pθ (z | c),

LKL = Eqϕ(z|I)

" KX

> c=1

qϕ(c | z) log qϕ(z | I) − log pθ (z | c)#

(11) 

+ Eqϕ(z|I)

DKL 

 qϕ(C | z) ∥ P (C) .

Both encoder factors admit closed-form evaluation: qϕ(z | I) = N (μϕ(I), σ 2

> ϕ

(I)) is Gaussian by construction, the Gaussian log-density log pθ (z | c) = log N (z; μc, σ 2 

> c

) is available in closed form, and the second term is the KL between a categorical and the learned categorical prior P (C).As discussed in App. C, this is what enables marginalizing C analytically during training without resorting to discrete sampling. It remains to explain why the encoder uses only I and not X. The optimal variational posterior for the bound above is pθ (Z, C | I, X ), and dropping X from the encoder simply enlarges the family of distributions over which the bound is loose. The choice is deliberate: at sampling time the user supplies X via do( X=x), and the latents (Z, C ) play the role of the unobserved exogenous structure of the CASCM (Def. 2.1), which by construction is sampled from its joint prior under intervention rather than from any conditional. An X-dependent encoder would learn to mix in information from the observational conditional p(z, c | I, X ), leaking the confounding pathway X ↔ (Y, Z ) into the latents and biasing the interventional law (5) toward the observational conditional P θ (I | X=x).The factorization qϕ(z, c | I) = qϕ(z | I) qϕ(c | z) is the standard VaDE choice [ 12 ] and reflects the generative ordering C → Z → I.Two phenomena prevent (6) from being a sufficient training objective on its own, and motivate the additional terms in the full C AU VADE objective (9) . First, since X enters only through the decoder and is not predicted by any term, a decoder that ignores X entirely—reconstructing I from 

(Z, C )—can attain the same ELBO value as a decoder that uses X, provided (Z, C ) already encodes 

X implicitly through I; the interventional distribution (5) of such a model is degenerate in x. The treatment-consistency term LX in (7) closes this loophole by requiring (Z, C ) to predict X through an auxiliary classifier head, forcing the latents to carry the treatment information that the structural equation for X in the CASCM encodes. Second, (6) selects a single solution per local optimum and provides no mechanism for traversing the feasible region; the cluster-entropy regularizer Lent in (8) ,weighted by γ, supplies this mechanism. The full objective (9) combines LELBO = Lrec − L KL 

(entering with sign-flipped weights α, β > 0 in the minimization formulation) with LX and Lent .When X and Y are absent and the dataset reduces to {Ii}Ni=1 , (6) collapses to the VaDE objective [12 ]: the reconstruction term becomes Eqϕ [log pθ (I | Z, C )] and the KL term retains the form (11) with the same Gaussian-mixture prior rθ (z, c ) = P (c) pθ (z | c). C AU VADE can therefore be read as VaDE augmented with (i) the treatment variable X as an extra conditioning input to the decoder, (ii) the post-treatment attribute Y marginalized inside the image likelihood as in (10) , and (iii) the auxiliary terms LX and Lent that enforce treatment-aware decoding and trace the feasible region. 

# C Training Algorithm 

This appendix gives pseudocode for training C AU VADE at a single value of the entropy weight 

γ (Alg. 1) and for the outer sweep that traces the feasible region (Alg. 2). The notation matches Sec. 3: D = {(xi, I i)}Ni=1 is the confounded training set, K is the number of mixture components, and (θ, ϕ, ψ ) collect the decoder, encoder, and classifier-head parameters. As in Sec. 3, Z is the augmented continuous latent and C is the discrete cluster. The GMM parameters {P (c), μ c, σ 2 

> c

}Kc=1 

are part of θ but are initialized separately by the VaDE-style pretraining phase [12]. Two design choices keep training tractable. First, gradients with respect to ϕ flow through Z via the standard Gaussian reparametrization z = μϕ(I) + σϕ(I) ⊙ ϵ with ϵ ∼ N (0 , I). Second, we marginalize the discrete cluster C analytically rather than sampling, following VaDE [ 12 ]: the categorical posterior qϕ(c | z) is bounded ( K values) and computable in closed form, so every loss term that involves C is written as a finite sum 

Eqϕ(C|z)[g(C)] = 

> K

X

> c=1

qϕ(c | z) g(c),

14 Algorithm 1 CAU VADE training at fixed γ

Require: Dataset D = {(xi, I i)}Ni=1 ; cluster count K; weights (α, β, λ, γ ); learning rate ηlr ; number of epochs T ; batch size B; pretrained GMM parameters {μ(0)  

> c

, σ 2,(0)  

> c

, P (0) (c)}Kc=1 .

Ensure: Trained parameters (θ, ϕ, ψ ). 

> 1:

Initialize encoder ϕ, decoders θ ⊇ { μc, σ 2 

> c

, P (c)}Kc=1 , classifier head ψ; load pretrained GMM parameters into θ. 

> 2:

for epoch = 1 , . . . , T do  

> 3:

for minibatch B ⊂ D of size B do  

> 4:

Encode (μϕ(I), σ ϕ(I)) ← Enc ϕ(I) for each I ∈ B . 

> 5:

Sample z = μϕ(I) + σϕ(I) ⊙ ϵ, ϵ ∼ N (0 , I). ▷ Gaussian reparam.  

> 6:

Compute cluster posterior qϕ(c | z) ∈ ∆K−1 for c = 1 , . . . , K . ▷ Closed form.  

> 7:

Compute per-cluster decoder log-likelihoods ℓc ← log pθ (I | X, z, c ) for c = 1 , . . . , K .

▷ K decoder passes.  

> 8:

Compute Lrec , LKL , LX , Lent on B via (6) , (7) , (8) , with Eqϕ(C|z)[ · ] = P 

> c

qϕ(c | z) [ · ].

▷ Analytic.  

> 9:

L ← α Lrec + β LKL + λ LX + γ Lent . ▷ Eq. (9).  

> 10:

(θ, ϕ, ψ ) ← (θ, ϕ, ψ ) − ηlr ∇(θ,ϕ,ψ )L. 

> 11:

end for  

> 12:

end for  

> 13:

return (θ, ϕ, ψ ).

Algorithm 2 CAU VADE γ-sweep for the feasible region 

Require: Dataset D; cluster count K; entropy-weight grid Γ = {γ1, . . . , γ M }; weights (α, β, λ );target intervention values {x(1) , . . . , x (L)} ⊂ D X ; sample size S.

Ensure: Family of interventional sample sets S(m)   

> xm=1 ,...,M ;x∈{ x(1) ,...,x (L)}

. 

> 1:

Pretrain GMM parameters {μ(0)  

> c

, σ 2,(0)  

> c

, P (0) (c)}Kc=1 on D as described above.  

> 2:

for m = 1 , . . . , M do  

> 3:

(θm, ϕ m, ψ m) ← TRAIN CAU VADE( D, K, α, β, λ, γ m, {μ(0)  

> c

, σ 2,(0)  

> c

, P (0) (c)}). ▷ Alg. 1.  

> 4:

for x ∈ { x(1) , . . . , x (L)} do  

> 5:

S(m) 

> x

← ∅ . 

> 6:

for s = 1 , . . . , S do  

> 7:

Sample c ∼ Pθm (C). 

> 8:

Sample z ∼ pθm (Z | c) = N (μc, σ 2 

> c

I). 

> 9:

Sample y ∼ pθm (Y | x, z, c ). 

> 10:

Sample I ∼ pθm (I | x, y, z, c ). 

> 11:

S(m) 

> x

← S (m) 

> x

∪ { I}. 

> 12:

end for  

> 13:

end for  

> 14:

end for  

> 15:

return S(m) 

> x

.and gradients propagate through the probabilities qϕ(c | z) directly. This applies to all four compo-nents Lrec , LKL , LX , Lent : the reconstruction term becomes P 

> c

qϕ(c | z) log pθ (I | X, z, c ), and the KL and entropy terms decompose similarly into closed-form expressions involving qϕ(c | z),

P (c), and the cluster-conditional Gaussian parameters (μc, σ 2 

> c

). Hard categorical samples of C are drawn only at inference time, when generating images under intervention. Pretraining initializes (μc, σ 2 

> c

) by fitting a Gaussian mixture model to the embeddings produced by a vanilla VAE pretrained for a few epochs on D with Lrec + LKL alone (i.e., λ = γ = 0 , K = 1 

in the pretraining stage). This avoids the well-known cluster-collapse issue of jointly training a Gaussian-mixture VAE from scratch and provides a common initialization across all values of γ in the sweep, so that differences in the learned cluster posterior reflect the regularizer rather than the initialization. 15 The sample sets S(m) 

> x

returned by Alg. 2 are empirical realizations of P θm 

> x

(I) for each γm. By Lem. 3.1, varying m traces a continuous curve through interventional distributions all consistent with the observational law P ⋆(X, I ) to within the decoder’s approximation error; by Prop. 3.2, taking K

large and Fθ expressive recovers the full feasible region in the W1 closure. The γ-sweep is therefore the practical mechanism through which the density and coverage results are exposed to the user. 

# D Proofs 

This appendix collects the proofs of the three results stated in the main text. Theorem 2.2 shows that any ASCM compatible with Fig. 3a can be approximated, both observationally and interventionally, by a CASCM with a discrete confounder; this is the density result that licenses replacing the search over ASCMs by a search over the parametric C AU VADE family. Lemma 3.1 characterizes the entropy regularizer: as γ is swept, the average cluster-posterior entropy traces every value in [0 , log K], and the resulting interventional law varies continuously and—when the feasible region is non-trivial— non-trivially. Proposition 3.2 combines the two to conclude that the closure of the C AU VADE family of interventional image distributions, taken over cluster counts and decoder configurations, coincides with the closure of the feasible region induced by all compatible ASCMs. The three proofs share a common Wasserstein-coupling toolkit, introduced once at the start of Sec. D.1 and reused thereafter. 

D.1 Proof of Theorem 2.2 

We use two standard facts repeatedly. The first is push-forward stability of the Wasserstein distance: if f : A → B is uniformly continuous on bounded sets with modulus ω, then 

W1(f#P, f #Q) ≤ ω W1(P, Q ),

with the linear bound L · W1(P, Q ) when f is L-Lipschitz [ 36 , Theorem 5.10]. The second is the conditional Knothe–Rosenblatt construction: any random variable W on Rk with a continuous distribution can be written as g(E) for some measurable g and E ∼ Unif([0 , 1] k); when W | V is continuous in W uniformly in V , there exists g(V, E) continuous in E with g(v, E) d

= W | V = v

[36, Chapter 1]. 

Lemma D.1. Let cM = ⟨{ V , I }, {U , EI }, F, P (U ), P (EI )⟩ be an ASCM compatible with Fig. 3a, with V = ( X, Y, Z ), continuous structural functions, and bounded image support DI . For every 

δ > 0 there exists a CASCM bN , with discrete confounder U and shared image mechanism f N 

> I

≡ fI ,such that 

W1

 P (V ; cM), P (V ; bN ) ≤ ωV (δ) + δD V ,

and the same bound holds uniformly in x for Px(V ; ·), where ωV is a joint modulus of continuity for 

(fX , f Y , f Z ) and DV = diam( DV ) < ∞.Proof. Fix δ > 0. By tightness, choose a compact KU ⊂ D U with P (U /∈ KU ) < δ , and partition 

KU into measurable cells A1, . . . , A d of diameter at most δ. Define the discrete confounder 

U ′(U ) = 

> d

X

> j=1

j · I[U ∈ Aj ] + ( d+1) · I[U /∈ KU ],

so the residual bin j = d+1 carries mass at most δ.Let U ∼ P (U ′) and let EZ , EX , EY be independent uniform noises on cubes of appropriate dimension. Apply the conditional Knothe–Rosenblatt construction in turn to define 

f N 

> Z

(j, EZ ) d

= fZ (U ) | U ∈ Aj , f N 

> X

(z, j, EX ) d

= fX (z, U ) | U ∈ Aj ,

and analogously for f N 

> Y

; the residual bin is handled by conditioning on U /∈ KU . Set f N 

> I

≡ fI and retain EI . The resulting bN is a CASCM (Def. 2.1): U is discrete, the noises are independent, and f N

> I

inherits invertibility from cM.We construct a coupling between V M and V N . Sample U ∼ P (U ), set U = U ′(U ), and generate 

V M from the original mechanisms and V N from FN . By construction, conditional on U = j,16 V N has the same law as V M | U ∈ Aj , so they may be coupled within the cell by an optimal W1

transport. For j ≤ d, uniform continuity of (fX , f Y , f Z ) on Aj yields 

EρV (V M, V N ) | U ∈ Aj

 ≤ ωV (δ).

On the residual event {U /∈ KU }, bound ρV ≤ DV trivially. Aggregating, 

W1

 P (V ; cM), P (V ; bN ) ≤ E[ρV (V M, V N )] ≤ ωV (δ) + δD V .

Under the intervention X ← x, the mechanisms fY , f Z and the noise distributions are unchanged. The same cell-wise coupling, applied with X ≡ x fixed, gives the identical bound for Px(V ; ·),uniformly in x since ωV and DV do not depend on x.

Proof of Theorem 2.2. Let bN be the CASCM produced by Lemma D.1 for some δ to be chosen. Since both models share the image mechanism fI and the noise EI , we couple EI to itself and 

V M, V N as in the lemma, obtaining 

E[ρI (IM, I N )] = EρI

 fI (V M, EI ), f I (V N , EI ) ≤ ωI

 ωV (δ) + δD V

 =: η(δ),

where the inequality uses uniform continuity of fI on DV × supp( EI ) and Jensen’s inequality on the concave ωI . Marginalizing onto (I, X ) yields 

W1

 P (I, X ; cM), P (I, X ; bN ) ≤ η(δ).

The same argument applied under intervention gives, for every x ∈ D X ,

W1

 Px(I; cM), P x(I; bN ) ≤ η(δ),

and integrating over DX (which has finite measure by assumption), 

Z

> DX

W1

 Px(I; cM), P x(I; bN ) dx ≤ |D X | · η(δ).

Since η(δ) → 0 as δ → 0, choosing δ so that max {η(δ), |D X |η(δ)} < ϵ yields both (2) and (3) .

D.2 Proof of Lemma 3.1 

Write L0(θ) = αLrec +βLKL +λLX for the unregularized objective and H(ϕ) = EP (I)H[qϕ(c | I)] 

for the average cluster-posterior entropy, so the full objective is L0 + γH . The argument below is for the population objective with universal Fθ ; finite-network behavior is empirical and discussed in Sec. G. 

Proof. We first establish the bijection between γ and η. The γ-regularized problem is the Lagrangian relaxation of 

max  

> θ

L0(θ) subject to H(ϕ) = η. (12) Feasibility of (12) on [0 , log K] is straightforward: H is continuous in ϕ, equals 0 at any one-hot encoder and log K at the uniform encoder, and the convex combination qt = (1 − t)qhot + tq unif 

attains every intermediate value. Such combinations are exact for mixture-of-encoder constructions and approximated arbitrarily well by sufficiently expressive amortization networks. For the duality, fix θ\ϕ and consider the partial optimum 

ϕ⋆(γ) = arg max 

> ϕ

L0(θ\ϕ, ϕ ) + γH (ϕ).

By the envelope theorem, γ 7 → H(ϕ⋆(γ)) is non-decreasing and continuous wherever the optimum is unique. The boundary values are H(ϕ⋆(0)) = 0 , since the unregularized optimum collapses clusters to maximize reconstruction, and lim γ→∞ H(ϕ⋆(γ)) = log K. The intermediate value theorem then yields, for every η ∈ [0 , log K], some γ(η) ≥ 0 with H(ϕ⋆(γ(η))) = η. At plateaus of H, any γ in the plateau interval realizes the same η; the choice does not affect what follows. We next bound the observational deterioration. Let θ⋆ denote the unregularized optimum and θ(η)

the constrained optimum at entropy η. Set δ0 = W1(P θ⋆

(X, I ), P ⋆(X, I )) , which vanishes as Fθ

17 becomes universal by standard universality of GMM-VAEs under (A1). The Lagrangian inequality gives 

L0(θ⋆) − L 0(θ(η)) ≤ γ(η) η :

constraining the entropy costs at most γη in unregularized objective. Since Lrec controls W1 on the bounded support DI via standard ELBO/Pinsker bounds, there exists a constant C depending only on 

DI , DX such that 

W1

 P θ(η)(X, I ), P ⋆(X, I ) ≤ δ0 + Cpγ(η) η =: δ(η).

For η < log K, γ(η) is finite by the strict monotonicity established above, so δ(η) → 0 as Fθ

becomes universal; the endpoint η = log K is reached as a limit. It remains to establish continuity and non-degeneracy of η 7 → P θ(η) 

> x

(I). Continuity follows from a chain of continuous maps: η 7 → θ(η) by Berge’s maximum theorem (where the constrained optimum is unique), θ 7 → (Pθ (I | x, z, c ), P (c)) trivially, and the latter to P θx (I) in W1 by (A1)–(A2). Suppose, for contradiction, P θ(η) 

> x

(I) were constant in η. The cluster posterior qϕ(η) varies non-trivially with η, going from one-hot at η = 0 to uniform at η = log K, and Theorem 2.2 associates each posterior shape with a CASCM of distinct interventional law—unless PASCM  

> x

(δ) is a singleton, in which case Px(I) is point-identified from P (X, I ) and a constant trajectory is the correct answer. This contradicts the assumption that the feasible region contains more than one element. 

D.3 Proof of Proposition 3.2 

We restate the proposition. Fix the diagram in Fig. 3a and assume (A1)–(A3). For δ ≥ 0, let 

PASCM  

> x

(δ) = 

n

Px(I; cM) : cM compatible with Fig. 3a , W 1

 P (X, I ; cM), P ⋆(X, I ) ≤ δ

o

,

P CAU VADE  

> x

(δ; K, Fθ ) = 

n

P θx (I) : θ ∈ F θ , K clusters , W 1

 P θ (X, I ), P ⋆(X, I ) ≤ δ

o

.

Assume Fθ is dense in continuous mechanisms: for any continuous g on a compact domain and any 

η > 0, there exists gθ ∈ F θ with sup ∥g − gθ ∥∞ < η . We show 

[

> K≥1

P CAU VADE  

> x

(δ; K, Fθ ) = PASCM  

> x

(δ) (13) in the W1 topology, for almost every x ∈ D X and every δ ≥ 0.

Lemma D.2. Let T1, T 2 : A → B be continuous with sup ∥T1 −T2∥∞ < η , and let μ be a probability measure on bounded A. Then 

W1(T1# μ, T 2# μ) ≤ η. 

Proof. Couple T1(a) with T2(a) for a ∼ μ; the transport cost is at most η.

Proof of Proposition 3.2. We first prove the inclusion P CAU VADE  

> x

(δ; K, Fθ ) ⊆ P ASCM  

> x

(δ). Every CAU VADE configuration is itself an ASCM compatible with Fig. 3a: the cluster c plays the role of the unobserved confounder, the conditionals Pθ (z | c), P θ (X | z, c ), P θ (Y | X, z, c ), P θ (I | X, Y, z, c )

specify continuous structural mechanisms after reparameterization through standard noises, and the data-fit constraint is identical. The inclusion is therefore preserved under union over K and closure. We next prove the reverse inclusion. Let μ ∈ P ASCM  

> x

(δ), witnessed by an ASCM cM with observa-tional error at most δ and Px(I; cM) = μ. We show that for every η > 0 there exist K = K(η) and 

θ = θ(η) ∈ F θ such that 

W1

 P θ (X, I ), P ⋆ ≤ δ + η, W1

 P θx (I), μ  ≤ η for almost every x. 

Sending η → 0 and using outer regularity of the δ-sublevel sets places μ in the closure of S 

> K

P CAU VADE  

> x

(δ; K, Fθ ).18 By Theorem 2.2 at tolerance η/ 3, there is a CASCM bN with discrete confounder U ∈ { 1, . . . , d (η)}

such that 

W1

 P (X, I ; cM), P (X, I ; bN ) < η/ 3,

Z

> DX

W1

 Px(I; cM), P x(I; bN ) dx < η/ 3.

By Markov’s inequality, the bad set B1 = {x : W1(Px(I; cM), P x(I; bN )) > pη/ 3} has Lebesgue measure at most pη/ 3 |D X |, which vanishes as η → 0.We now realize bN as a C AU VADE configuration. Set K = d(η) and identify the categorical cluster c

with U , with P (c) = P (U ). The cluster-conditional Gaussian P (z | c) = N (μc, σ 2 

> c

) reparameterizes the independent CASCM noises via the standard Gaussian-mixture latent representation; on the bounded support implied by (A1)–(A2), this is exact up to a transformation absorbed into the decoders. Applying the density assumption to each of fZ , f X , f Y , f I , we obtain decoders f θ 

> •

with 

sup ∥f• − f θ 

> •

∥∞ < η ′, where η′ will be chosen below. Lemma D.2, applied along the structural cascade, controls the propagation of these perturbations: a perturbation of size η′ in fZ becomes one of size LfX η′ in fX (fZ (·), ·), and so on, with all Lipschitz constants finite by (A1)–(A2). Therefore there exist constants Cobs , C int depending only on the moduli of continuity and support diameters such that 

W1

 P (X, I ; bN ), P θ (X, I ) ≤ Cobs η′, W1

 Px(I; bN ), P θx (I) ≤ Cint η′

for every x ∈ D X . Choose η′ = η/ [3 max( Cobs , C int )] , so both right-hand sides are at most η/ 3.Combining via the triangle inequality, for the observational law, 

W1

 P θ (X, I ), P ⋆ ≤ η 

> 3

+ η 

> 3

+ δ ≤ δ + η, 

and for the interventional law on x / ∈ B 1,

W1

 P θx (I), μ  ≤ η 

> 3

+ pη/ 3 ≤ η

for sufficiently small η. Since |B 1| → 0 as η → 0, the interventional bound holds almost everywhere in the limit. Taken together, these constructions show that for every η > 0,

PASCM  

> x

(δ) ⊆ [

> K

P CAU VADE  

> x

(δ + η; K, Fθ ) + BW1 (η) for a.e. x, 

where BW1 (η) denotes the W1-ball of radius η. Closing both sides and intersecting over η > 0

collapses the slack on both the data-fit tolerance and the W1-neighborhood, yielding 

PASCM  

> x

(δ) ⊆ [

> K

P CAU VADE  

> x

(δ; K, Fθ ).

Combined with the first inclusion, this is (13). 

# E Experimental Setup 

We evaluate C AU VADE on three datasets that span a wide range of visual complexity: synthetic Color-MNIST at 28 ×28 resolution, CelebA face attributes at 64 ×64 , and chest radiographs from MIMIC-CXR-JPG at 224 ×224 . Each dataset is constructed in matched confounded and unconfounded 

variants that share the same marginals over (X, Y ) but differ in whether the unobserved factor U

is allowed to influence the treatment X. The unconfounded variant is held out at training time and used only as a reference distribution against which interventional samples are evaluated, allowing us to measure how closely each model’s Px(I) approaches a ground-truth interventional distribution it never directly observes. Throughout, we follow the labeling assumption of Sec. 2: only the treatment X is fully labeled, while the outcome Y , covariates Z, and confounder U are not used as supervision signals, so no model in the comparison has access to U at any stage. The remainder of this appendix details the dataset construction protocol (Appendices E.2–E.4), training and architectural hyperparameters (Appendix E.5), and additional qualitative samples for each dataset. 19 E.1 Dataset Construction Protocol 

For each dataset we target a specified joint distribution P ∗ over the observed variables, with U

marginalized out, and obtain it either by filtering and resampling existing data or by synthetic generation. The two variants of each dataset differ only in the dependence structure of X on U .The confounded variant generates X conditional on U , opening a backdoor path X ← U → Y

and producing the spurious observational correlation P (Y | X)̸ = Px(Y ) that we wish to expose. The unconfounded variant breaks this dependence by sampling X independently of U , severing the backdoor path so that P (Y | X) ≈ Px(Y ). The next three subsections instantiate this protocol on Color-MNIST, CelebA, and MIMIC-CXR-JPG respectively. 

E.2 Confounded Color-MNIST 

Color-MNIST is built from three binary variables: treatment X, outcome Y , and unobserved confounder U , with P (U =0) = 0 .6 and P (U =1) = 0 .4. The outcome follows the XOR mechanism 

Y = U ⊕ X, taking value 1 whenever U and X disagree. A Conditional GAN, conditioned on the 

(X, Y ) stratum, then generates 28 ×28 coloured digit images. We use a deliberately small sample size of N = 1 ,000 : because the underlying causal structure is exact by construction, larger N does not improve the approximation to P ∗. The two variants differ only in the dependence of X on U :• Confounded : P (X=1 | U =0) = 0 .7 and P (X=1 | U =1) = 0 .3, inducing the spurious correlations P (Y =1 | X=0) = 0 .609 and P (Y =1 | X=1) = 0 .778 .• Unconfounded : X ⊥⊥ U , yielding the ground-truth interventional probabilities Px(Y =1 |

X=0) = 0 .60 and Px(Y =1 | X=1) = 0 .40 .

E.3 Confounded CelebA 

CelebA [ 19 ] is built by mapping five attributes to causal variables: Z (Male ), U (Attractive ,unobserved), X (Young , treatment), W (Wearing_Earrings ), and Y (Heavy_Makeup , outcome). The joint distribution is specified by P (Z=1) = 0 .4, P (U =1 | Z=0) = 0 .60 , P (U =1 | Z=1) = 0.40 , and P (W =1 | X, U ) rising monotonically from 0.1 at (X, U ) = (0 , 0) to 0.9 at (X, U ) = (1 , 1) . Images are then resampled from the original CelebA corpus to match the target joint distribution over (Z, X, W ) with U marginalized out. The two variants differ only in the dependence of X on U :• Confounded (N = 10 ,000 ): P (X=1 | U =0) = 0 .1 and P (X=1 | U =1) = 0 .9.• Unconfounded (N = 10 ,000 ): P (X=1) = 0 .5 regardless of U .

E.4 Confounded MIMIC-CXR-JPG 

MIMIC-CXR-JPG [ 13 , 14 , 5] is built from three causal variables: X (Pneumonia , treatment), Y

(Lung Opacity , outcome), and U (unobserved confounder), where U = 1 if the patient is aged 

≥ 60 or the radiograph was acquired in the anteroposterior (AP) view. This composite confounder reflects the clinical reality that older or more severely ill patients are more likely both to receive AP acquisitions and to present with pneumonia and pulmonary opacities. The dataset is partitioned into four atomic pools by (X, Y ) stratum, from which samples are drawn to match the target distribution; 

P (U =1) = 0 .7 is held fixed across both variants. The two variants differ only in the dependence of 

X on U :• Confounded (N = 20 ,000 ): P (X=1 | U =1) = 0 .9 and P (X=1 | U =0) = 0 .1.• Unconfounded (N = 20 ,000 ): P (X=1 | U ) = 0 .5 regardless of U .

E.5 Training Details and Hyperparameters Optimization. All models are trained with the AdamW optimizer [ 21 ] and weight decay 1×10 −5.Color-MNIST and CelebA experiments run on a single NVIDIA RTX 3080 Ti (16GB VRAM); MIMIC-CXR-JPG experiments run on a single NVIDIA A100 (80GB VRAM) on an internal cluster. 

Architectural scaling. To keep the architecture consistent across resolutions, we expose two scaling factors. The base channel capacity h controls the width of the first convolutional layer; deeper 20 (a) (b) (c)   

> (d) (e) (f)

Figure 9: Generated samples from C AU VADE on Colored MNIST across varying constraint strengths: (a) γ = 1 ; (b) γ = 2 ; (c) γ = 10 ; (d) γ = 20 ; (e) γ = 50 ; and (f) γ = 100 .stages compute their channel dimensions as multiples of h (i.e. h, 2h, 4h, . . . ), so the model’s representational capacity scales with image complexity. The latent dimension dz defines the size of the global latent z and is increased on higher-resolution datasets to capture finer visual structure. 

Dataset-specific configurations. Table 3 reports the per-dataset configuration used in our experiments. The reconstruction, KL, and SCM loss weights ( α, β, λ) are calibrated against the input resolution to maintain stable convergence and avoid posterior collapse; the cluster count K = 8 is held fixed across datasets, with extra clusters absorbed by the prior P (C) rather than overfit. The entropy weight γ is swept over a per-dataset grid, producing the family of generators reported in the main text. Table 3: Dataset-specific hyperparameters for C AU VADE. 

Hyperparameter Colored MNIST CelebA MIMIC-CXR 

Input Resolution 28 × 28 64 × 64 224 × 224 

Total Epochs 400 800 800 Batch Size 128 64 128 Learning Rate 1 × 10 −4 1 × 10 −4 1 × 10 −4

Base Channel Capacity ( h) 8 64 32 Latent Dimension ( dz ) 64 512 512 Number of Clusters ( K) 8 8 8Recon. Weight ( α) 10.0 15.0 15.0 KL Weight ( β) 0.1 0.05 0.05 SCM Weight ( λ) 5.0 3.0 3.0 Entropy Weight ( γ) {0, 1, 2, 10 , 20 , 50 , 100 } {0, 1, 10 , 100 }

E.6 Additional Confounded Color-MNIST Samples 

Figure 9 shows representative C AU VADE samples on Confounded Color-MNIST as γ varies across the sweep, illustrating the visual fidelity of the model at 28 ×28 resolution and how the entropy regularizer reshapes the cluster posterior at each setting. 

E.7 Additional Confounded CelebA Samples 

Figure 10 compares samples from the confounded training set, the unconfounded ground truth, two baselines (VAE and ANCM), and C AU VADE at four values of γ, illustrating the visual fidelity of the model at 64 ×64 resolution and the diversity of generators C AU VADE produces across the sweep. 21 (a) (b)    

> (c) (d)
> (e) (f)
> (g) (h)

Figure 10: Visualizing data distributions on CelebA. Samples are drawn from: (a) the confounded training set; (b) the unconfounded ground truth; (c) synthetic outputs from VAE; (d) synthetic outputs from ANCM; (e) synthetic outputs from C AU VADE with γ=0 ; (f) synthetic outputs from CAU VADE with γ=1 ; (g) synthetic outputs from C AU VADE with γ=10 ; and (h) synthetic outputs from C AU VADE with γ=100 .

E.8 Additional Confounded MIMIC-CXR-JPG Samples 

Figure 11 shows C AU VADE samples under both interventions, PX=0 (I) and PX=1 (I), illustrating the visual fidelity of the model at 224 ×224 resolution. 

# F Broader Impact 

CAU VADE addresses a structural failure mode of generative modeling under hidden confounding: when the data does not point-identify the interventional distribution, standard models silently com-mit to a single causal explanation, often the one that reproduces the spurious correlation in the training distribution. The downstream consequences are most acute in safety-critical settings. In medical imaging, a generator that conflates the position-induced distortion of a chest radiograph with the disease itself would, under intervention, produce samples that overstate the disease–imaging 22 Figure 11: Generated samples from C AU VADE under interventions PX=0 (I) (top) and PX=1 (I)

(bottom). association—potentially propagating diagnostic bias into any downstream model trained on its output, audit, or counterfactual explanation. C AU VADE mitigates this by exposing the feasible region for partial identification rather than collapsing onto a single explanation, giving practitioners a tool to inspect the family of causal stories compatible with their data and to flag when that family is wide enough to warrant collecting additional structural information. We see this as the more honest default for generative modeling in domains where the unobserved confounders are unlikely to be either fully documented or eliminable. The risks of C AU VADE are largely those of generative modeling in general rather than ones introduced by the partial-identification framing. Like any generator trained on face images or chest radiographs, CAU VADE could be misused to produce synthetic media—photorealistic faces under counterfactual demographic edits, or chest X-rays under counterfactual disease labels. The ability to sample under intervention does not, by itself, make this misuse easier or harder than for an unconfounded conditional generator; the substantive risk is in the underlying decoder family rather than the causal mechanism on top. We do not release pretrained models. A more specific concern is that the diversity of C AU VADE’s output could be misread as a license to cherry-pick: a user with a preferred causal explanation could select the value of γ whose interventional law most flatters their hypothesis. The interpretation we intend is the opposite—the family is the answer, and any single point inside it is overconfident relative to what the data supports. We discuss this calibration gap and other limitations in Appendix G. 

# G Limitations 

We state plainly the aspects of C AU VADE that the present work does not resolve. 

Coverage is asymptotic. Thm. 2.2 and Prop. 3.2 guarantee approximation only in the limit of cluster count K and decoder capacity, with no concrete prescription for choosing K from data; we treat it as a hyperparameter. A finite-sample bound on the gap between the realizable family and the partial-identification region—as a function of K, decoder size, sample size, and the geometry of the diagram—would convert our coverage guarantee from a density theorem into an operational one, and is left to future work. 

The γ-sweep is uncalibrated. Sweeping γ traces the partial-identification region in the closure, but no individual γ corresponds to a calibrated point—a sharp upper or lower bound on Px(I), a Manski endpoint, or a quantile of the feasible set. The sweep is a generative analogue of sensitivity analysis, not a procedure for recovering bounds derivable from prior knowledge. Connecting specific values of 

γ to calibrated bounds, when such bounds exist (e.g., for binary outcomes), would let practitioners read off interventional uncertainty intervals directly from the sweep. 

Joint-optimization theory is missing. Our theoretical results assume access to optima of the regularized objective. In practice, each γ requires a separate training run, and we do not characterize the convergence behavior or basin geometry of the joint optimization over encoder, decoder, and classifier-head parameters. Practical robustness across random seeds is verified empirically (App. E), but a guarantee that the trained model lies near the optimum its γ is supposed to select is currently absent. 23 Labeling is conservative. CAU VADE uses only labels of the treatment X; the post-treatment attribute Y and pre-treatment covariate Z are recovered unsupervised. This is intentional—it matches realistic image-domain settings where exhaustive attribute annotation is impractical—but discards information that may be available in some applications. Extending the framework to incorporate partial Y or Z labels, weak attribute predictors trained on auxiliary data, or pretrained vision encoders would tighten the recovered family and is a natural next step. 

Diagram is fixed. We assume the causal diagram in Fig. 3a is given. Misspecification—e.g., a covariate that is post-treatment rather than pre-treatment, or a missing latent pathway—would invalidate the partial-identification guarantee, since the feasible region is defined relative to the assumed diagram. Combining C AU VADE with structure-discovery methods, or extending the density theorem to a class of compatible diagrams, would relax this assumption. 

Empirical scope is limited. Our evaluation covers one synthetic dataset (Color-MNIST), one face dataset (CelebA), and one medical dataset (MIMIC-CXR-JPG), each with a single binary or categorical treatment. We do not study large-scale natural images, multiple simultaneous interventions, continuous treatments, or settings where the confounder is partially observed. Whether the structural-ensemble behavior reported here extrapolates to those regimes is an empirical question the present evaluation cannot settle. 24