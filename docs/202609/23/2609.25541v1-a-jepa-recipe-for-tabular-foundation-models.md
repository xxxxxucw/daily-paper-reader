---
title: A JEPA Recipe for Tabular Foundation Models
title_zh: 面向表格基础模型的 JEPA 配方
authors: "Mingyu Jeon, Suwan Cho, Jae Young Suh"
date: 2026-09-22
pdf: "https://arxiv.org/pdf/2609.25541v1"
tags: ["query:tabular-data"]
score: 9.0
evidence: 结合JEPA自监督的表格基础模型
tldr: "70、回归8:24，且需1.42倍步数和1.66倍墙钟时间。"
source: arxiv
selection_source: fresh_fetch
motivation: 将JEPA式表示空间自监督用于表格基础模型时，潜变量项易坍塌并导致编码器退化为常量映射，需要稳定训练配方。
method: 提出配方：值头直接读取编码器场而非预测器，目标为EMA差值；两臂均按平台期规则停止，不设固定步数预算。
result: "70、回归8:24落后纯值臂，分类差距小、回归更大；需1.42倍步数和1.66倍墙钟时间。"
conclusion: 该配方使JEPA潜变量项与值目标共同收敛，但在表格基础模型上JEPA仍逊于纯值目标，且训练成本更高。
---

## 摘要
表格基础模型学习在上下文中预测单元格值，而世界模型自监督则要求在表示空间中进行预测（LeCun, 2022; Assran et al., 2023）。在表格基础模型先验上，联合嵌入预测架构（JEPA）的潜在项在我们早前的运行中发生坍缩，并把编码器一起带成了常数映射。我们报告一种配方，在该配方下，潜在项与值目标并存，并一直存活到收敛：值头读取编码器场而非预测器，且目标为指数移动平均（EMA）差值。为了限定其相对于仅值分支的成本，两个分支都训练到平台期规则将其停止，没有固定步数预算。固定的训练时域曾将放缓与性能上限混为一谈，因为仅值分支在远超通常预算之后仍在持续改进。在收敛时，每个分支各运行一次，JEPA 分支在 147 个真实数据集上落后于仅值分支：分类上胜负为 32:70（每个数据集名称仅取一条记录时为 29:63），回归上为 8:24；分类上差距较小，回归上差距更大，并且胜负计数在每个分层和每个基准中都倾向同一方向。JEPA 分支（jepa）达到其平台期所需的步数是仅值分支（ds）的 1.42 倍，墙钟时间是其 1.66 倍。

## Abstract
Tabular foundation models learn to predict cell values in context, whereas world-model self-supervision asks for prediction in representation space (LeCun, 2022; Assran et al., 2023). On a tabular foundation-model prior, the latent term of a joint-embedding predictive architecture (JEPA) collapsed in our earlier runs and took the encoder with it to a constant map. We report a recipe under which the latent term survives to convergence beside the value objective: the value head reads the encoder field rather than the predictor, and the target is an exponential moving average (EMA) difference. To bound its cost against the value-only arm, both arms train until a plateau rule stops them, with no fixed step budget. A fixed horizon had confounded a slowdown with a ceiling, since the value-only arm was still improving well past the usual budget. At convergence, in one run per arm, the JEPA arm trails the value-only arm across 147 real datasets, 32:70 wins to losses on classification (29:63 with one entry per dataset name) and 8:24 on regression, the margin small on classification and wider on regression, and the count leans the same way in each stratum and each benchmark. The JEPA arm (jepa) needs 1.42 times as many steps as the value-only arm (ds), and 1.66 times its wall-clock, to reach its plateau.

---

## 论文详细总结（自动生成）

# 论文总结：A JEPA Recipe for Tabular Foundation Models

## 1. 核心问题与整体含义

- **背景**：表格基础模型（如 TabPFN、TabICL）通常通过合成先验进行上下文学习，目标是在数据空间中预测被隐藏单元格的值。其表示完全由值损失塑造。
- **动机**：世界模型式自监督（JEPA）要求在表示空间中预测缺失部分的表示，而不是像素或数值。这与表格基础模型的值预测目标方向不同：值损失奖励能预测单元格的表示，潜变量损失奖励能预测其表示的表示，二者未必一致。
- **核心问题**：在表格基础模型的合成先验上，JEPA 式潜变量目标能否与值目标共同训练而不坍塌？其成本是多少？是否能在真实数据上带来收益？
- **早期失败**：作者早前尝试中，JEPA 潜变量项发生坍塌，把编码器一起带成常数映射；由于值头读取预测器输出，而预测器输出已被潜变量项压平，值头也随之失效。
- **整体含义**：论文不是提出新的正则化器，而是报告一种可让潜变量项存活到收敛的“配方”，并严格测量其相对纯值目标的成本。结论偏负面：能训练，但不提升真实数据准确率，且更慢。

## 2. 方法论

### 核心思想

- 两个分支共享同一个单元格级 Transformer 编码器与值头：
  - **ds**：仅值目标分支。
  - **jepa**：加入潜变量预测目标的分支。
- 关键配方有三点：
  1. **值头读取编码器场，而不是预测器输出**。隐藏单元格处使用编码器自身的填充结果。
  2. **潜变量目标为 EMA 差值**：EMA 目标编码器对完整表的嵌入减去对掩码表的嵌入，经 LayerNorm 后 stop-gradient。
  3. **隐藏单元格以 mask token 进入预测器**，使隐藏单元格彼此不可见。

### 技术细节与流程

- 共享骨干：6 层、8 头、宽度 256 的单元格级 Transformer；值头为 MLP，输出 32 个 bin 的分布。
