---
title: Risk-Aware Online Conformal State Probing
title_zh: 风险感知的在线共形状态探测
authors: "Pietro Talli, Petar Popovski, Osvaldo Simeone"
date: 2026-09-22
pdf: "https://arxiv.org/pdf/2609.25889v1"
tags: ["query:cp"]
score: 9.0
evidence: 在线共形探测，无需分布假设的可靠性保证
tldr: 安全关键场景中，AI自主智能体需从机器人或边缘设备获取状态以决策，平均意义保证不足，且何时探测状态成为关键问题。论文提出在线保形状态探测（OCSP），联合决定动作与探测时机，不依赖分布假设即可认证最坏情况可靠性。OCSP可证明控制漏查询误差（MQE），即本应探测却未探测的比例，同时最小化探测率。它无需重训练即可用于已有预训练价值型控制策略，并通过仿真验证理论保证与状态预测器校准带来的性能权衡。
source: arxiv
selection_source: fresh_fetch
motivation: 安全关键自治系统中，平均情况保证不足，需在不确定状态预测下决定何时探测，以兼顾可靠性与探测成本。
method: 提出在线保形状态探测（OCSP），联合选择动作与探测时机，不依赖分布假设，可证控制漏查询误差MQE并最小化探测率。
result: 数值仿真验证最坏情况可靠性理论保证，并揭示状态预测器校准程度对探测率与性能权衡的影响。
conclusion: OCSP无需重训练或微调即可增强已有价值型控制策略，为安全关键在线状态获取提供可证明且风险感知的探测方案。
---

## 摘要
基于AI的自主智能体通常部署在数据中心，必须从机器人或边缘设备获取状态信息，以便做出明智的控制决策。在安全关键场景中，管理状态的不确定性尤为重要，因为平均情况保证不足。在此背景下，我们研究一个序贯决策过程，该过程在可访问任意状态预测模型的条件下，联合决定采取哪些动作以及何时进行探测。我们提出在线共形状态探测（OCSP），这是一种动作与探测策略，能够在无需依赖分布假设的情况下证明最坏情况可靠性水平。OCSP旨在可证明地控制漏查询错误（MQE），即探测本会有益的实例所占比例，同时最小化探测率。OCSP可应用于现有的预训练基于价值的控制策略，无需重新训练或微调。我们通过数值仿真验证OCSP，以验证理论保证，并评估作为状态预测器校准函数的性能权衡。

## Abstract
AI-based autonomous agents, typically hosted at data centers, must acquire state information from robots or edge devices in order to issue informed control decisions. Managing uncertainty about the state is particularly consequential in safety-critical settings, in which average-case guarantees are insufficient. In this context, we study a sequential decision maker process that jointly decides which actions to take and when to probe given access to an arbitrary state prediction model. We propose online conformal state probing (OCSP), an action and probing policy that certifies worst-case reliability levels without relying on distributional assumptions. OCSP is designed to provably control the missed query error (MQE), i.e., the fraction of instances where probing would have been beneficial, while minimizing the probing rate. OCSP can be applied to existing pre-trained value-based control policies without requiring retraining or fine-tuning. We validate OCSP through numerical simulations to verify theoretical guarantees and to assess performance trade-offs as a function of the calibration of the state predictor.

---

## 论文详细总结（自动生成）

# 论文总结：Risk-Aware Online Conformal State Probing

## 1. 核心问题与整体含义
- **研究背景**：AI 自主智能体通常部署在云端数据中心，但控制机器人或边缘设备时需要及时获取其状态。持续、及时地查询遥测状态往往成本高，因此智能体只能在部分可观测条件下决策。
- **核心问题**：智能体不仅要决定执行什么动作，还要决定何时探测真实状态。在安全关键场景中，平均性能保证不足，偶尔的灾难性失败不可接受，因此需要不依赖环境分布假设的最坏情况可靠性保证。
- **问题形式化**：论文研究序贯决策过程，智能体维护状态信念 \(b_t\)，选择控制动作 \(a_t\)，并可选择探测 \(P_t\in\{0,1\}\)。若动作相对最优动作的 regret 超过阈值 \(R_{\max}\)，则发生 outage。论文关注漏查询误差 MQE，即在本应探测的 outage 实例中未探测的比例。
- **整体含义**：提出一种可包装任意状态预测器的在线探测策略，在无需重训练控制策略的前提下，可证明地控制 MQE，同时尽量降低探测率。这为云边自治系统提供了一种风险感知、分布无关的主动状态获取框架。

##
