---
title: "CORDIAL: Calibrating Ordinal LLM Outputs from Few Labels"
title_zh: CORDIAL：从少量标签校准有序 LLM 输出
authors: "Xiangwei Wang, Peng Wang, Saman Halgamuge"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.29807v1"
tags: ["query:llm-calib"]
score: 8.0
evidence: 面向大语言模型有序输出的可靠校准与不确定性估计
tldr: LLM 将文本映射为有序评分分布时，常出现饱和、压缩、夸大和方向性偏差，在少量标签下校准尤其困难。CORDIAL 将模型输出视作真实标签的含噪观测，用仅五个可解释参数构成信道进行校正，并证明校准结果保持一阶随机占优。在 Amazon 评论和 CMU-MOSEI 转录文本上，四款 LLM 的 80 个设置中有 76 个取得最低 log loss；主 7B 模型仅用 20 个标签，即可媲美最强基线用 28 至 54 个标签的效果。同一后验还能从其他任务学习先验并融合多个 LLM，但 Dirichlet 等无约束校准器只有在标签增至数百上千时才反超。
source: arxiv
selection_source: fresh_fetch
motivation: LLM 输出的有序评分分布存在饱和、压缩、夸大和方向性偏差，少量标签下现有校准器难以稳定估计。
method: CORDIAL 把 LLM 输出当作真实标签的含噪观测，用五参数可解释信道校正后验，并证明其保持一阶随机占优。
result: 在 Amazon 评论与 CMU-MOSEI 转录文本上，四款 LLM 的 80 个设置中 76 个取得最低 log loss；主 7B 模型仅用 20 个标签即媲美需 28 至 54 个标签的最强基线。
conclusion: 同一后验支持跨任务先验学习和多 LLM 融合；Dirichlet 等无约束校准器仅在大规模标签下反超。
---

## 摘要
大语言模型（LLM）可以将一段文本转化为有序尺度上的分布，但该分布是一种含噪测量：饱和、压缩或夸大，并且偏差方向一致。我们提出 CORDIAL，它将模型输出视为对真实标签的含噪读数，并利用一个含五个可解释参数的通道对其进行校正。该通道足够小，使其后验可由少量标签平均得到；我们证明，由此得到的校准保持一阶随机序。在 Amazon 评论和 CMU-MOSEI 转写文本上，使用四个 LLM，在 5 到 100 个标签的 80 种设置中，CORDIAL 在 76 种设置中取得了九种校准器里最低的对数损失；在使用 20 个标签和主 7B 读取器时，它匹配了使用 28-54 个标签的最强基线。同一后验使我们能够从其他任务中学习先验，并融合多个 LLM。诸如 Dirichlet 校准等无限制校准器只有在校准集增长到数百或数千时才会超过它。

## Abstract
A large language model (LLM) can turn a text into a distribution over an ordered scale, but that distribution is a noisy measurement: saturated, compressed or exaggerated, and biased in a consistent direction. We propose CORDIAL, which treats the model's output as a noisy reading of the true label and corrects it with a channel of five interpretable parameters. The channel is small enough for its posterior to be averaged from a handful of labels, and we prove that the resulting calibration preserves first-order stochastic order. On Amazon reviews and CMU-MOSEI transcripts with four LLMs, CORDIAL has the lowest log loss among nine calibrators in 76 of 80 settings with 5 to 100 labels; with 20 labels and the main 7B reader, it matches the strongest baseline using 28-54 labels. The same posterior lets us learn priors from other tasks and fuse several LLMs. Unrestricted calibrators such as Dirichlet calibration overtake it only as the calibration set grows into the hundreds or thousands.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：LLM 可将文本映射为有序尺度上的概率分布（如 1–5 星评分、情感等级），但这种输出通常不是校准统计量，而是含噪测量：概率饱和、分布压缩或夸大，并存在一致方向偏差。
- **核心问题**：在只有少量标注标签的场景下，如何校准 LLM 的有序输出？传统温度缩放、标量可靠性加权无法把概率质量移动到相邻类别；无约束的 K×K 矩阵或 Dirichlet 校准参数量大，在少标签下估计误差高。
- **整体含义**：CORDIAL 把 LLM 输出视为真实有序标签的“含噪读数”，用仅 5 个可解释参数的结构化信道进行贝叶斯校正。其目标是少标签、可解释、保序，并能扩展到跨任务先验学习、多 LLM 融合和用户个性化。

## 2. 方法论

### 2.1 核心思想

- 设真实有序标签为 \(z\)，LLM 对文本 \(x\) 输出选项 logits \(\ell(x)\)，经温度缩放得到读数  
  \[
  q(y|x)=\text{softmax}(\ell/\tau)_y
  \]
  其中 \(y\in\{0,\dots,K-1\}\) 为有序类别。
- 将 \(q(y|x)\) 视为对 \(z\) 的噪声观测，通过行随机信道 \(T\) 校正：
  \[
  \hat p(z|x)=\sum_y T(z|y)q(y|x)
  \]
- LLM 保持冻结，校准问题转化为从少量标注文本中估计信道 \(T\)。

### 2.2 五参数结构化信道

- 参数为 \(\phi=(\tau,d,s,\kappa,\rho)\)：
  - \(\tau\)：温度；
  - \(d\)：偏移，修正系统性高估/低估；
  - \(s>0\)：增益，修正压缩或夸大；
  - \(\kappa>0\)：集中度，控制分布扩散；
  - \(\rho\)：读数相对标签先验 \(\pi(z)\) 的强度。
- 使用高斯型有序轮廓：
  \[
  G(z|c)=\
