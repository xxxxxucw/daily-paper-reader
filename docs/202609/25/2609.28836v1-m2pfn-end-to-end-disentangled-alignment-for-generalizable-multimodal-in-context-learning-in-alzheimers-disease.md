---
title: "M$^2$PFN: End-to-End Disentangled Alignment for Generalizable Multimodal In-Context Learning in Alzheimer's Disease"
title_zh: M$^2$PFN：面向阿尔茨海默病可泛化多模态上下文学习的端到端解耦对齐
authors: "Lujia Zhong, Shuo Huang, Jianwei Zhang, Xinyu Nie, Yonggang Shi"
date: 2026-09-23
pdf: "https://arxiv.org/pdf/2609.28836v1"
tags: ["query:tabular-data"]
score: 8.0
evidence: 将表格基础模型TabPFN扩展到多模态上下文学习
tldr: "多模态阿尔茨海默病诊断常受跨队列泛化限制，而TabPFN等表格基础模型的上下文学习虽泛化强，其合成表格先验却难匹配3D-MRI导出的特征。为此，M²PFN将冻结的TabPFN转化为多模态AD预测器：以可微推理反传任务梯度训练影像与表格编码器，通过解耦和对比学习对齐共享子空间，并借助可学习门控捷径融合纯表格预测。在ADNI三分类CN/MCI/AD上，它取得65.55% macro-F1与82.21% macro-AUC；切换回归头后MMSE MAE为1.743。在OASIS-3和SCAN外部队列免重训练仍获最优AUC与最低MMSE MAE，且能跨认知量表迁移。"
source: arxiv
selection_source: fresh_fetch
motivation: 多模态AD诊断受跨队列泛化差困扰，TabPFN的表格先验与影像特征不匹配，阻碍其上下文学习优势向多模态AD分析迁移。
method: M²PFN冻结TabPFN引擎，以可微推理反传梯度训练3D-MRI和表格编码器，用解耦与对比目标对齐共享子空间，并加可学习门控捷径融合纯表格预测。
result: "在ADNI三分类上达65.55% macro-F1和82.21% macro-AUC，MMSE回归MAE 1.743，均超所有基线；在OASIS-3与SCAN外部队列免重训练获最优AUC和最低MMSE MAE。"
conclusion: M²PFN证明冻结表格基础模型的上下文学习可经端到端多模态对齐扩展至AD，保持免重训练跨队列与跨量表泛化能力。
---

## 摘要
尽管已提出多种结合影像与表格数据的多模态方法用于阿尔茨海默病（AD）诊断，但它们通常在不同队列间的泛化能力有限。上下文学习（ICL）在 TabPFN 等基础表格模型中展现出优异的泛化性能和高灵活性。要将 TabPFN 的 ICL 扩展到多模态 AD 分析，主要障碍在于 TabPFN 是在合成表格先验上进行元训练的，而这些先验与图像衍生特征的统计结构并不天然匹配。我们提出 M$^2$PFN，一个端到端框架，将这一表格基础模型转化为多模态 AD 预测器。M$^2$PFN：（i）通过 TabPFN 的 transformer 执行可微推理，将任务梯度反向传播到 3D-MRI 和表格编码器；（ii）通过解耦和对比目标，将两种模态对齐到一个共享子空间，并与 ICL 引擎的先验相匹配；（iii）通过可学习的门控捷径，将冻结的纯表格预测融入其中。由于 ICL 引擎保持冻结，其上下文机制得以保留，用于测试时泛化，而端到端训练则将编码器塑造成其可利用的特征。在 ADNI（$n=2240$，三分类 CN/MCI/AD）上，M$^2$PFN 达到 $65.55\%$ macro-F1 和 $82.21\%$ macro-AUC，超越了一整套单模态和多模态基线。仅将预测头替换为 TabPFN 回归器，相同架构便在包含 $1250$ 名受试者的子队列上回归基线 MMSE，测试 MAE 为 $1.743$，优于所有多模态基线。在两个外部队列（OASIS-3 和 SCAN）上无需重新训练，M$^2$PFN 在所有基线中取得了最佳 AUC 和最低 MMSE MAE，并且即使认知量表发生变化也能迁移。

## Abstract
While various multimodal methods combining imaging and tabular data for Alzheimer's disease (AD) diagnosis were proposed, they are often limited in generalization across cohorts. In-context learning (ICL) has demonstrated excellent generalization performances and high flexibility in foundational tabular models such as TabPFN. To extend TabPFN's ICL to multimodal AD analysis, the main obstacle is that TabPFN is meta-trained on synthetic tabular priors that do not naturally match the statistical structure of image-derived features. We propose M$^2$PFN, an end-to-end framework that turns this tabular foundation model into a multimodal AD predictor. M$^2$PFN (i) performs differentiable inference through TabPFN's transformer, back-propagating task gradients into 3D-MRI and tabular encoders; (ii) aligns the two modalities into a shared subspace, via disentanglement and a contrastive objective, matched to the ICL engine's prior; and (iii) folds in a frozen tabular-only prediction through a learnable gated shortcut. Because the ICL engine stays frozen, its in-context mechanism is preserved for test-time generalization, while end-to-end training shapes the encoders into features it can exploit. On ADNI ($n=2240$, three-class CN/MCI/AD), M$^2$PFN attains $65.55\%$ macro-F1 and $82.21\%$ macro-AUC, surpassing a comprehensive set of unimodal and multimodal baselines. By swapping only the head for a TabPFN regressor, the same architecture regresses baseline MMSE on a $1250$-subject sub-cohort to test MAE $1.743$, outperforming every multimodal baseline. On two external cohorts (OASIS-3 and SCAN) with no retraining, M$^2$PFN achieves the best AUC and the lowest MMSE MAE across all baselines, and transfers even when the cognitive instrument changes.

---

## 论文详细总结（自动生成）

# M²PFN 论文结构化总结

## 1. 核心问题与整体含义

- **研究动机**：阿尔茨海默病（AD）诊断中，结构 MRI 与表格数据（人口学、APOE、CSF 生物标志物等）联合分析已被证明优于单模态。但现有多模态方法多为任务/数据集特定的参数化融合模型，跨队列、跨扫描仪、跨特征可用性时泛化差，往往需要重训练或大量微调。
- **ICL 的机会与障碍**：TabPFN 等表格基础模型通过上下文学习（ICL）在中小规模表格数据上具有强泛化能力，推理时只需替换支持集即可适配新任务，无需更新权重。然而 TabPFN 只接受表格输入，且其元训练先验来自合成表格数据生成过程，与 3D MRI 导出的图像特征统计结构不匹配。
- **关键矛盾**：TabPFN 的预处理流程包含不可微操作，阻断梯度从 ICL 引擎回传到影像/表格编码器，因此无法直接与可学习编码器联合训练。
- **整体含义**：论文提出 M²PFN，将冻结的 TabPFN 改造成多模态 AD 预测器。其核心思想是：**保持 ICL 引擎冻结以保留测试时泛化与免重训练适配能力，同时通过可微推理让任务梯度塑造前端多模态表示**，使其匹配 TabPFN 的上下文先验。这为“基础模型 + 端到端表示学习”的多模态医学 AI 提供了新范式。

## 2. 方法论

### 2.1 问题形式化

- 每个受试者表示为 \(x_i=(x_i^{img}, x_i^{tab})\)：
  - \(x_i^{img}\in\mathbb{R}^{1\times128\times128\times128}\)：3D MRI 体积；
  - \(x_i^{tab}\in\mathbb{R}^{d}\)：连续与序数编码后的表格变量。
- 任务目标：
  - 分类：\(y_i\in\{0,1,2\}\) 对应 CN/MCI/AD；
  - 回归：\(y_i\in\mathbb{R}\)，如 MMSE。
- 给定查询受试者 \(q\) 与带标签支持集 \(D_S=\{(x_j,y_j)\}_{j\in S}\)，预测：
  \[
  p_{\Theta_{PFN}}\left(y_q \mid \tilde h_q, \{(\tilde h_j,y_j)\}_{j\in S}\right)
  \]
  其中 \(\tilde h_i\) 是送入 TabPFN 的多模态表示，\(\Theta_{PFN}\) 为冻结的 TabPFN 参数。
- 支持集仅作为上下文输入，因此推理时可替换 \(D_S\)，实现免梯度跨队列适配。

### 2.2 多模态表示学习

- **影像编码器**：MONAI 3
