---
title: "Learning to Adapt and Calibrate: Score Distribution Alignment for Few-Shot Uncertainty Prediction in Medical VLMs"
title_zh: 学习适应与校准：面向医学视觉语言模型小样本不确定性预测的分数分布对齐
authors: "Xuan Cuong Ngo, Ngan Le"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.10333v1"
tags: ["query:cp"]
score: 9.0
evidence: 具有分布无关覆盖保证的共形预测不确定性估计
tldr: 医学视觉语言模型在少样本迁移中难以满足标准共形预测的可交换性与大校准集假设，支持集微调还会使非共形分数分布偏移，导致覆盖不可靠。AlignCP 在监督少样本适配与共形不确定性估计之间取得协调，学习重加权校准分布，对齐有标签支持集与无标签查询集的一维非共形分数分布。该方法无需查询标签即可缩小适配引发的覆盖差距，同时保留支持集监督更新带来的任务适配能力。
source: arxiv
selection_source: fresh_fetch
motivation: 标准共形预测依赖校准与测试可交换且需大校准集，少样本医学VLM迁移中支持集微调会偏移非共形分数分布，破坏覆盖有效性。
method: AlignCP学习重加权校准分布，对齐有标签支持集与无标签查询集的一维非共形分数分布，以缩小适配引起的分数层面差异。
result: 摘要未报告具体实验数值；AlignCP旨在无需查询标签的情况下缩小适配引发的覆盖差距，并维持监督微调的任务适配收益。
conclusion: AlignCP将共形预测作为适配模型的不确定性层，而非阻止监督更新，为少样本医学VLM提供兼顾任务适配与可靠覆盖的思路。
paper_media_checked: true
reading_section: deep
---

## 摘要
使用保形预测对医学视觉-语言模型（VLM）进行不确定性估计，因其无分布假设的覆盖保证而日益受到关注。然而，标准保形预测依赖于校准数据与测试数据之间的可交换性，并且通常需要足够大的校准集才能获得可靠的覆盖。这些假设在小样本迁移场景中难以满足：此时仅有少量带标签的支持集可用于将预训练 VLM 适配到新的医学任务，而评估使用无标签的查询集。在支持集上进行有监督微调会改变模型参数，进而使非一致性分数分布发生偏移，破坏校准样本与查询样本之间的可交换性，并在分布偏移下导致不可靠的覆盖。现有的转导式保形自适应方法通常通过避免有监督更新来保持有效性。虽然这有助于维持保形假设，但未能充分利用稀缺的带标签支持数据，并限制了任务适应，而任务适应正是小样本学习的主要目标。在这种场景下，保形预测应作为支持已适应模型的不确定性估计层，而不是阻碍适应本身。为此，我们提出 AlignCP，一个在非可交换性下协调有监督小样本适应与保形不确定性估计的框架。AlignCP 学习一个重加权校准分布，以减小带标签支持集与无标签查询集之间的分数级差异。通过对齐一维非一致性分数分布，AlignCP 旨在无需查询标签的情况下缩小由适应引起的覆盖差距。

## Abstract
Uncertainty estimation for medical vision--language models (VLMs) using conformal prediction has gained increasing attention due to its distribution-free coverage guarantees. However, standard conformal prediction relies on exchangeability between calibration and test data and typically requires a sufficiently large calibration set to obtain reliable coverage. These assumptions are difficult to satisfy in few-shot transfer settings, where only a small labeled support set is available to adapt a pretrained VLM to a new medical task, while an unlabeled query set is used for evaluation. Supervised fine-tuning on the support set changes the model parameters and consequently shifts the nonconformity score distribution, breaking exchangeability between calibration and query samples and leading to unreliable coverage under distribution shift. Existing transductive conformal adaptation methods often preserve validity by avoiding supervised updates. While this helps maintain conformal assumptions, it underutilizes the scarce labeled support data and limits task adaptation, which is the primary objective in few-shot learning. In this setting, conformal prediction should serve as an uncertainty estimation layer that supports the adapted model, rather than preventing adaptation itself. To this end, we propose AlignCP, a framework that reconciles supervised few-shot adaptation with conformal uncertainty estimation under non-exchangeability. AlignCP learns a reweighted calibration distribution that reduces the score-level discrepancy between the labeled support set and the unlabeled query set. By aligning the one-dimensional nonconformity score distributions, AlignCP aims to close the coverage gap induced by adaptation without requiring query labels.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义
- **研究背景**：医学视觉–语言模型（VLM）在影像分类、视觉问答、跨模态推理等临床任务中迁移能力增强，但其在迁移场景下的可靠性与不确定性仍缺乏充分研究。
- **共形预测的吸引力与前提**：Split Conformal Prediction（SCP）可在可交换性假设下提供无分布假设的有限样本覆盖保证，但标准 SCP 依赖校准集与测试集可交换，且通常需要足够大的校准集。
- **少样本医学迁移中的矛盾**：少样本场景只有少量带标签支持集，却同时被用于监督微调与共形校准。模型在支持集上适配后，其非一致性分数分布会向支持样本偏移，与无标签查询样本不再可交换，导致朴素 SCP 覆盖不足。
- **现有方法的取舍**：转导式共形适配方法常通过避免监督更新来保持共形有效性，但这浪费了稀缺标签，限制任务适配，而任务适配正是少样本学习的首要目标。
- **论文整体含义**：作者主张在少样本迁移中，共形预测应作为已适配模型的不确定性估计层，而非限制监督适配的约束。为此提出 **AlignCP**，在监督少样本适配与共形不确定性估计之间进行协调，通过分数分布对齐缩小适配引起的覆盖差距。

### 2. 方法论：AlignCP
- **核心思想**：先在有标签支持集上监督适配医学 VLM，再复用同一支持集做共形校准；但显式纠正适配导致的校准分数与查询分数之间的分布错配。
- **问题形式化**：
  - 源分布 \(P\)、目标分布 \(Q\)；校准集 \(D_{cal}\) 来自 \(P\)，查询集 \(D_{qry}\) 来自目标边缘分布 \(Q_X\)，无查询标签。
  - 适配后模型 \(f_{\theta'}\)，非一致性分数 \(s(x,y)\)；源/目标诱导分数分布分别为 \(P_s=s_\#P\)、\(Q_s=s_\#Q\)。
  - 覆盖率差距定义为 \(\Delta_{P,Q}(\alpha)=|Cov_P(\alpha)-Cov_Q(\alpha)|\)。
  - 标准 SCP 阈值由校准分数经验分位数给出：\(\hat q_{1-\alpha}(S_{cal})=S_{(\lceil(n+1)(1-\alpha)\rceil)}\)。
  - 目标覆盖满足下界：\(Cov_Q(\alpha)\ge 1-\alpha-\Delta_{P,Q}(\alpha)\)，因此减小分数分布差异可缩小
