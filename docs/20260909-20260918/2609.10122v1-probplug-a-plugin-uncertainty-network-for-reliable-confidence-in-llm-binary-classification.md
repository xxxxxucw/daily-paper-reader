---
title: "ProbPlug: A Plugin Uncertainty Network for Reliable Confidence in LLM Binary Classification"
title_zh: ProbPlug：一种用于LLM二分类中可靠置信度的插件式不确定性网络
authors: "Jianzong Wang, Chuhang Liu, Botao Zhao, Zuheng Kang, Xulong Zhang, Xiaoyang Qu, Junqing Peng, Zhiewei Ye, Yayun He"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.10122v1"
tags: ["query:llm-calib"]
score: 9.0
evidence: 面向大语言模型分类的置信度校准
tldr: 大语言模型在分类任务上表现强劲，但在高风险场景中，其预测可靠性仍是部署障碍。针对基于LLM的分类置信度校准研究不足，本文提出ProbPlug，一个轻量级置信度估计框架，利用冻结LLM的内部token特征判断二分类输出是否正确。它通过自注意力聚合隐藏表示，可无缝接入原推理流程且不修改基座模型。在文本与多模态任务上，ProbPlug提供更可靠置信度、以极小开销提升分类性能，并展现良好跨任务泛化能力。
source: arxiv
selection_source: fresh_fetch
motivation: LLM分类应用广泛，但高风险场景下预测可靠性不足；现有置信度估计多聚焦生成，面向LLM分类的校准研究仍欠缺。
method: 提出ProbPlug轻量置信度框架：用冻结LLM内部token特征，经自注意力聚合隐藏表示，预测二分类输出是否正确，无需改基座模型。
result: 在文本与多模态任务上，ProbPlug置信度更可靠，以极小额外开销提升分类性能，并展现跨任务泛化。
conclusion: 表明ProbPlug可作为LLM二分类置信度估计的实用方案，助力高风险场景可靠部署，代码已公开。
---

## 摘要
大语言模型（LLM）在广泛的分类设置中取得了强劲性能，然而其预测的可靠性仍然是在高风险场景中部署的主要障碍。尽管针对LLM的置信度估计已被广泛研究，但面向基于LLM的分类的置信度校准仍探索不足。我们提出ProbPlug，一种面向基于LLM的二分类的轻量级置信度估计框架，它利用从冻结LLM中提取的内部token特征来预测输出是否正确。ProbPlug采用自注意力模块聚合隐藏表示，并且可以在不修改基础模型的情况下集成到原始推理流程中。涉及基于文本和多模态大模型的多个任务的实验表明，ProbPlug提供了更可靠的置信度估计，以可忽略的额外开销提升分类性能，并在跨任务上表现出强泛化能力。这些结果表明，ProbPlug可作为基于LLM的分类中置信度估计的实用解决方案。我们的代码已在Github公开。

## Abstract
Large language models (LLMs) have achieved strong performance across a broad range of classification settings, yet the reliability of their predictions remains a major obstacle to deployment in high-stakes scenarios. Although confidence estimation for LLMs has been widely studied, confidence calibration for LLM-based classification remains underexplored. We introduce ProbPlug, a lightweight confidence estimation framework for LLM-based binary classification, which predicts whether an output is correct using internal token features extracted from a frozen LLM. ProbPlug employs a self-attention module to aggregate hidden representations and can be integrated into the original inference pipeline without modifying the base model. Experiments across multiple tasks involving both text-based and multimodal large models show that ProbPlug provides more reliable confidence estimates, improves classification performance with negligible additional overhead, and exhibits strong generalization across tasks. These results indicate that ProbPlug serves as a practical solution for confidence estimation in LLM-based classification. Our code is publicly available at Github.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究动机**：LLM 在文本与多模态分类任务中表现强劲，但其输出通常只有离散答案（如 “Yes/No”），缺乏可靠、可校准的置信度，限制其在高风险场景中的部署。
- **背景缺口**：现有 LLM 置信度估计方法多面向生成任务，如 Verbalization、Logit、Self-Consistency、CISC、SAPLMA 等；但面向 **LLM 分类任务** 的置信度校准仍探索不足。
- **整体含义**：论文提出 **ProbPlug**，一个轻量、插件式置信度估计框架，用于判断 LLM 二分类输出是否正确，从而提升 LLM 分类的可靠性与可部署性。

## 2. 方法论
- **核心思想**：冻结基座 LLM，仅训练一个外部轻量网络；利用 LLM 内部多层隐藏表示估计“当前输出正确”的概率。灵感来自人脑决策中信号跨脑区流动，假设 LLM 决策置信度编码于不同 Transformer 层表示中。
- **输入表示**：
  - 给定 prompt，LLM 生成 “Yes/No” 等答案 token。
  - 提取最终生成 token 在所有 Transformer 层的隐藏状态：  
    \(Z=[z^{(1)},z^{(2)},...,z^{(L)}]\)，默认只取最后生成 token，即 \(S=1\)。
- **Token Compression, TC**：
  - 对每层 token 表示做非线性变换与两层全连接压缩，将 token 级特征压缩为每层单向量：  
    \(\tilde z=W_2\sigma(W_1 z^{(l)}+b_1)+b_2\)。
- **Layer Information Aggregation, LIA**：
  - 使用多头注意力自适应融合不同层信息。
  - 设可学习 query \(q\)，计算 \(Q=W_Q q\)，\(K=W_K\tilde Z\)，\(V=W_V\tilde Z\)。
  - 注意力权重：\(\alpha=\mathrm{softmax}(QK^T/\sqrt{d_k})\)。
  - 聚合表示：\(z_{agg}=\alpha V\)。
- **置信度预测**：
  - 通过轻量分类头输出概率：\(P=\sigma(W_c z_{agg}+b_c)\)，表示 LLM 输出正确的置信度。
- **跨任务与多类扩展**：
  - 跨任务：许多分类可改写为 Yes/No 问题，修改 prompt 即可复用置信度估计器。
  - 多类：分解为多个 one-vs-rest 二分类决策，再用 softmax 归一化得到类别概率。

## 3. 实验设计
- **数据集 / 场景**：
  - 文本分类：SMS Spam Collection、SST-2、Toxic Comment Classification、Civil Comments、Amazon Polarity。
  - 语音情感 / 多模态：IEMOCAP，包含 5,531 条语音、4 类情绪。
- **Benchmark 指标**：
  - 分类性能：F1-score、AUPRC。
  - 校准性能：ECE、Brier Score。
  - 语音任务：UA、WA、F1-score。
- **对比方法**：
  - 基座：Qwen3-8B；语音多模态基座：Qwen2-Audio。
  - 置信度 / 推理方法：Verbalization、Logit、Self-Consistency、CISC、SAPLMA。
  - 语音任务还对比非 LLM 方法：data2vec 2.0 large、Whisper large v3、Emotion2vec large 等。
- **实验设置**：
  - 在 SMS-SPAM 上训练，评估 in-task 与 cross-task 表现。
  - 多类与多模态任务使用 Qwen2-Audio 训练 ProbPlug。
  - 额外进行校准实验、消融实验和注意力可视化。

## 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量、训练时长、显存、参数量或总计算量。
- 文中仅强调 ProbPlug 是轻量插件，额外推理开销可忽略，但未提供具体训练与推理成本统计。
- 因此无法从论文中评估其实际算力需求、训练碳成本或复现所需硬件条件。

## 5. 实验数量与充分性
- **实验组数量**：
  - 约 6 个数据集 / 场景：5 个文本数据集 + IEMOCAP 语音情感。
  - 多组对比方法：Verbalization、Logit、Self-Consistency、CISC、SAPLMA 等。
  - 消融实验：w/o TC、w/o LIA、10 token、完整 ProbPlug。
  - 校准实验：ECE、Brier Score，并给出可靠性图。
  - 可解释性分析：36 层 Transformer 注意力权重可视化。
- **充分性评价**：
  - 覆盖 in-task、cross-task、多类、多模态、校准和消融，整体较充分。
  - 但缺少统计显著性检验、置信区间、更多随机种子报告。
  - 数据集多为英文文本和单一语音数据集，领域覆盖仍有限。
- **公平性评价**：
  - 统一使用 Qwen3-8B / Qwen2-Audio 作为基座，结果多次运行平均，较有可比性。
  - 但 baseline 的实现细节、超参搜索、prompt 设计等未充分说明，可能影响公平性判断。

## 6. 主要结论与发现
- ProbPlug 在多数 in-task 和 cross-task 场景下提升 F1 与 AUPRC。
  - SMS-SPAM：F1 94.78，AUPRC 97.56。
  - SST2：F1 88.47，AUPRC 97.42。
  - Toxic Comment：F1 81.68，AUPRC 73.20。
  - Civil Comments：F1 67.20，AUPRC 58.02。
  - Amazon Polarity：F1 94.72，AUPRC 98.72。
- 校准性能优于 SAPLMA：
  - 平均 ECE：ProbPlug 0.0805，SAPLMA 0.0982。
  - 平均 Brier Score：ProbPlug 0.0824，SAPLMA 0.0943。
- 多类 / 多模态任务有效：
  - IEMOCAP 上 UA 77.33、WA 77.32、F1 77.76，优于多种对比方法。
- 消融表明 TC 与 LIA 均有效。
- 只使用最终生成 token（S=1）比使用 10 个 token 跨任务泛化更好。
- 注意力可视化显示高置信预测集中于早期词法层和末端推理层，中间层权重较低。

## 7. 优点
- **轻量插件式设计**：不修改冻结 LLM，只训练外部置信度网络，易集成。
- **单次推理**：相比 Self-Consistency、CISC 等多次推理方法，效率更高。
- **利用内部表示**：从多层隐藏状态提取信息，结合 LLM 泛化能力与非 LLM 校准能力。
- **跨任务泛化**：通过修改 prompt 即可迁移到新任务，减少重训练需求。
- **可扩展性**：可扩展到多类分类和多模态大模型。
- **校准与可解释性**：ECE / Brier 改善，并通过层注意力权重提供一定可解释性。
- **实验维度较丰富**：涵盖文本、语音、多模态、校准、消融与跨任务。

## 8. 不足与局限
- **算力信息缺失**：未报告 GPU、训练时长、参数量等，复现成本不透明。
- **白盒依赖**：需要访问 LLM 所有层的隐藏状态，不适用于仅提供 API 的闭源模型。
- **任务范围有限**：主要聚焦 Yes/No 二分类；多类采用 one-vs-rest，可能增加推理开销。
- **数据集覆盖有限**：以英文文本和 IEMOCAP 语音为主，缺少更多语言、领域和高风险真实场景。
- **阈值与部署问题**：虽强调可阈值过滤，但未深入讨论阈值选择、代价敏感决策和分布漂移。
- **统计严谨性不足**：缺少显著性检验、误差棒、多次随机种子细节。
- **校准并非全面最优**：例如 SST2 上 ECE 略低于 SAPLMA，说明校准表现存在任务差异。
- **公平性风险**：baseline 超参、prompt、训练细节未充分披露，可能影响对比公平性。
- **应用限制**：跨任务泛化仍可能受 prompt 模板和源任务分布影响，实际部署需额外验证。

（完）
