---
title: "Multi-source conformal prediction: leveraging heterogeneity via localization"
title_zh: 多源共形预测：通过局部化利用异质性
authors: "Rohan Hore, Anirban Chatterjee, Sayantan Choudhury"
date: 2026-09-13
pdf: "https://arxiv.org/pdf/2609.14531v1"
tags: ["query:cp"]
score: 9.0
evidence: 多源共形预测与局部覆盖保证
tldr: 多源异构数据预测中，测试分布常偏离任一源，但不同源可在特征空间不同区域提供互补信息。本文提出多源随机局部化共形预测 MS-RLCP，在 RLCP 的局部覆盖基础上，通过数据自适应源选择融合多源。假设各源与测试总体共享条件响应分布，利用包络分布刻画总体特征表示，建立有限样本覆盖界，并允许测试特征分布关于包络绝对连续，超越源分布混合。正则条件下还证明渐近测试条件覆盖；模拟与真实实验验证其适应不同异质性。
source: arxiv
selection_source: fresh_fetch
motivation: 多源异构数据中，测试分布可能不同于任一源；但源间互补性也带来机会，关键在于如何利用局部特征空间覆盖差异。
method: 提出 MS-RLCP，基于随机局部化共形预测的局部覆盖，通过数据自适应源选择融合多源，并用包络分布建立有限样本覆盖界。
result: 在共享条件响应假设下，测试特征分布可关于包络绝对连续，超越源混合；正则条件下获得渐近测试条件覆盖，实验显示适应不同异质性。
conclusion: MS-RLCP 将局部化共形预测扩展到多源场景，借助源选择与包络分析提升异构数据下的覆盖与适应性，为多源不确定性量化提供实用框架。
paper_media_checked: true
reading_section: quick
---

## 摘要
许多现代预测任务涉及来自多个异质来源的数据，而测试分布可能与任一单个来源存在显著差异。尽管异质性带来挑战，但它也提供了机会：不同来源可能提供互补信息，特征空间的某些区域在某个来源中比在另一个来源中得到更好的表示。我们提出多源随机局部化共形预测（MS-RLCP），它建立在随机局部化共形预测（RLCP）（Hore and Barber, 2025）的局部覆盖性质之上，并通过数据自适应的来源选择将其扩展到多个来源。在广泛采用的假设下，即各来源和测试总体在给定特征时的响应分布相同，我们利用一个可解释的包络分布概念来建立有限样本覆盖界，该概念刻画了它们的聚合特征空间表示。我们的分析允许测试特征分布关于该包络绝对连续，从而超越了源分布的混合。在额外的正则条件下，我们还建立了渐近的测试条件覆盖。模拟和真实世界实验表明，MS-RLCP 在不同程度的数据异质性下均有效。

## Abstract
Many modern prediction tasks involve data from multiple heterogeneous sources, while the test distribution may differ substantially from any individual source. Although heterogeneity poses challenges, it also offers an opportunity: different sources may provide complementary information, with some regions of the feature space better represented in one source than another. We propose Multi-Source Randomly Localized Conformal Prediction (MS-RLCP), which builds on the local coverage properties of randomly localized conformal prediction (RLCP) (Hore and Barber, 2025) and extends it to multiple sources through data-adaptive source selection. Under the widely adopted assumption of a shared response distribution conditional on the features across sources and the test population, we establish finite-sample coverage bounds using an interpretable notion of envelope distribution that captures their aggregate feature-space representation. Our analysis allows the test feature distribution to be absolutely continuous with respect to the envelope, extending beyond mixtures of source distributions. Under additional regularity conditions, we also establish asymptotic test-conditional coverage. Simulations and real-world experiments demonstrate the effectiveness of MS-RLCP across varying levels of data heterogeneity.

---

## 论文详细总结（自动生成）

# 论文总结：Multi-source conformal prediction: leveraging heterogeneity via localization

## 1. 核心问题与整体含义

- **研究背景**：现代预测任务常涉及来自多个异质来源的数据，例如不同医院、传感器网络、地理区域等。标准共形预测依赖可交换性，即训练与测试数据同分布，但多源场景下测试分布可能不同于任一来源，导致可交换性失效。
- **核心问题**：如何利用多个异质来源的互补信息，在测试分布可能偏离所有源分布的情况下，构造具有可靠覆盖保证的共形预测集。
- **关键机会**：异质性不仅是挑战，也是机会。不同来源在特征空间的不同区域可能具有更好的局部代表性，因此可以针对测试点选择局部表示最强的来源。
- **整体含义**：论文提出 **MS-RLCP**（Multi-Source Randomly Localized Conformal Prediction），将单源随机局部化共形预测 RLCP 扩展到多源场景，通过数据自适应源选择利用异质性，并在共享条件响应分布假设下提供有限样本覆盖界与渐近测试条件覆盖。

## 2. 方法论

- **核心思想**：
  - 在每个源上独立构造具有局部覆盖性质的 RLCP 预测集；
  - 在测试时，根据扰动后的测试特征，选择局部特征表示最强的源；
  - 返回该源的 RLCP 预测集作为最终预测集。
- **RLCP 回顾**：
  - 对测试特征 \(X_{n+1}\) 生成扰动特征 \(\tilde X_{n+1} \sim H(X_{n+1}, \cdot)\)；
  - 使用加权共形方法，权重由核 \(H(X_i, \tilde X_{n+1})\) 决定；
  - 构造加权经验分布，并取 \(1-\alpha\) 分位数作为阈值。
- **MS-RLCP 算法流程**：
  1. 生成扰动测试特征 \(\tilde X_{n+1}\)；
  2. 对每个源 \(k\)，拆分训练/校准集，训练源特定分数 \(s_k\)；
  3. 计算源对齐分数 \(\hat w_k = \frac{1}{|D_{k,\text{train}}|}\sum H(X_{i,k}, \tilde X_{n+1})\)；
  4. 选择 \(\hat k = \arg\max_k \hat w_k\)；
  5. 在选定源 \(\hat k\) 的校准集上计算加权分位数 \(\hat q\)，权重为 \(H(X_{i,\hat k}, \tilde X_{n+1})/Z\)，并包含测试点权重；
  6. 返回 \(\hat C_n(X_{n+1}) = \{y: s_{\hat k}(X_{n+1}, y) \le \hat q\}\)。
- **包络分布**：
  - 定义包络密度 \(\bar f_X(x) = B^{-1}\max_k f_{k,X}(x)\)，其中 \(B\) 为归一化常数；
  - 包络分布 \(\bar P_X\) 刻画所有源在特征空间中的聚合表示；
  - 每个源特征分布关于 \(\bar P_X\) 绝对连续，任意源混合也与 \(\bar P_X\) 等价。
- **理论假设与结果**：
  - 假设各源与测试总体共享条件响应分布 \(P_{Y|X}\)，即协变量偏移；
  - 测试特征分布 \(P_{\text{test},X}\) 关于包络分布 \(\bar P_X\) 绝对连续，允许测试分布不是源分布的混合；
  - 有限样本覆盖下界包含目标水平 \(1-\alpha\)、有限样本修正 \(1/n_{\text{eff}}\)、局部化/扰动项 \(O(h)\) 以及局部表示不足的尾概率项；
  - 在额外正则条件下，证明渐近测试条件覆盖，即 \(P(Y \in \hat C_n(X) | X=x_0) \to 1-\alpha\)。

## 3. 实验设计

- **合成数据实验**：
  - 特征空间 \(\mathbb{R}^5\)，\(K=10\) 个源，\(P_{k,X} = N(\nu_k, I_5)\)，通过坐标对取 \(-\nu\)、其余取 \(\nu\) 构造结构化异
