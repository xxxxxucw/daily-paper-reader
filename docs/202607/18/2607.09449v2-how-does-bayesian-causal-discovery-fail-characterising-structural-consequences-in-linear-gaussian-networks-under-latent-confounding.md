---
title: How Does Bayesian Causal Discovery Fail? Characterising Structural Consequences in Linear Gaussian Networks under Latent Confounding
title_zh: 贝叶斯因果发现如何失败？表征潜变量混淆下线性高斯网络的结构性后果
authors: "Debargha Ghosh, Silja Renooij, Anna V. Kononova"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09449v2"
tags: ["query:causal-infer"]
score: 9.0
evidence: 分析贝叶斯因果发现在潜在混淆下的后验行为
tldr: 贝叶斯因果发现通过后验推断量化有向无环图的不确定性，但其在潜变量混淆下的行为尚不明确。本文针对线性高斯因果模型，分析了两个观测变量间存在加性潜在混淆的情形，推导出一个相关性阈值：超过该阈值时，评分函数会倾向于包含混淆变量间的伪边，且该阈值随样本量增加而降低。进一步刻画了两种后验失败模式，由混淆变量局部结构决定。研究揭示了更多数据反而可能加剧因果发现错误的机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09449v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1312, \"height\": 216, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09449v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1355, \"height\": 394, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09449v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1480, \"height\": 347, \"label\": \"Table\"}]"
motivation: 现有工作忽略混淆对后验分布的细粒度影响，需揭示贝叶斯因果发现在潜混淆下的具体失败模式。
method: 推导线性高斯模型中加性潜在混淆下的评分函数相关性阈值，并分析样本量效应。
result: 发现阈值随样本量递减，超过阈值后出现两种后验失败模式，伪边被错误支持。
conclusion: 揭示了贝叶斯因果发现中“更多数据导致更差结果”的反直觉现象，需谨慎处理潜混淆。
---

## 摘要
贝叶斯因果发现因其通过后验推断量化有向无环图（DAG）上认知不确定性的能力而被广泛使用。然而，其在潜变量混淆下的行为仍然知之甚少，因为现有研究通常指出混淆破坏了可识别性，但没有描述DAG后验分布如何响应。在这项工作中，我们分析了线性高斯因果模型中潜变量混淆下的后验行为，重点关注恰好两个观测变量之间的加性潜变量混淆。我们推导出一个临界相关阈值，高于该阈值时评分函数倾向于在混淆变量之间引入虚假边的图，并表明该阈值随样本量增加而降低——更多数据降低了虚假边被偏好所需的相关性。超过该阈值，我们根据混淆变量周围的局部结构刻画了两种不同的后验失败模式。我们的发现得到了多个图结构上的精确后验计算的支持，验证了预测的失败模式。

## Abstract
Bayesian causal discovery is widely used for its ability to quantify epistemic uncertainty over directed acyclic graphs (DAGs) through posterior inference. However, its behaviour under latent confounding remains poorly understood, as existing work typically notes that confounding breaks identifiability without characterising how the posterior distribution over DAGs responds. In this work, we analyse posterior behaviour under latent confounding in linear Gaussian causal models, focusing on additive latent confounding between exactly two observed variables. We derive a critical correlation threshold above which the score function favours graphs with a spurious edge between the confounded variables, and show that this threshold decreases with sample size -- more data lowers the correlation required for the spurious edge to be favoured. Beyond this threshold, we characterize two distinct posterior failure regimes determined by the local structure around the confounded variables. Our findings are supported by exact posterior computations on multiple graph structures, demonstrating both the predicted failure regimes.

---

## 论文详细总结（自动生成）

# 详细中文总结：贝叶斯因果发现如何失败？表征潜变量混淆下线性高斯网络的结构性后果

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：贝叶斯因果发现通过后验分布 \(p(G \mid D)\) 量化有向无环图（DAG）的认知不确定性，广泛应用于基因调控网络、临床流行病学等领域。但该方法假设因果充分性（无潜变量混淆），而实际数据中混淆普遍存在。现有工作多指出混淆破坏可识别性，却未系统刻画后验分布在混淆下的具体行为变化。
- **核心问题**：当贝叶斯因果发现（基于DAG的评分）应用于包含潜混淆的数据时，后验概率是如实反映不确定性，还是系统地集中于错误结构？这种失败是否有结构性规律？
- **整体含义**：揭示“更多数据反而可能使因果发现更易受混淆欺骗”的反直觉现象，为实践者提供后验诊断依据，推动混淆感知的不确定性度量。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：聚焦线性高斯因果模型，考虑两个观测变量间的加性潜混淆。通过局部评分比较，推导伪边被偏好所需的临界偏相关阈值，并依据混淆区域（confounded region）的碰撞器（collider）结构，将后验失败分为“静默”与“噪声”两种模式。
- **关键技术细节**：
  - **模型设定**：线性高斯SCM，潜变量 \(H \sim N(0, \sigma_H^2)\) 加性影响 \(X_j, X_k\)；观测变量间无真实边。
  - **评分函数**：BGe（Bayesian Gaussian equivalent）评分，具有可分解性和分数等价性。
  - **临界阈值推导（Proposition 1）**：比较仅差一条边 \(X_k \to X_j\) 的两个DAG的评分差：
    \[
    s(G') - s(G) = \frac{N}{2} \log\left(\frac{1}{1 - \hat{\rho}_{jk|P}^2}\right) - \frac{1}{2}\log N + O_p(1)
    \]
    当 \(N \log(1/(1-\rho_{jk|P}^2)) > \log N\) 时，伪边被偏好，临界偏相关 \(\rho_c^2 = 1 - N^{-1/N}\)，大样本近似 \(\rho_c \approx \sqrt{(\log N)/N}\)。
  - **方向不对称性（Section 3.2）**：由于被解释变量的父集不同，两个方向的偏相关数值不等，评分倾向指向有更多父变量的节点。
  - **失败模式刻画（Section 4.2）**：依据混淆区域是否含碰撞器及伪边是否新成/破坏碰撞器，分为：
    - **静默失败（Silent failure）**：伪边引入后碰撞器数量不变或增加，MEC小，后验高度自信（伪边和真边均高概率），但结构错误。
    - **噪声失败（Noisy failure）**：伪边引入后碰撞器减少或原本无碰撞器且不新增，MEC大，后验熵高，真边概率低，不确定性明显。

## 3. 实验设计：使用的数据集/场景、基准、对比方法

- **数据集/场景**：合成数据。生成20个不同（DAG, 混淆对）实例，分属4种结构情况（Case 1(a), 1(b), 2(a), 2(b)）。每个实例生成10个独立数据集（重采样边权重和噪声），共200个数据集/情况。样本量 \(N=5000\)，混淆强度 \(\alpha=0\)（无混淆）和 \(\alpha=0.8\)（超过阈值）。
- **基准**：无混淆（\(\alpha=0\)）作为对照基线。
- **对比方法**：未与外部方法对比，采用精确DAG枚举计算确切后验，避免近似误差。

## 4. 资源与算力

- **未明确说明**：论文未提及GPU型号、数量、训练时长等算力信息。由于实验采用5节点小图精确枚举，计算资源需求较低，可能仅需普通CPU。

## 5. 实验数量与充分性

- **实验数量**：4种结构情况，每种20个DAG×10数据集 = 200个数据集，共800个数据集。每个DAG测量后验熵、真实边边缘概率、伪边主导方向概率。
- **充分性评估**：
  - **优势**：覆盖了四种理论预测的结构情况，通过小图精确后验分离了混淆效应与推断误差，验证了阈值和失败模式。
  - **不足**：仅考虑5节点图，未在更大图上验证（文中指出未来工作需考虑近似推断）；仅一种混淆强度（\(\alpha=0.8\)），未系统扫描不同阈值附近的行为（尽管图3展示了单个实例的强度扫描）；未与真实数据集或现有因果发现方法（如FCI）对比。

## 6. 论文的主要结论与发现

1. **临界阈值与样本量关系**：BGe评分偏好伪边的偏相关阈值 \(\rho_c \approx \sqrt{(\log N)/N}\)，随样本量增大而减小。更多数据使得更弱的混淆相关性也能诱发伪边。
2. **对称方向选择**：评分倾向指向父集更大的节点（即更“复杂”的方向），导致伪边方向有偏差。
3. **两种后验失败模式**：
   - **静默失败**：后验熵低，真实边和伪边均高概率，结构错误但看似可靠（Cases 1(a), 2(a)）。
   - **噪声失败**：后验熵高，真实边概率显著下降，不确定性自我宣告（Cases 1(b), 2(b)）。
4. **局部性**：失败模式仅由混淆区域的碰撞器结构决定，可推广至更大图。

## 7. 优点

- **理论创新**：首次严格推导了贝叶斯结构学习在混淆下的临界阈值，并给出样本量依赖的显式公式，解释了“更多数据导致更差结果”的机制。
- **结构化刻画**：将后验行为归因于局部碰撞器结构，便于实践者根据领域知识预判风险。
- **实验设计严谨**：采用小图精确枚举，消除了近似推断的干扰，直接验证理论预测。
- **实践启示**：区分静默与噪声失败，提醒实践者“高置信度不一定可靠”，并意识到增加样本量反而可能隐藏问题。

## 8. 不足与局限

- **模型假设限制**：仅考虑线性高斯、加性单潜变量混淆两个观测变量。非高斯、离散、多混淆器、更大混淆区域等扩展未覆盖。
- **实验规模**：仅限于5节点图，虽结构足够但未验证在更大规模或真实数据集上的表现；未与现有处理混淆的方法（如FCI、ADMG学习）对比后验行为。
- **阈值验证**：主要展示\(\alpha=0.8\)（超过阈值）和\(\alpha=0\)，对阈值附近相变行为仅通过单个实例展示（图3），缺乏系统统计。
- **计算成本**：精确枚举在超过5节点后不可行，实用性受限于近似方法（论文未评估）。
- **未讨论先验影响**：假设均匀先验，实际中不同先验可能改变后验行为。

（完）
