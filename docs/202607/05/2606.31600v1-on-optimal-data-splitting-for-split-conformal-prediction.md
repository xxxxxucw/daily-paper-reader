---
title: On Optimal Data Splitting for Split Conformal Prediction
title_zh: 关于分割共形预测中数据最优分割的研究
authors: "Sayan Das, Bahram Yaghooti, Todd A. Kuffner, Soumendra N. Lahiri"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31600v1"
tags: ["query:cp"]
score: 10.0
evidence: 分割共形预测的最优数据划分
tldr: 针对分裂共形预测中训练-校准数据分割比例的选择问题，本文提出一个理论框架来刻画长度最优的分割比。在一般设定下推导了对称和非对称情形下的解析解，并具体化到线性回归、非参数回归和神经网络等场景。还给出了数据驱动的比例选择方法，实验验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31600v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1683, \"height\": 2095, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31600v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1684, \"height\": 690, \"label\": \"Figure\"}]"
motivation: 分裂共形预测中训练-校准样本的分割比例对预测区间长度有重要影响，但已有研究缺乏理论指导。
method: 通过理论分析建立最小化区间长度的最优分割比框架，给出解析解及数据驱动选择方法。
result: 在不同回归模型下获得最优分割比的显式表达式，并验证了方法在合成和真实数据上的适用性。
conclusion: 提供了基于理论的最优数据分割指导，可显著缩短预测区间长度，提升共形预测效率。
---

## 摘要
共形预测及其变体（包括分割共形预测）通过构建具有有限样本覆盖保证的预测区间或集合，提供了一种无分布的量化不确定性框架。这些区间的统计效率在很大程度上取决于数据如何分割为训练样本和校准样本。尽管具有实际重要性，但如何在保持覆盖的同时最小化预测区间长度的训练-校准分割原则性表征仍然在很大程度上未得到解决。在本文中，我们发展了一个用于分割共形预测中数据最优分割的理论框架。我们首先在一般设置下分析该问题，并在对称和非对称两种情形下推导出长度最优分割比的解析特征。然后，我们展示了这些一般结果如何具体适用于几种常用的回归设置，包括线性回归、非参数回归和神经网络，从而阐明了该框架的应用范围。我们还描述了一种基于数据的选择最优比例的方法。我们的分析阐明了模型相关特征如何控制训练和校准之间样本的最优分配，并为构建更短的预测区间提供了原则性指导。在合成和真实数据集上的实验证明了所提出方法在各种实际场景中的适用性。

## Abstract
Conformal prediction and its variants, including the split conformal prediction, provide a distribution-free framework for uncertainty quantification by constructing prediction intervals or sets with finite-sample coverage guarantees. The statistical efficiency of these intervals depends critically on how the data are split into training and calibration samples. Despite its practical importance, a principled characterization of the training-calibration split that minimizes prediction interval length while maintaining coverage has remained largely unresolved. In this paper, we develop a theoretical framework for optimal data splitting in split conformal prediction. We first analyze the problem in a general setting and derive analytical characterizations of the length-optimal split ratio under both symmetric and asymmetric regimes. We then show how the general results specialize to several commonly used regression settings, including linear regression, nonparametric regression, and neural networks, thereby demonstrating the scope of the framework. We also describe a data-based method for selecting the optimal proportion. Our analysis clarifies how model-related features govern the optimal allocation of samples between training and calibration and provides principled guidance for constructing shorter prediction intervals. Experiments on both synthetic and real-world datasets demonstrate the applicability of the proposed methodology across a variety of practical scenarios.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义
- **研究动机**：分割共形预测（Split Conformal Prediction）通过将数据划分为训练集和校准集来构建预测区间，在保持有限样本覆盖保证的同时，具有计算高效的优点。然而，训练-校准样本的分割比例对预测区间长度有显著影响：更大的训练集提高预测精度，更大的校准集增强不确定性量化的可靠性。现有实践通常使用固定比例（如1:1）或随机分割，缺乏理论指导，可能导致区间长度次优。
- **整体含义**：本文首次系统研究了分割共形预测中数据分割的最优比例问题，旨在最小化预测区间长度同时维持名义覆盖水平。通过理论分析，为线性回归、非参数回归、神经网络等常见回归模型推导了最优分割比的解析形式，并提供了数据驱动的选择算法。该工作填补了理论空白，具有重要的实践指导意义。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将预测区间长度（对称或非对称）的期望和方差展开为训练集大小 \(n_1\) 和校准集大小 \(n_2\) 的函数，通过平衡估计误差（随 \(n_1\) 减小）与校准误差（随 \(n_2\) 减小）来推导最优分割比。
- **关键技术细节**：
  - **对称共形预测**：区间长度为 \( \hat{L} = 2\hat{q} \)，其中 \(\hat{q}\) 是校准集绝对残差的顺序统计量。在对称误差分布下，期望区间长度展开为：  
    \[
    E[\hat{L}] = 2F^{-1}_{|\epsilon|}(\mu_\alpha) + 2G_\epsilon(F^{-1}_{|\epsilon|}(\mu_\alpha)) E[E_1(\hat{\gamma}_1^2(X))] + \sigma^2_\alpha F^{-1}_{|\epsilon|}(\mu_\alpha) + o(\cdots)
    \]  
    其中 \(E_1(\hat{\gamma}_1^2(X))\) 为训练集上的均方误差。
  - **非对称共形预测**：区间长度为 \(\tilde{L} = \tilde{q}_u - \tilde{q}_l\)，其期望展开依赖于 \(E[\text{Var}_1(\hat{\gamma}_1(X))]\)，可能比对称情形更小，且不要求误差对称。
  - **最优分割比条件**：对于线性回归（OLS），偏置最优要求 \(n_1 \asymp n_2\)；非参数回归（Nadaraya-Watson）要求 \(n_1 \asymp n_2^{1+d/4}\)（d为维度）；神经网络（ReLU）近似满足 \(n_1 \asymp n_2^{1+\bar{K}/(2\bar{p})}\)。
  - **数据驱动的选择算法**：基于子采样（subsampling），将数据分为K个子集，在每个子集上对候选分割比例网格评估区间长度，选取最小者，平均后通过方程(3.1)外推至全样本：  
    \[
    x = n^{b-1} \hat{c}_\ell / [\ell^{b-1} (1-\hat{c}_\ell)^b (1-x)^b]
    \]  
    其中b由模型类别决定（如前文θ关系）。

## 3. 实验设计
- **使用数据集/场景**：
  - **合成数据**：线性回归模型（\(y = x^\top \beta + \epsilon\)，\(x \in [0,1]^5\)）；非参数回归（两个函数 \(f_1, f_2\)，5维协变量）；神经网络回归（相同函数形式，单隐藏层或双隐藏层）。误差分布包括正态、学生t（5 df）、对数正态（标准化）。
  - **真实数据**：混凝土抗压强度数据集（Concrete Compressive Strength），824个训练样本，206个测试样本。
- **Benchmark与对比方法**：
  - 主要对比不同分割比例（\(n_1/n\) 从40%到100%变化）下的预测区间平均长度。
  - 没有直接对比其他优化方法（因现有工作多采用固定比例，如1:1）。作者在引言和实验中强调其方法与常见固定比例（如1:1）相比的优势。
- **基线方法**：文中提到“oracle prediction lengths”（真实预测区间长度）作为参考虚线。

## 4. 资源与算力
- 文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。神经网络训练仅提到“最多100个epoch”，但未提供具体硬件配置。因此无法评估计算资源消耗。

## 5. 实验数量与充分性
- **实验数量**：
  - 合成数据：对于每个模型（线性、非参数、神经网络）和每个误差分布，在样本量 {100, 200, 300, 500, 800, 1000, 2000} 等至少5种大小下进行实验，每种配置重复1000次。总计约几十组实验。
  - 真实数据：使用混凝土数据集，基于4个子集（各206样本）评估不同分割比例，再在完整训练集验证，结果如图2。
- **充分性**：
  - 覆盖了多种回归模型和误差分布，重复次数足够（1000次），统计可靠性高。
  - 但缺少与传统固定比例（如1:1）的正式显著性对比或覆盖率验证（仅隐含比较了区间长度曲线）。
  - 未包含更多真实数据集或高维数据。消融实验仅体现分割比例变化，未涉及其他超参数（如核函数带宽、网络结构）的联合优化。
  - 公平性方面：与理论预期一致，但未与现有其他区间长度优化方法（如[20]的minimax方法）直接对比。

## 6. 主要结论与发现
- 最优数据分割比例取决于回归模型的收敛速率：线性回归最优比约为1:1；非参数回归和神经网络需要更大的训练集比例（如非参数回归当n=2000时最优训练比例约91%）。
- 非对称共形预测在理论上有更小的近似误差（依赖条件方差而非二阶矩），可产生更短的区间。
- 提出的数据驱动子采样方法能够有效估计最优分割比，与理论解一致，无需在大数据集上多次训练。
- 实验验证了理论推导的正确性：在不同的模型和误差分布下，最优比例的位置与理论预测吻合。

## 7. 优点
- **理论创新**：首次严格推导了分割共形预测中最小化区间长度的最优数据分割比的解析形式，填补了理论空白。
- **模型无关性**：框架适用于多种回归模型（线性、非参数、神经网络），且可推广到其他学习方法。
- **数据驱动实用性**：提供的子采样算法避免在大规模数据上重复训练，计算高效，适合实际部署。
- **分析深入**：区分了偏置最优和MSE最优，清晰揭示了训练-校准权衡的数学本质。
- 实验设计系统，覆盖多种误差分布和样本量，证实了理论的有效性。

## 8. 不足与局限
- **覆盖范围局限**：实验仅包含一个真实数据集（混凝土强度），且均为回归任务，缺乏分类任务或其他复杂应用（如时间序列、图像）的验证。
- **未考虑计算成本**：未报告训练时间或资源消耗，对实际部署的算力建议不足。
- **假设限制**：理论依赖若干光滑性和尾部条件（如噪声密度有界、多项式尾），对于严重重尾或非光滑噪声可能失效。
- **缺乏对最优比例的灵敏度分析**：未讨论当分割比例偏离最优时区间长度的恶化程度，实际中轻微偏差可能影响不大。
- **未与现有区间长度优化方法（如conformalized quantile regression、minimax方法）进行直接对比**，仅与固定比例对照，说服力有限。
- **算法实现细节**：子采样算法中网格大小和K值的选择可能影响结果，文中未做敏感性分析。

（完）
