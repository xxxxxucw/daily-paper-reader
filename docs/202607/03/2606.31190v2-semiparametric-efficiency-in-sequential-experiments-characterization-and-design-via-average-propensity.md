---
title: "Semiparametric Efficiency in Sequential Experiments: Characterization and Design via Average Propensity"
title_zh: 序贯实验中的半参数效率：通过平均倾向得分的表征与设计
authors: "Jiachun Li, David Simchi-Levi"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31190v2"
tags: ["query:two-sided"]
score: 9.0
evidence: 针对平台干预的序贯实验进行因果目标估计
tldr: 现代序列实验常因自适应分配、协变量平衡和约束条件偏离独立同分布抽样，缺乏统一的效率基准。本文提出平均倾向得分，并证明半参数下界等价于该得分下的独立同分布效率基准，从而将序列设计转化为选择或学习高效分配规则。进而开发了两种批处理自适应设计：基于高效影响函数的回归调整和自适应协变量平衡，在标准条件下逼近基准，线性功能时达到二阶率，且仅需少量策略更新。该框架为表征和设计高效序列实验提供了统一途径，在AI医疗助手评估等应用中展现了实际效率提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31190v2/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 1536, \"height\": 1024}, {\"url\": \"assets/figures/arxiv/2606.31190v2/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 1701, \"height\": 925}, {\"url\": \"assets/figures/arxiv/2606.31190v2/fig-003.webp\", \"caption\": \"\", \"page\": 16, \"index\": 3, \"width\": 1717, \"height\": 652}, {\"url\": \"assets/figures/arxiv/2606.31190v2/fig-004.webp\", \"caption\": \"\", \"page\": 24, \"index\": 4, \"width\": 1649, \"height\": 681}]"
motivation: 序列实验因自适应分配和约束偏离i.i.d.，缺乏统一效率基准和通用设计方法，亟需理论指导。
method: 定义平均倾向得分并建立半参数下界，开发两种批处理自适应设计：高效影响函数回归调整和自适应协变量平衡。
result: 两种方法在标准条件下逼近i.i.d.效率基准，线性功能时达二阶率，仅需少量策略更新。
conclusion: 提供统一框架表征和设计高效序列实验，数值与实证验证了效率提升。
---

## 摘要
现代实验，包括对AI赋能服务及平台干预措施的评估，往往偏离独立同分布（i.i.d.）抽样，因为分配可能是自适应的、跨协变量平衡的，或受到曝光、公平性和预算限制等部署约束。本文研究了此类序贯实验中估计因果目标的效率基准。我们证明，每个非预见性设计都会诱导出一个平均倾向得分，并建立了一个半参数下界：对于正则局部无偏估计量，可达精度受限于在该诱导得分下评估的i.i.d.效率基准。因此，平均倾向得分作为一个通用基准和设计目标，使得序贯实验设计可以被视为选择或学习一个有效的分配规则，操作约束在存在时通过容许集进入。随后，我们开发了可实施的批处理自适应设计，通过两种互补机制接近该基准。第一种基于有效影响函数的回归调整；对于一般光滑估计量，它在标准 nuisance 率条件下达到基准，而对于结果均值的线性泛函，则实现了一个尖锐的二阶速率。第二种利用自适应协变量平衡，通过分配机制达到相同基准，从而能够进行简单的矩估计。两种途径都只需要少量策略更新，使其能够兼容延迟反馈，并且在操作部署中更易于监控。数值实验和AI医疗助手评估的实证研究展示了实际的效率提升，包括在多处理设置中。总体而言，本文为表征和设计高效序贯实验提供了一个统一框架。

## Abstract
Modern experiments, including evaluations of AI-enabled services and platform interventions, often depart from independent and identically distributed (i.i.d.) sampling because assignments may be adaptive, balanced across covariates, or subject to rollout constraints such as exposure, fairness, and budget limits. This paper studies the efficiency benchmark for estimating causal targets in such sequential experiments. We show that every non-anticipating design induces an average propensity score, and we establish a semiparametric lower bound: for regular locally unbiased estimators, attainable precision is bounded by the i.i.d. efficiency benchmark evaluated at this induced score. The average propensity score thereby serves as a common benchmark and design target, allowing sequential experimental design to be viewed as choosing or learning an efficient allocation rule, with operational constraints entering through the admissible set when present. We then develop implementable batched adaptive designs that approach this benchmark through two complementary mechanisms. The first uses regression adjustment based on efficient influence functions; for general smooth estimands it attains the benchmark under standard nuisance-rate conditions, while for linear functionals of outcome means it achieves a sharp second-order rate. The second uses adaptive covariate balancing to attain the same benchmark through the assignment mechanism, enabling simple moment-based estimation. Both routes require only a small number of policy updates, making them compatible with delayed feedback and easier to monitor in operational deployments. Numerical experiments and an empirical study of AI medical-assistant evaluation demonstrate the practical efficiency gains, including in multi-treatment settings. Overall, the paper provides a unified framework for characterizing and designing efficient sequential experiments.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现代实验（如AI助手评估、平台干预）常因自适应分配、协变量平衡、预算和公平性约束而偏离独立同分布（i.i.d.）抽样，但缺少统一的统计效率基准和通用的最优设计方法。现有理论分散在自适应实验、分层设计、两阶段设计等不同领域，彼此不兼容。
- **核心问题**：在序贯实验中，如何表征任意非预见性设计的因果估计效率界限？如何设计可实施的自适应流程达到该界限？
- **整体含义**：本文证明所有非预见性设计都由一个诱导“平均倾向得分”概括，其统计效率基准等价于该得分下的 i.i.d. 半参数效率界限。这为复杂序贯实验提供了统一的基准语言，并将设计问题转化为选择/学习高效平均倾向得分。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：平均倾向得分（Average Propensity Score）是“设计的充分统计量”：任何非预见性设计都会在每组协变量值上产生一个平均分配质量 $\bar{e}_a(x) = \frac{1}{n}\sum_{t=1}^n \mathbb{E}[e_t(a \mid x, \mathcal{F}_{t-1})]$ 。半参数下界表明，正则局部无偏估计量的渐近方差 ≥ $V(\bar{e})/n$，其中 $V$ 是 i.i.d. 基准方差。
- **关键技术细节**：
  - **回归调整路线（Algorithm 1）**：批处理交叉拟合设计，将样本分为对数级批次，每批内使用固定倾向，批末用累积数据估计第二矩函数，通过最小化设计目标（广义Neyman分配）更新下一批倾向。最终评估采用有效影响函数（EIF）交叉拟合估计。
  - **协变量平衡路线（Algorithm 2）**：两层批处理自适应设计。批内通过空间分箱和组排列实现局部平衡，使“平衡余项”范数以 $m^{-1/(d+4)}$ 速度趋零；批间利用数据估计第二矩，更新倾向（与回归调整相同）。最终使用简单矩估计器，无需在估计阶段进行非参数 nuisance 拟合。
  - **关键公式**：线性功能下的 EIF 形式（式13）；设计目标 $L(e) = \mathbb{E}\left[\sum_a m_a(X)/e_a(X)\right]$；Neyman 分配 $e_a^\star(x) \propto \sqrt{m_a(x)}$；基于辅助函数的平衡方向 $\omega_a(X)$。

## 3. 实验设计：数据集/场景、基准、对比方法

- **合成实验**：
  - 数据生成过程：连续高斯协变量 $X \sim N(0, I_d)$, $d \in \{2,3,8\}$, $K=4$ 臂，条件均值 $\mu_a = \alpha_a + \beta_a^\top X$，条件对数方差 $\log\sigma_a^2 = c_a + \gamma_a^\top X_{1:2}$，误差为高斯。通过截距 $c = (-2.0,-0.7,0.7,2.0)$ 引入跨臂异方差。
  - 评估三个目标：标量 ATE（臂2 vs 臂4）、边际中位数 QTE（$\tau=0.5$）、向量 ATE（臂1,2,4 vs 臂3，trace MSE）。
  - 基准：均匀 i.i.d. 分配（uniform）、oracle 固定 i.i.d. Neyman 设计（oracle iid）、oracle 平衡设计（oracle cb）。
  - 对比方法：可行自适应回归调整（alg1 ra）、可行自适应协变量平衡（alg2 cb）。所有方法使用 XGBoost 作为 nuisance 估计器。
- **真实应用（HELPMed）**：
  - 使用 Bean et al. (2026) 的 AI 医疗助手评估数据，包含 4 个 AI 助手（K=4）、1298 名参与者、2400 条交互。结果变量为二元（处置是否匹配黄金标准）。协变量为6维：临床场景、年龄带、教育水平、在线健康频率、LLM健康使用频率、性别。
  - 目标为向量 ATE（三个竞争助手 vs 基准助手），trace MSE。
  - 样本量 $n \in \{1200, 2400, 4800\}$，使用原始数据重抽样。对比 uniform、oracle iid、oracle cb、alg1 ra、alg2 cb。引入“top-2设计”变体：利用因果森林每批选择两个最重要协变量。
- **统计检验**：配对差异法，3000 或 5000 次重复，报告归一化 MSE 及 ±2 标准误差区间，按“BEATS/TIES/LOSES”标注。

## 4. 资源与算力

- 文中未明确说明使用的 GPU 型号、数量或训练时长。仅提到 nuisance 回归使用 XGBoost（50棵树，深度3），所有实验重复 3000–5000 次，但无具体硬件信息。

## 5. 实验数量与充分性

- **数量**：合成实验覆盖 9 个主细胞（3 个估计量 × 3 个维度），每细胞 3000 次重复；另含 d=8 时全特征 vs top-2比较（辅助分析）。HELPMed 实验 3 个样本量 × 2 种设计变体 × 5 种方法（部分），每细胞 5000 次重复。
- **充分性**：
  - 优点：使用公共随机种子进行配对比较，统计显著性检验合理；覆盖了标量和向量、线性与非线性目标；对比了远界和可行算法；补充了高维敏感性分析（额外 binning 方案）。
  - 不足：仅使用一种 DGP（连续高斯）和一种真实数据集；未测试极高维（如 d>50）或离散协变量场景；未考虑滞后效应或非平稳环境；基线方法仅包括均匀、oracle iid、oracle cb，缺少其他自适应方法（如 Thompson sampling）的对比。整体而言，实验设计规范但可扩展性有限。

## 6. 论文的主要结论与发现

- **理论贡献**：平均倾向得分是序贯实验效率的统一表征对象；半参数下界将复杂设计简化为 i.i.d. 基准在该得分下的评价。
- **构造方法**：批处理自适应回归调整对一般光滑目标在 nuisance 率优于 $n^{-1/4}$ 时达到渐近效率；对线性功能达到最优二阶率（$n^{-1-2\alpha} \vee n^{-1-2\beta}$），并提供匹配下界。
- **构造方法（平衡路线）**：通过协变量平衡在标准条件下达到相同基准，避免 estimator-side nuisance 瓶颈，但维度依赖 $m^{-1/(d+4)}$。
- **实证结论**：
  - 合成实验：oracle 设计提升显著，alg1 ra 接近 oracle iid（捕获大部分增益），alg2 cb 在低维时有效，高维时需降维 binning。
  - HELPMed 应用：oracle 设计可减少 ~5–13% trace MSE；可行算法实现 ~5–8% 统计显著降低；top-2 版本比全特征版本更稳健。
- **运营含义**：两种路线仅需对数级策略更新，适合延迟反馈和监控；效率提升等价于所需样本量同比例减少。

## 7. 优点

- **统一性**：为之前分散的文献（自适应、分层、局部随机化）提供了单一理论框架。
- **实用性**：开发了可直接实施的低更新频率算法（仅对数级批次更新），适合现实部署环境（延迟反馈、审计需求）。
- **理论深度**：证明了一般光滑目标和线性功能的不同收敛行为，并为线性功能给出了确切的二阶最优率及匹配下界。
- **实验严谨**：配对比较、多重重复、统计显著性检验、baseline 包括理论远界可向限。
- **方法论创新**：将实验设计问题转化为平均倾向得分优化，广义 Neyman 分配覆盖向量目标。

## 8. 不足与局限

- **理论假设较强**：需要严格正倾向、Lipschitz 平衡函数、紧致协变量空间。在非标准设置（如离散或超高维协变量）可能不适用。
- **协变量平衡几何依赖**：平衡余项速率 $m^{-1/(d+4)}$ 在高维时会变得很慢，需要低维有效信息筛选。
- **回归调整的 nuisance 瓶颈**：一般光滑目标要求 nuisance 率快于 $n^{-1/4}$，这在复杂实际任务中可能难以满足。
- **实验覆盖有限**：仅测试了一种连续 DGP 和一个真实医疗场景；未与其他在线自适应方法（如政策学习、汤普森采样）直接对比；未涉及许多实际挑战（如多个异质时间段、数据非平稳、多任务目标）。
- **应用限制**：HELPMed 数据以交互为单位，未处理参与者之间的独立性假设；结果外推受限。论文本身为方法论，未讨论实际部署中的可解释性或公平性风险。

（完）
