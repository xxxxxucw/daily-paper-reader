Title: Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift

URL Source: https://arxiv.org/pdf/2607.02173v1

Published Time: Fri, 03 Jul 2026 01:27:20 GMT

Number of Pages: 23

Markdown Content:
Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

# Conformal Bayes for Two-Sided Censored Gaussian Regression Under Label Shift 

## Seungjin Choi 

CROID Research and aSSIST University, Korea 

Abstract 

Prediction under label shift becomes nonstandard when responses are censored. In a two-sided censored Gaussian model, latent values below L and above U are recorded at the boundary values, so the observed predictive distribution is mixed, with atoms at L and U and a continuous density on ( L, U ). In this paper we develop conformal Bayes for this mixed-space setting by combining posterior predictive tilting with weighted conformal calibration. Under a two-sided Tobit Gaussian Bayesian prediction head with a Laplace posterior approximation, the tilted predictive distribution has left-atom, interior, and right-atom components, with a three-term closed-form normalizer. The resulting prediction set is a mixed highest density region that can combine boundary atoms with an interior interval and can reduce to atom-only sets under strong censoring. The main technical issue is that latent label shift does not directly give an ordinary density ratio on the observed censored scale. A latent exponential tilt induces tail-averaged atom weights at the censored boundaries, while the interior ratio remains density based. This yields a mixed observed-space calibration weight with two atom ratios and one interior density ratio. The weight corrects the calibration measure, while predictive tilting gives target-adapted mixed-HDR geometry. Synthetic experiments show that weighted tilted conformal Bayes restores marginal coverage with smaller sets than weighted source-score calibration, while revealing a trade-off between marginal coverage and component-wise behavior across atoms and interior observations. 

## 1 Introduction 

Many scientific measurements are reported only within a finite detection range (Helsel, 2011; Williams et al., 2020). A latent continuous response below a lower limit L is recorded as eY = L, a response above an upper limit U is recorded as eY = U , and only values in the interval ( L, U ) are observed exactly. This type of censoring appears in pharmacokinetic assays, binding measurements, solubility measurements near saturation, and toxicological endpoints (Keizer et al., 2015). It is often accompanied by label shift, where the marginal response distribution changes between training and deployment (Saerens et al., 2002; Lipton et al., 2018; Azizzadenesheli et al., 2019; Alexandari et al., 2020; Garg et al., 2020). A model may be trained on compounds with moderate activity, but later deployed on lead candidates enriched for high activity or on safety screens enriched for extreme outcomes. The observed distribution then changes through both the continuous part of the response and the proportions of left and right censored observations. Let Y ∗ denote the latent response and define 

eY = T (Y ∗) = 



L, Y ∗ ≤ L, Y ∗, L < Y ∗ < U, U, Y ∗ ≥ U, 

(1) where T : R → { L} ∪ (L, U ) ∪ { U } is the two-sided censoring map. Even when Y ∗ | X = x is Gaussian, the observed response eY | X = x is not Gaussian. It is a mixed distribution on 

{L} ∪ (L, U ) ∪ { U },

1

> arXiv:2607.02173v1 [stat.ME] 2 Jul 2026 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift

with two point masses at the detection limits and a continuous density on the interior. For a fixed input x,the observed conditional distribution is a mixed measure, 

dP eY |X=x(y) = πL(x)δL(dy ) + f (y | x)1{L < y < U } dy + πU (x)δU (dy ), (2) where 1{·} denotes the indicator function, δL and δU are point masses at the two censoring boundaries, 

πL(x) = P( eY = L | X = x), πU (x) = P( eY = U | X = x),

are the conditional left- and right-censoring probabilities, and f (y | x) is the conditional density of the exactly observed response on the interior interval ( L, U ). In the latent Gaussian model, these atom probabilities are induced by the tails of Y ∗ | X = x, namely 

πL(x) = P(Y ∗ ≤ L | X = x), πU (x) = P(Y ∗ ≥ U | X = x),

as shown in Fig. 1. 

L U

πL(x) δL πU (x) δU

> left censored tail mass
> right censored tail mass

Y ∗ | x ∼ N (μ(x), σ 2(x)) 

dP eY |X=x(y) = πL(x)δL(dy ) + f (y | x)1{L < y < U } dy + πU (x)δU (dy )response value density / probability mass Figure 1: Two-sided censored Gaussian observation model. A latent Gaussian response Y ∗ | x is censored at lower and upper detection limits L and U . The left and right latent tail probabilities collapse into two Dirac atoms πL(x)δL and πU (x)δU , while the interior part remains continuous with density f (y | x) on (L, U ). A classical way to model censored Gaussian responses is Tobit regression (Tobin, 1958; Amemiya, 1984). In its simplest form, Tobit regression assumes a latent Gaussian response whose value is observed exactly inside the measurement range and recorded at a boundary outside that range. The likelihood combines Gaussian density terms for uncensored observations with Gaussian tail probabilities for censored observations. We use a Bayesian prediction head based on two-sided Tobit regression as the predictive model, but the conformal calibration is carried out on the observed mixed space. A direct use of standard conformal prediction based on residual scores for eY does not exploit the mixed nature of the observed response. The two boundary values are treated as ordinary numerical responses, although each boundary value represents an entire latent tail. Weighted conformal prediction (Tibshirani et al., 2019) can correct for distribution shift once the density ratio on the observed space has been specified. Under censoring, this observed-space ratio has three components: a left atom ratio, an interior density ratio, and a right atom ratio. For the mixed-HDR score used below, prediction sets also change form. They are level sets of a mixed density and may contain one or both boundary atoms together with an interior interval. Conformal Bayes combines Bayesian predictive modeling with conformal calibration (Wasserman, 2011; Fong and Holmes, 2021). The Bayesian model provides a posterior predictive distribution and an adaptive 2Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

score, while the conformal step calibrates this score using held-out data to obtain finite-sample coverage. In the uncensored Gaussian setting, split conformal Bayes under label shift can be implemented by tilting the Bayesian predictive distribution and using weighted conformal calibration (Choi, 2026). That construction relies on an ordinary continuous response density. The censored case requires a new observed-space formulation because the predictive distribution contains both atoms and a continuous component. We develop conformal Bayes for two-sided censored Gaussian regression under label shift. The method combines posterior predictive tilting with weighted conformal calibration on the observed mixed space. The Bayesian predictive distribution yields a score that adapts to the target label distribution, while the conformal step restores finite-sample marginal coverage when the required observed-space importance identity is available. The resulting procedures are summarized by three score and weight pairs: unweighted tilted calibration (UT), weighted source calibration (WS), and weighted tilted calibration (WT), which are described in Section 4.1. The main contributions of this paper are summarized as follows. (i) We derive the source and tilted predictive distributions for a two-sided Tobit Gaussian Bayesian prediction head with a Laplace posterior approximation. The tilted normalizer decomposes into left-atom, interior, and right-atom terms, which yields closed-form expressions for the mixed predictive law. (ii) We identify the main censoring-specific issue in label shift. A latent label tilt does not induce an ordinary density ratio on the observed censored scale. Instead, the atom weights are tail-averaged ratios, while the interior weight remains density based. Marginal atom ratios are therefore approximations unless additional structure makes them exact. (iii) We characterize mixed highest density prediction sets on the observed censored space. Under the Gaussian prediction head, the set is the union of optional boundary atoms and a possibly empty interior interval. It may include L, U , both atoms, or reduce to atom-only sets under strong censoring. Disconnected interior geometry can arise only with a multimodal interior predictive density and therefore does not occur under the Gaussian prediction head. (iv) We compare unweighted tilted, weighted source, and weighted tilted calibration. The analysis separates the exact candidate-weighted construction from practical calibration-only plug-in rules based on estimated observed-scale importance weights. Synthetic experiments show that weighted tilted calibration recovers marginal coverage with smaller sets than weighted source calibration, while coverage across atoms and interior observations can remain uneven. 

## 2 Related Work 

Conformal prediction provides finite-sample marginal coverage under exchangeability and can be imple-mented through full or split calibration schemes (Vovk et al., 2005; Papadopoulos et al., 2002; Shafer and Vovk, 2008; Barber et al., 2023; Angelopoulos and Bates, 2023). A central question is how to retain coverage when calibration and test data are not identically distributed. Under covariate shift, weighted conformal prediction uses likelihood ratios in the conformal quantile (Tibshirani et al., 2019). More general nonexchangeable settings have also been studied through weighted and robust variants (Barber et al., 2023). Under label shift, weighted conformal prediction uses important weights in the form of marginal label density ratios (Lee et al., 2025). For continuous label shift, recent work develops weighted conformal regression and split conformal Bayes with predictive tilting (Choi, 2026). These methods assume that the response is observed on an ordinary continuous scale. The present paper asks what remains valid, and what must be changed, when the observed response has atoms at censoring limits. Bayesian conformal methods use Bayesian predictive distributions as conformal ingredients rather than replacing Bayesian modeling by a purely residual-based procedure. The broad motivation goes back to the frequentist calibration of Bayesian procedures (Wasserman, 2011). Conformal Bayesian computation 3Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

constructs calibrated Bayesian predictive sets by reweighting posterior samples through an add-one importance scheme (Fong and Holmes, 2021). Split conformal Bayes under label shift takes a different but complementary route: it tilts the posterior predictive distribution to align the score with a target label distribution and then uses weighted conformal calibration (Choi, 2026; Lee et al., 2026). Our contribution is not a new principle of conformal Bayes. It is the mixed-space extension needed when the Bayesian predictive distribution has both Dirac atoms and an interior density. Censored Gaussian regression has a long statistical history. The Tobit model was introduced for limited dependent variables (Tobin, 1958), and classical surveys describe a family of censored and truncated regression models (Amemiya, 1984). In a two-sided Tobit model (Rosett and Nelson, 1975), uncensored observations contribute Gaussian density terms, while left and right censored observations contribute Gaussian tail probabilities. Bayesian implementations can use analytic approximations, such as Laplace approximation (Tierney and Kadane, 1986), or latent variable augmentation schemes related to probit and censored normal models (Albert and Chib, 1993). We use a two-sided Tobit Bayesian prediction head with a Laplace approximation because it gives a tractable posterior predictive distribution with explicit atom probabilities and an interior Gaussian density. The conformal problem then begins after this predictive distribution has been formed. There is also related work on conformal prediction for censored outcomes, especially survival analysis. Conformalized survival analysis and its extensions use censoring weights, imputation, doubly robust correction, or sensitivity analysis to handle event times that may be unobserved because of right censoring or dependent censoring (Cand` es et al., 2023; Jin et al., 2023; Gui et al., 2024; Davidov et al., 2025; Sesia and Svetnik, 2025). Recent work by Davidov et al. (2025) treats general right censored data, while Sesia and Svetnik (2025) develops a doubly robust conformalized survival method for right censored data. This literature is concerned with missing or partially observed event times and the censoring mechanism that hides them. Our setting is different. The response is a measured quantity subject to lower and upper detection limits, so the observed outcome itself is a mixed object with two boundary atoms and one continuous interior component. The goal is not to infer an unobserved event time, but to construct predictive sets for the censored observed response under label shift. The closest comparison is with uncensored split conformal Bayes under continuous label shift (Choi, 2026). In that setting, Gaussian predictive tilting shifts the posterior predictive mean and the resulting highest density set is an interval. In the censored setting, tilting produces a three-part normalizer, the density ratio separates into atom ratios and an interior ratio, and the highest density region may include boundary atoms or reduce to a boundary atom alone. The distinction between latent label shift and observed mixed-space label shift is also new. A latent exponential tilt induces tail-averaged atom weights, which need not equal simple marginal atom ratios. This is the main technical reason why the censored extension is not just a notational variant of the uncensored Gaussian case. Table 7 in Section G summarizes these differences point by point. 

## 3 Model: Two-Sided Censored Gaussian with a Bayesian Prediction Head 

We now specify the censored Gaussian predictive model and the observed-scale density ratios used later for weighted conformal calibration. 

3.1 Latent Gaussian model and censoring 

Let Y ∗ be a latent continuous response and let eY be the censored observation 

eY = T (Y ∗) = 



L, Y ∗ ≤ L, Y ∗, L < Y ∗ < U, U, Y ∗ ≥ U. 

(3) 4Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

The Bayesian prediction head places a Gaussian model on top of a fixed representation φ : X → Rd:

Y ∗ 

> i

| xi, θ ∼ N  θ⊤φ(xi), σ 2

> ε

 , θ ∼ N (0 , τ 2Id). (4) Only the final linear head is inferred. The representation may come from a pretrained encoder or any fixed feature extractor. We write σ2 

> ε

for the latent Gaussian noise variance and bσ2 

> ε

for its fitted value. Because observations are censored at both L and U , the likelihood is a two-sided Tobit likelihood, also known as a two-limit Tobit likelihood (Tobin, 1958; Rosett and Nelson, 1975). Let Φ and ϕstd denote the cumulative distribution function (CDF) and density of the standard normal distribution, respectively. For a single observation, 

p(eyi | xi, θ ) = 



Φ

 L − θ⊤φ(xi)

σε



, eyi = L, 

1

σε

ϕstd 

 eyi − θ⊤φ(xi)

σε



, L < eyi < U, 

1 − Φ

 U − θ⊤φ(xi)

σε



, eyi = U. 

(5) An interior observation contributes a Gaussian density, while a censored observation contributes a Gaussian tail probability. Then we write ℓ(θ; σε) for the observed-data log-likelihood of the two-sided Tobit model, viewed as a function of θ conditional on the noise scale. 

ℓ(θ; σε) = X 

> i:eYi=L

log Φ 

 L − θ⊤φ(xi)

σε



+ X 

> i:L< eYi<U

"

− log σε + log ϕstd 

eYi − θ⊤φ(xi)

σε

!# 

+ X 

> i:eYi=U

log 



1 − Φ

 U − θ⊤φ(xi)

σε

 

. (6) With the Gaussian prior θ ∼ N (0 , τ 2Id), we approximate the posterior of θ by a Laplace approximation at the MAP, 

π(θ | D tr ) ≈ N (bθ, bΣθ ), (7) where 

bθ = arg max 

> θ



ℓ(θ; bσε) − 12τ 2 ∥θ∥2



, bΣθ =

 1

τ 2 Id − ∇ 2 

> θ

ℓ(bθ; bσε)

−1

. (8) Details on the gradient, Mills ratio derivative, and Hessian are given in Section A. 

3.2 Mixed predictive distribution 

The Laplace approximation gives a Gaussian predictive distribution for the latent response. For a new input x,

Y ∗ | x, Dtr ∼ N  bμ(x), bσ2(x) , bμ(x) = bθ⊤φ(x), bσ2(x) = bσ2 

> ε

+ φ(x)⊤ bΣθ φ(x). (9) After censoring, the predictive distribution of eY given X = x is mixed: 

dP s(ey | x, Dtr ) = πL(x)δL(dey) + fs(ey | x)1{L < ey < U } dey + πU (x)δU (dey). (10) The conditional atom masses are 

πL(x) = Φ 

 L − bμ(x)

bσ(x)



, πU (x) = 1 − Φ

 U − bμ(x)

bσ(x)



, (11) and the interior density is 

fs(ey | x) = 1

bσ(x) ϕstd 

 ey − bμ(x)

bσ(x)



, L < ey < U. (12) 5Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Here fs is the ordinary Gaussian density evaluated on the interior interval. It is not renormalized over (L, U ), since 

πL(x) + 

Z UL

fs(y | x) dy + πU (x) = 1 .

It is convenient to view (10) as a density with respect to a single mixed dominating measure 

μ = δL + Leb (L,U ) + δU , (13) where δL, δ U are unit point masses at the censoring limits and Leb (L,U ) is Lebesgue measure on the open interior. With respect to μ, the conditional mixed predictive density is 

ps(ey | x, Dtr ) = πL(x) 1{ey = L} + fs(ey | x) 1{L < ey < U } + πU (x) 1{ey = U }, (14) so that at the atoms the density value equals the atom mass and on the interior it equals the Gaussian density. The tilted predictive distribution introduced below is likewise a density with respect to the same 

μ. These mixed densities are exactly what the negative-log-density scores in Section 4 evaluate, and the prediction-set level sets in Section 4.2 are taken with respect to μ.

3.3 Marginal observed label ratio 

The marginal observed label ratio is given by 

w(ey) = dP t

> eY

dP s

> eY

(ey). (15) The marginal observed label distribution has the same three component structure as the predictive distribution because censoring forces eY to live on {L} ∪ (L, U ) ∪ { U }. The components are different, however, because they are averaged over the input distribution. For each domain j ∈ { s, t }, we write 

dP j 

> eY

(ey) = ρj,L δL(dey) + gj (ey)1{L < ey < U } dey + ρj,U δU (dey). (16) For example, under the source distribution, 

ρs,L = Ps( eY = L), ρs,U = Ps( eY = U ), gs(y) = 

Z

fs(y | x) dP Xs (x), L < y < U. 

Thus ρs,L is not the same object as πL(x). The former is a marginal censoring probability, while the latter is a predictive atom mass at a particular input x.Taking the Radon Nikodym derivative of (16) gives the three component importance weight 

w(ey) = 



ρt,L 

ρs,L 

, ey = L, gt(ey)

gs(ey) , L < ey < U, ρt,U 

ρs,U 

, ey = U. 

(17) The weight w(ey) is not a single ordinary density ratio on R. It is a mixed-space ratio. At the left boundary it is a ratio of censoring probabilities, on the interior it is an ordinary density ratio, and at the right boundary it is again a ratio of censoring probabilities. This is the central difference from the uncensored Gaussian setting. 6Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

3.4 Predictive tilting by the observed label ratio 

The same marginal weight w(ey) plays two distinct roles. First, it weights calibration examples in weighted conformal calibration. This use is justified by the observed-space importance identity 

Es{w( eY )h(X, eY )} = Et{h(X, eY )} for all measurable h. (18) Second, under the predictive tilting approximation used in split conformal Bayes (Choi, 2026), the same observed label ratio is used to modify the source predictive distribution and form a target-aligned score. This second use is stronger than calibration weighting alone. It assumes that the fitted source predictive distribution can be transported to the target environment by tilting its observed label coordinate while keeping the input x fixed. Under this approximation, 

pt(ey | x, Dtr ) = ps(ey | x, Dtr )w(ey)

Z(x) , (19) where the normalizer is the mixed integral 

Z(x) = πL(x)w(L) + 

Z UL

fs(y | x)w(y) dy + πU (x)w(U ). (20) Consequently, 

pt(ey | x, Dtr )

ps(ey | x, Dtr ) = w(ey)

Z(x) . (21) The ratio in (21) is a predictive ratio at a fixed input x. It is not the calibration weight. The calibration weight remains the marginal observed label ratio w(ey) in (17). In practice we use the parametric exponential family inherited from Gaussian split conformal Bayes to model the interior ratio. For L < ey < U ,

w(ey) = exp( βey)

Zw

. (22) The two atom weights are scalar marginal ratios, 

w(L) = ρt,L 

ρs,L 

, w(U ) = ρt,U 

ρs,U 

. (23) This gives the practical tilted predictive distribution 

dP t(ey | x, Dtr ) = πtL(x)δL(dey) + ft(ey | x)1{L < ey < U } dey + πtU (x)δU (dey), (24) with 

πtL(x) = πL(x)w(L)

Z(x) , πtU (x) = πU (x)w(U )

Z(x) , (25) and 

ft(ey | x) = fs(ey | x) exp( βey)/Z w

Z(x) , L < ey < U. (26) The interior integral in (20) is available in closed form: 

Z UL

fs(y | x) exp( βy )

Zw

dy = exp  β bμ(x) + 12 β2 bσ2(x)

Zw

{Φ( b(x)) − Φ( a(x)) }, (27) where 

a(x) = L − bμ(x) − βbσ2(x)

bσ(x) , b(x) = U − bμ(x) − βbσ2(x)

bσ(x) . (28) 7Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

On the interior, the tilted density is proportional to a Gaussian density with shifted mean 

μt(x) = bμ(x) + βbσ2(x). (29) The atom masses are adjusted separately through w(L) and w(U ). Combining (27) with the two atom terms in (20) gives the practical closed-form normalizer 

Z(x) = πL(x)w(L) + exp  β bμ(x) + 12 β2 bσ2(x)

Zw

{Φ( b(x)) − Φ( a(x)) } + πU (x)w(U ). (30) The tilted atom masses and interior density are given by (25) and (26). 

3.5 From latent tilt to observed atom weights 

The previous subsection defines the method in observed space. The importance weight is the marginal observed label ratio w(ey) = dP t 

> eY
> dP s
> eY

(ey), and the tilted predictive distribution is obtained by multiplying the source predictive distribution by this observed ratio and normalizing. This subsection explains how such a mixed observed weight arises when the shift is modeled first on the latent scale. Suppose that the latent response satisfies a label shift model, 

Ps(X | Y ∗) = Pt(X | Y ∗), Ps(Y ∗)̸ = Pt(Y ∗), (31) with an exponential latent ratio 

w∗(y∗) = dP ∗

> t

dP ∗

> s

(y∗) = exp( βy ∗)

Zw

. (32) On the interior, censoring is invertible, so the latent ratio pushes forward to the pointwise interior ratio in (22) . At the atoms, however, censoring collapses an entire latent tail. The observed atom ratios are therefore tail averages: 

w(L) = 

R L 

> −∞

exp( βy ∗) dP ∗ 

> s

(y∗)

ZwPs(Y ∗ ≤ L) , w(U ) = 

R ∞ 

> U

exp( βy ∗) dP ∗ 

> s

(y∗)

ZwPs(Y ∗ ≥ U ) . (33) For a Gaussian source marginal Y ∗ ∼ N (μp, σ 2 

> p

), this becomes 

w(L) = Φ (L − μp − βσ 2 

> p

)/σ p



Φ(( L − μp)/σ p) , w(U ) = 1 − Φ (U − μp − βσ 2 

> p

)/σ p



1 − Φ(( U − μp)/σ p) . (34) A negative tilt increases the left atom ratio, while a positive tilt increases the right atom ratio. There is one additional distinction between the exact latent pushforward and the practical observed-space implementation. If the latent tilt is applied to the predictive distribution at a fixed input x, the exact atom weights are generally input-dependent: 

wx(L) = 

R L 

> −∞

ps(y∗ | x, Dtr )w∗(y∗) dy ∗

πL(x) , wx(U ) = 

R ∞ 

> U

ps(y∗ | x, Dtr )w∗(y∗) dy ∗

πU (x) . (35) The practical method instead uses the scalar marginal atom ratios in (23) . Thus the latent model is useful as a source of the observed mixed-space weight, but the algorithm itself is defined by the observed marginal ratio in (17) and the tilted predictive distribution in (19) . The full conditional pushforward calculation is shown in Section B. 8Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Censoring          

> LU
> Y∗ |x
> πL(x)πU(x)
> eY=T(Y∗)

Mixed label ratio    

> source
> target
> w(ey) = dP t
> eY
> dP s
> eY
> (ey)two atom ratios + one density ratio

Predictive tilting              

> ps (ey|x)
> pβt(ey|x)
> pβt(ey|x) = ps(ey|x)w(ey)
> Z(x)

Mixed HDR set              

> e−bq
> [a, b ]{U}
> bC(x) = {ey:pβt(ey|x)≥e−bq}

Figure 2: Schematic of split conformal Bayes on the censored mixed space. Censoring maps a latent Gaussian response to an observed response with two boundary atoms and one continuous interior density. The observed label ratio w(ey) = dP t 

> eY

/dP s 

> eY

(ey) is therefore a mixed-space ratio, consisting of two atom-mass ratios and one interior density ratio. This ratio tilts the source predictive distribution, and weighted conformal calibration selects a density threshold. The resulting prediction set is a mixed highest-density region that may contain boundary atoms as well as an interior interval. All predictive densities ps, p βt are conditioned on the input x and the training data Dtr ; this conditioning is suppressed in the panels for readability. 

Remark 1 (Latent shift becomes a mixed observed-space weight) . A latent exponential tilt does not push forward to a single ordinary density ratio on the observed censored scale. On the interior, the censoring map is invertible and the observed weight is the pointwise density ratio in (22) . At each boundary, censoring collapses an entire latent tail, so the latent tilt induces tail-averaged atom weights. These conditional tail averages are generally input-dependent, as in (35) . Weighted conformal calibration, however, uses the marginal observed-space ratio in (17) . Thus the conformal weight is the mixed object with a left atom ratio, an interior density ratio, and a right atom ratio, rather than the latent ratio itself. 

Remark 2 (Marginal atom-weight approximation) . The practical tilted predictive distribution replaces the input-dependent atom weights wx(L), w x(U ) in (35) by scalar marginal atom ratios in (23) . These scalar ratios are the correct atom components of the marginal observed-space importance weight. As substitutes inside the predictive tilt at a fixed input x, they are exact only when the tail-averaged atom weights do not depend on x, or when additional model structure makes the marginal and conditional atom corrections agree. Otherwise this replacement is an approximation, and it is the main censoring-specific distinction between the exact latent-pushforward construction and the practical observed-space implementation. 

The tilted predictive distribution in (24) defines the conformal score, while the marginal observed-label ratio in (17) defines the calibration weight. The next section combines these two ingredients to define the UT, WS, and WT calibration configurations. 

## 4 Calibration and Prediction Sets on the Mixed Space 

This section turns the mixed-space ratio from Section 3 into a practical split conformal Bayes procedure. The method has four main ingredients: choose a score–weight pair, compute a weighted conformal threshold, convert the WT threshold into a mixed-space HDR set, and estimate the mixed density ratio used by the practical algorithm. The recommended configuration is WT, which uses the observed label ratio for calibration and the tilted mixed predictive distribution for set geometry. Figure 2 gives the overall pipeline, and Figure 3 summarizes the three calibration configurations. 

4.1 Calibration configurations: UT, WS, and WT 

The calibration set is drawn from ps, the test point from pt. Label shift requires two distinct corrections: the importance weight w(ey) corrects the calibration measure , while the tilted Bayesian score corrects the geometry of the candidate set. Censoring does not alter this logic, but it does make the score a 9Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Table 1: Calibration configurations in the two-sided censored setting. “Oracle valid” refers to the candidate-weighted rule (38), not the calibration-only approximation. Method Score Calibration weight Oracle valid Geometry UT sUT = − log pt 1 No target HDR, miscalibrated WS sWS = − log ps w(ey) Yes source HDR WT sWT = − log pt w(ey) Yes target HDR mixed-space object. We use the method labels themselves as superscripts and define each configuration by a score–weight pair ( sr , w r ), r ∈ { UT , WS , WT }:

sUT  

> i

= − log pt(eyi | xi, Dtr ), wUT  

> i

= 1 ,sWS  

> i

= − log ps(eyi | xi, Dtr ), wWS  

> i

= w(eyi),sWT  

> i

= − log pt(eyi | xi, Dtr ) = sWS  

> i

− log w(eyi) + log Z(xi), wWT  

> i

= w(eyi).

(36) Each quantity is evaluated on the appropriate mixed component: w(L), w (U ) at atoms, eβey /Z w on the interior, and closed-form Z(xi) from (30) . Because sWT is a negative log-density under the target mixed predictive, its fixed-threshold level sets are target mixed-space HDRs. The practical calibration-only thresholds are 

bqUT = Quantile 1−α

 {sUT  

> i

}, bqr = inf 

n

q :    

> P
> iwri1{sri≤q}
> P
> jwrj

≥ 1 − α

o

, r ∈ { WS , WT }. (37) UT uses tilted geometry but no measure correction; WS uses measure correction but source geometry; WT uses both and is the recommended procedure. This notation avoids the artificial mapping from earlier route labels to the three method names. 

Oracle exact rule. The exact finite-sample construction adds the candidate’s own test weight. For 

r ∈ { WS , WT } and candidate ey, let wr (ey) = w(ey) and evaluate the candidate score sr (ey, x test ) with the corresponding source or tilted predictive. Then 

bqr (ey) = inf 

n

q :        

> P
> iwri1{sri≤q}+wr(ey)
> P
> iwri+wr(ey)

≥ 1 − α

o

, ey ∈ C r 

> exact

⇐⇒ sr (ey, x test ) ≤ bqr (ey). (38) This candidate-dependent rule has exact finite-sample validity under known weights. The fixed-threshold rule (37) approximates it up to sup ey w(ey)/ P 

> i

w(eyi) = O(1 /n ) under bounded weights. Conceptually: the exact WT set is not literally a fixed-level HDR at finite n, whereas the practical and population WT rules are. 

4.2 Mixed-space HDR prediction set 

The WT prediction set is a density level set of the tilted mixed predictive distribution. Let 

λ∗ = e−bqWT .

Then 

CWT (x) = {ey : pt(ey | x, Dtr ) ≥ λ∗}. (39) Because pt has atoms at L, U and a Lebesgue density on ( L, U ), this level set is taken with respect to the mixed measure μ of (13); equivalently, for a set A,

μ(A) = 1[L ∈ A] + Leb( A ∩ (L, U )) + 1[U ∈ A]. (40) With respect to μ, the atom masses and the interior density are all compared to the same threshold. Thus the boundary decisions are simply 

L ∈ C WT (x) ⇐⇒ πt

> L

(x) ≥ λ∗, U ∈ C WT (x) ⇐⇒ πt 

> U

(x) ≥ λ∗. (41) 10 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Source predictive 

ps(ey | x)

Tilted predictive 

pβt (ey | x)

Importance weight 

w(ey) = dP t

> eY

dP s

> eY

(ey)

UT: unweighted tilted Score 

sUT (x, ey) = − log pβt (ey | x)

Calibration weight 

wUT (ey) = 1 

Threshold 

bqUT = Q1−α

> unweighted conformal quantile tilted score + un-weighted calibration

WS: weighted source Score 

sWS (x, ey) = − log ps(ey | x)

Calibration weight 

wWS (ey) = w(ey)

Threshold 

bqWS = Qw

> 1−α
> weighted conformal quantile source score + im-portance weighting

WT: weighted tilted Score 

sWT (x, ey) = − log pβt (ey | x)

Calibration weight 

wWT (ey) = w(ey)

Threshold 

bqWT = Qw                          

> 1−α
> weighted conformal quantile tilted score + im-portance weighting
> Conformal prediction set
> bCUT (x)=ney:pβt(ey|x)≥e−bqUT
> o
> tilted mixed-HDR geometry
> Conformal prediction set
> bCWS (x)=ney:ps(ey|x)≥e−bqWS
> o
> source mixed-HDR geometry
> Conformal prediction set
> bCWT (x)=ney:pβt(ey|x)≥e−bqWT
> o
> tilted mixed-HDR geometry

Figure 3: Visual summary of the three calibration configurations in split conformal Bayes. UT uses the tilted predictive score without importance weighting. WS uses the source predictive score with the mixed-space importance weight w(ey) = dP t 

> eY

/dP s 

> eY

(ey). WT uses both the tilted predictive score and the importance weight. All three outputs are mixed-HDR-type prediction sets for their corresponding predictive densities: UT and WT use tilted geometry, while WS uses source geometry. Among the three, WT combines target-adapted geometry with weighted calibration. All predictive densities are conditioned on the input x and the training data Dtr ; this conditioning is suppressed in the panels for readability. It remains to describe the interior part. Define 

I(x) = {ey ∈ (L, U ) : ft(ey | x) ≥ λ∗}.

The tilted interior density is Gaussian-shaped and unimodal on ( L, U ), with shifted center μt(x) = 

bμ(x) + βbσ2(x). Therefore I(x) is either empty or a single interval. Solving the Gaussian density inequality gives the half-width and clipped endpoints 

r(x) = bσ(x)

r

2

h

β bμ(x) + 12 β2 bσ2(x) − log  Z(x)Zw bσ(x)√2π − log λ∗

i

,yℓ(x) = max {L, μ t(x) − r(x)}, yu(x) = min {U, μ t(x) + r(x)}.

(42) The interior set is empty when the radicand in (42) is negative. Equivalently, λ∗ exceeds the maximum interior density, which is ft(μt(x) | x) if μt(x) ∈ (L, U ) and max {ft(L+ | x), f t(U − | x)} otherwise. 

Proposition 1 (Structure of the mixed HDR set) . The WT set decomposes as 

CWT (x) = AL(x) ∪ I(x) ∪ AU (x),

11 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

where 

AL(x) = 

(

{L}, πt

> L

(x) ≥ λ∗,

∅, πt

> L

(x) < λ ∗, AU (x) = 

(

{U }, πt 

> U

(x) ≥ λ∗,

∅, πt 

> U

(x) < λ ∗,

and I(x) is either empty or the interval [yℓ(x), y u(x)] in (42) . Consequently: 

(a) if I(x)̸ = ∅, the set is an interior interval possibly unioned with one or both boundary atoms; 

(b) if I(x) = ∅, the set is atom-only, two-atom-only, or empty. 

Proof in Section C. All components are closed form once bθ, bΣθ , bβ, bqWT are fixed: the interior endpoints come from (42) , and the two atom decisions come from (41) . Under increasing censoring the dominant forms are the boundary-extended interval and, eventually, the atom-only set. Disconnected forms are possible in the abstract mixed-space representation but are non-generic for the tilted Gaussian prediction head used here (Remark 3). 

Remark 3 (Atom-only and disconnected sets) . Two non-interval outputs deserve comment. First, under strong censoring the interior super-level set can be empty and CWT can collapse to an atom-only set, such as {U } under positive shift or {L} under negative shift. This reports that the response is plausibly beyond a detection limit, and it is the dominant high-censoring regime in Section 6.3. Second, a disconnected set such as {L} ∪ [yℓ, y u] ∪ { U } would require both atom masses to exceed λ∗ while the adjacent interior densities fall below it. For a single tilted Gaussian this configuration is non-generic: the interior is unimodal and the monotone tilt eβey raises one atom while suppressing the other. The same mechanism makes the two-atom-only form {L} ∪ { U } (empty interior, both atoms retained) non-generic, which is why neither form appears in Table 5. A genuinely disconnected mixed set would therefore be more naturally associated with a multimodal interior predictive, for example a non-Gaussian or mixture head. 

4.3 Density-ratio estimation and pseudo-label bias 

The practical method requires estimates of the three components of w(ey). Censoring makes these compo-nents different in difficulty. The two atom weights are probabilities of observable events, while the interior density ratio requires modeling how the target labels would be distributed inside ( L, U ). 

Atom weights. The boundary weights are 

w(L) = Pt( eY = L)

Ps( eY = L) , w(U ) = Pt( eY = U )

Ps( eY = U ) .

When target censoring status or aggregate target assay summaries are available, these weights can be estimated by censoring-fraction ratios. If only target inputs are available, the atom weights must be supplied by a predictive model, treated as sensitivity parameters, or evaluated in an oracle experiment. Rare atoms should be smoothed or clipped because fraction ratios can have high variance. 

Interior ratio. On the interior, the log ratio is modeled as log w(ey) = βey − log Zw, L < ey < U. 

Estimating β from target inputs requires pseudo-labels. A point pseudo-label bμ(X) is attractive but discards the within-input predictive variance. This is exactly the uncertainty that the conformal Bayes score is meant to use. To see the bias, write the latent response as Y = M + ε, with Var s(M ) = a and Var s(ε) = b. An exponential tilt shifts the target mean by β(a + b), with predictable part βa and residual part βb .

Lemma 1 (Variance compression) . With v2 

> s

= Var s(Y ∗) and ¯σ2 

> s

= Eps(x)[bσ2(x)] , point pseudo-labels 

eY = bμ(X) satisfy 

Var ps(x)( eY ) = Var ps(x)[bμ(X)] ≈ v2 

> s

− ¯σ2 

> s

,

removing the within-input variance. 

12 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Table 2: Bias structure and recommended estimators for the mixed censored ratio. Component Estimator Main issue Recommendation 

w(L), w (U ) censoring-fraction ratio rare-atom noise smooth or clip if rare 

β LR-point variance compression and missing shift diagnostic only 

β source-predictive sampling restores variance, attenuates tilt ablation 

β tilted predictive sampling depends on initializer principled default 

β moment matching predictable shift only initializer or strong baseline The approximation uses the posterior predictive variance bσ2(x) = bσ2 

> ε

+ φ(x)⊤ bΣθ φ(x) in place of the latent conditional variance σ2 

> ε

. These coincide only when posterior parameter uncertainty is negligible, as in the oracle regime used in Section 6. 

Proposition 2 (Point and source-predictive pseudo-label bias) . In the ideal decomposition, tilting gives 

Yt ∼ N (β(a + b), a + b) and Mt ∼ N (βa, a ). Point pseudo-labels eYpoint = Mt compress variance from a + b

to a and miss the residual shift βb . Source-predictive sampling eYsrcPS = Mt + ε′, ε′ ∼ N (0 , b ), restores variance but still misses βb . A log-linear slope fit to such samples is therefore attenuated to 

bβsrcPS ≈ β aa + b = βR 2BRR .

Proofs of Lemma 1 and Proposition 2 are given in Section D. 

Tilted predictive sampling. A direct way to restore the missing residual shift is to sample from the tilted predictive distribution. Given an initial value bβ(0) , draw 

ey(s) 

> j

= bμ(xj ) + bβ(0) bσ2(xj ) + bσ(xj )ϵ(s) 

> j

, ϵ(s) 

> j

∼ N (0 , 1) , s = 1 , . . . , S. (43) Draws outside ( L, U ) are assigned to the corresponding atom. Moment matching, 

bβ(0) MM = ¯μt − ¯μs

bv2 

> μ

+ λ ,

with a small ridge λ ≥ 0, is a stable initializer and a strong baseline in the oracle linear-Gaussian setting. If bβ(0) = β, the samples in (43) have conditional mean bμ(x) + βbσ2(x) and variance bσ2(x), matching the tilted predictive distribution before censoring. Thus tilted predictive sampling is the principled default when the predictive variance is trusted, while moment matching is useful as an initializer and benchmark. 

4.4 Practical algorithm 

The practical WT implementation follows the construction above. It first fits the censored Bayesian prediction head, then estimates the mixed ratio, calibrates the WT score with the estimated weights, and finally returns the closed-form mixed HDR set. After the Bayesian prediction head and ratio estimates are fixed, prediction for a new input requires only closed-form evaluations of bZ(x), the two atom masses, and the interval endpoints. The main computational cost is the one-time Laplace Hessian for the Bayesian prediction head. 

## 5 Validity and Efficiency of Mixed-Space Calibration 

This section separates the two main theoretical claims. Weighted conformal validity is an oracle statement about the observed mixed-space importance weight. The WT score is an efficiency device: it changes the geometry of the prediction set, but is not required for validity once the weights are correct. 13 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Algorithm 1 Practical WT SCB-C 

Require: Training data Dtr , calibration data Dcal , target covariates Dtg = {xj }, test input xtest , bounds 

L < U , backbone φ, level 1 − α

Ensure: Prediction set CWT (xtest ) 

> 1:

Fit censored Bayesian prediction head. Obtain the MAP mean parameter bθ and fitted noise variance bσ2 

> ε

from the two-sided Tobit likelihood (5) ; compute the Laplace covariance bΣθ from (8) ;evaluate bμ(x) and the predictive variance bσ2(x) using (9).  

> 2:

Estimate mixed ratio. Using target covariates but not target responses, estimate the interior tilt bβ

by tilted predictive sampling as in Section 4.3. Form the interior ratio bw(ey) ∝ exp ( bβey) on ( L, U ), and compute bw(L) and bw(U ) from the induced tilted marginal masses on the observed censored scale.  

> 3:

Compute WT calibration scores. For each ( xi, eyi) ∈ D cal , compute log bZ(xi) from (30) and 

sWT  

> i

= − log ps(eyi | xi) − log bw(eyi) + log bZ(xi). 

> 4:

Calibrate threshold. Compute bqWT by the weighted quantile rule (37) using scores {sWT  

> i

} and weights { bw(eyi)}. 

> 5:

Return mixed HDR set. Set λ∗ = e−bqWT ; include atoms using (41) ; compute the interior interval using (42); return CWT (xtest ) = AL ∪ I ∪ AU .

5.1 Validity hierarchy: UT, WS, and WT Proposition 3 (Correction hierarchy) . Let (xi, eyi)ni=1 ∼ ps i.i.d., (xtest , eYtest ) ∼ pt, and assume a known mixed-space importance weight w(ey) satisfying (18) .

(i) UT is generally miscalibrated: 

Ppt { eYtest ∈ C UT } − (1 − α) ≤ dTV {L ps (sWT ), Lpt (sWT )} + O(n−1/2),

and it may over- or under-cover. 

(ii) WS and WT are oracle valid: the candidate-weighted sets (38) satisfy 

Ppt { eYtest ∈ C r

> exact

} ≥ 1 − α, r ∈ { WS , WT }.

This statement does not require the two-sided Tobit likelihood, Laplace approximation, or tilted score to be correct. 

(iii) Calibration-only is practical, not exact: if 0 < w ≤ Wmax and n−1 P 

> i

w(eyi) is bounded away from zero, the fixed-threshold rule differs from (38) by OP(1 /n ) uniformly over bounded-weight candidates. 

Proof in Section E. The single ingredient behind the oracle statement is the mixed-space importance identity (18) . The Bayesian prediction head and the tilted score affect the usefulness and size of the set, not the oracle coverage proof for a fixed score. We therefore distinguish an oracle regime with known weights and candidate-weighted calibration from the plug-in regime used by the practical algorithm. 

5.2 Efficiency of the mixed-HDR geometry Proposition 4 (Mixed-HDR optimality) . Fix a target mixed density pt(ey | x) with respect to μ. Among all rules with Ppt { eY ∈ C(X)} ≥ 1 − α, any minimizer of E[μ(C(X))] is, up to ties, a global density-threshold rule 

C∗(x) = {ey : pt(ey | x) ≥ λ∗}.

Hence the fixed-threshold WT rule is the population mixed-HDR rule; WS forms source HDRs and is generally suboptimal under shift. 

14 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Proof in Section F. The message is orthogonality: weights correct the calibration aggregation, while the tilted score corrects the geometry of the returned set. Under censoring, this geometry controls both the interior interval and the binary atom-inclusion decisions. The optimality statement is with respect to the mixed size convention μ in (40) , which charges one unit for each included atom and Lebesgue length on the interior. It is therefore not invariant to rescaling the response variable; it is an HDR optimality statement for the adopted mixed measure. 

## 6 Experiments 

The theoretical development above separates validity from efficiency. The experiments therefore use a controlled simulation suite to test candidate-weighted validity on the observed mixed space, the approximation introduced by scalar atom weights and calibration-only thresholds, the efficiency gain from the tilted mixed score, and the behavior of density-ratio estimators. 

Common setup. We use a latent Gaussian regression model 

X ∼ N (0 , I d), Y ∗ = θ⊤X + ε, ε ∼ N (0 , σ 2 

> ε

),

with d = 5, ∥θ∥2 = 1, and σε = 1. The target distribution is generated by latent exponential tilting, 

dP t

dP s

(x, y ∗) = exp {βy ∗ − 12 β2Var s(Y ∗)},

which implies Xt ∼ N (βθ, I d) and Y ∗ 

> t

| Xt = x ∼ N (θ⊤x + βσ 2 

> ε

, σ 2 

> ε

). The observed response is eY = T (Y ∗)with two-sided censoring at the source marginal censoring quantiles. Unless otherwise stated, the censoring level is 10% per side, ncal = 600, ntest = 1000, and results are averaged over 200 independent repetitions. We use the oracle Gaussian predictive model in these diagnostics, so the experiments isolate the conformal layer and density-ratio approximations rather than posterior fitting error. Mean mixed-space size is measured with respect to the mixed measure μ of (13) : it is the interior Lebesgue length plus one unit for each included atom. The compared rules are as follows. Source CP uses the source mixed score and an unweighted quantile. UT uses the target tilted mixed score and an unweighted quantile. WS uses the source mixed score with the exact observed-space candidate weight. WT exact uses the target tilted mixed score with the exact observed-space candidate weight. WT scalar uses the practical scalar-atom plug-in and calibration-only threshold. The exact candidate-weighted rules use the true observed joint ratio, including the input-specific atom ratios induced by latent pushforward; set sizes for these rules are approximated by a fine grid over (L, U ). 

6.1 Experiment 1: method hierarchy 

Table 3 gives the basic method hierarchy. Under no shift, all methods are near the nominal level. Under strong positive or negative shift, the unweighted source score undercovers mildly, while UT severely overcovers because its target-aligned score is not paired with target/source measure correction. WS restores marginal coverage but remains wide. WT exact restores marginal coverage with much smaller mixed-space sets; for β = 1 .2, the mean size drops from 2 .894 for WS to 1 .740 for WT exact, a reduction of about 40%. WT scalar nearly matches the marginal coverage and size of WT exact in this controlled setting. The component-wise columns should not be read as conditional guarantees. They show the opposite: pooled mixed-space calibration reallocates coverage toward the censoring atom favored by the target shift. For β = 1 .2, WT exact has right-atom coverage 0 .990 but interior coverage 0 .730 and left-atom coverage 0.064; for β = −1.2, the pattern reverses. This is why the main theory is stated as a marginal mixed-space coverage result, not a component-wise result. 15 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Table 3: Experiment 1: method hierarchy under latent label shift and two-sided censored observation. Values are averages over 200 repetitions.                                                                                                 

> βMethod Coverage Size Left cov. Interior cov. Right cov. -1.2 Source CP 0.880 2.761 0.866 0.910 0.763 -1.2 UT 0.992 2.826 1.000 0.979 0.592 -1.2 WS exact 0.905 2.904 0.894 0.928 0.782 -1.2 WT exact 0.902 1.737 0.990 0.731 0.093 -1.2 WT scalar 0.901 1.751 0.998 0.713 0.128 0.0 Source CP 0.901 3.045 0.779 0.930 0.787 0.0 UT 0.901 3.045 0.779 0.930 0.787 0.0 WS exact 0.901 3.040 0.779 0.930 0.787 0.0 WT exact 0.901 3.040 0.779 0.930 0.787 0.0 WT scalar 0.899 3.031 0.776 0.928 0.784 1.2 Source CP 0.880 2.773 0.740 0.909 0.866 1.2 UT 0.992 2.837 0.572 0.978 1.000 1.2 WS exact 0.902 2.894 0.754 0.926 0.890 1.2 WT exact 0.901 1.740 0.064 0.730 0.990 1.2 WT scalar 0.901 1.755 0.108 0.714 0.998

Table 4: Experiment 2: approximation gap for positive shift ( β = 1 .2).                                                                  

> Censoring Method Coverage Size Interior cov. High-atom cov. 0.05 WT exact 0.900 2.132 0.815 0.979 0.05 calib-only exact ratio 0.897 2.120 0.812 0.976 0.05 scalar atom ratio 0.903 2.160 0.822 0.978 0.05 scalar score+ratio 0.901 2.159 0.803 0.991 0.10 WT exact 0.902 1.728 0.731 0.990 0.10 calib-only exact ratio 0.900 1.724 0.728 0.989 0.10 scalar atom ratio 0.905 1.752 0.740 0.990 0.10 scalar score+ratio 0.902 1.750 0.716 0.998 0.20 WT exact 0.902 1.283 0.530 0.995 0.20 calib-only exact ratio 0.901 1.280 0.527 0.995 0.20 scalar atom ratio 0.905 1.298 0.547 0.996 0.20 scalar score+ratio 0.901 1.288 0.506 1.000

6.2 Experiment 2: approximation gap 

Table 4 isolates the practical approximations used by WT. The exact candidate rule uses the exact observed joint ratio. The calibration-only rule keeps the exact joint ratio on calibration points but omits the candidate weight from the quantile. The scalar atom rule replaces input-specific atom ratios by marginal atom ratios. The scalar score+ratio rule also uses the scalar mixed predictive score induced by this approximation. The main finding is that the approximation gap is small for marginal coverage in this controlled setting. Even at 20% censoring per side and β = 1 .2, all four variants remain between 0 .901 and 0 .905 marginal coverage. The difference appears more clearly in component behavior: as censoring becomes stronger, interior coverage drops and the high-shift atom becomes nearly always covered. Thus scalar atom weights and calibration-only thresholds are empirically useful approximations, but they should not be interpreted as component-wise validity. 16 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Table 5: Experiment 3: mixed-HDR geometry for positive shift ( β = 1 .2). Entries are frequencies of set forms.                                                  

> Censoring Method IL+II+UU
> 0.01 WT exact 0.358 0.000 0.634 0.009 0.01 WT scalar 0.355 0.000 0.644 0.001 0.05 WT exact 0.134 0.000 0.764 0.102 0.05 WT scalar 0.090 0.000 0.864 0.045 0.10 WT exact 0.064 0.000 0.686 0.249 0.10 WT scalar 0.027 0.001 0.808 0.163 0.20 WT exact 0.024 0.002 0.401 0.573 0.20 WT scalar 0.001 0.003 0.467 0.529

6.3 Experiment 3: mixed-HDR geometry 

Table 5 is the most censoring-specific diagnostic. It records the structural form of the WT prediction set for positive shift. With weak censoring, an ordinary interior interval I occurs often. As censoring becomes severe, atom-containing sets dominate. At 20% censoring per side, WT exact produces the atom-only upper set {U } in 57 .3% of evaluated test inputs, and the form I + U in 40 .1%. WT scalar exhibits the same qualitative behavior. No disconnected set appears in any cell, consistent with its non-genericity for a unimodal tilted-Gaussian interior (Remark 3); the {U } column is the empty-interior atom-only regime of Proposition 1, which the corrected structural characterization now names explicitly. This experiment demonstrates why the censored problem should not be presented as a mechanical Gaussian interval extension. The output is a mixed-HDR set on {L} ∪ (L, U ) ∪ { U }, and its geometry changes with the censoring regime and target shift. 

6.4 Experiment 4: density-ratio estimation 

The preceding experiments used known ratios. Table 6 tests practical estimation of the interior tilt parameter β. Two estimators that ignore the residual shift βb —point pseudo-labels and source-predictive sampling—recover only β a/ (a + b) of the true slope, here exactly half since a = Var s(bμ) = ∥θ∥2 = 1 and 

b = ¯σ2 

> s

= σ2 

> ε

= 1; the attenuated slope ( bβ ≈ ± 0.6) produces conservative, oversized sets (coverage ≈ 0.955, size ≈ 2.35). The moment-matching initializer and tilted predictive sampling both recover the full slope and nearly match the known-β rule. Two caveats temper this table. First, it separates the attenuated estimators (point, source-predictive) from the consistent ones (moment matching, tilted predictive), but it does not separate moment matching from tilted predictive sampling. In this linear-Gaussian oracle, ¯μt − ¯μs = β∥θ∥2 and bv2 

> μ

= ∥θ∥2, so moment matching returns β exactly and the residual correction that tilted sampling supplies is not needed. The theoretical advantage of tilted predictive sampling—restoring the residual shift βb that mean-based matching omits (Proposition 2)—becomes visible only when the predictable component bμ(X)underestimates the explained variance, i.e. when the backbone is imperfect or the conditional mean is nonlinear, a regime this oracle diagnostic does not exercise. We therefore report moment matching and tilted predictive sampling as comparable here, retain tilted predictive sampling as the principled default on the theoretical grounds of Section 4.3, and defer a discriminating comparison to a fitted-head study (Section 7). Second, the atom ratios are taken as known censoring fractions throughout; estimating them jointly with β under input-only target data is left to future work. 

Summary of empirical findings. The experiments support four claims. First, the exact candidate-weighted mixed-space rule delivers the intended marginal coverage under latent label shift. Second, target-score tilting is essential for efficiency. WT is much shorter than WS when both have valid weighting. Third, scalar atom weights and calibration-only thresholds can be accurate for marginal coverage, but 17 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Table 6: Experiment 4: density-ratio parameter estimation.                                                        

> βEstimator bβAbs. error Coverage Size -1.2 known -1.200 0.000 0.901 1.756 -1.2 moment matching -1.199 0.025 0.899 1.740 -1.2 point pseudo-label -0.599 0.601 0.954 2.344 -1.2 source predictive -0.600 0.600 0.954 2.345 -1.2 tilted predictive -1.189 0.031 0.903 1.764 1.2 known 1.200 0.000 0.901 1.745 1.2 moment matching 1.199 0.024 0.900 1.747 1.2 point pseudo-label 0.599 0.601 0.955 2.345 1.2 source predictive 0.600 0.600 0.955 2.356 1.2 tilted predictive 1.190 0.028 0.901 1.752

component-wise coverage may still be uneven because a single pooled threshold is used. Fourth, the censored setting produces genuinely mixed prediction sets, often including boundary atoms or even atom-only forms under severe censoring. A real-data study with censored molecular endpoints remains an important next step, but the synthetic section now matches the revised theoretical claims. 

## 7 Conclusion 

This paper developed conformal Bayes for two-sided censored Gaussian regression under label shift. The censoring-specific contributions are the mixed posterior predictive distribution with atoms at the detection limits, the three-term closed-form normalizer Z(x), the mixed-space score that unifies the two atom components and the interior density, and the closed-form mixed-HDR characterization, which ranges from an interior interval to a boundary atom alone. Validity and efficiency are cleanly separated. Exact oracle validity is model-free but requires a valid mixed-space importance identity and the candidate-weighted rank, whereas efficiency is a population mixed-HDR property of the target score. The orthogonality of measure weighting and score tilting carries over to the mixed space. The two genuinely censoring-induced subtleties are the latent-to-mixed-space shift Remark 1 and the marginal atom-weight approximation Remark 2, both of which are stated explicitly rather than hidden in implementation details. Several limitations remain. The method relies on a Laplace-approximated censored posterior, whose approximation error may grow near the censoring limits. The two-sided Tobit head assumes Gaussian latent errors, and richer likelihoods such as skew-t or Weibull models would require numerical estimation of 

Z(x). The experiments are controlled synthetic diagnostics using an oracle Gaussian predictive model, so a fitted-head study and real censored molecular data remain important next steps. If non-Gaussian or mixture predictive heads are used, disconnected mixed sets may arise and will require a clear communication protocol for domain scientists. Future work includes full censored conformal Bayes with leave-one-out calibration, parameter-posterior correction through reweighted fine-tuning of the two-sided Tobit head, and non-Gaussian censored likelihoods, where the tilting identity and method hierarchy persist but Z(x) is estimated numerically. 

## References 

J. H. Albert and S. Chib. Bayesian analysis of binary and polychotomous response data. Journal of the American Statistical Association , 88(422):669–679, 1993. A. M. Alexandari, A. Kundaje, and A. Shrikumar. Maximum likelihood with bias-corrected calibration is hard-to-beat at label shift adaptation. In Proceedings of the International Conference on Machine Learning (ICML) , 2020. 18 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

T. Amemiya. Tobit models: A survey. Journal of Econometrics , 24:3–61, 1984. A. N. Angelopoulos and S. Bates. Conformal prediction: A gentle introduction. Foundations and Trends ®

in Machine Learning , 16(4):494–591, 2023. K. Azizzadenesheli, F. Yang, A. Liu, and A. Anandkumar. Regularized learning for domain adaptation under label shifts. In Proceedings of the International Conference on Learning Representations (ICLR) ,2019. R. F. Barber, E. J. Cand` es, A. Ramdas, and R. J. Tibshirani. Conformal prediction beyond exchangeability. 

The Annals of Statistics , 51(2):816–845, 2023. E. Cand` es, L. Lei, and Z. Ren. Conformalized survival analysis. Journal of the Royal Statistical Society Series B , 85(1):24–45, 2023. S. Choi. Conformal Bayes under label shift: Post-hoc calibration vs. in-training adaptation. In The 2nd Workshop on Epistemic Intelligence in Machine Learning , 2026. URL https://arxiv.org/abs/2606. 11865 .H. Davidov, S. Feldman, G. Shamai, R. Kimmel, and Y. Romano. Conformalized survival analysis for general right-censored data. In Proceedings of the International Conference on Learning Representations (ICLR) , 2025. E. Fong and C. Holmes. Conformal Bayesian computation. In Advances in Neural Information Processing Systems (NeurIPS) , 2021. S. Garg, Y. Wu, S. Balakrishnan, and Z. C. Lipton. A unified view of label shift estimation. In Advances in Neural Information Processing Systems (NeurIPS) , 2020. Y. Gui, R. Hore, Z. Ren, and R. F. Barber. Conformalized survival analysis with adaptive cut-offs. 

Biometrika , 111(2):459–477, 2024. D. R. Helsel. Statistics for Censored Environmental Data Using Minitab ® and R . John Wiley Sons, Inc., 2011. Y. Jin, Z. Ren, and E. J. Cand` es. Sensitivity analysis of individual treatment effects: A robust conformal inference approach. Proceedings of the National Academy of Sciences, USA , 120(6), 2023. R. J. Keizer, R. S. Jansen, H. Rosing, B. Thijssen, J. H. Beijnen, J. H. M. Schellens, and A. D. R. Huitema. Incorporation of concentration data below the limit of quantification in population pharmacokinetic analyses. Pharmacol Res Perspect. , 3(2), 2015. H. Lee, J. Kim, E. Jadamba, S. Choi, and H. Shin. Conformal prediction for molecular properties under label shift. In NeurIPS 2025 Workshop on Reliable ML from Unreliable Data , 2025. H. Lee, J. Kim, E. Jadamba, S. Choi, and H. Shin. Split conformal prediction with label-shift-adjusted Bayesian scores. In The 2nd Workshop on Epistemic Intelligence in Machine Learning , 2026. Z. C. Lipton, Y.-X. Wang, and A. J. Smola. Detecting and correcting for label shift with black box predictors. In Proceedings of the International Conference on Machine Learning (ICML) , 2018. H. Papadopoulos, K. Proedrou, V. Vovk, and A. Gammerman. Inductive confidence machines for regression. In Proceedings of the European Conference on Machine Learning (ECML) , 2002. R. N. Rosett and F. D. Nelson. Estimation of the two-limit probit regression model. Econometrica , 43(1): 141–146, 1975. M. Saerens, P. Latinne, and C. Decaestecker. Adjusting the outputs of a classifier to new a priori probabilities: A simple procedure. Neural Computation , 2002. 19 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

M. Sesia and V. Svetnik. Doubly robust conformalized survival analysis with right-censored data. In 

Proceedings of the International Conference on Machine Learning (ICML) , 2025. G. Shafer and V. Vovk. A tutorial on conformal prediction. Journal of Machine Learning Research , 9: 371–421, 2008. R. J. Tibshirani, R. F. Barber, E. J. Cand` es, and A. Ramdas. Conformal prediction under covariate shift. In Advances in Neural Information Processing Systems (NeurIPS) , 2019. L. Tierney and J. B. Kadane. Accurate approximations for posterior moments and marginal densities. 

Journal of the American Statistical Association , 81(393):82–86, 1986. J. Tobin. Estimation of relationships for limited dependent variables. Econometrica , 26(1):24–36, 1958. V. Vovk, A. Gammerman, and G. Shafer. Algorithmic Learning in a Random World . Springer, 2005. L. Wasserman. Frasian inference. Statistical Science , 26(3):322–325, 2011. J. R. Williams, H.-W. Kim, and C. M. Crespi. Modeling observations with a detection limit using a truncated normal distribution with censoring. BMC Medical Research Methodology , 20, 2020. 20 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

## A Gradient, Mills-ratio derivative, and Hessian 

This appendix gives the calculus behind the Laplace approximation used for the two-sided Tobit head in Section 3. We derive the score and negative Hessian of the observed-data log-likelihood, show how the Mills-ratio derivative enters the censored terms, and justify the concavity statement used to define the Gaussian approximation around the MAP. With ηLi = ( L − θ⊤φ(xi)) /σ ε, ηUi = ( U − θ⊤φ(xi)) /σ ε and λ(t) = ϕstd (t)/Φ( t), the chain rule gives 

∇θ ηLi = −φ(xi)/σ ε and ∇θ (−ηUi ) = φ(xi)/σ ε, so 

∇θ ℓ = − 1

σε

X

> eyi=L

λ(ηLi )φ(xi) + 1

σ2

> ε

X 

> eyi∈(L,U )

(eyi − θ⊤φ(xi)) φ(xi) + 1

σε

X

> eyi=U

λ(−ηUi )φ(xi).

Since ϕ′

> std

(t) = −tϕ std (t) and Φ ′(t) = ϕstd (t), the quotient rule gives λ′(t) = −tλ (t) − λ(t)2 = −λ(t)( λ(t) + 

t) < 0 (because λ is strictly decreasing). Differentiating each term yields the negative Hessian (8) ; each censored block contributes −λ′(η)φφ ⊤/σ 2 

> ε

⪰ 0 and each interior block φφ ⊤/σ 2 

> ε

⪰ 0. If the design {φ(xi)}

has full column rank these sum to a positive-definite matrix, so ℓ is strictly concave; in any case the Gaussian prior adds τ −2Id ≻ 0, making the penalized MAP objective strictly concave with a unique global maximizer. 

## B Latent-Pushforward Tilting Details 

This appendix records the conditional latent-pushforward calculation that is only summarized in Section 3. The main text uses the observed-space ratio w(ey) = dP t 

> eY

/dP s 

> eY

(ey) to define the practical tilted predictive distribution. Here we instead start from a latent ratio w∗(y∗) and then censor the resulting tilted latent predictive distribution. This derivation explains why exact atom weights are tail averages and why the scalar atom weights used in the main algorithm are an observed-space approximation. The latent tilted predictive satisfies 

plat  

> t

(y∗ | x, Dtr ) = ps(y∗ | x, Dtr )w∗(y∗)

R ps(u | x, Dtr )w∗(u) du .

Pushing this law forward through T gives the left atom 

plat  

> t

( eY = L | x, Dtr ) = 

R L 

> −∞

ps(y∗ | x, Dtr )w∗(y∗) dy ∗

R ps(u | x, Dtr )w∗(u) du = πL(x)wx(L)

Zlat 

> x

,

where 

Zlat  

> x

= πL(x)wx(L) + 

Z UL

fs(y | x)w∗(y) dy + πU (x)wx(U ).

The right atom is analogous. On the interior T is one-to-one, so the density is 

fs(ey | x)w∗(ey)

Zlat 

> x

, L < ey < U. 

This derivation gives the exact conditional latent pushforward construction. The practical method in the main text replaces wx(L), w x(U ) by the scalar marginal ratios in (23) and uses the observed-space tilted predictive distribution (19) . This replacement is exact only under the observed-space identity (18) or an equivalent condition making the tail-averaged ratio independent of x.

## C Proof of Proposition 1 (Structural Characterization) 

This appendix proves that the WT mixed-HDR set is the union of two independently thresholded atoms and one interior super-level interval. This is the precise structural statement that replaces the earlier fixed enumeration of possible forms. 21 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

Proof. Atom membership is a direct threshold comparison on the μ-density: L ∈ C WT (x) ⇐⇒ πt

> L

(x) ≥ λ∗

and U ∈ C WT (x) ⇐⇒ πt 

> U

(x) ≥ λ∗, which define AL, A U . On ( L, U ) the tilted density ft ∝ ϕstd (( ey−μt)/bσ)is a Gaussian restricted to the interval and is therefore unimodal, so its super-level set I(x) = {ey ∈

(L, U ) : ft ≥ λ∗} is convex, i.e. empty or a single interval. When nonempty, the defining inequality is (ey − μt)2/(2 bσ2) ≤ β bμ + 12 β2 bσ2 − log (Z(x)Zw bσ√2π) − log λ∗, which yields r(x) and [ yℓ, y u] as in (42) ;it is empty exactly when the right-hand side is negative, i.e. when λ∗ exceeds the interior maximum. Since AL, AU , and I are determined by three independent thresholdings, CWT = AL ∪ I ∪ AU ranges over precisely the unions listed in (a)–(b). Within (a), the union is connected iff every included atom adjoins the interval (an included L requires yℓ = L, equivalently ft(L+) ≥ λ∗; symmetrically at U ), and disconnected otherwise. This accounts for every combination, including atom-only sets and the empty set when I = ∅. No claim of a fixed number of forms is made: the partition is by the three independent decisions, not by an enumerated list. 

## D Proofs of Lemma 1 and Proposition 2 

This appendix collects the short calculations behind the pseudo-label bias discussion in Section 4.3. The first proof explains why point pseudo-labels remove within-input predictive variance. The second proof derives the attenuation factor for source-predictive pseudo-label sampling. 

Lemma 1. By the law of total variance, Var s(Y ∗) = Var ps(x){E[Y ∗ | X]} + Eps(x){Var (Y ∗ | X)}; substi-tuting E[Y ∗ | x] ≈ bμ(x), Var( Y ∗ | x) ≈ bσ2(x) gives Var ps(x)[bμ(X)] ≈ v2 

> s

− ¯σ2 

> s

.

Proposition 2. Tilting a Gaussian component shifts its mean by β× its variance. With M ⊥ ε, tilting by eβ(M +ε) shifts M by βa and ε by βb . Point pseudo-labels keep Mt ∼ N (βa, a ); source-PS adds a mean-zero variance-b residual, giving N (βa, a + b). Comparing with the source label marginal N (0 , a + b)yields slope βa/ (a + b). 

## E Proof of Proposition 3 (Correction Hierarchy) 

This appendix proves the validity hierarchy for UT, WS, and WT. The proof separates the calibration effect of the importance weights from the geometric effect of the chosen score. 

Proof. (i) UT thresholds the source quantile bqUT of Lps (sWT ) but is evaluated under Lpt (sWT ), so the coverage error is |Ft(bqUT ) − Fs(bqUT )| ≤ dTV (Lpt (sWT ), Lps (sWT )) + O(n−1/2); direction depends on how shift changes atom masses and interior density. (ii) By the observed-space identity (18) ,

Eps {w( eY )f (X, eY )} = Ept {f (X, eY )} for all measurable f —the only ingredient for weighted conformal validity. Applying the weighted rank argument to the n+ 1 augmented sequence (with the test weight 

w(eytest )) gives Ppt ( eYtest ∈ C r

> exact

) ≥ 1 − α for any score, including sWS , s WT ; no Tobit/Laplace prop-erty is used. (iii) Dropping the single candidate term from the weighted CDF changes it by at most 

w(ey)/(P 

> i

w(eyi) + w(ey)); the supremum over candidates is ϵcand = OP(1 /n ) under bounded, nondegenerate weights. 

## F Proof of Proposition 4 (Mixed-HDR Optimality) 

This appendix gives the population optimality argument for mixed-HDR sets under the adopted mixed measure. The result is conditional on this size convention, which charges one unit for each atom and Lebesgue length for the interior. 

Proof. Minimize EX [μ(C(X))] subject to EX

R 

> C(X)

pt(ey | X) dμ ≥ 1 − α. With multiplier η ≥ 0, the per-point Lagrangian contribution is 1 − η p t(ey | x), so a point enters iff pt(ey | x) ≥ 1/η =: λ∗. The argument is measure-theoretic and unchanged on a mixed space because atoms and interior share the 22 Conformal Bayes for Two-Sided Censored Gaussian Regression under Label Shift 

dominating measure μ. For WT, {sWT ≤ bqWT } is the level set with λ∗ = e−bqWT ; WS thresholds ps and targets a source HDR, generally suboptimal for E[μ(C)] under pt when ps̸ = pt.

## G SCB vs. SCB-C 

This appendix summarizes how the censored method differs from the uncensored split conformal Bayes construction. The main differences are the mixed observed outcome space, the three-component label ratio, and the atom-plus-interior geometry of the prediction set. Table 7: Uncensored SCB (Choi, 2026) vs. two-sided censored SCB-C. Dimension SCB (uncensored) SCB-C (censored) Outcome space R {L} ∪ (L, U ) ∪ { U } (mixed) Predictive N (bμ, bσ2) atoms + Gaussian interior Tilted predictive N (bμ + βbσ2, bσ2) reweighted atoms + shifted trunc. Gaussian 

Z(x) eβ bμ+ 12 β2 bσ2

/Z w (1 term) 3 terms (left atom, partial MGF, right atom) Prediction set symmetric interval mixed HDR: atoms plus interior interval Coverage focus marginal coverage marginal coverage on mixed observed space Density ratio 1 function w(y) 3 objects w(L), w (ey), w (U )

## H Commutativity of censoring and posterior averaging 

The atom mass can be computed by averaging the latent likelihood over the posterior and then censoring (“averaging first,” used in the main text), or by censoring the likelihood and then averaging (“censoring first”); the two commute. Under the Laplace approximation π(θ | D tr ) ≈ N (bθ, bΣθ ), averaging first gives the Gaussian latent predictive N (bμ(x), bσ2(x)) and P( eY = L | x) = Φ(( L − bμ(x)) /bσ(x)); censoring first gives Eθ [Φ(( L − θ⊤φ(x)) /σ ε)], which by the Gaussian-CDF convolution identity equals the same Φ(( L−bμ(x)) /bσ(x)). Under S MCMC draws the equivalence persists: both yield 1

> S

P 

> s

Φ(( L−θ(s)⊤φ(x)) /σ ε), because the CDF of a Gaussian mixture is the mixture of component CDFs. The practical pitfall is summarizing a non-Gaussian MCMC posterior by its mean ¯θ and plugging into a single Φ: passing E[θ]through the nonlinear Φ violates Jensen’s inequality and discards posterior skew/heavy tails. With MCMC, the “censoring first” (per-sample) average is therefore preferred; under the Gaussian Laplace posterior used here, “averaging first” is exact. 23