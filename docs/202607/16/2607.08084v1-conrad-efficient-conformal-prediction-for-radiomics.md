---
title: "ConRad: Efficient Conformal Prediction for Radiomics"
title_zh: "ConRad: 面向放射组学的高效保形预测"
authors: "Matt Y. Cheung, Ashok Veeraraghavan, Guha Balakrishnan"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08084v1"
tags: ["query:cp"]
score: 10.0
evidence: 放射组学目标的分布无关共形预测框架
tldr: 放射组学特征从分割模型预测的掩模计算，但模型可能过度自信，使特征可靠性低。共形预测可提供有覆盖保证的区间，但黑盒方法忽略测试时图像外观、掩模几何和分割不确定性，导致效率低下。本文提出ConRad，利用预测掩模、输入图像、预测放射组学和边界不确定性等协变量构建自适应区间。在五个2D数据集和171个放射组学目标上，ConRad提升效率并保持近名义覆盖，消融表明边界不确定性贡献最大。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08084v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1708, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08084v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08084v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1715, \"height\": 652, \"label\": \"Figure\"}]"
motivation: 现有共形预测忽略图像外观、掩模几何和分割不确定性等测试时信息，导致放射组学区间效率低。
method: 提出ConRad，使用预测掩模、输入图像、预测放射组学和边界不确定性作为协变量，构建自适应共形区间。
result: 在五个2D医学影像数据集、171个放射组学目标上，ConRad提升区间效率且保持近名义覆盖，边界不确定性贡献最大。
conclusion: ConRad通过利用测试时协变量，有效提升了放射组学共形预测的效率与可靠性。
---

## 摘要
从医学图像和分割掩膜中提取的放射组学特征用于支持临床成像流程中的决策制定。在实践中，这些特征通常由预测掩膜计算得出，但分割模型可能过于自信或校准不佳，导致衍生测量值看似比实际更可靠。保形预测提供了无分布假设的预测区间，并具有有限样本边际覆盖保证，但针对分割衍生放射组学的黑箱区间可能效率低下，因为它们忽略了关于图像外观、掩膜几何形状和分割不确定性的测试时信息。我们提出ConRad，一种针对标量放射组学目标的保形框架，该框架利用从预测掩膜、输入图像、预测放射组学和边界不确定性中导出的协变量来构建自适应区间，同时保持覆盖。在五个2D医学影像数据集和171个保留的放射组学目标上，我们展示了ConRad相比基线提高了特征级效率，同时保持了接近名义经验覆盖率。消融结果进一步表明，分割边界不确定性特征对区间效率的贡献最大。

## Abstract
Radiomic features derived from medical images and segmentation masks are used to support decision making in clinical imaging pipelines. In practice, these features are often computed from predicted masks, but segmentation models can be overconfident or poorly calibrated, making derived measurements appear more reliable than they are. Conformal prediction (CP) provides distribution-free prediction intervals with finite-sample marginal coverage guarantees, but black-box intervals for segmentation-derived radiomics can be inefficient because they ignore test-time information about image appearance, mask geometry, and segmentation uncertainty. We propose ConRad, a conformal framework for scalar radiomic targets that uses covariates derived from the predicted mask, input image, predicted radiomics, and boundary uncertainty to construct adaptive intervals while maintaining coverage. Across five 2D medical imaging datasets and 171 retained radiomic targets, we show that ConRad improves feature-level efficiency compared to baselines while maintaining near-nominal empirical coverage. Ablation results further indicate that segmentation boundary uncertainty features are the largest contributors to interval efficiency.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在医学成像流程中，放射组学特征（如形状、强度、纹理）常从分割模型预测的掩膜中计算得出。然而分割模型可能过度自信或校准不佳，导致特征看似可靠实则偏差大。
- **现有方法的不足**：传统共形预测（CP）虽能提供无分布假设的预测区间和有限样本边际覆盖保证，但黑箱方法直接对放射组学值建模，忽视了测试时可获得的图像外观、掩膜几何形状和分割不确定性等丰富信息，导致区间效率低（即区间过宽）。
- **目标**：利用测试时分割输出的信息（如对象大小、边界位置、分割不确定性）来构建更窄的不确定性区间，同时保持形式化的覆盖保证。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：使用**共形化分位数回归（CQR）** 框架，将测试时可获得的协变量（包括预测放射组学值、掩膜几何特征、图像强度分布、边界不确定性特征）纳入条件分位数模型，从而自适应调整区间宽度。
- **关键技术步骤**：
  1. **训练阶段**：对每个放射组学目标，使用训练集拟合下分位数模型 $\hat{Q}_{\alpha/2}(X)$ 和上分位数模型 $\hat{Q}_{1-\alpha/2}(X)$，其中 $X_i = (\hat{Y}_i, \hat{Z}_i)$，$\hat{Y}_i$ 为预测放射组学值，$\hat{Z}_i$ 包括几何/拓扑、强度分布、预测放射组学汇总和边界不确定性特征。
  2. **校准阶段**：在独立的校准集上计算非一致性分数 $R_i = \max\{\hat{Q}_{\alpha/2}(X_i) - Y_i, Y_i - \hat{Q}_{1-\alpha/2}(X_i)\}$，取 $\hat{q}$ 为 $\lceil (1-\alpha)(n+1) \rceil$ 阶统计量。
  3. **测试阶段**：测试集区间为 $S(X_{\text{test}}) = [\hat{Q}_{\alpha/2}(X_{\text{test}}) - \hat{q}, \hat{Q}_{1-\alpha/2}(X_{\text{test}}) + \hat{q}]$，满足边际覆盖概率 $\geq 1-\alpha$。
- **特征工程**：四类测试时特征：
  - **几何与拓扑**：预测区域像素数、图像占比、连通分量数。
  - **强度分布**：预测区域内强度的均值、标准差、最小/最大值、百分位数等。
  - **预测放射组学**：来自三个家族（8个形状、13个一阶强度、19个纹理）的预测值。
  - **边界不确定性**：基于预测概率图在掩膜内部、边界带和全局的统计量（均值、标准差、10%/90%分位数、[0.4,0.6]概率像素比例）。
- **公式**：引理1给出CQR的保形保证，标准分割共形性质。

## 3. 实验设计：数据集、场景、Benchmark与对比方法

- **数据集**：5个2D医学影像分割数据集：
  - HAM10000（皮肤镜图像）
  - Kvasir（内窥镜图像）
  - COVID-QU-EX（胸部X光）
  - TG3K（甲状腺超声）
  - TN3K（甲状腺结节超声）
- **目标筛选**：每个数据集经过过滤（去除缺失值、常数、不稳定目标），共保留171个放射组学目标（分别为26、27、40、38、40）。
- **对比方法**（3个基线 + ConRad）：
  1. **SCP** (Split Conformal Prediction)：基于绝对残差的黑箱区间。
  2. **CQR-$\hat{y}$**：CQR仅使用预测放射组学值作为特征。
  3. **CQR-Generic**：CQR使用 $\hat{Y}_i$ + 几何/拓扑 + 强度分布 + 预测放射组学特征。
  4. **ConRad**：在CQR-Generic基础上增加边界不确定性特征。
- **评估指标**：因目标量纲不同，使用**目标级胜率**（WR）和**对称相对改进**（$\Delta$），并报告经验覆盖率。所有指标基于50次随机训练/校准/测试划分（0.4/0.4/0.2），名义误覆盖 $\alpha=0.1$。
- **超参数**：线性岭正则化分位数回归，$\ell_2$ 惩罚 $5\times10^{-2}$，Adam优化1200迭代，学习率 $5\times10^{-2}$，所有目标共用。

## 4. 资源与算力

- **文中未明确说明**：未提及GPU型号、数量、训练时长等具体计算资源。仅提到使用Adam优化训练分位数回归模型（线性模型，训练较快），但整体实验规模适中（5个数据集×171个目标×50次重复，每次训练约1200迭代）。未声明是否使用GPU，可能基于CPU即可完成。

## 5. 实验数量与充分性

- **实验数量**：共5个数据集，171个放射组学目标，每个目标50次随机划分，总计约8550次独立实验。每个实验包含训练、校准、测试全流程。
- **消融实验**：进行了分组特征消融，移除每个特征家族（几何+拓扑、强度分布、放射组学、边界不确定性）后重新拟合和校准，评估区间宽度变化。
- **充分性与公平性**：
  - 数据集覆盖多种模态（皮肤镜、内镜、X光、超声），具有一定多样性。
  - 对比基线逐步增加特征，可清晰评估每类特征的贡献。
  - 使用了目标级、规模标准化指标（胜率、相对改进），避免因量纲差异导致的不公平比较。
  - 但**局限**：仅2D数据集，缺少3D验证；超参数未针对每个目标调优，可能对部分目标非最优；线性分位数回归假设线性关系，复杂关系可能未被捕获。

## 6. 主要结论与发现

- ConRad在4个数据集上取得显著正平均相对改进（HAM10000: 5.47%, COVID-QU-EX: 4.94%, TG3K: 5.83%, TN3K: 2.03%），对应胜率82.5%~91.3%。Kvasir表现最弱（近0%改进，胜率55.3%）。
- 经验覆盖率接近名义90%：最小目标平均覆盖率89.9%，170/171个目标达到90%以上。
- 消融分析表明**边界不确定性特征贡献最大**（平均归一化重要性40.5%，最大宽度增加4.95%），尤其在纹理目标上主导。
- 结论：利用测试时协变量（特别是边界不确定性）能有效提升放射组学区间效率，同时保持覆盖保证。

## 7. 优点

- **方法创新**：首次将分割掩膜的结构化信息（几何、强度、边界不确定性）系统引入放射组学共形预测，突破黑箱方法。
- **实验设计严谨**：使用目标级标准化指标（胜率、对称相对改进），避免量纲偏差；消融实验层次清晰，归因可靠。
- **通用性**：不依赖特定分割模型或放射组学软件包，仅需预测概率图，适用性强。
- **统计保证**：保持有限样本边际覆盖，无需数据分布假设。

## 8. 不足与局限

- **仅支持2D**：实验局限于2D分割，3D放射组学在稳定性、特征行为上可能不同，需另行验证。
- **边际覆盖而非条件覆盖**：不能保证每个子组或个体的条件覆盖，对高风险子群可能有偏差。
- **依赖交换性假设**：若出现数据集偏移（如不同扫描仪、患者人群），需重新校准或采用额外诊断。
- **依赖有用概率图**：边界不确定性特征需可靠的分割概率图，若分割模型未输出有意义概率（如阈值硬分割），该方法受限。
- **单目标处理**：当前对每个放射组学目标独立建模，未考虑多目标联合结构（如相关性），未来可扩展至多元共形集。
- **计算资源未报告**：缺乏对训练/推理效率的定量评估。

（完）
