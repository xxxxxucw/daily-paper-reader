---
title: "Personalized Causal Recourse: A Human-In-The-Loop Approach"
title_zh: 个性化因果补救：一种人在回路中的方法
authors: "Denise Tampieri, Giovanni De Toni, Paolo Giudici"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03425v1"
tags: ["query:causal-infer"]
score: 9.0
evidence: 个性化因果反事实解释方法
tldr: 在机器学习做出不利决策时，用户需要个性化补救建议，但传统方法依赖最近反事实或先验因果结构，忽略个体语境。本文提出一种人在回路框架，通过交互式查询用户反馈，利用贝叶斯推理逐步近似用户的因果模型，从而生成个性化、成本有效的补救。在线性和非线性因果模拟中展现出前景，但对复杂非线性结构捕获仍有挑战。该框架实现了真正个性化因果补救，增强用户参与和模型透明度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03425v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 391, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03425v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03425v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03425v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1269, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03425v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1267, \"height\": 431, \"label\": \"Figure\"}]"
motivation: 传统算法补救方法忽略用户个体语境和特征交互，难以提供真正个性化的建议。
method: 提出人在回路框架，通过贝叶斯推理迭代查询用户反馈，近似其结构因果模型以生成补救。
result: 在线性和非线性因果模型模拟中，该方法能生成合理且成本有效的个性化补救，但对复杂非线性结构仍有局限。
conclusion: 该框架实现了个性化因果补救，但需改进对非线性结构的近似和噪声分布建模。
---

## 摘要
算法补救解决了在潜在高风险场景中向受不利机器学习决策影响的用户提供量身定制建议的挑战。传统的补救方法通常依赖于最近的反事实解释或假设用户因果结构的先验知识，导致干预措施忽略了个体背景和具体的特征交互。为克服这些局限，我们研究了一种人在回路的框架，该框架通过贝叶斯推断的交互式查询逐步逼近用户的结构因果模型，然后生成补救建议。该框架利用人类的反馈来改进因果效应的识别，从而实现个性化补救，使其既合理、成本效益高，又符合每个用户的实际因果依赖。作为概念验证，我们通过模拟人类响应来评估该框架。在线性和非线性因果模型上的模拟显示了有希望的结果，尽管在捕捉复杂的非线性结构方面仍存在挑战，这强调了精确近似和稳健噪声分布建模的重要性。

## Abstract
Algorithmic recourse addresses the challenge of providing tailored recommendations to users affected by unfavorable machine learning decisions, in potentially high-stakes scenarios. Traditional approaches to recourse often rely on the closest counterfactual explanations or assume a priori knowledge of a user's causal structure, resulting in interventions that overlook individual contexts and specific feature interactions. To overcome these limitations, we study a human-in-the-loop framework that iteratively approximates the user's structural causal model through interactive queries via Bayesian inference before producing recourse recommendations. This framework exploits humans' feedback to improve the identification of causal effects, allowing personalized recourse that is plausible, cost-effective, and aligned with the actual causal dependencies of each user. As a proof of concept, we evaluate this framework through simulated human responses. Our simulations across linear and non-linear causal models show promising results, though challenges remain in capturing complex, non-linear structures, emphasizing the importance of accurate approximations and robust noise distribution modeling.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在机器学习模型做出高利害决策（如贷款审批）后，受不利影响的用户迫切需要获得可操作的补救建议（algorithmic recourse）。传统方法要么直接给出最近的反事实解释，要么假设用户的因果模型已知，忽略了用户个体之间的因果结构差异（例如教育和工资的因果关系因人而异），导致推荐干预措施脱离用户实际、成本高或无效。
- **研究动机**：用户的内在因果机制是主观且个性化的，从数据中难以统一获取。因此需要一种能够自动适应用户个人因果认知的补救方法，将用户纳入循环，利用其反馈推断其特定因果结构，从而生成真正个性化、合理且低成本的补救行动。
- **整体意义**：提出“人在回路中”的个性化因果补救框架，首次将贝叶斯推理与交互式查询相结合，逼近用户的结构因果模型（SCM），为打破传统“单一方适合所有”的因果假设提供了新路径，增强了算法补救的实用性和用户中心性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：不假设访问用户真实SCM，而是通过向用户提出一系列定向干预查询（intervention queries），收集用户的响应反馈（如“如果提高你的工资，你的储蓄会变化多少？”），利用贝叶斯推断估计一个代理SCM（surrogate SCM），再基于该估计模型生成成本最低的有效补救。用户反馈被视为有噪声的因果效应观测。
- **关键技术细节**：
    1. **模型假设**：采用加性噪声模型（ANM），假设变量拓扑顺序已知（即因果图结构已知），每个内生变量建模为线性函数加上独立高斯噪声：\(X_i := \sum_{j < i} \theta_{ij} X_j + U_i, \quad U_i \sim \mathcal{N}(0,1)\)，系数 \(\theta_{ij}\) 为待估参数。
    2. **用户响应模型**：模拟真实用户的不完美反馈。设置一个伯努利变量 \(z_t\) 表示第 \(t\) 次回答是否基于真实因果参数 \(\theta^{GT}\)（概率 \(1-\alpha\)），否则基于某个扰动参数 \(\theta^*\)（概率 \(\alpha\)），从而引入响应噪声。
    3. **后验推断**：利用MCMC（Zeus集成切片采样器）从参数后验 \(P(\tilde{\theta} \mid \mathcal{D})\) 中采样，先验系数限定在区间 \([\theta_{\min}, \theta_{\max}]\) 内的均匀分布。似然函数采用软约束：至少 \((1-\beta)\) 比例的响应（在容差 \(\sigma\) 内）应与模型预测一致。
    4. **补救生成**：从估计的SCM \(\tilde{M}\) 出发，求解鲁棒因果补救优化问题（公式2），使用梯度优化（如SGD）寻找最小成本干预，使得反事实结果被分类器 \(h\) 判定为正面，并保持对 \(\epsilon\)-球内扰动的鲁棒性。
- **算法流程**（文字说明）：
    1. 初始定义先验参数空间与拓扑顺序。
    2. 循环 \(t=1 \dots T\)：随机选择一个变量进行软干预，查询用户预测的后果响应值。
    3. 收集所有干预-响应对 \(\mathcal{D} = \{(a_t, r_t)\}_1^T\)。
    4. 利用MCMC从后验 \(P(\tilde{\theta} \mid \mathcal{D})\) 采样，得到估计系数。
    5. 在估计的SCM下求解鲁棒因果补救问题，输出最小成本动作。

## 3. 实验设计：使用的数据集/场景、基准和对比方法

- **数据集/场景**：全部使用合成数据生成三种不同的SCM（表示三个不同用户）：
    - **M1**：简单线性SCM，高斯噪声。
    - **M2**：线性SCM（来自文献[16]），混合高斯噪声，中等因果依赖。
    - **M3**：非线性加性噪声模型（来自文献[16]），包含非线性机制与异方差噪声。
    每个SCM结构为3个变量，拓扑顺序已知。基于这些SCM生成1500个训练样本用于训练MLP分类器，额外500个被分类为负类的样本用于补救评估。
- **基准（Baseline）**：
    - **真实SCM（M_GT）**：直接基于真实因果模型生成最优补救（上界）。
    - **先验基线（Prior baseline, MP）**：不进行用户查询，仅使用先验约束（均匀分布）的SCM估计，模拟无因果知识的情形。
- **对比方法**：将估计SCM（\(\tilde{M}\)）与上述两个基准进行比较，评估有效性和成本。
- **参数变化**：改变用户噪声水平 \(\alpha \in [0,1]\)、鲁棒性要求 \(\epsilon \in \{0, 0.1\}\)、交互次数 \(T\)、学习率 \(\gamma\)、容差 \(\sigma\) 等。

## 4. 资源与算力

- **未明确说明**：论文没有提及所使用的GPU型号、数量、训练时长等硬件资源信息。所有实验基于合成数据和小型模型（3变量SCM、MLP分类器），推测可在普通CPU或单GPU上快速完成。作者仅说明实验使用Zeus MCMC采样器，未提供硬件细节。

## 5. 实验数量与充分性

- **实验数量**：三种SCM场景（M1、M2、M3），对每个场景重复5次模拟。针对不同参数组合（\(\alpha, \epsilon, \gamma, T\)）均进行了有效性和成本的对比。例如，图2、3展示了不同学习率下的结果，图4、5分析了干预变量分布。消融实验包括对用户噪声、鲁棒性边界、交互次数等的敏感度分析。
- **充分性**：
    - **优点**：覆盖了线性、非线性、混合噪声等不同情况，与真实SCM和先验基线对比，评估指标清晰。重复5次并报告标准差，体现一定统计可靠性。
    - **局限**：仅基于合成数据（模拟用户反馈），未涉及真实用户实验；仅3变量低维场景，未扩展至高维；用户响应模型为简单的混合噪声，未考虑认知偏差、锚定效应等更真实的行为特征。实验未系统评估不同先验选择或不同容差\(\sigma\)、\(\beta\)的影响。总体而言，作为概念验证实验是充分的，但远未达到实际部署的充分性。

## 6. 论文的主要结论与发现

- **线性SCM下有效**：对于线性高斯模型M1，估计SCM生成的补救有效性接近真实SCM，且成本相近，显著优于无因果先验基线。
- **模型偏差导致性能下降**：在M2（混合高斯噪声）和M3（非线性）场景下，由于线性高斯假设不匹配，有效性和成本估计均出现退化，但估计SCM仍比先验基线获得更低成本的补救，证明即使不精确的因果结构也能带来收益。
- **干预模式趋近真实**：从干预变量分布（图4、5）看，估计SCM推荐的干预目标组合与真实SCM更相似，而先验基线则较为分散。
- **鲁棒性与可行性的权衡**：提高鲁棒性要求 \(\epsilon\) 会降低所有模型的有效性，包括真实SCM，说明存在内在冲突。
- **用户噪声与交互次数影响**：用户噪声 \(\alpha\) 增大导致估计质量单调下降；合理设置容差 \(\sigma\) 可改善估计；大多数增益在前5-6次干预内获得。
- **非线性模型是主要挑战**：线性假设不足以捕捉非线性因果机制，导致有效性明显下降，但成本仍优于基线。

## 7. 优点

- **创新性**：首次将用户交互式因果模型估计与个性化因果补救结合，打破了“已知统一因果模型”的强假设，使补救更符合个体实际。
- **方法稳健性**：采用贝叶斯框架，自然处理用户反馈的不确定性；引入鲁棒约束（ϵ-球）提升补救在扰动下的稳定性。
- **实验设计规范**：在合成数据上进行了全面的参数敏感性分析，对比多个基线，量化了估计误差对最终补救的影响。
- **代码开源**：提供完整代码和复现脚本，提升可重复性。
- **清晰的局限性分析**：诚实指出了模型假设的局限、非线性下的挑战以及未来方向，显示了科研严谨性。

## 8. 不足与局限

- **模型假设过强**：假设因果图拓扑顺序已知，且使用线性加性高斯噪声模型。这在许多现实场景中不成立，尤其非线性依赖和复杂噪声分布难以拟合。
- **用户响应模型简单**：仅用混合噪声模拟不完美反馈，忽略了人类认知偏差（锚定效应、一致性偏见）等复杂因素，降低模拟的真实性。
- **仅限低维合成数据**：仅3变量，未在高维或真实数据集上验证，可扩展性未知。高维干预空间下查询选择策略的复杂性未讨论。
- **缺乏真实用户实验**：框架核心依赖“人在回路”，但论文未招募真实用户进行评估，概念验证的有效性有待直接检验。
- **优化过程本身存在局限**：即使使用真实SCM，并非所有负类样本都能找到有效补救，说明优化器（梯度方法）可能陷入局部最优，补救成功率受限于优化算法。
- **超参数敏感**：性能强烈依赖于容差σ、用户噪声α、学习率γ等参数，实际部署中难以事先确定最优值。
- **未考虑时间动态**：补救建议可能随时间失效（如用户特征变化），文中未讨论时间因素（尽管在相关工作[4]中提及）。

（完）
