---
title: Order-based Causal Discovery for Multistage Processes
title_zh: 基于顺序的多阶段过程因果发现
authors: "Eun-Yeol Ma, Junsub Jung, Heeyoung Kim"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03971v1"
tags: ["query:causal-infer"]
score: 8.0
evidence: 基于顺序的多阶段过程因果发现
tldr: 多阶段过程因果发现中，现有方法忽略过程知识，结果不可靠且计算效率低。提出Order-based Causal Discovery for Multistage Processes (OCDM)，利用阶段信息推断因果顺序，再以随机门控神经网络剪枝。实验表明OCDM能准确识别因果结构，优于现有方法，为多阶段数据提供了高效且背景知识一致的因果发现方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03971v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1315, \"height\": 1089, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 792, \"height\": 1206, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1585, \"height\": 1040, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1585, \"height\": 1039, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1586, \"height\": 1040, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1872, \"height\": 1381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03971v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1586, \"height\": 1036, \"label\": \"Figure\"}]"
motivation: 现有因果发现方法无法利用多阶段过程的层级与顺序知识，导致结果违背过程常识且计算开销大。
method: 采用结构知识引导的因果顺序推断算法，再通过随机门控神经网络剪枝消除伪边，提升效率与准确性。
result: 在多个数据集上，OCDM相比现有方法更准确地推断多阶段因果结构，且计算效率更高。
conclusion: OCDM有效融合过程知识，为多阶段系统因果发现提供了实用且高效的新工具。
---

## 摘要
因果关系已成为深入理解复杂系统日益重要的工具。在各种因果分析方法中，因果发现通过从数据中识别变量间的因果关系，被广泛应用于揭示不同过程中的潜在因果机制。然而，尽管多阶段过程在许多领域普遍存在，现有的因果发现方法在已知过程知识的情况下可能产生违反直觉的结果，并且处理多阶段过程典型的大数据集时计算效率不高。为解决这一问题，我们提出了一种新颖的因果发现方法，称为基于顺序的多阶段过程因果发现（OCDM）。OCDM旨在推断多阶段数据的因果结构，同时通过将过程知识明确纳入因果发现过程来保留其固有的层级和顺序结构。具体而言，我们提出了一种结构知识引导的顺序推断算法，该算法基于自然适合固有顺序多阶段数据的基于顺序的因果发现框架，通过整合每个变量来源阶段的信息来推断变量的因果顺序。此外，为了消除基于推断的因果顺序生成的初始因果图中的虚假边，我们引入了一种使用随机门控神经网络的新型剪枝技术，与现有方法相比，该技术提供了更高的计算效率。通过在多个数据集上的实验，我们证明了OCDM有效推断多阶段过程的因果结构，优于现有方法。

## Abstract
Causality has become an increasingly important tool for gaining a deeper understanding of complex systems. Among various causal analysis methods, causal discovery, which identifies causal relationships among variables from data, has been widely used to uncover underlying causality in diverse processes. However, while multistage processes are prevalent in many fields, existing causal discovery methods may produce counterintuitive results, given the known process knowledge, and may not be computationally efficient for handling large datasets typical of multistage processes. To address this gap, we propose a novel causal discovery method called Order-based Causal Discovery for Multistage Processes (OCDM). OCDM is designed to infer the causal structure of multistage data while preserving their inherent hierarchical and sequential structure by explicitly incorporating process knowledge into the causal discovery process. Specifically, we propose a structural knowledge-informed order-inferring algorithm that infers the causal order of variables by incorporating information about the stage from which each variable originates, based on an order-based causal discovery framework naturally suited for inherently ordered multistage data. Furthermore, to eliminate spurious edges from the initial causal graph generated based on the inferred causal order, we introduce a novel pruning technique using stochastic gated neural networks, which offers greater computational efficiency compared to existing methods. Through experiments on various datasets, we demonstrate that OCDM effectively infers the causal structure of multistage processes, outperforming existing methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：多阶段过程（如制造、信息系统、服务运营）普遍存在，数据通常具有层级和顺序结构。因果发现能从数据中识别变量间的因果关系，但现有方法对此类过程效果不佳。
- **核心问题**：
  - 现有方法忽略已知的阶段信息（如变量所属的阶段顺序），可能产出违反直觉的因果结构（例如下游变量被错误地推断为上游变量的原因）。
  - 多阶段数据通常高维（变量数随阶段数增长），而传统因果发现算法的搜索空间随节点数超指数增长，计算效率低下。
- **整体意义**：提出一种专门针对多阶段过程的因果发现方法OCDM，通过显式利用阶段知识（变量的来源阶段）来指导顺序推断，并采用更高效的剪枝策略，从而实现更准确、更高效的因果结构学习。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：
  1. **阶段知识引导的因果顺序推断**：基于顺序的因果发现框架，在推断变量全局顺序时，按照阶段反向顺序（从最后阶段到第一阶段）进行，确保每一阶段内节点顺序确定后再进入上一阶段。这利用多阶段数据的层级有向无环图（layered DAG）结构，避免下游节点出现在上游节点之前。
  2. **随机门控神经网络剪枝**：传统CAM剪枝（基于稀疏样条回归）在高维样本下效率和精度不足，改用随机门控神经网络（STG-NN）进行变量选择，从而剔除顺序连接图中的虚假边。

- **关键技术细节**：
  - **顺序搜索算法（伪代码见算法1）**：
    - 假设数据来自非线性加性高斯噪声模型：\(X_i = f_i(Pa(X_i)) + U_i, U_i\sim N(0,\sigma_i^2)\)。
    - 利用得分匹配（score matching）性质：叶节点的得分函数对自身变量的Jacobian的方差为零。通过迭代识别当前剩余变量中Jacobian方差最小的节点作为叶节点。
    - 阶段约束：从最后阶段 \(L_w\) 开始，依次只在该阶段内查找叶节点，确定该阶段所有节点顺序后，再处理上一阶段。
    - 使用DiffAN作为基础得分估计器（基于扩散概率模型），并利用掩码机制移除已排序变量。
  - **剪枝方法**：
    - 根据推断的因果顺序生成顺序连接图（每个节点连向所有后继节点）。
    - 对每个目标变量 \(Y_i = X_{\pi_i}\)，以所有前驱变量作为输入，训练一个带随机门控的神经网络：
      \[
      z_i = \max(0, \min(1, \alpha_i + \epsilon_i)), \quad \epsilon_i \sim N(0,\sigma_z^2)
      \]
    - 训练后，若 \(\alpha_j \leq 0\) 则门控 \(z_j=0\)，对应边被剪除。
    - 该过程比CAM剪枝（组LASSO+样条回归）更灵活，能捕捉非线性交互，且L0正则化直接控制变量数量，避免L1的收缩效应。

### 3. 实验设计
- **数据集与场景**：
  - **仿真数据（Simulation 1 & 2）**：使用Erdős–Rényi模型生成多层DAG，每层节点数k=10。Simulation 1：3个阶段（30节点）；Simulation 2：20个阶段（200节点）。链接函数采样自高斯过程，噪声方差均匀分布。还生成了**噪声阶段信息**的数据（P=0.1~0.5变量标签随机重分配）和**混合类型数据**（30%变量为二值，其余连续）。
  - **伪真实数据（causalAssembly）**：模拟真实装配线，5个阶段共98个变量，因果关系由领域专家定义。
- **Benchmark**：对比方法包括约束型（PC-algorithm）、得分型（NOTEARS-MLP、DAG-GNN、GraN-DAG）和基于顺序的方法（CAM、SCORE、DAS、DiffAN）。对OCDM分别测试了使用CAM剪枝和STG-NN剪枝的变体。
- **评估指标**：AUROC、AUPRC、结构汉明距离（SHD）、结构干预距离（SID）、拓扑顺序散度（D_top）。

### 4. 资源与算力
- 文中提及：所有实验运行于一台Windows工作站，配置Intel Core i5-11400 CPU、NVIDIA RTX 3070 GPU、64GB DDR4 RAM。
- 实际训练时长：Simulation 2中，OCDM（STG-NN）总耗时约2.75小时（其中剪枝约1.2小时，对比CAM剪枝约2.2小时），其他方法（SCORE >12h，DAG-GNN ~4h，PC ~2.1h）。但未给出单次实验的具体GPU小时数，算力信息比较粗略。

### 5. 实验数量与充分性
- **实验数量**：每个方法在每个数据集上重复10次。主要实验包含：
  - 仿真数据(S1, S2)的因果顺序和DAG推断。
  - 噪声阶段信息实验（5个噪声水平）。
  - 混合类型数据实验。
  - 伪真实数据实验。
- **充分性评价**：
  - 覆盖了不同变量数（30、200）、不同阶段数（3、20、5）、噪声标签、混合类型等场景，较全面。
  - 对比了多种主流方法（10种以上），并比较了两种剪枝策略。
  - 但未在极高维度（如>500变量）或真实工业数据（仅有伪真实数据）上验证，且未对STG-NN超参数进行系统敏感性分析。总体而言，实验设计相对充分，结论有支撑。

### 6. 论文的主要结论与发现
- OCDM在所有数据集上均优于现有方法，尤其在拓扑顺序散度（D_top）上显著更低（Simulation 1: 均值3，比最佳基线DiffAN低67%；Simulation 2: 比SCORE低90%）。
- 在因果DAG估计上，OCDM（无论CAM剪枝还是STG-NN剪枝）在所有指标（AUROC、AUPRC、SHD、SID）上领先。
- 在高维场景（200节点）下，STG-NN剪枝比CAM剪枝准确率更高且耗时更短（约1.2h vs 2.2h）。
- 对噪声阶段信息（p≤0.3）和混合类型数据，OCDM仍保持较强鲁棒性，优于基线。
- 在伪真实数据causalAssembly上，OCDM的SID优势尤为突出，表明其推断的结构更适合下游因果推理。

### 7. 优点
- **方法创新**：首次将阶段知识显式纳入顺序搜索，遵循分层DAG约束，避免反直觉的因果方向。
- **剪枝改进**：采用STG-NN进行变量选择，利用深度学习的建模能力和L0正则化，在高维场景下效率与精度均优于传统样条回归。
- **实验全面**：涵盖仿真、噪声、混合类型、伪真实数据；对比多种经典/最新方法；使用多个指标（包括因果结构评价SID）。
- **计算效率**：顺序搜索复杂度保持与DiffAN一致（O(n+d^3)），STG-NN剪枝可并行化，实际运行时间在200节点下显著快于多数基线。
- **鲁棒性**：即使阶段信息有少量错误（<30%），仍能优于基线；对混合类型数据也表现良好。

### 8. 不足与局限
- **假设限制**：依赖非线性加性高斯噪声模型，不适用于非高斯噪声或存在潜在混淆因子（因果充分性假设）的场景。
- **实验覆盖有限**：
  - 最大节点数仅为200，未测试更高维（如>500）的实际工业数据。
  - 仅使用一个伪真实数据集，缺乏多个真实场景验证。
  - 未对STG-NN的超参数（如正则化强度、网络结构）进行详细敏感性分析。
- **剪枝缺乏理论保证**：STG-NN和CAM剪枝均无恢复真实父集的保证；存在过度剪枝风险（实验显示部分真实边可能被移除）。
- **硬件限制**：算力信息不完整，未报告单次GPU训练时长，复现成本可能较高。
- **噪声信息下的性能**：当阶段标签错误率超过40%时，OCDM性能低于基线，因此要求阶段信息至少部分可靠。

（完）
