---
title: "The Table Says Otherwise: Testing LLMs with Counterfactual Relational Data"
title_zh: 表格并非如此：用反事实关系数据测试大语言模型
authors: "Xinzhi Wang, Chunwei Liu"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23667v1"
tags: ["query:causal-infer"]
score: 6.0
evidence: 用于评估LLM在表格中反事实推理的基准
tldr: 大型语言模型在表格问答中可能依赖预训练记忆而非表格内容。为此，提出ContraTable基准，包含原始与反事实配对数据库，设计214个多层级问题。实验表明，强指令微调模型在直接查找上表现良好，但多表连接和时间推理中可靠性下降，反事实准确率差距暴露模型回退到先验知识。该工作强调评估应重视对数据库的忠实度，而不只是准确率。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23667v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1850, \"height\": 370, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23667v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1843, \"height\": 583, \"label\": \"Table\"}]"
motivation: 现有表格问答评估无法区分模型是阅读表格还是依赖预训练知识，需要反事实测试来检测模型忠实度。
method: ContraTable通过修改关系属性构造反事实数据库，保持模式不变，设计单表、多表和时间推理三类214个匹配问题。
result: 模型在直接查找上准确率较高，但多表和时间推理任务准确率显著下降，原始与反事实准确率差距表明模型依赖先验知识。
conclusion: 表问答评估应权衡准确率与对数据库的忠实度，反事实测试是检测模型依赖预训练知识的有效手段。
---

## 摘要
大语言模型（LLMs）越来越多地被用于回答结构化数据上的自然语言问题。然而，当表格包含熟悉的现实世界事实时，模型是通过读取提供的数据还是通过回忆预训练期间学到的知识来回答，这一点尚不明确。这种区分对于数据库应用至关重要，因为提供的表格应该是事实的来源。在本文中，我们引入了ContraTable，一个配对的原始-反事实基准，用于评估LLMs是否将其答案基于关系表格。我们构建了两个对齐版本的基准：一个包含现实世界事实的原始数据库，以及一个保留相同模式、标识符和关系但更改了所选国家、俱乐部和球员属性的反事实数据库。我们设计了214个匹配的问题，涵盖三个层次：单表查找、多表查找和多表时间推理。对商业闭源和开源模型的实验表明，强大的指令调优模型通常能处理直接查找，但它们的可靠性随着问题需要连接、比较和时间推理而下降。原始准确率与反事实准确率之间的差距表明，当表格证据与熟悉的事实冲突时，模型可能会依赖于先验知识。这些结果表明，表格问答评估不仅应衡量准确性，还应衡量对给定数据库的忠实度。

## 速览
**TLDR**：LLM在回答表格问题时可能依赖预训练知识而非提供的数据。为此构建ContraTable基准，包含原始和反事实两个版本，设计214个跨三个层级的问题。实验表明强指令微调模型在直接查找上表现好，但在连接、比较和时间推理上可靠性下降，原版和反事实准确率差距显示模型回退到先验知识。该工作强调评估应同时关注准确性和对数据库的忠实度。 \
**Motivation**：LLM在表格问答中可能依赖预训练知识而非提供的数据，这对数据库应用至关重要。 \
**Method**：构建ContraTable基准，包含原始和反事实两个版本的数据集，设计214个跨三个层级的问题（单表查找、多表查找、多表时序推理）。 \
**Result**：强指令微调模型在直接查找上表现好，但随问题复杂度增加可靠性下降，原版与反事实准确率差距显示模型回退到先验知识。 \
**Conclusion**：表格问答评估应同时衡量准确性和对提供数据库的忠实度，反事实测试可揭示模型依赖预训练知识的问题。

---

## Abstract
Large language models (LLMs) are increasingly used to answer natural-language questions over structured data. However, when a table contains familiar real-world facts, it is unclear whether the model answers by reading the provided data or by recalling knowledge learned during pretraining. This distinction is important for database applications, where the provided tables should be the source of truth. In this paper, we introduce ContraTable, a paired original-counterfactual benchmark for evaluating whether LLMs ground their answers in relational tables. We build the benchmark with two aligned versions: an original database with real-world facts and a counterfactual database that preserves the same schemas, identifiers, and relationships while changing selected country, club, and player attributes. We design 214 matched questions across three levels: single-table lookup, multi-table lookup, and multi-table temporal reasoning. Experiments on commercial closed-source and open-source models show that strong instruction-tuned models can often handle direct lookup, but their reliability drops as questions require joins, comparison, and temporal reasoning. The gap between original and counterfactual accuracy reveals that models may fall back on prior knowledge when table evidence conflicts with familiar facts. These results suggest that table-QA evaluation should measure not only accuracy, but also faithfulness to the provided database.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- 研究动机：大语言模型（LLMs）在表格问答中既可能真正读取提供的表格数据，也可能依赖预训练阶段记忆的现实世界知识。这在数据库应用中是个严重问题——因为表格数据可能是私有的、临时的或假设性的，而用户希望模型以提供的数据为事实来源。
- 背景：现有表格问答基准大多基于真实世界实体，难以区分模型是“推理表格”还是“回忆记忆”。作者借鉴知识编辑中的反事实测试思想，提出配对原始-反事实基准，专门用于测量模型对数据库的忠实度。

## 2. 方法论
- 核心思想：构建两个版本的关系数据库——原始版（真实世界事实）和反事实版（修改选定的属性值，如国家首都、俱乐部所属国家、球员国籍等），但保持相同的模式、标识符和关系结构。问题模板相同，答案总是依据提供的数据。
- 关键技术细节：
  - 数据来源：Transfermarkt足球转会数据集（2023-2025年），涉及超过37,000名球员、400家俱乐部、80,000场比赛等。
  - 反事实设计原则：
    - 修改的属性是LLM可能已知的（国家首都、大洲、球员出生信息等），且不破坏数据库关系。
    - 分类值采用一对一链式交换，无自映射或重复替换；数值和日期字段作小幅有效扰动；结构性记录（转会、比赛事件）保持不变。
    - 使用随机种子0生成。
  - 问题层次（共214个匹配问题）：
    - Level 1 (61题)：单表直接查找（选择+投影）。
    - Level 2 (79题)：多表连接查找（通过外键连接后读取属性）。
    - Level 3 (74题)：多表时间推理（需要过滤、排序、聚合、比较等复杂操作）。
- 生成过程：由GPT-5.5草拟模板，人工审核推理路径，确定实体占位符，用确定性程序计算参考答案；每个模板对原始和反事实数据库独立实例化。

## 3. 实验设计
- 数据集 / 场景：ContraTable基准（基于足球转会数据的原始和反事实配对数据库）。
- 基准：自建，无现有同类基准直接对比。
- 对比模型：
  - 商业闭源：Gemini-3.1-Flash-Lite、GPT-5.4-Mini。
  - 开源：Gemma-4-E2B-it、Gemma-4-E4B-it、Qwen3.5-2B、Qwen3.5-9B、Llama-3.1-8B、Llama-3.1-8B-Instruct、Llama-3.2-3B-Instruct、Llama-3.2-1B-Instruct。
- 评估方式：GPT-4o作为二元评判器，根据模型生成的答案和解释，结合参考答案与支持证据判断正确性。同一流程用于原始和反事实设置。

## 4. 资源与算力
- 论文未明确说明评估所用的 GPU 型号、数量或运行时长。所有实验均直接使用已有的预训练模型进行推理（非训练），因此未涉及训练算力需求。

## 5. 实验数量与充分性
- 实验数量：214个问题 × 2个版本（原始/反事实） × 多个模型（约10个变体）。结果报告在表2中，分三个层级显示准确率。
- 充分性评估：
  - 优点：覆盖了不同规模、不同指令调优的有无的模型，层次化问题设计有助于诊断忠实度随难度的变化。
  - 不足：仅使用单一领域（足球）数据，泛化性受限；缺乏对prompt策略（如few-shot、chain-of-thought）或上下文长度对忠实度影响的探究；没有消融实验分析不同反事实修改类型的影响；评估依赖GPT-4o，可能引入评判偏差（但论文称其与人工一致性较好，但未给出具体验证）。
- 总体：实验设计合理且具有洞察力，但规模较小，属于初步验证性研究。

## 6. 主要结论与发现
- 强指令调优模型在直接查找（Level 1）上表现优秀，原始与反事实准确率差距很小（如GPT-5.4-Mini差距为0）。
- 随着问题复杂度增加（Level 2和3），反事实差距显著扩大。例如GPT-5.4-Mini在Level 2差距10.13%，Level 3差距18.92%。这说明模型在复杂推理中更容易回退到预训练知识而不忠实于表格。
- 更强的模型（如Gemini-3.1-Flash-Lite）在Level 3仍保持较高原始和反事实准确性，但差距依然存在。
- 指令调优大大提升了原始数据上的准确性（Llama-3.1-8B从5.14%升至68.22%），但并未消除反事实差距，说明指令调优不足以克服先验知识冲突。
- 模型在解释中常引用现实世界知识而非表格内容，尤其在第3级问题中明显。

## 7. 优点
- 方法创新：首次将反事实配对设计应用于关系数据库的忠实度评估，而非孤立的文本事实。
- 问题分层清晰：与数据库操作（select-project-join-temporal）对应，便于诊断模型在何种推理环节失效。
- 评估全面：涵盖商业和开源、不同规模和是否指令调优的模型，揭示了普遍现象。
- 设计严谨：反事实数据库通过链式交换保持内部一致性，确保测试的是矛盾证据而非随机噪声。
- 未来导向：强调应同时关注准确性和忠实度，为后续基准设计提供了新思路。

## 8. 不足与局限
- 领域局限性：仅基于足球转会数据，结论是否适用于其他领域（如企业数据库、医疗数据）尚待验证。
- 问题数量有限（214个），可能无法充分覆盖所有推理模式。
- 评估依赖GPT-4o作为评判器，存在评判偏见风险（尤其在反事实场景下，可能因一致性错觉而误判）。
- 未探索prompt工程的影响（如少量示例、思维链是否能缓解反事实差距）。
- 未进行细粒度错误分析（如区分是检索错误还是推理错误）。
- 仅聚焦于直接LLM回答，未比较text-to-SQL pipeline的忠实度与效率。
- 反事实修改类型仅限于属性值，未涉及表结构或关系的变化。

（完）
