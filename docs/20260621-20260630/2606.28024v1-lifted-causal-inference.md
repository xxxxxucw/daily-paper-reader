---
title: Lifted Causal Inference
title_zh: 提升因果推断
authors: "Malte Luttermann, Tanya Braun, Ralf Möller, Marcel Gehrke"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28024v1"
tags: ["query:two-sided"]
score: 9.0
evidence: 提出了提升因果推断算法以高效计算因果效应
tldr: 因果推断在关系域中计算效率低。本文提出参量化因果因子图（PCFG）以融入因果知识，并给出干预的形式语义。在此基础上提出提升因果推断（LCI）算法，在提升层面计算因果效应，大幅加速推断。进一步扩展至部分有向PCFG，处理部分因果知识，扩展了适用范围。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28024v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1306, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28024v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 461, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28024v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 459, \"height\": 350, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28024v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 925, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28024v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 802, \"height\": 283, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28024v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1337, \"height\": 309, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28024v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1355, \"height\": 546, \"label\": \"Table\"}]"
motivation: 现有提升推理方法主要针对概率查询，未处理因果关系。需要将提升技术扩展到因果推断以提高效率。
method: 提出PCFG模型并定义干预语义，设计LCI算法利用对象间的不可区分性在提升层面计算因果效应。进一步提出PD-PCFG处理部分因果知识。
result: LCI算法在关系域中显著加速因果效应计算，优于命题因果贝叶斯网络推断。
conclusion: 提升因果推断能高效处理关系域中的因果查询，并适应部分因果知识，扩展了因果推断的应用场景。
---

## 摘要
提升推理利用概率图模型中的不可区分性，通过使用不可区分对象的代表来加速查询回答，同时保持精确答案。在本文中，我们展示了如何将提升应用于关系域中因果效应的高效计算。具体而言，我们引入参数化因果因子图（PCFGs）以在提升模型中融入因果知识，并给出其中干预的形式语义。我们进一步提出了提升因果推断（LCI）算法，在提升层面上计算因果效应，从而相对于命题推断（例如因果贝叶斯网络）大幅加速因果推断。此外，我们提出了部分有向参数化因果因子图（PD-PCFGs）作为PCFGs的推广，以处理部分因果知识，并将LCI扩展为在PD-PCFG中执行提升因果推断，从而将提升因果推断的适用性扩展到更广泛的模型，这些模型需要较少的关于因果关系的先验知识。

## 速览
**TLDR**：概率图模型中的提升推理利用对象不可区分性加速精确查询，但未有效支持因果推断。本文提出参数化因果因子图(PCFG)建模因果结构，并设计提升因果推断(LCI)算法在保持精确性的同时大幅提升计算效率。进一步推广到部分有向PCFG，使提升因果推断适用于仅部分已知因果关系的场景。该工作将提升推理的优势引入因果推断，降低了先验因果知识的依赖。 \
**Motivation**：现有提升推理方法未涉及因果关系，而关系域中因果效应计算效率低下，亟需提升级方法。 \
**Method**：引入PCFG与PD-PCFG模型，定义干预语义，并设计LCI算法在提升层执行因果推断。 \
**Result**：LCI相比命题因果推断大幅加速，且PD-PCFG扩展支持部分因果知识，提升实用性。 \
**Conclusion**：将提升推理扩展到因果推断，提高效率并减少对完整因果知识的依赖。

---

## Abstract
Lifted inference exploits indistinguishabilities in probabilistic graphical models by using a representative for indistinguishable objects, thereby speeding up query answering while maintaining exact answers. In this article, we show how lifting can be applied to efficiently compute causal effects in relational domains. More specifically, we introduce parametric causal factor graphs (PCFGs) to incorporate causal knowledge in lifted models and give a formal semantics of interventions therein. We further present the Lifted Causal Inference (LCI) algorithm to compute causal effects on a lifted level, thereby drastically speeding up causal inference compared to propositional inference, e.g., in causal Bayesian networks. In addition, we present partially directed parametric causal factor graphs (PD-PCFGs) as a generalisation of PCFGs to handle partial causal knowledge and extend LCI to perform lifted causal inference in a PD-PCFG, thereby extending the applicability of lifted causal inference to a broader range of models requiring less prior knowledge about causal relationships.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：在关系域（如包含多个员工、公司等对象的场景）中，传统因果推断（如因果贝叶斯网络）需要将所有对象实例化（grounding），导致计算量随对象数量指数增长。与此同时，概率图模型中的“提升推理”（lifted inference）能够通过利用不可区分对象的代表来加速查询，但尚未被应用于因果效应计算。
- **核心问题**：如何将提升推理扩展到关系域中的因果推断，从而在不牺牲准确性的前提下大幅加速干预效应的计算，并处理部分已知的因果知识。
- **整体含义**：本文首次系统性地将提升技术引入因果推断，提出了参数化因果因子图（PCFG）及其变体部分有向PCFG（PD-PCFG），并设计了对应的提升因果推断算法（LCI和ELCI），显著降低了关系域中因果效应计算的复杂度。

## 2. 论文提出的方法论

### 核心思想
- 利用对象间的不可区分性，通过参数化随机变量（PRV）和参数因子（parfactor）在提升层面表达模型，避免逐一处理每个对象。
- 在提升模型中定义干预（do‑operation）的语义，并通过模型修改（分裂参数因子、改变父因子）将干预效应转化为概率查询，从而调用现有的提升概率推理算法（如LVE）来高效计算。

### 关键技术细节
- **参数化因果因子图（PCFG）**：将因果知识（有向边）融入参数化因子图，每个参数因子节点至多一条出边（表示因果方向），PRV可带逻辑变量和约束。
- **干预的形式语义**：干预 `do(R'=r')` 将所有指向R'的父因子替换为指示函数（当R'取指定值时值为1，否则0），保持其他因子不变。
- **LCI算法**（算法1）：
  1. 基于每个干预变量分裂相关参数因子，使被干预的实例从原组中分离。
  2. 修改所有指向干预变量的父参数因子，使其仅映射到干预值（其他值映射为0）。
  3. 调用提升变量消除（LVE）计算修改后模型中的条件概率 `P(Q | R'=r')`，该结果等价于原模型中的干预查询。
- **部分有向PCFG（PD-PCFG）**：允许参数因子节点无出边（表示因果方向未知），通过一致扩展（consistent extension）概念表示所有可能的全有向PCFG。
- **ELCI算法**（算法2）：
  1. 枚举干预变量所有可能的父集（其无向邻居需形成团以避免改变条件独立）。
  2. 对每种合法父集，将相应无向边朝向干预变量定向，得到扩展并应用类似LCI的步骤。
  3. 返回所有可能结果的集合（若存在唯一结果则唯一确定）。

### 公式说明
- 截断乘积公式（g‑formula）：`P(Q | do(R'=r')) = Σ_{others} ∏_{非干预变量} P(child | parents)`。
- 定理1：当所有参数因子表示条件概率时，干预等价于在修改后的模型中进行条件概率查询。

## 3. 实验设计

- **使用场景**：一个关于员工能力（Com）、公司收入（Rev）和员工薪资（Sal）的关系域模型（PCFG）。域大小为员工数量 d ∈ {8, 16, 32, 64, 128, 256, 512, 1024, 2048, 4096}。
- **数据集**：生成数据（未使用真实数据集），模型参数随机设定，但保证三个模型（PCFG、CFG、CBN）编码相同的联合分布。
- **Benchmark**：将LCI（内部调用LVE）与命题版本上的变量消除（VE）进行对比：
  - VE on 因果贝叶斯网络（CBN）
  - VE on 因果因子图（CFG）
- **对比方法**：仅对比了VE在两种命题模型上的运行时间，未与其他提升因果方法（因文献中未有）或结构学习方法比较。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长**。实验仅测量CPU上的运行时间（毫秒），未提及硬件配置。因此无法评估算力需求。

## 5. 实验数量与充分性

- **实验组数**：一组对比实验，改变域大小（10个点），每个点可能运行多次取平均（但未明确）。
- **充分性**：
  - 仅针对一个简单模型（3个PRV、3个参数因子），没有涉及更复杂的图结构（如多层、多关系）。
  - 缺少消融实验（例如不同干预数量、不同分裂策略的影响）。
  - 未与其他可能的提升因果方法（如基于关系因果模型改编的方法）进行比较。
  - 结果客观展示了提升推理在规模增长时的优势，但充分性有限，仅作为概念验证。

## 6. 论文的主要结论与发现

- LCI算法能在提升层面高效计算因果效应，运行时间随域大小增长缓慢（多项式级），而命题VE呈指数级增长，证实了提升因果推断的巨大加速效果。
- 当存在部分未知因果方向时，ELCI通过枚举所有可能父集来处理，且若干预变量无未知父集则结果唯一。
- 该方法可推广至更复杂的决策问题（如参数化决策模型），为关系域中的因果决策提供了高效工具。

## 7. 优点

- **方法创新性**：首次将提升推理严格应用于因果推断，提出PCFG和PD-PCFG两种形式化模型。
- **理论贡献**：给出了干预在提升模型中的语义，并证明了通过模型修改可将干预转化为概率查询（定理1），以及在部分有向情况下结果唯一的条件（定理4）。
- **算法简洁高效**：LCI直接利用现有的LVE算法，无需重新发明轮子，实现成本低。
- **实验对比清晰**：直观展示了提升推理在规模扩展上的绝对优势，对实践有指导价值。

## 8. 不足与局限

- **实验覆盖不足**：仅用单一小型模型，缺乏对多关系、复杂结构、不同干预场景的验证，结论的泛化性存疑。
- **未处理隐藏混淆变量**：假设因果充分性（所有共同原因在模型中），未讨论存在隐藏混淆时的识别问题。
- **未考虑非域可提升模型**：仅适用于“域可提升”（domain‑liftable）的模型，实际中某些模型可能无法通过LVE多项式求解。
- **缺少与现有工作的定量比较**：虽然声称现有工作都是命题级，但未与任何其他关系域因果推断方法（如基于关系因果模型的近似方法）进行性能对比。
- **PD-PCFG中枚举所有合法父集**：在最坏情况下可能指数级（但文中指出在提升层面仅受邻居数量影响，而非所有对象），但实际中当无向边多时仍可能组合爆炸。

（完）
