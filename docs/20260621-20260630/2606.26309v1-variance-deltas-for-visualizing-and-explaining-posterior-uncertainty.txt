Title: Variance Deltas for Visualizing and Explaining Posterior Uncertainty

URL Source: https://arxiv.org/pdf/2606.26309v1

Published Time: Fri, 26 Jun 2026 00:09:09 GMT

Number of Pages: 57

Markdown Content:
# Variance Deltas for Visualizing and Explaining Posterior Uncertainty 

## Collin Cademartori Department of Statistical Sciences, Wake Forest University cademac@wfu.edu June 26, 2026 

Abstract 

In observational settings, where the data generating process and possibly the sample size are not controlled, it is typically impossible to guarantee a priori that quantities of interest will be estimated with sufficient precision. However, even when the data do not determine the quantities of interest, they may still allow determination of what is missing – unobserved information which, if observed, would meaningfully reduce uncertainty. We propose an interactive visualization system, termed variance deltas, to enable the discovery of such missing information from a Bayesian posterior distribution. This system, which we provide as a software package, represents missing information as subsets of unobserved model quantities, organized into a tree based on how well each subset explains uncertainty about the quantity of interest. This system both automates the construction of candidate subsets from minimal user input and implements interactive operations for the division and combination of subsets, allowing the efficient discovery of interesting and useful explanations. We demonstrate this system by using it to discover nonobvious explanations of uncertainty for (1) a treatment effect parameter in a simulated causal inference problem and (2) a population proportion in a forecasting model of real polling data with many sources of bias. 

Keywords: Uncertainty Quantification, Graphical Models, Bayesian Statistics, Sensitivity Analysis 1

> arXiv:2606.26309v1 [stat.ME] 24 Jun 2026

# 1 Introduction 

When modeling observational data where the data generating process is both uncontrolled and complex, statistical uncertainty about quantities of interest may be too great to permit any substantive inference (Greenland 2009, 2005). Common causes of this symptom include having insufficient amounts of data, having data of the wrong type, and having a nonidentified nuisance parameter on which a quantity of interest depends. Remedies may include collecting more and/or different data, bringing prior information to bear, or rethinking the substantive goals of the analysis. Finding the correct explanation for such uncertainty is often necessary to properly mitigate or accommodate it. But discovering such explanations typically requires manual analysis of the model and substantial guesswork. Furthermore, existing summarization and visualization tools often do not contain the information that we need. In Bayesian inference, Gelman et al. (2020) point out that the usual histogram- and scatter plot-based posterior visualizations “do not fully capture the multiple levels of variation and uncertainty in our inferences.” We propose an interactive visualization system to structure and partially automate the search for such explanations in the Bayesian setting. The user of this system only needs to specify a small set of initial potential explanations, from which further explanations are automatically generated and organized into a tree structure. The branches of these trees may then be interactively divided, extended, and merged to produce new explanations. In these trees, we think of information as flowing from the root to the leaf explanations, with each successive explanation weaker than the last. Consequently, we term these visualizations variance deltas, in analogy with the tree-like flow of a river delta. We implement this system in a software package, variance-deltas , which we use to produce all figures. 21.1 Quantifying Explanations 

To visualize uncertainty explanations, we must first measure explanatory power. The literature on explainable and interpretable machine learning has proposed many measures for explaining model predictions (Hullman et al. 2025, Linardatos et al. 2021). The popular SHAP method, for instance, essentially performs a sensitivity analysis of predictions with respect to the predictive features (Lundberg & Lee 2017, Owen 2014). We follow a similar approach for explaining uncertainty. We begin with a posterior density p(ψ | y) for a vector of unknowns ψ ∈ RD given observed data y ∈ RN . Let I∗ = {1, 2, . . . D }. For any I = {i1, i 2, . . . , i k} ⊆ I∗, define the vector 

ψI = ( ψi1 , ψ i2 , . . . , ψ ik ) ∈ Rk.

The total index set I∗ is partitioned into two disjoint subsets: 

I∗ = Iπ ∪ I˜y, Iπ ∈ RP , I ˜y ∈ RM ,

where P + M = D. The unknowns ψIπ are the model parameters, and the unknowns ψI˜y

are unobserved data which could hypothetically be observed. For notational simplicity, we will write π = ψIπ and ˜y = ψI˜y

. We also isolate a distinguished parameter index iθ ∈ Iπ

which corresponds to a scalar quantity of interest θ, i.e. θ = ψiθ . (If there are multiple scalar quantities of interest, we assume that they are taken one at a time.) In practice, we often begin with a posterior p(π | y) over just the parameters. When 

dim ( ˜y) > 0, the probability model must then be extended over this additional data, as in Bayesian models of missing data (e.g. for imputation) or replicated data (e.g. for posterior predictive model checking). In some cases, this extension may require additional parameters as well, such as observation-level random effects, in which case these are also included in π.3Potential explanations of our uncertainty about quantity of interest θ are represented by vectors ψI with I ⊆ I∗ \ { iθ}. The degree to which ψI explains uncertainty about θ – the explanatory power of ψI – can be quantified by the expected posterior variance of θ given 

ψI , as a fraction of the overall posterior variance: 

U 2 

> I

= E [Var ( θ | y, ψ I ) | y]Var ( θ | y) . (1) We refer to U 2 

> I

as the uncertainty index of ψI . By the law of total variance, 0 ≤ U 2 

> I

≤ 1,and so ψI is a powerful explanation if U 2 

> I

≪ 1. We can also write the uncertainty index as 

U 2 

> I

= 1 − Var [ E (θ | y, ψ I ) | y]Var ( θ | y) . (2) The rightmost term is a form of the Sobol’ index (Sobol’ 1990), which measures the sensitivity of the conditional mean E (θ | y, ψ I ) to ψI . Thus, in our framework, locating powerful explanations ψI involves analyzing the sensitivity of θ to the components of ψ.

## 1.2 Toy Example: Normal-Normal Model 

We introduce variance deltas with a simple hierarchical model. Suppose that we obtain N

observations from each of J groups, yielding data y = (yij )N,J i,j =1 . Consider the following model: 

y1j , . . . , y N j iid 

∼ normal( ϕj , σ j ) for 1 ≤ j ≤ J, (3) 

ϕ1, . . . , ϕ Jiid 

∼ normal( θ, τ ), θ ∼ normal(0 , 1) . (4) We take the superpopulation mean θ to be our quantity of interest. We treat τ and the 

σj as fixed hyperparameters for simplicity. For this example, we simulate data y from (3) 4τ = 2 −2

σ1 = 2 −1 σ2 = 2 0 σ3 = 2 1

ϕ1 = −1/2 ( ∗) ϕ2 = 0 ( ∗) ϕ3 = 1 /2 ( ∗)

Table 1: Hyperparameters for simulating data and posterior sampling from the model (3) and (4). Quantities marked with (∗) are treated as random for posterior sampling. with N = 2 observations from each of J = 3 groups. We then generate samples from the posterior p(ϕ1, ϕ 2, ϕ 3, θ | y). Table 1 summarizes the choices of hyperparameters for these steps. The parameters π consist of the overall mean θ and group-level means ϕ = (ϕ1, ϕ 2, ϕ 3). The hypothetical data ˜y = (˜yij )N,J i,j =1 are then sampled from (3) independently of y. Introducing these hypothetical data points allows us to understand the impact of the sample size N

on our uncertainty for θ. Overall, our unknowns are ψ = [π, ˜y], where [u, v ] denotes the concatenation of the vectors u and v.A variance delta is a tree rooted at the quantity of interest θ with each non-root node representing a possible explanation ψI with I ⊆ I∗ \ { iθ}. Figure 1 displays one possible variance delta for θ. We interpret this tree as follows: 

• Each node is labelled with the corresponding model quantities in ψI . If a quantity is multidimensional (e.g. a vector or matrix), then the indices of included components are indicated next to the quantity name. For instance, the group-level mean ϕ1, which is the first component of the vector ϕ, is indicated with the label .

• The leaves correspond to the hypothetical data from each group ˜yj = ( ˜yij )Ni=1 .

• The paths θ → ϕi → ˜yj reflect the hierarchical structure of the model. In particular, each path is a Markov chain in that θ ⊥⊥ ˜yj | ϕj .

• The horizontal position of a node containing explanation ψI is given by the root 5uncertainty index UI =

> √

U 2 

> I

(where the square root is taken so that UI is on the standard deviation scale). 

Figure 1: A variance delta constructed for the toy model given in (3) and (4). This first tree shows that all three groups exert similar influence on our uncertainty about 

θ. In particular, exact knowledge of any one ϕj cannot be expected to reduce posterior uncertainty about θ to less than ≈ 60% of its original level. This raises the question: how much more information would be obtained by learning all group-level means at once. This question is answered by adding a node to our variance delta corresponding to the combined group-level means ϕ. Figure 2 displays the result. Learning the entire vector ϕ

reduces expected posterior uncertainty to about 40% of its current level – a substantial improvement. However, to reduce our uncertainty by more than half, this tree also suggests that we would need data from additional groups. 

Figure 2: The tree from Figure 1 with a new node added for the set ϕ = ( ϕ1, ϕ 2, ϕ 3)..This example demonstrates how variance deltas can both answer initial questions (“Which group is most important for our uncertainty about θ?”) and redirect attention to more 6important questions (“How much can we learn using data from only three groups?”). In the more complex models of our two later examples, variance deltas are used to discover explanations that are both more useful and harder to anticipate. 

## 1.3 Related Work 

This work follows a growing literature on Bayesian workflow, which addresses all stages of the statistical modeling pipeline in an integrated manner. Other applications of static or interactive graphics to problems in Bayesian workflow include the shinystan and bayesplot packages (Gabry & Veen 2022, Gabry & Mahr 2025) – tools for plotting MCMC outputs and diagnostics. Outside the Bayesian setting, graph-based visualizations have been used to reveal model structure and variable importance (Champion & Elkan 2017, Inglis et al. 2021). Previous work has also applied tools from sensitivity analysis to Bayesian workflow problems. Much of this work studies the sensitivity of inference to non-random aspects of the analysis, such as the prior specification (Roos et al. 2015, Kallioinen et al. 2023) and the data sample (Nguyen et al. 2024). Decision-theoretic applications, on the other hand, analyze sensitivity with respect to random, modeled quantities, as we do here. For instance, the expected value of partial perfect information is defined as the reduction in expected loss when a decision is made with and without conditioning on some unknown quantity (Felli & Hazen 1998). When the loss is quadratic, this is closely related to our uncertainty index (1) (Jackson et al. 2019). Whereas such value of information analyses generally assume that we know which quantities we want to condition on in advance, this work is concerned with discovering interesting sets of unknowns to analyze. The structure of our proposed visualizations is also inspired by John Tukey’s early work on interactive statistical graphics (Friedman & Stuetzle 2002). In moving beyond direct 7visualization of posterior samples, we are influenced by Tukey’s suggestion that, to invent useful new forms of visualization, we should “find new phenomena of potential interest, and then learn how to make displays that will be likely to reveal them” (Tukey 1990). 

## 1.4 Notation 

Whenever S is a set, P(S) denotes the power set of S. We abuse notation slightly throughout this paper, using the same letter (e.g. ψ) for both a random variable (as in Var (ψ)) and a possible value of that random variable (as in the density expression p(ψ)). Ambiguities are resolved by context or otherwise explicitly disambiguated. We assume that model parameters ψ and data y are vectors. Occasionally, we treat some components of the parameters and/or data as matrices, which we write with boldface characters. When we think of such a matrix as part of the vectors ψ or y, we assume the matrix is “flattened” into a vector (e.g. in column-major order, though this will not be specified). The ith row or column of a matrix A is denoted Ai· and A·i, respectively. 

## 1.5 Outline 

The remainder of this paper is organized as follows. Section 2 defines variance deltas, presents an algorithm for generating them semi-automatically, and introduces a set of interactive operations for transforming them. The next two sections apply variance deltas to find nontrivial explanations of uncertainty. Section 3 applies our method to a simulated causal inference problem using panel data, where the quantity of interest is a treatment effect. Then, in Section 4, we examine a polling model applied to real data from the 2016 U.S. presidential election, where the quantity of interest is the Democratic share of the electorate on election day. Section 5 presents concluding thoughts. 82 Variance Deltas 

Variance deltas are constructed from two components: the underlying tree which encodes conditional independence relationships in the model and a mapping from tree nodes to estimates of the corresponding uncertainty indices. The formal presentation of variance deltas is divded into four parts: (i) the definition of the distributary trees underlying them, (ii) a method for estimating the uncertainty indices needed to plot them, (iii) an algorithm for semi-automatically constructing them, and (iv) the interactive operations that transform and extend them. 

## 2.1 Distributary Trees 

A variance delta is a visualization of a tree rooted at the quantity of interest θ where the non-root nodes represent possible explanations ψI with I ⊆ I∗ \ { iθ}. Maintaining our hydrological analogy, we term these distributary trees 1. The paths in a distributary tree are Markov chains (in a sense which we define below), and thus represent a diminishing flow of information from the start to the end node. 

Definition 1 (Distributary Trees) . Let T = ( N , P ) be a tree, where 

• N is the set of tree nodes. Each node ν ∈ N is associated with a subset I(ν) ⊆ I∗.To simplify notation, we write ψ(ν) for ψI(ν), the unknowns associated to ν.

• P : N \ { ν0} → N maps each non-root node to its unique parent, where ν0 is the root. The undirected graph with vertices N and edges {{ P (ν), ν }} ν∈N \{ ν0} must be acyclic and connected, so that P yields a well-defined tree. Then T is a distributary tree for quantity of interest θ if it satisfies the following constraints. 1. ( Rooted at θ) We must have I(ν0) = {iθ}.

> 1In a river delta, a distributary is an outflowing branch of the river which does not rejoin the main stream of the river.

92. (Markov Paths ) A path is a sequence (ν1, . . . , ν k) with νi = P (νi+1 ) for 1 ≤ i ≤ k − 1.Every path with k ≥ 2 must be a Markov chain in the sense that 

ψ(ν≤i) ⊥⊥ ψ(ν>i ) | ψ(νi) for all 1 ≤ i ≤ k − 1, (5) where ψ(ν≤i) = ψI≤i with I≤i = ∪ij=1 I(νj ) and ψj>i = ψI>i with I>i = ∪kj=i+1 I(νj ).3. ( Unique Children ) For any ν, ν ′ ∈ N , if P (ν) = P (ν′), then I(ν)̸ = I(ν′).As we will see momentarily, constraints 1 and 2 above are the basic properties that make distributary trees useful. Constraint 3 implies that distinct rooted paths in T correspond to distinct Markov chains. Note also that, subject to constraint 3, I(·) : N → P (I∗) need not be injective, so the same model quantities ψI can occur at different nodes of the same distributary tree. 

## 2.2 Visualizing and Estimating Uncertainty 

When we plot distributary trees in a variance delta, the horizontal position of each node ν

is determined by an estimate of its root uncertainty index UI(ν), as in Figure 1. It follows from Definition 1 that UI(ν) decreases from child to parent nodes. 

Lemma 1. Let T be a distributary tree . Then U 2 

> I(ν)

decreases from child to parent nodes in T , and U 2 

> I(ν0)

= 0 , where ν0 is the root of T .Proof. It follows directly from the definition (1) that U 2 

> I(ν0)

= U 2  

> {iθ}

= 0 . Next, suppose 

ν = P (ν′). Then by passing successively from child to parent nodes, we can construct a path beginning at ν0 and terminating at ν′ which contains ν. Constraint 2 then implies 10 that θ → ψ(ν) → ψ(ν′) is a Markov chain, and the law of total variance gives 

EVar ( θ | ψ(ν′)) = EVar ( θ | ψ(ν), ψ (ν′)) + EVar ( E [θ | ψ(ν), ψ (ν′)] | ψ(ν′)) 

≥ EVar ( θ | ψ(ν), ψ (ν′)) = EVar ( θ | ψ(ν)) ,

where the final equality follows by the conditional independence of ψ(ν′) and θ given ψ(ν).Plugging the first and last terms above into (1) immediately yields U 2 

> I(ν′)

≥ U 2

> I(ν)

.When working with approximate posterior samples S = {ψ(s)}Ss=1 (as produced by any MCMC algorithm), we estimate the denominator of (1) by the sample variance var ( S) =

> 1
> S−1
> ∑Ss=1

(θ(s) − ¯θ)2. To estimate the numerator of (1) , we use a data splitting strategy. Assuming S is even, we use the first S/ 2 samples SE to construct an estimator ˆEI (SE , ·)

of the conditional expectation E [θ | ψI = ·, y ]. Then, using the last S/ 2 samples SV , we estimate the expected conditional variance E [Var ( θ | y, ψ I )] as 

cvar I, ˆE (S) = 2

S

> ∑
> ψ(s)∈S V
> (

θ(s) − ˆEI

> (

SE , ψ (s)

> I
> )) 2

. (6) With these, our estimate of the uncertainty index (1) is then just 

ˆU 2 

> I

= cvar I, ˆE (S)var ( S) . (7) Splitting the data in this way eliminates overfitting bias in the conditional variance estimate (6) . In particular, if the expected mean squared error (MSE) of ˆE vanishes asymptotically, then (6) is asymptotically unbiased for the conditional variance (proof in supplementary materials). In most situations, an ˆE with vanishing MSE will not be available, and (6) will be biased upward by the expected MSE. However, we find in our experiments that, as long 11 as S is large enough that the variance of ˆE small, the performance of (7) is sufficient. The construction (7) still requires us to choose an estimator ˆE. Throughout our examples, we take ˆEI (SE , ·) to be a random forest regressor, chosen primarily for its ability to handle nonlinearities and interactions without explicit modeling. However, we emphasize that (7) can be used with any choice of ˆE.

## 2.3 Constructing Distributary Trees 

Constructing useful distributary trees faces two difficulties, which we address in this section. 1. Constructing distributary trees manually requires the user to know both (i) which unknowns ψI should be selected for the nodes of the tree and (ii) how to connect the nodes so that the Markov condition (5) is satisfied for every path in the tree. We resolve this problem by defining an algorithm to construct distributary trees from a specification of just the root node (the quantity of interest) and the leaf nodes. 2. For any given model, the root and leaf nodes alone usually do not determine a unique valid distributary tree. This is an instance of a more general problem that arises in sensitivity analysis: the need to constrain the subsets of quantities with respect to which the analysis will be performed (Constantine & Diaz 2016, Jacques et al. 2006, Iooss & Lemaître 2015). Therefore, we need an additional criterion that allows us to choose between multiple options for constructing the tree at any given stage of the algorithm. For this, we introduce a measure of explanatory specificity. To resolve these difficulties, we need the concepts of factorizations and factor graphs. 

Definition 2 (Factorization) . Let h(ψ) be a nonnegative function. Then h factorizes over 12 F ⊆ P (I∗) (equivalently, F is a factorization of h), if, for functions fI : R|I| → R+,

h(ψ) = ∏

> I∈F

fI (ψI ). (8) Suppose h(ψ) = p(ψ | y), the posterior density for a joint model p(ψ, y ), and that p factorizes over F. Then F is a Bayesian factorization if F = FL ∪ F P with FL ∩ F P = ∅, and where: 1. The prior p(π) factorizes over FP .2. The likelihood (for observed and hypothetical data) p(y, ˜y | π) factorizes over FL.A factorization F can be conveniently represented using an undirected graph. 

Definition 3 (Undirected Graph) . An undirected graph M is a pair (VM , E M ), where VM

is a set of vertices and EM is a set of undirected edges. Specifically: 

EM ⊆ {{ x, y } | x, y ∈ VM , x ̸ = y} .

An undirected graph is bipartite if the vertices can be partitioned as VM = V1 ∪ V2, where 

V1 ∩ V1 = ∅, and 

EM ⊆ {{ x, y } | x ∈ V1, y ∈ V2} ,

i.e. all edges connect a vertex in V1 with a vertex in V2.

Definition 4 (Factor Graph) . The factor graph GF corresponding to a factorization F is defined as 

VGF = I∗ ∪ F , EGF =

> {

{i, I } 

> ∣∣∣∣

I ∈ F , i ∈ I

> }

.

In words, GF is a bipartite graph with a vertex for every factor I ∈ F and index i ∈ I∗,with an edge between i and I if and only if i ∈ I.13 2.3.1 Measuring the Specificity of an Explanation 

If we want to minimize U 2 

> I

, the best we can do is to observe all unknown quantities other than θ. In other words, we have 

I∗ \ { iθ} ∈ arg min  

> I⊆I∗\{ iθ}

U 2 

> I

. (9) Despite its maximal explanatory power, ψI∗\{ θ} cannot tell us which quantities specifically 

explain our uncertainty about θ and is thus not a useful explanation. To generate trees containing potentially useful explanations, we need to exclude explanations that are overly non-specific. This also provides a useful criterion to resolve the second difficulty above: When faced with multiple choices for the next node in a tree, we can always choose the node ν with more specific explanation ψ(ν). We quantify specificity in terms of the distance between the unknowns ψ(ν) and likelihood factors in the posterior factor graph. Let F be a Bayesian factorization of the posterior density p(ψ | y) with GF the corresponding factor graph. We start with a few desiderata: 1. If I′ ⊆ I, then ψI′ must be at least as specific as ψI .2. An explanation ψI is maximally specific if the quantities in ψI are involved in exactly one likelihood factor, i.e. if there is exactly one I′ ∈ F L with I ⊆ I′. These explanations point to a particular quantity of (hypothetically) observable information which is insufficient or missing. 3. Conversely, ψI is minimally specific if ψI is equally involved in modeling every data point, as with a global mean or scale parameter. Such explanations do not suggest particular remedies, since any additional data could be relevant to learning ψI .Our specificity measure interpolates these extremes by measuring the distance between 14 explanations ψI and likelihood factors in GF . A path in GF is any sequence of (distinct) vertices v = ( v1, v 2, . . . , v k) where (vj , v j+1 ) are joined by an edge for all 1 ≤ j ≤ k − 1. Let 

Pk(GF ) be the set of all paths in GF of length k. For each i ∈ I∗, the depth dF (i) of i, is defined as the length of the shortest path from i to a likelihood factor. Symbolically: 

d(i) = min  

> f∈F L

d(i, f ), where d(i, f ) = min 

> {

k 

> ∣∣∣∣

∃v ∈ P k(GF ) s.t. v 1 = i, v k = f

> }

. (10) Similarly, the set of most relevant likelihood factors for i is defined as 

FL(i) = {f ∈ F L | d(i, f ) = d(i)} . (11) Finally, for any index set I ⊆ I∗, the likelihood specificity is defined as 

Slik (I) = 1

|F L|

> ∣∣∣∣∣⋃
> i∈I

FL(i) 

> ∣∣∣∣∣

, (12) In words, sets I with low Slik (I) index unknowns ψI which are most relevant to a relatively small set of likelihood factors. Furthermore, it is easy to see that likelihood specificity satisfies the above three desiderata. Note in particular that Slik (I) is minimized when ψI

occurs in exactly one likelihood factor, and is maximized when some ψi with i ∈ I is a global quantity, in the sense that d(f, i ) = d(i) for all f ∈ F L.

2.3.2 Generating Distributary Trees From Leaves 

Given our measure of specificity Slik , we may now define a procedure for generating distributary trees. To do this, we need to leverage information about the dependencies between model quantities, which can be derived from the factor graph GF . Specifically, for 15 a factor graph GF , define the Markov graph MF with vertices VMF = I∗ and edges 

EMF =

> {

{i1, i 2} 

> ∣∣∣∣

∃I ∈ F s.t. i1, i 2 ∈ I

> }

.

In other words, we connect two indices in MF if they belong to a common factor in F.We henceforth assume that the factor graph GF is connected, which implies also that the Markov graph MF is connected. If MF were not connected, then we could partition I∗ as 

I∗ = I1 ∪ I2 with I1 and I2 disconnected in MF and with iθ ∈ I1. It is then easy to show that ψI1 ⊥⊥ ψI2 , and hence U 2 

> I2

= 1 . In other words, ψI2 is irrelevant to θ, so if MF were disconnected, we could replace MF by the connected component of iθ and proceed without loss of generality. A subset I′ ⊆ I∗ separates disjoint sets I1, I 2 ⊆ I∗ \ I′ if every path from i ∈ I1 to j ∈ I2 in 

MF includes some k ∈ I′. If p(ψ | y) factorizes over F, then standard results for graphical models tell us that if I′ separates I1 and I2 in MF , then ψI1 ⊥⊥ ψI2 | ψI′ . In other words, we can identify conditional independence relationships in p(ψ | y) by locating separating sets in MF . We refer the reader to Maathuis et al. (2020) for details on graphical models. This suggests a procedure for constructing a distributary tree from a root node ν0 and a set of leaf nodes {ˇνi}li=1 . In particular, by iteratively searching MF for separating sets, we can construct Markov chains starting at θ = ψ(ν0) and terminating at the leaf quantities ψ( ˇνi).We first sketch this procedure for the first leaf ˇν1, before giving a formal presentation in Algorithm 1. At each iteration k ≥ 1, we start with a chain I = ( I1, I 2, . . . , I k+1 ) such that 

ψ(I) def 

= (

ψI1 , ψ I2 , . . . , ψ Ik+1 

> )

satisfies the Markov condition (5), and such that ψI1 = θ and 

ψIk+1 = ψ( ˇν1). We also specify an insertion point 1 ≤ j ≤ k. We then attempt to insert a set I′ between Ij and Ij+1 , forming a new chain I′ such that ψ(I′) retains property (5). To achieve this, we form Il = (I1, . . . , I j ) and Ir = (Ij+1 , . . . , I k+1 ) and attempt to construct 16 two separating sets S∗ 

> M

(Il; Ir) ⊆ I∗ and S∗ 

> M

(Ir; Il) ⊆ I∗ neighboring Il = ⋃ji=1 Ii and 

Ir = ⋃k+1  

> i=j+1

Ii respectively. If S∗ 

> M

(Il; Ir) = S∗ 

> M

(Ir; Il) = ∅, then no separator exists, and we terminate. If the separators are nonempty, we take I′ = S∗ 

> M

(Il; Ir) if Slik (S∗ 

> M

(Il; Ir)) ≤

Slik (S∗ 

> M

(Ir; Il)) , and I′ = S∗ 

> M

(Ir; Il) otherwise. We then insert I′ at index j, forming the new sequence I′ = (I1, . . . , I j , I ′, I j+1 , . . . , I k+1 ). Our insertion point at iteration k + 1 is then j + 1 or j depending on whether I′ neighbors Ij or Ij+1 .

Algorithm 1 Markov Chain Construction 

Require: Connected, undirected graph M , start and end sets Is, I e ⊆ I∗, threshold γ ∈

(0 , 1) .

function MarkovChain (M , Is, Ie, γ)

I ← (Is, I e) ; i∗ ← 1; 

F1 ← Is; F2 ← Ie;

while NM (F1) ∩ F2 = ∅ do 

S1 ← S∗ 

> M

(F1; F2) ; S2 ← S∗ 

> M

(F2; F1) ; 

C1 ← Slik (S1); C2 ← Slik (S2); 

if min {C1, C 2} > γ then 

break ;

else if C1 ≤ C2 then 

S ← S1; F1 ← F1 ∪ S;

i∗ ← i∗ + 1; 

else if C1 > C 2 then 

S ← S2; F2 ← F2 ∪ S;

I ← (

I≤i∗ , S, I >i ∗

> )

;

output I

This procedure is initialized with the sequence (I1, I 2) = ({iθ}, I (ˇ νi)) and j = 1 . We terminate when we either cannot locate a separating set I′, or when the likelihood specificity 

Slik (I′) exceeds a user-specified threshold γ ∈ (0 , 1) . Algorithm 1 gives the full iteration. A formal definition of the separating set operator S∗ 

> M

(·; ·) as well as a proof that Algorithm 1 produces valid Markov chains is given in the supplementary material. To combine the generated chains (one for each leaf node) into a tree, we need two more 17 operations. First, we can trivially promote any single chain I = (I1, I 2, . . . I k) into a tree as follows: 

Tree( I) = (

(νi)ki=1 , [ν2 = ν1, ν 3 = ν2, . . . , ν k = νk−1])

, (13) where I(νi) = Ii, and [ν2 = ν1, ν 3 = ν2, . . . , ν k = νk−1] defines the parent mapping P in Definition 1. Then, given a distributary tree T and chain I = (I1, I 2, . . . , I k) with I1 = {iθ},we need a method of “grafting” I onto T .

Definition 5 (Grafting) . Let ν∗ be the terminal node of the longest rooted path 

(ν1, ν 2, . . . , ν m) in T such that I(νi) = Ii for all 1 ≤ i ≤ m ≤ k. (The uniqueness of this longest path is guaranteed by the uniqueness of children in Definition 1.) For 

j = 1 , . . . , k − m, define nodes νnew  

> j

with I(νnew  

> j

) = Im+j . Then the tree with I grafted is defined as 

Graft 

> I

(T ) = (

NT ∪ { νnew  

> j

}k−mj=1 , P [

νnew 1 = ν∗, ν new 2 = νnew 1 , . . . , ν new  

> k−m

= νnew 

> k−m−1
> ])

, (14) where P [... ] denotes the parent relation P in T with the additional relations ... added. 

Algorithm 2 Distributary Tree Construction 

Require: Connected, undirected graph M , root index iθ, leaf nodes {ˇνi}li=1 , threshold 

γ ∈ (0 , 1) .

for i ∈ { 1, . . . , l } do 

Ii ← MarkovChain ( M, I s = {iθ}, I e = I(ˇ νi), γ ) ; 

T ← Tree (

I1

> )

;

for j ∈ { 2, . . . , l } do 

T ← Graft 

> Ij

(T ) ; 

output T

With the promotion and grafting operations defined, we can build a tree out of a sequence of leaf nodes {ˇνi}li=1 using Algorithm 2. A proof that Algorithm 2 yields valid distributary trees is given in the supplementary materials. 18 2.4 Operations on Distributary Trees 

Given an initial distributary tree , we may want to add nodes to find the better explanatory sets. To this end, we develop a suite of operations which modify distributary trees. 

Branching from a node. Given a node ν in T , we may ask if a proper subset I′ ⊂ I(ν)

is almost as good in the sense that U 2 

> I′

≈ U 2

> I(ν)

. We add I′ to T with a branching operation. 

Definition 6 (Branching) . Let T = (N , P ) be a distributary tree, ν a node in T , and 

I′ ⊂ I(ν) a proper subset. Then we define the tree branched from I(ν) to I′ as 

Branch  

> ν⊃I′

(T ) = 

> 

T, ∃ν′′ ∈ T with P (ν′′ ) = ν and I(ν′′ ) = I′

(N ∪ { ν′}, P [ν′ = ν]) , otherwise ,

(15) where ν′ is a new node with I(ν′) = I′.

Subdividing a branch. Consider a pair of nodes νp and νc with P (νc) = νp. If U 2 

> I(νp)

≪

U 2

> I(νc)

, we may look for a third set that splits the difference between I(ψc) and I(ψp). This can be achieved by the following subdivision operation. 

Definition 7 (Subdividing) . Let T = (N , P ) be a distributary tree and νp, ν c be nodes in 

T with P (νc) = νp. For any I′ 

> p

⊆ I(νp), let Is = I′ 

> p

∪ I(νc). We subdivide the branch from 

νp to νc by Is as follows: 

Divide 

> νp⊃I′
> p→νc

(T ) = 

> 

(N , P [νc = ν′]) , ∃ν′ ∈ T with P (ν′) = νp and I(ν′) = Is

(N ∪ { νs}, P [νc = νs, ν s = νp]) , otherwise ,

(16) where νs is a new node with I(νs) = Is.

Merging two nodes. For two nodes ν1 and ν2, it may be useful to consider the combination 

ψI(ν1)∪I(ν2). For example, let ν∗ be the deepest common ancestor of ν1 and ν2. If ν∗ is much 19 more explanatory than ν1 or ν2 separately – i.e. if U 2 

> I(ν∗)

≪ U 2 

> I(ν1)

and U 2 

> I(ν∗)

≪ U 2 

> I(ν2)

– we may ask how much of this gap can be bridged by I(ν1) ∪ I(ν2). To place this union on a distributary tree , we merge the corresponding nodes. 

Definition 8 (Merging) . Let T = (N , P ) be a distributary tree, and let ν1 and ν2 be nodes with deepest common ancestor ν∗. Let Imerge = MarkovChain ( M, I (ν∗), I (ν1) ∪ I(ν2), γ ).Then the tree with nodes ν1 and ν2 merged is given as 

Merge 

> ν1←ν∗→ν2

(T ) = Branch  

> νm⊃I(ν2)

◦ Branch  

> νm⊃I(ν1)

◦ Graft 

> Imerge

(T ), (17) where ◦ denotes compoisition of functions, and νm is the unique leaf node introduced in 

Graft 

> Imerge

(T ) with I(νm) = I(ν1) ∪ I(ν2).Each of these operations preserve the invariants of distributary trees . 

Lemma 2. Let p be a density which factorizes over F, and let MF be the corresponding Markov graph. Let T = (N , P ) be a distributary tree for p and MF generated by Algorithm 2. Let ν1 and ν2 be nodes in T .1. If I′ ⊂ I(ν1), then Branch  

> ν1⊃I′

(T ) is a distributary tree. 2. If P (ν2) = ν1 and I′ ⊆ I(ν1), then Divide 

> ν1⊃I′→ν2

(T ) is a distributary tree . 3. If ν∗ is the deepest common ancestor of ν1 and ν2, then Merge 

> ν1←ν∗→ν2

(T ) is a distributary tree . Proof. See supplementary materials. 

Remark: The statement of Lemma 2 requires that the distributary tree be generated by Algorithm 2 because this algorithm produces objects with slightly stronger independence properties than those guaranteed by the definition of distributary trees. A definition of these 20 stronger objects, which we term separating trees, is given in the supplementary material. 

# 3 Example: Causal Inference in Simulated Panel Data 

Our goal in this example is inference for a treatment effect parameter in a simulated causal inference problem based on panel data. We simulate data yit for 1 ≤ i ≤ N = 6 units over 

1 ≤ t ≤ T = 10 time points, with unit 1 exposed to a treatment after time tδ = 6 . Figure 3 plots the simulated data, with vertical lines showing the treatment time for unit 1. 

Figure 3: The simulated panel data. Units 1-3 are all identical in the pre-treatment period, but divergent in the post-treatment period. Only unit 1 is treated. Estimating treatment effects relies on inferring correlations between units in the pre-treatment period which can be extrapolated into the post-treatment period. In particular, we rely on the post-treatment behavior of the untreated units to learn how the treated unit was likely to behave in the absence of treatment. This strategy and the corresponding inference for the treatment effect can be undermined in multiple ways. A lack of relevant data is the most straightforward threat to inference. For instance, in Figure 3, units 4 – 6 are a poor match to unit 1 in the pre-treatment period, and thus provide little reliable information about unit 1 in the post-treatment period. On the other hand, units 2 and 3 closely match unit 1 in the pre-treatment period, but create a different 21 threat to inference: Despite matching unit 1 equally well before treatment, units 2 and 3 diverge and give incompatible predictions in the post-treatment period. In this case, we have too much potentially relevant information to infer the treatment effect with confidence. 

## 3.1 Parameters and Model 

We fit the data in Figure 3 using a latent factor model augmented with a treatment effect parameter. Our model closely resembles Bayesian approaches to synthetic control, a causal inference methodology for observational panel data (Abadie et al. 2010, Pang et al. 2022). Specifically, the matrix of observations y ∈ RN ×T is modeled as the sum of three terms: 

y = λϕ + δ + ϵ. (18) From right to left: 1. ϵ ∈ RN ×T is a matrix of independent, zero-mean Gaussian errors which are independent of all other quantities. The entries of ϵ have constant variance σ2 

> i

along each row 

1 ≤ i ≤ N , i.e. Var( ϵit ) = σ2 

> i

for all 1 ≤ t ≤ T .2. δ ∈ RN ×T is the matrix of treatment effects, i.e. δit = δt−tδ 1{i=1 ,t>t δ }, where 1S is the indicator of S, and δ ∈ RT −tδ is a vector of nonzero treatment effects. 3. ϕ ∈ RK×T represents K time-varying latent factors, and λ ∈ RN ×K contains factor loadings for each unit. The product of λ and ϕ is referred to as the latent component. The latent factors (i.e. rows of ϕ) are independent and assigned AR (1) priors with unit stationary variance. The loadings matrix λ is assumed lower triangular with positive diagonal in order to eliminate nonidentification that otherwise arises from the product λϕ .This constraint on λ is typical in latent factor models and, importantly, does not constrain the latent component λϕ (Bai & Wang 2015). 22 The untreated potential outcome is defined as y(0) = y − δ, i.e. what would be observed had the treatment not occurred. Conditional on λ and the σ2 

> i

, the cross-sectional covariance of the untreated potential outcomes 

y·t(0) = ( y1t(0) , y 2t(0) , . . . , y N t (0)) 

is λλ T + di (σ2) for all 1 ≤ t ≤ T , where di (σ2) is the N × N diagonal matrix with 

di (σ2)ii = σ2 

> i

. Therefore, the elements of λ directly control the cross-sectional latent correlation between (untreated) units. Complete details of the prior specification can be found in the supplementary materials. We fit the model (18) to the simulated data in Figure 3 using Stan (Stan Development Team 2025). As expected, we find weakly identified marginal posteriors for all effects δt. For simplicity, we take the effect at the final time, δ4, as our quantity of interest. The posterior for δ4 is centered near 0 with a 95% credible interval of [−3.95 , 4.17] . Over the pre-treatment period, the treated unit had an observed standard deviation of 1.9, so essentially any plausible effect remains possible a posteriori. 

## 3.2 Analysis with Variance Deltas 

We consider two types of hypothetical data: (1) observations ˜y ∈ RN ×T ∗

of each observed unit at T ∗ = 6 additional pre-treatment times, and (2) observations ˜ynew ∈ RT +T ∗

of a new unit at all times, resulting in N ∗ = N + 1 units. The hypothetical data are sampled from their posterior predictive distribution (details given in the supplementary materials). Using Algorithm 2, we construct a distributary tree rooted at δ4 with N ∗ leaf nodes representing the hypothetical data for each unit. Figure 4 displays the corresponding variance delta. All leaf nodes have root uncertainty indices near 1, indicating that observing the hypothetical 23 Figure 4: The generated variance delta with leaves for ˜yi· (1 ≤ i ≤ N ) and ˜ynew .data for any single unit is unlikely to improve the identification of δ4. Therefore, if we want to learn about δ4 using this hypothetical data, it will be necessary to combine hypothetical data across units. While this can be achieved with the merge operation defined in Section 2, we don’t yet know which nodes to merge. 

Figure 5: The variance delta for the leaves corresponding to units 1-3, with leaves 1 & 2 merged (top plot) and all leaves merged (bottom plot). Rather than manually guess, we can automatically search for the pair of leaves ˇν1 and ˇν2 such that U 2 

> I(ˇ ν1)∪I(ˇ ν2)

is minimal. The variance-deltas package implements this automation, 24 and the result of applying it twice is displayed in Figure 5, showing only the newly added paths. The greatest reduction in uncertainty comes from observing ˜y1· and ˜y2· together, with further improvement from adding ˜y3·. Specifically, combining ˜y1·, ˜y2·, and ˜y3· yields expected posterior standard deviation < 75% of the original posterior standard deviation, at the cost of observing 30% more data. Furthermore, in Figure 4, the paths terminating in ˜y2· and ˜y3· (the top two paths) also included the best intermediate nodes. These intermediate nodes correspond to the loadings 

λ2· and λ3· respectively, along with the latent factors ˜ϕ for the new pre-treatment times. Since the loadings λ control the latent correlations between units, our analysis so far suggests that the primary bottleneck to learning about δ4 is uncertainty about the latent correlations between unit 1 (the treated unit) and units 2 and 3. To confirm this, we extend our tree again, merging the intermediate nodes and then branching to isolate the loadings. Figure 6 displays the path from this node to the root. As expected, exact knowledge of the loadings for units 2 and 3 reduces expected uncertainty to nearly a quarter of its current level. 

Figure 6: The path connecting the node for λ2· and λ3· to the root. 

# 4 Polling Model Example 

Our second example applies variance deltas to characterize uncertainty in a complex model of polls from the 2016 U.S. presidential election. In particular, we analyze the model 25 of Heidemanns et al. (2020), which includes components for temporal trends, interstate correlation, and several sources of potential bias/measurement error. We use variance deltas to isolate two model components which together explain most of the posterior uncertainty and which allow us to determine the extent to which this uncertainty could be reduced. The data are a subset of polls from the 2016 U.S. presidential election at the state and national level. Each poll reports the number of respondents and the number supporting the Democratic candidate, Hilary Clinton. Our goal is to forecast the proportion of voters who will support the Democratic candidate on election day in each state. 

## 4.1 Parameters and Model 

Let 1 ≤ t ≤ T index the number of days in the model, with t = 1 being the day of the first recorded poll, and t = T being election day. The proportion of voters supporting the Democratic candidate in each state on each day is represented (on the logistic scale) by the matrix μb ∈ R51 ×T . The columns of μb are assigned a time series prior: 

μb 

> ·t

| μb 

> ·(t+1)

∼ normal (

μb

> ·(t+1)

, Sμ)

for 1 ≤ t ≤ T − 1, and μb 

> ·T

∼ normal (

mf , Sf )

. (19) Here Sμ ∈ R51 ×51 is a hyperparameter encoding correlation between states and variation over time. Likewise, mf ∈ R51 and Sf ∈ R51 ×51 are hyperparameters governing the prior on the election day outcome. Results of state and national polls are given a binomial model combining μb with terms for various types of polling bias. Letting i = 1 , . . . , N state index state polls and yi denote the number of respondents supporting the Democratic candidate out of ni respondents in poll i,we have 

yi ∼ binomial (

logit −1 (

μbsi,t i + βi

> )

, n i

> )

, (20) 26 where si, ti denote the state and day for poll i, and βi models various sources of bias. National polls are modeled similarly, except that state-level terms including μst are averaged with weights accounting for each state’s share of the national vote in the previous election. The bias terms βi in (20) , also modeled on the logistic scale, are decomposed into several further terms which are designed to capture the following sources of polling bias: 

• A pollster-specific “house” effect μc, modeling the nonzero, temporally stable bias that each pollster exhibits toward one party or the other. 

• A poll mode effect μm, reflecting the phenomenon that the averages of phone- and internet-based polls tend to differ. 

• A population effect μr, modeling differences between registered and likely voter polls. 

• A time-varying partisan nonresponse effect ϵt, modeling differential rates of response between members of the two parties. The ϵt are assigned an AR(1) prior. 

• State-level measurement error ξ accounting for aggregate differences in the accuracy of polls in different states. This is assigned a multivariate normal prior with covariance determined by state correlations in previous elections and demographics. 

• Poll-level measurement error ζ, a catch-all term representing additional variance in the polls’ sampling frames unaccounted for by the above terms. These terms are assigned different prior scales at the state and national levels. The complete model specification is given in Heidemanns et al. (2020). Code and 2016 polling data for the model can be accessed at https://github.com/TheEconomist/us-potus-model. We fit this model to available 2016 data using Stan (Stan Development Team 2025). Uncertain inference for the election-day Democratic support parameter μ∗ 

> s

= μsT is partic-ularly troublesome in states s with E [μ∗ 

> s

| y] ≈ 0.5, i.e. those believed to be swing states. 27 As an example, we analyze the state of Wisconsin (indexed by s∗ = 49 ), taking μ∗ 

> s∗

as our quantity of interest. Transforming to the proportion scale, the 95% credible interval for logit −1 (μ∗ 

> s∗

) is [.478 , 0.564] , indicating failure to forecast the winner of Wisconsin. A summary of analyses for other swing states is given in the supplementary material. 

## 4.2 Analysis with Variance Deltas 

We begin by constructing hypothetical polls ˜y from the most common pollster of Wisconsin, conducted one week before the election ( t = 39 ), which is after the last available polls in our data set. For each hypothetical poll ˜yj , we choose one of 3 polling modes, 3 target populations, and whether to adjust the result for differential response rates across parties, for a total of 3 × 3 × 2 = 18 simulated polls. The initial distributary tree is constructed from a single leaf node combining all 18 hypo-thetical polls. Figure 7 displays this tree. We note two important observations: 1. The node labelled 2 corresponds to μb 

> ·40

— the logistic-transformed Democratic support across all states on the day after the hypothetical polls are conducted. This node occurs with root uncertainty index ≈ 1/3. Therefore, at least 1/3 of the uncertainty about μ∗ 

> s∗

is purely temporal, and would remain even if we could learn the Democratic support μbs∗,39 in Wisconsin on the day of the polls exactly. 2. However, all 18 hypothetical polls together (the last node in Figure 7) have root uncertainty index ≈ 0.9. Thus, observing these new hypothetical polls likely would not come close to fully informing us about even μbs∗,39 .The node containing μbs∗,39 (labelled 3 in Figure 7) also contains the bias parameters which affect the hypothetical polls. One or more these biases may thus explain the lack of information about μbs∗,39 in ˜y. To test this, we insert a new node containing all bias parameters and ˜y (using the subdivision operation defined in Section 2). This new node 28 Figure 7: Initial variance delta with a single leaf representing all hypothetical polls. represents how much would be learned from the hypothetical polls given exact knowledge of all biases. Figure 8 shows the distributary tree obtained from this subdivision. The new node (labelled 4 in Figure 8) and its parent achieve essentially identical uncertainty indices. Since these nodes differ only in whether they include μbs∗,38 or ˜y, this implies that the lack of information in ˜y is due almost entirely to the bias terms. To determine which of the bias parameters in nodes 3 and 4 are most important, we create new branches from node 4, one for each pair of ˜y and a single bias parameter. Figure 9 displays the result. Only one bias parameter makes a substantial improvement in expected uncertainty when combined with ˜y — the state-level polling bias ξ. Observing both ˜y and ξ

yields about 2/3 of the uncertainty reduction achievable by observing ˜y along with all of the bias parameters, and about 1/2 of the uncertainty reduction achieved by exact knowledge of μb

> ·40

, the Democratic support in all states on the subsequent day. In summary, we are able to draw several meaningful conclusions: 1. Absent additional information, conducting more polls is unlikely to yield much im-provement in the election day forecast for Wisconsin. 2. Additional polling could substantially reduce our uncertainty in combination with additional information about the state-level measurement error ξ.29 Figure 8: The result of subdividing the initial variance delta , adding a new node that combines the additional polls ˜y with all corresponding bias parameters. 

Figure 9: The result of adding branches from node 7 for each combination of additional polls ˜y and one of the bias parameters. 3. Even with this information, there is a nonnegligible limit to how much we can learn about the election day Democratic support in Wisconsin from polls conducted a week earlier, due to extrapolation uncertainty. 4. Quantitatively, about 2/3 of the expected remaining uncertainty about μ∗ 

> s∗

given ˜y is explained by uncertainty about ξ and extrapolation uncertainty. 30 5 Conclusion 

We introduce variance deltas, an interactive visualization method for explaining posterior uncertainty about scalar quantities of interest in Bayesian models, and implement this system in a software package variance-deltas . When the Bayesian model has a factorization F,we show that the corresponding Markov graph MF can be utilized to semi-automatically construct and transform variance deltas, enabling the efficient generation and exploration of potential explanations. Our two examples demonstrate that variance deltas are capable of rapidly eliminating poor explanations of uncertainty and focusing our attention on specific, interpretable subsets of model quantities which can explain a large fraction of our overall uncertainty. In the simulated panel data example, we are able to identify small combinations of units which would be useful to observe over a longer period, while determining that other units are of vanishing small value. In the polling model example, we are able to isolate a pair of parameters that explain most of our uncertainty, directing us towards particular types of information which are needed to remove bottlenecks from our analysis. The usefulness and general applicability of variance deltas is limited, however, by the structure of the graph MF . In particular, our ability to discover useful and specific explanations depends on MF being not too dense. When MF is almost sparse – in that a few vertices have many neighbors – we can recover a sparse graph by conditioning on these vertices at every step. (Details of this adjustment are given in the supplementary materials.) But when the graph MF is very far from sparse, as can occur for instance in large regressions, it may be impossible to construct useful variance deltas, or the explanatory sets I may be too large to reasonably estimate the uncertainty index U 2 

> I

.31 Acknowledgements 

The AI tool Claude Code (version 2.0.76) was used for the following tasks during the preparation of this manuscript: (1) identification of some related literature, (2) generation of small amounts of code, and (3) review of mathematical arguments. AI tools were not used to generate any of the text of this manuscript (including references and supplementary material). AI-generated code was manually reviewed line by line, and all publications identified by AI were manually verified as existing and appropriate references for the present work. 

# Declaration of Interest Statement 

The authors report there are no competing interests to declare. 

# References 

Abadie, A., Diamond, A. & Hainmueller, J. (2010), ‘Synthetic control methods for compar-ative case studies: Estimating the effect of california’s tobacco control program’, Journal of the American Statistical Association 105 (490), 493–505. 

URL: https://doi.org/10.1198/jasa.2009.ap08746 

Bai, J. & Wang, P. (2015), ‘Identification and bayesian estimation of dynamic factor models’, 

Journal of Business & Economic Statistics 33 (2), 221–240. 

URL: https://doi.org/10.1080/07350015.2014.941467 

Champion, C. & Elkan, C. (2017), ‘Visualizing the consequences of evidence in bayesian networks’, (arXiv:1707.00791). arXiv:1707.00791 [cs]. 

URL: http://arxiv.org/abs/1707.00791 

32 Constantine, P. G. & Diaz, P. (2016), ‘Global sensitivity metrics from active subspaces’. 

URL: https://arxiv.org/abs/1510.04361 

Felli, J. C. & Hazen, G. B. (1998), ‘Sensitivity analysis and the expected value of perfect information’, Medical Decision Making 18 (1), 95–109. PMID: 9456214. 

URL: https://doi.org/10.1177/0272989X9801800117 

Friedman, J. H. & Stuetzle, W. (2002), ‘John W. Tukey’s work on interactive graphics’, 

The Annals of Statistics 30 (6), 1629 – 1639. 

URL: https://doi.org/10.1214/aos/1043351250 

Gabry, J. & Mahr, T. (2025), ‘bayesplot: Plotting for bayesian models’. R package version 1.14.0. 

URL: https://mc-stan.org/bayesplot/ 

Gabry, J. & Veen, D. (2022), shinystan: Interactive Visual and Numerical Diagnostics and Posterior Analysis for Bayesian Models . R package version 2.6.0. 

URL: https://github.com/stan-dev/shinystan 

Gelman, A., Vehtari, A., Simpson, D., Margossian, C. C., Carpenter, B., Yao, Y., Kennedy, L., Gabry, J., Bürkner, P.-C. & Modrák, M. (2020), ‘Bayesian workflow’. 

URL: https://arxiv.org/abs/2011.01808 

Greenland, S. (2005), ‘Multiple-bias modelling for analysis of observational data’, Journal of the Royal Statistical Society: Series A (Statistics in Society) 168 (2), 267–306. 

URL: https://rss.onlinelibrary.wiley.com/doi/abs/10.1111/j.1467-985X.2004.00349.x 

Greenland, S. (2009), ‘Relaxation Penalties and Priors for Plausible Modeling of Nonidenti-fied Bias Sources’, Statistical Science 24 (2), 195 – 210. 

URL: https://doi.org/10.1214/09-STS291 

Heidemanns, M., Gelman, A. & Morris, G. E. (2020), ‘An Updated Dynamic Bayesian 33 Forecasting Model for the U.S. Presidential Election’, Harvard Data Science Review 2(4). https://hdsr.mitpress.mit.edu/pub/nw1dzd02. Hullman, J., Guo, Z. & Ustun, B. (2025), ‘Explanations are a means to an end’. 

URL: https://arxiv.org/abs/2506.22740 

Inglis, A., Parnell, A. & Hurley, C. (2021), ‘Visualizing variable importance and variable interaction effects in machine learning models’, (arXiv:2108.04310). arXiv:2108.04310 [stat]. 

URL: http://arxiv.org/abs/2108.04310 

Iooss, B. & Lemaître, P. (2015), A Review on Global Sensitivity Analysis Methods , Springer US, Boston, MA, pp. 101–122. Jackson, C., Presanis, A., Conti, S. & Angelis, D. D. (2019), ‘Value of information: Sensitivity analysis and research design in bayesian evidence synthesis’, Journal of the American Statistical Association 114 (528), 1436–1449. 

URL: https://doi.org/10.1080/01621459.2018.1562932 

Jacques, J., Lavergne, C. & Devictor, N. (2006), ‘Sensitivity analysis in presence of model uncertainty and correlated inputs’, Reliability Engineering & System Safety 91 (10), 1126– 1134. The Fourth International Conference on Sensitivity Analysis of Model Output (SAMO 2004). 

URL: https://www.sciencedirect.com/science/article/pii/S0951832005002231 

Kallioinen, N., Paananen, T., Bürkner, P.-C. & Vehtari, A. (2023), ‘Detecting and diagnosing prior and likelihood sensitivity with power-scaling’, Statistics and Computing 34 (1). 

URL: http://dx.doi.org/10.1007/s11222-023-10366-5 

Linardatos, P., Papastefanopoulos, V. & Kotsiantis, S. (2021), ‘Explainable ai: A review of 34 machine learning interpretability methods’, Entropy 23 (1). 

URL: https://www.mdpi.com/1099-4300/23/1/18 

Lundberg, S. M. & Lee, S.-I. (2017), A unified approach to interpreting model predictions, 

in ‘Proceedings of the 31st International Conference on Neural Information Processing Systems’, NIPS’17, Curran Associates Inc., Red Hook, NY, USA, p. 4768–4777. Maathuis, M., Drton, M., Lauritzen, S. & Wainwright, M. (2020), Handbook of Graphical Models , Chapman and Hall/CRC Handbooks of Modern Statistical Methods Series, CRC Press. 

URL: https://books.google.com/books?id=6cb3zQEACAAJ 

Nguyen, T. D., Giordano, R., Meager, R. & Broderick, T. (2024), ‘Sensitivity of mcmc-based analyses to small-data removal’. 

URL: https://arxiv.org/abs/2408.07240 

Owen, A. B. (2014), ‘Sobol’ indices and shapley value’, SIAM/ASA Journal on Uncertainty Quantification 2(1), 245–251. 

URL: https://doi.org/10.1137/130936233 

Pang, X., Liu, L. & Xu, Y. (2022), ‘A bayesian alternative to synthetic control for compara-tive case studies’, Political Analysis 30 (2), 269–288. Roos, M., Martins, T. G., Held, L. & Rue, H. (2015), ‘Sensitivity Analysis for Bayesian Hierarchical Models’, Bayesian Analysis 10 (2), 321 – 349. 

URL: https://doi.org/10.1214/14-BA909 

Sobol’, I. M. (1990), ‘On sensitivity estimation for nonlinear mathematical models’, Matem-aticheskoe modelirovanie 2(1), 112–118. Stan Development Team (2025), Stan User’s Guide .

URL: https://mc-stan.org/users/documentation/ 

35 Tukey, J. W. (1990), ‘Data-Based Graphics: Visual Display in the Decades to Come’, 

Statistical Science 5(3), 327 – 339. 

URL: https://doi.org/10.1214/ss/1177012101 

Wright, M. N. & Ziegler, A. (2017), ‘ranger: A fast implementation of random forests for high dimensional data in c++ and r’, Journal of Statistical Software 77 (1), 1–17. 

URL: https://www.jstatsoft.org/index.php/jss/article/view/v077i01 

36 6 Supplement 

## 6.1 Software Package and Replication Data 

The variance-deltas software package may be accessed at https://github.com/collin-cademartori/variance-deltas/tree/main. Binary distributions of stable releases are provided for many major platforms (including Windows, MacOS, and Linux). Please report any suspected bugs by opening an issue on the linked GitHub repository. Scripts and data to reproduce the variance deltas from the three examples in this paper are openly available at the Harvard Dataverse https://doi.org/doi:10.7910/DVN/FVHLGG. 

## 6.2 Separating Set Construction 

First, we review some basic terminology from graph theory and establish notation. An undirected graph M = ( VM , E M ) consists of set of vertices VM and edges 

EM ⊆ {{ i, j } | i, j ∈ VM } . (21) A path in M is any sequence of vertices (i1, i 2, . . . , i m) ∈ V mM such that {ik, i k+1 } ∈ EM for all 1 ≤ k ≤ m − 1 and any m ≥ 2. For any set of vertices F ⊆ VM , we define the following: 1. NM (F ) is the set of neighbors of F in the graph M , given as 

NM (F ) = 

> {

j ∈ VM \ F 

> ∣∣∣∣

{i, j } ∈ EM for some i ∈ F

> }

(22) If i ∈ VM is a single vertex, then we will write NM (i) for NM ({i}).2. CM (F ) is the connected component of F in M , i.e. the set of all j ∈ VM reachable by 37 a path starting at some i ∈ F . This may be given explicitly as 

CM (F ) = 

> ∞⋃
> k=0

N kM (F ), (23) where N kM (F ) = NM ◦ NM ◦ · · · ◦ NM   

> ︸︷︷ ︸
> ktimes

(F ) for k ≥ 1, and N 0 

> M

(F ) = F .3. M − F is the graph with vertex set VM −F = VM \ F and edge set 

EM −F =

> {

{i, j } ∈ EM 

> ∣∣∣∣

i, j ∈ VM \ F

> }

. (24) Recall that a density p(x) which factorizes over F has the useful property that conditional independence relationships between components of x can be described by separating sets. 

Lemma 3. Let p(x) with x ∈ RD factorize over F, and let MF be the corresponding Markov graph. Let F1, F2, and S be subsets of {1, . . . , D }. Suppose that every path from a vertex 

i ∈ F1 to a vertex j ∈ F2 includes a vertex in S. Then we have that 

xF1 ⊥⊥ xF2 | xS . (25) 

In this case, the set S is called a separating set for F1 and F2. If there exists any separating set for F1 and F2, then these sets are said to be separable .

A separating set S for vertex sets F1 and F2 is called a minimal separating set if no proper subset of S is a separating set. For separable F1 and F2, there may be many minimal separating sets. The following lemma gives one method to find a minimal separator. 

Lemma 4. Let F1 and F2 be separable vertex sets in a connected, undirected graph M .Then the following are minimal separators of F1 and F2:

SM (F1; F2) = NM (G1) ∩ NM (F1), SM (F2; F1) = NM (G2) ∩ NM (F2), (26) 38 where G1 = CM −NM (F1)(F2), and G2 = CM −NM (F2)(F1).

If M is connected, then Lemma 4 implies a simple algorithm for computing SM (F1; F2)

and SM (F2; F1). To calculate G1, for instance, we can perform a depth-first search starting at any node in F2, collecting all discovered vertices while ignoring all edges with a vertex in NM (F1). Then calculating SM (F1; F2) from G1 reduces to trivial set operations, and 

SM (F2; F1) may be computed similarly. 

Proof. We prove that SM (F1; F2) is a minimal separator of F1 and F2. The proof that 

SM (F2; F1) is a minimal separator is entirely symmetric. We begin by showing that SM (F1; F2) is a separator for F1 and F2. Let p be any path of length m from a vertex in F1 to a vertex in F2. First, we select the last vertex in p which lies in NM (F1) Specifically, define 

Ip =

> {

1 ≤ k ≤ m 

> ∣∣∣∣

pk ∈ NM (F1)

> }

,i∗ = max Ip.

(27) We now claim that Ip̸ = ∅ and i∗ < m . To show Ip̸ = ∅, we also define Jp = {1 ≤ k ≤ m |

pk /∈ F1} and j∗ = min Jp. Clearly Jp̸ = ∅ since pm ∈ F2 by definition, and F1 ∩ F2 = ∅

since F1 and F2 are assumed separable. Furthermore, j∗ > 1 since p1 ∈ F1 by definition. Thus, we must have pj∗−1 ∈ F1. Since {pj∗−1, P j∗ } ∈ EM , it follows that pj∗ ∈ NM (F1), and thus j∗ ∈ Ip, so Ip̸ = ∅.Furthermore, if m ∈ Ip , then there would exist some i′ ∈ F1 with (i′, p m) ∈ EM . But then 

(i′, p m) would be a path from F1 to F2, contradicting the assumption that F1 and F2 are separable. Therefore, i∗ < m , as claimed. Now consider the truncated path p′ = ( pi∗+1 , . . . , p m). Note that pm ∈ F2, and that 

p′ 

> k

/∈ NM (F1) for all 1 ≤ k ≤ | p′|. If this were not true, there would exist a k∗ > i ∗ such 39 that pk∗ ∈ NM (F1), which contradicts the definition of i∗. In other words, p′ 

> 1

is connected to F2 by a path which does not pass through NM (F1), and thus p′ 

> 1

∈ CM −NM (F1)(F2). Since 

p′ 

> 1

= pi∗+1 , this implies that pi∗ ∈ NM (CM −NM (F1)(F2)) .Putting these pieces together, we conclude that 

pi∗ ∈ NM (F1) ∩ NM (CM −NM (F1)(F2)) = NM (F1) ∩ NM (G1), (28) and therefore, since p was an arbitrary path from F1 to F2, NM (F1) ∩ NM (G1) must separate 

F1 and F2.To show that this set is also a minimal separator, let H ⊂ NM (F1) ∩ NM (G1). Then let 

j′ ∈ (NM (F1) ∩ NM (G1)) \ H. Because j′ ∈ NM (F1), there exists i′ ∈ F1 such that {i′, j ′} ∈ 

EM . Note also that i′ /∈ H since i′ /∈ NM (F1). Furthermore, since j′ ∈ NM (CM −NM (F1)(F2)) ,there exists a path p′ (of length m′) such that {j′, p ′

> 1

} ∈ EM , p′ 

> m

∈ F2, and p′ 

> k

/∈ NM (F1)

for all 1 ≤ k ≤ m′. The latter property implies in particular that p′ 

> k

/∈ H for all 1 ≤ k ≤ m.Putting this all together, (i′, j ′, p ′

> 1

, . . . p ′

> m

) is a path from F1 to F2 which does not include any vertex in H, showing that H is not a separator for F1 and F2. Since H was an arbitrary subset of NM (F1) ∩ NM (G1), we conclude that this set is in fact a minimal separator of F1

and F2, as claimed. Finally, the separating set function S∗ 

> M

(F1; F2) used in Algorithm 2 is produced from 

SM (F1; F2) as 

S∗ 

> M

(F1; F2) = 

> 

SM (F1; F2), F1, F 2 separable ,

∅, otherwise ,

and similarly for S∗ 

> M

(F2; F1).40 6.3 Proof that Algorithm 1 Produces Markov Chains 

Rather than directly prove that Algorithm 1 yields valid Markov chains, we prove a slightly stronger result. We start with a definition. 

Definition 9 (Separating paths and trees) . Let I = (I1, I 2 . . . , I k) be such that Ii ⊆ I∗

for all 1 ≤ i ≤ k. Let M be a connected, undirected graph with VM = I∗. Then I is a 

separating chain with respect to M if, for all 1 ≤ j ≤ k,

CM −Ij (I≤j \ Ij ) ∩ (I≥j \ Ij ) = ∅, (29) where I≤j = ∪ji=1 Ii and I≥j = ∪ki=j Ii. In words, each component Ij of I must separate its ancestors and descendants into distinct connected components in M .Let F be a factorization with connected Markov graph MF . Let p factorize over F, and let 

T be a distributary tree for p. If, for all paths (ν1, . . . , ν k) in T , (I(ν1), I (ν2), . . . , I (νk)) is a separating chain with respect to MF , then T is a separating tree for p and F.For T to be a separating tree, it is sufficient that the property (29) holds for all maximal paths in T . We note that, in light of Lemma 3, the separating property (29) implies the Markov property (5) required for distributary trees. We prove that Algorithm 1 produces valid separating chains, and it will follow immediately from this that it also produces valid Markov chains. First, we need the following lemma. 

Lemma 5. Let M be a connected, undirected graph with VM = I∗. For any separable 

I1, I 2 ⊂ I∗, let IS denote either S∗ 

> M

(I1; I2) or S∗ 

> M

(I2; I1). Then for any vertex i ∈ IS , there exist paths p1 and p2 connecting i to I1 and I2 respectively, such that p1 

> j

/∈ I2 and p2 

> k

/∈ I1

for all 1 ≤ j ≤ | p1| and 1 ≤ k ≤ | p2|.Proof. We prove the lemma for IS = S∗ 

> M

(I1; I2), as the proof for the other case is entirely 41 symmetric. Let i ∈ IS . Because i ∈ NM (I1), there exists some i′ ∈ I1 such that {i, i ′} ∈ EM .In fact, since (I1 ∪ IS ) ∩ I2 = ∅, we have {i, i ′} ∈ EM −I2 . Thus, p1 = ( i, i ′) is a path from i

to I1 in M − I2.We also have i ∈ NM

> (

CM −NM (I1)(I2))

, so there exists some i′′ ∈ CM −NM (I1)(I2) ⊆ M − I1

such that {i, i ′′ } ∈ EM −I1 . Furthermore, by definition of the connected component, there exists some path ˜p of length k with ˜p1 = i′′ , ˜pk ∈ I2, and ˜pj /∈ I1 for all 1 ≤ j ≤ k (since 

I1 and I2 are disconnected in M − NM (I1)). Letting ◦ denote the composition of paths, it follows that p2 = ( i, i ′′ ) ◦ ˜p is a path in M − I1 connecting i to I2, as required. With this, we can now proceed with the proof that Algorithm 1 produces separating chains. 

Lemma 6. The output of algorithm 1 is always a separating chain. Proof. We prove the slightly stronger claim that Algorithm 1 produces valid separating chains at the end of each iteration, proceeding by induction on the iteration. In the base case (i.e. first iteration), I = ( Is, I e), which trivially satisfies (29). Next, assume that at the end of the iteration k ≥ 2, we have valid separating chain 

I′ = (I1, I 2, . . . , I k). Let i∗ ∈ { 1, . . . , k } be the insertion point after iteration k, and define 

Il = ∪i∗

> i=1

Ii and Ir = ∪ki=i∗+1 Ii. By construction F1 = Il and F2 = Ir. If Il and Ir are not separable, then Algorithm 1 outputs I′, and we are done. If Il and Ir are separable, let I be the chain resulting from applying one iteration of the while loop in Algorithm 1 to I′, F1 = Il, F2 = Ir, and i∗. Then it is easy to see that 

I = (

I′

> 1

, . . . , I ′ 

> i∗

, I S , I ′

> i∗+1

, . . . , I ′

> k
> )

, (30) where IS is either S∗

> M
> (

Il; Ir)

or S∗

> M
> (

Ir; Il)

. For j = i∗ + 1 , it is clear that I satisfies (29) by Lemma 4. 42 Next, suppose that j ≤ i∗. Let I′ 

> S

= Ij = I′ 

> j

. Then we must show that I′ 

> S

separates 

I<j = I′ 

> <j

and I>j = I′ 

> >j

∪ IS . Clearly, I′ 

> S

separates I′ 

> <j

and I′ 

> >j

since I′ already satisfies (29) . Thus, it suffices to show that there is no path from I<j to IS that does not pass through I′ 

> S

.If I<j = ∅, we are done. Otherwise, we demonstrate a contradiction. Suppose that there exists such a path p originating at a vertex j1 ∈ I<j = I′ 

> <j

and terminating in some vertex 

j2 ∈ IS with no vertices in I′ 

> S

. Then by Lemma 5 above, it follows that there exists another path p′ originating at j2 and terminating in I′ 

> >i ∗

⊆ I′ 

> >j

such that p′ does not pass through 

I′≤i∗ ⊃ I′ 

> S

. It immediately follows that p ◦ p′ is a path originating in I′ 

> <j

and terminating in I′ 

> >j

which does not pass through I′ 

> S

= I′ 

> j

, where ◦ denotes the composition of paths. But this contradicts the assumption that I′ satisfied the separating condition (29) for all 

1 ≤ j ≤ k.Therefore, there are no paths from I<j to IS which do not pass through I′ 

> S

, and thus Ij

separates I<j and I>j . The proof for j > i ∗ + 1 follows in exactly the same fashion, and we conclude that I is again a separating chain. It then follows by induction that Algorithm 1 produces valid separating chains. 

## 6.4 Proof that Algorithm 2 Produces Valid Distributary Trees 

As in the previous section, we prove a slightly stronger result – that Algorithm 2 produces valid separating trees. For this, it suffices to show the following two lemmas. 

Lemma 7. For every separating chain I starting at {iθ}, Tree (I) is a valid separating tree. Proof. To show that Tree (I) is a separating tree, we have to verify the constraints in Definition 1, as well as the separating condition (29) . Let ν0 be the root node of Tree (I). By construction, we have that ψ(ν0) = θ, so the first constraint is satisfied. The second constraint 43 of Definition 1 follows from the separating tree condition 29, and this is immediately satisfied for Tree (I) since I is a separating chain by assumption. Constraint 3 is trivially satisfied since each node has at most one child node. 

Lemma 8. For every separating chain I with I1 = {iθ} and every separating tree T rooted at θ, Graft 

> I

(T ) is a valid separating tree. Proof. We first note that the graft operation cannot modify the root node of T since any new nodes are added as children of existing nodes. Since T is already a valid distributary tree for θ, the first constraint is thus satisfied. Next suppose that ν = ( ν1, . . . , ν k) is a path in Graft 

> I

(T ). If νj ∈ T for all 1 ≤ j ≤ k, then ν

is also a path in T (since all nodes are in T and grafting does not remove any branches from 

T ). Because T is a valid separating tree, ν must satisfy the condition (29) . Now suppose that νj is not a node in T for some j ≥ 1. Then it follows from the definition of Graft 

> I

(T )

that (I(ν1), . . . , I (νk)) must be a sub-chain of I, i.e. 

(I(ν1), . . . , I (νk)) = (

Im, I m+1 , . . . , I n

> )

for some 1 ≤ m ≤ n ≤ | I| with n − m + 1 = k. But then ν satisfies the separating condition (29) again since I is a separating chain by assumption. Finally, the nodes introduced into T by the operation Graft 

> I

(T ) are all the unique children of their parents except for the node νnew 1 , which has parent node ν∗ in T . Recall that, by definition of the grafting operation, ν∗ is the terminal node of the longest rooted path ν∗

such that I(νi) = Ii for all 1 ≤ i ≤ | ν∗|. Now suppose that there exists another child ν′

of ν∗ for which I(νnew 1 ) = I(ν′). Then it is easy to see that ν′ must be in T . In this case, 

ν′ = ν∗ ◦ ν′ would be a rooted path in T for which I(ν′

> i

) = Ii for all 1 ≤ i ≤ | ν′|, where ◦

denotes the composition of paths. But ν′ is longer than ν∗ by construction, and ν∗ is not 44 the terminal node of ν′. This contradicts the definition of ν∗, and thus we can conclude that ν∗ cannot have any other children ν′ with I(ν′) = I(νnew 1 ). Thus, Graft 

> I

(T ) satisfies constraint 3, and is a valid distributary tree for θ, as claimed. With these lemmas, we can quickly prove that Algorithm 2 produces valid distributary trees 

Proof. By Lemma 6, the Ii in Algorithm 2 are all separating chains, and Lemma 7 therefore tells us that Tree (I1) is a separating tree. Finally, Lemma 8 implies that, for j = 2 , . . . l ,

Graft 

> Ij

(T ) is a separating tree as well, completing the proof. 

## 6.5 Proof of Lemma 2 

Proof. Branching preserves separating distributary tree constraints. Because all nodes in T are nonempty, branching cannot modify the root node. Thus, if T is rooted at θ,

Branch  

> ν⊃I′

(T ) will also be rooted at θ. To check the separating paths condition (29) , we note that Branch  

> ν⊃I′

(T ) adds only a single node ν′ to T , and thus we need only check this condition for paths terminating in ν′. Let ν = ( ν1, . . . , ν k) be a path with νk = ν′. If j = k, then 

I(ν≥j ) \ I(νj ) = ∅,

and condition (29) is trivially satisfied. Now define the path ν′ = (ν1, . . . , ν k−1). If 

1 ≤ j ≤ k − 1, then I (ν≥j ) = I(ν′≥j ) since, by definition, I(νk) ⊆ I(νk−1). Clearly, we also have I(ν≤j ) = I(ν′≤j ), so ν satisfies (29) for 1 ≤ j ≤ k − 1 if and only if ν′ does. But ν′ is a path in T , so ν′ must satisfy (29) . Finally, constraint 3 is satisfied by the definition of 

Branch  

> ν⊃I′

(T ).

Subdividing preserves separating distributary tree constraints. As with branching, constraints 1 and 3 are trivially satisfied by the definition of subdivision. In particular, subdivision cannot modify the root node of a tree, and does not add a node if the parent νp

45 already has a child ν′ with I(ν′) = Is (where Is is the subdividing set Is = I′ 

> p

∪ I(νc) for some I′ 

> p

⊆ I(νp)). Thus, we just need to verify that the separating paths condition (29) is preserved. Let νs be the child of νp in Divide 

> νp⊃I′
> p→νc

(T ) with Is = I′ 

> p

∪ I(νc). (This child node is unique since we know the subdivided tree still satisfies constraint 3 in Definition 1.) It is easy to verify that any maximal path in Divide 

> νp⊃I′
> p→νc

(T ) not containing νs and νc is also a path in T ,so it suffices to verify (29) for maximal paths in Divide 

> νp⊃I′
> p→νc

(T ) containing νs and νc. For any such path ν = (ν1, . . . , ν k) of length k, let i∗ be the index such that νi∗ = νs. Furthermore, define ν′ = (ν1, . . . , ν i∗−1, ν i∗+1 , . . . ν k). Then ν′ is a path in T , and thus ν′ satisfies (29) for all 1 ≤ j ≤ k. Our analysis is now divided into two cases. First, suppose j̸ = i∗. If j < i ∗, νj = ν′ 

> j

, I(ν≤j ) = I(ν′≤j ), and I(ν≥j ) = I(ν′≥j ) ∪ I(νs).But if j < i ∗, then I(ν′≥j ) ⊃ I(νp) ⊃ I′ 

> p

and I(ν′≥j ) ⊃ I(νc), so I(ν′≥j ) ⊃ I(νs). We thus conclude that I(ν≥j ) = I(ν′≥j ) ∪ I(νs) = I(ν′≥j ). By analogous reasoning, we can show that if j > i ∗, then νj = ν′

> j−1

, I(ν≥j ) = I(ν′≥j−1), and I(ν≤j ) = I(ν′≤j−1) ∪ I(νs) = I(ν′≤j−1).Since condition (29) holds for ν′ for all 1 ≤ j ≤ k − 1, it therefore also holds for ν for all 

j̸ = i∗.Next, consider j = i∗. In this case, we find that I(ν≤j ) = I(ν′≤j ), I(ν≥j ) = I(ν′≥j ) ∪ I′

> p

,and I(νj ) = I(ν′ 

> j

) ∪ I′

> p

. Thus, we have I(ν≤j ) \ I(νj ) ⊆ I(ν′≤j ) \ I(ν′ 

> j

) and I(ν≥j ) \ I(νj ) ⊆

I(ν′≥j ) \ I(ν′ 

> j

). It follows from this that 

CM −I(νj ) (I(ν≤j ) \ I(νj )) ⊆ CM −I(ν′ 

> j)

(

I(ν′≤j ) \ I(ν′ 

> j

))

. (31) 46 Putting this together, we find that 

CM −I(νj ) (I(ν≤j ) \ I(νj )) ∩(I(ν≥j ) \ I(νj )) ⊆ CM −I(ν′ 

> j)
> (

I(ν′≤j ) \ I(ν′ 

> j

))

∩(

I(ν′≥j ) \ I(ν′ 

> j

))

= ∅,

(32) so we conclude that ν satisfies (29) for all 1 ≤ j ≤ k, as required. 

Merging preserves separating distributary tree constraints. Because merging is defined as a sequence of grafting and branching operations, it follows from the above and Lemma 8 that merging preserves all distributary tree constraints. 

## 6.6 Uncertainty Index Estimation 

6.6.1 Random Forest Regressor 

We use a random forest regressor as our conditional expectation estimator ˆEI (SE , ψ I )

throughout all examples. Specifically, we use the implementation of random forests in the ranger package (Wright & Ziegler 2017). For simplicity, and to demonstrate that the usefulness of our estimator ˆU 2 does not depend on careful tuning of the underlying conditional expectation estimator, we use all of the default settings in ranger . These defaults are as follows. 1. Number of trees: 500 2. Fraction of variables considered at each split: 1/3 3. Bootstrap method: full data set sampled with replacement 4. Splitting criterion: node impurity (measured by variance) We refer the reader to Wright & Ziegler (2017) for full details on its implementation of random forests. 47 6.6.2 Asymptotic Unbiasedness of Conditional Variance Estimator 

In this section, all expectations, variances, and distributions are implicitly conditional on 

y. We suppress the explicit dependence on y for notational clarity. Recall that for I ⊆ I∗

and ψ′ ∈ RD, we use ˆEI (S, ψ ′ 

> I

) to denote an estimator of the conditional expectation 

E [θ | ψI = ψ′ 

> I

] based on a sample S = {ψ(s)}|S| 

> s=1

, where θ = ψiθ . Given such an estimator, we then construct the data-splitting expected conditional variance estimator cvar I, ˆE (S) as follows. First, we partition the sample as S = SE ∪ S V , where |S E | = |S V | and SE ∩ S V = ∅

(assuming |S| even). Then our estimator is defined as 

cvar I, ˆE (S) = 2

|S| 

> ∑
> ψ(s)∈S V
> (

θ(s) − ˆEI

> (

SE , ψ (s)

> I
> )) 2

. (33) We now establish a sufficient condition for the estimator (33) to be asymptotically unbiased. 

Lemma 9. Let S be an i.i.d. sample from p(ψ). Suppose that, for I ⊆ I∗, the average mean squared error (MSE) of our conditional expectation estimator ˆEI (SE , ψ I ) vanishes in the sense that 

E 

> [(

ˆEI (SE , ψ I ) − E [θ | ψI ])2]

= E

> [

E 

> [(

ˆEI (SE , ψ I ) − E [θ | ψI ])2 ∣∣∣∣ ψI

> ]]

→ 0 as |S| → ∞ ,

where the inner expectation above is the point-wise MSE of ˆEI (SE , ψ I ) and the outer expectation averages over inputs ψ, with ψ ⊥⊥ S . Then the data-splitting expected variance estimator (33) is asymptotically unbiased for the true expected variance E [Var ( θ | ψI )] .Proof. We take I ⊆ I∗ as fixed. We start by expanding each term in the conditional variance 48 estimator: 

(

θ(s) − ˆEI

(

SE , ψ (s)

> I

)) 2

= (

θ(s) − E [

θ | ψI = ψ(s)

> I

]) 2

︸ ︷︷ ︸

> A

+ ( ˆEI

(

SE , ψ (s)

> I

)

− E [

θ | ψI = ψ(s)

> I

]) 2

︸ ︷︷ ︸

> B

− 2 (

θ(s) − E [

θ | ψI = ψ(s)

> I

]) ( ˆEI

(

SE , ψ (s)

> I

)

− E [

θ | ψI = ψ(s)

> I

]) ︸ ︷︷ ︸

> C

.

The expected value of term A is exactly the estimand. Specifically: 

E [A] = E [

E [

A | ψI = ψ(s)

> I

]] 

= E

[

E

[(

θ(s) − E [

θ | ψI = ψ(s)

> I

]) 2 ∣∣∣∣ ψI = ψ(s)

> I

]] 

= E [Var ( θ | ψI )] .

Next, we observe that term C vanishes in expectation. Specifically, since SE is independent of ψ(s) ∈ S V , it follows that 

(

θ(s) − E [

θ | ψI = ψ(s)

> I

]) 

⊥⊥ ( ˆEI

(

SE , ψ (s)

> I

)

− E [

θ | ψI = ψ(s)

> I

]) ∣∣∣∣ ψ(s) 

> I

.

Therefore, iterating expectations and applying the above conditional independence, we can write the expected value of C as 

E [( 

θ(s) − E [

θ | ψI = ψ(s)

> I

]) ( ˆEI

(

SE , ψ (s)

> I

)

− E [

θ | ψI = ψ(s)

> I

])] 

= E

E

[(

θ(s) − E [

θ | ψI = ψ(s)

> I

]) ∣∣∣∣ ψI = ψ(s)

> I

]︸ ︷︷ ︸

> C′

E

[( ˆEI

(

SE , ψ (s)

> I

)

− E [

θ | ψI = ψ(s)

> I

]) ∣∣∣∣ ψI = ψ(s)

> I

] .

Now the factor C′ is just 

C′ = E [

θ | ψI = ψ(s)

> I

]

− E [

θ | ψI = ψ(s)

> I

]

= 0 ,

49 so in fact E [C] = 0 .Finally, E [B] is exactly the average mean squared error of the estimator ˆEI

> (

SE , ψ (s)

> I
> )

over inputs ψ(s). If this average MSE vanishes as |S| → ∞ , then the above shows that 

E [

cvar I, ˆE (S)]

= 2

|S| 

> ∑
> ψ(s)∈S V

E

> [(

θ(s) − ˆEI

> (

SE , ψ (s)

> I
> )) 2]

= E [Var ( θ | ψI )] + E [B]

→ E [Var ( θ | ψI )] ,

as claimed. In practice, most practical estimators ˆE will not have asymptotically vanishing average MSE, and Lemma 9 will not apply exactly. Instead, any residual bias in the estimator ˆE

will bias our estimate of the expected variance E [Var( θ | ψI )] , and of the uncertainty index, upwards. In this sense, the estimator (33) is conservative: It may yield “false negatives” by underestimating the uncertainty reduction associated with a set ψI , but it is unlikely to yield “false positives”. 

## 6.7 Causal Latent Factor Model Details 

This section provides all details of the model specification for the latent factor model used in the example of Section 3, as well as a description of how the posterior predictive data were generated for a new unit and at new time points. 50 6.7.1 Model Specification 

The latent factor model in Section 3 is a model of panel data y ∈ RN ×T . Recall that the factor model decomposes this data as 

y = λϕ + δ + ϵ, (34) where ϕ and λ are the latent factors and corresponding loadings, δ is the matrix of treatment effects (zero for all untreated units), and ϵ the matrix of independent, idiosyncratic normal errors. We now give detailed structural and distributional assumptions for these parameters. Most importantly, the factor loadings λ ∈ RN ×K are assumed to be lower triangular with nonnegative diagonal. This assumption is made to avoid nonidentification of the parameters 

λ and ϕ, which otherwise occurs due to the fact that y depends on these parameters only through their product λϕ (Bai & Wang 2015). Critically, this identifying assumption does 

not constrain the product λϕ .We now state our parametric and distributional assumptions. For each unit 1 ≤ i ≤ N ,the factor loadings λi· control both the scale of yi· (through the norm ∥λi·∥), and the relative importance of each latent factor to yi· (through the relative square magnitudes of the components λ2 

> ij

/∥λi·∥2). We parametrize λ to allow specifying priors for these two components separately. Specifically, we decompose λ as 

λ = SR ,

where: 

• S ∈ RN ×N is a diagonal matrix with Sii 

> def

= σ′ 

> i

> 0 controlling the scale of unit yi·.

• R ∈ RN ×K is the Cholesky factor of a correlation matrix, i.e. a lower diagonal matrix 51 with positive diagonal elements (i.e. Rkk > 0 for 1 ≤ k ≤ K) and unit vector rows (i.e. ∥Ri·∥ = 1 for 1 ≤ i ≤ N ). Note that the squared components of each row sum to 

1: ∑Kk=1 [Rik ]2. Thus, these squared components express the (standardized) unit yi· as a weighted average of the latent factors. For the scale parameters σ′

> i

, we assign priors 

σ′

> iiid

∼ InverseGamma (11 , 10 s) ,

where s is a fixed scale hyperparameter which we set to the mean standard deviation of the units yi. This prior has mean s and standard deviation s/ 3.Subject to the lower triangular and positive diagonal constraints, we take each row of the matrix R to have a uniform prior. Since the rows must be unit vectors, each row is constrained to a compact space, and thus a uniform distribution is a proper prior. The K latent factors are then assigned independent AR(1) process priors with autocorrela-tions ρk and stationary variances fixed to be 1:

ϕk1 ∼ normal 

> (

0,

> √

1 − ρ2

> k
> )

for 1 ≤ k ≤ K, ϕkt ∼ normal 

> (

ρkϕk(t−1) ,

> √

1 − ρ2

> k
> )

for 1 ≤ k ≤ K and 2 ≤ t ≤ Tρkiid 

∼ uniform(0 , 1) .

Finally, the nonzero treatment effects δt and idiosyncratic errors ϵ receive centered normal priors: 

δt ∼ normal (0 , σ δ) for 1 ≤ t ≤ T − tδ

ϵit ∼ normal (0 , f × σ′

> i

) for 1 ≤ i ≤ N and 1 ≤ t ≤ T, 

52 where σ′ 

> i

is the ith diagonal element of the scale matrix S in our decomposition of λ, and f

is a fixed scaling hyperparameter. This parameterization implies that σ′ 

> i

control the overall 

scale of yi·(0) (combining the latent and idiosyncratic components), and f controls the relative size of the idiosyncratic component. We set f to 0.025 to express the prior belief that the idiosyncratic component should typically be no more than 5% of the scale of the latent component. Choosing a small f is beneficial for identification, since either the latent or idiosyncratic component can account for a majority of the variation in the data absent some constraint. 

6.7.2 Posterior Predictive Sampling 

In order to generate hypothetical data at earlier time points and for an additional unit, we proceed as follows. First, we sample each of the latent factors at T ∗ earlier time points. Because the AR(1) prior for the latent factors ϕ is stationary and Gaussian, it is time-reversible. Therefore, we can sample the latent factors at earlier times by simply iteratively applying the autoregressive equations in reverse. Specifically, let ˜ϕ ∈ RK×T ∗

represent the latent factors at the earlier times in reverse temporal order (i.e. with ˜ϕ·1 immediately preceding ϕ·1 in time). Then, for each 1 ≤ k ≤ K, we simulate ˜ϕk· as: 

> ˜

ϕk1 = ρkϕk1 + ξk1, (35) 

> ˜

ϕkt = ρk ˜ϕk(t−1) + ξkt for 2 ≤ t ≤ T ∗, (36) 

ξkt iid 

∼ normal 

> 

0, 1

> √

1 − ρ2 

> k
> 

, (37) 

(ϕk1, ρ k) ∼ p(ϕk1, ρ k | y). (38) Hypothetical data for the N observed units are then generated at these earlier times according to the latent factor model (34) . For each unit 1 ≤ i ≤ N and time 1 ≤ t ≤ T ∗,53 the hypothetical data (also in reverse chronological order) is given by: 

> ˜

yit = λi· ˜ϕ·t + ϵit , (39) 

ϵit ∼ normal (0 , f × σi) (40) 

(λi·, σ i) ∼ p(λi·, σ i | y). (41) Note that the treatment effect term is omitted since all times at which we generate hypothetical data are pre-treatment times. Finally, to generate posterior predictive samples for the new unit, we first sample new loadings from the prior distribution: 

> ˜

λnew = σ × u, σ = 1

N

> N∑
> i=1

σi

u ∼ uniform (

Sk−1)

,

i.e. we multiply the average unit scale by a uniformly distributed unit vector in RK ; Given these, the new unit is simulated at each time point 1 ≤ t ≤ T + T ∗ according to 

> ˜

yt = ˜λT 

> new ˜

ϕ·t + ϵ′ 

> t

for 1 ≤ t ≤ T ∗,

> ˜

yT ∗+t = ˜λT

> new

ϕ·t + ϵ′ 

> t

for 1 ≤ t ≤ T, ϵ′ 

> t

∼ normal (0 , f × σ) ,

> (

ϕ, ˜ϕ, σ )

∼ p (

ϕ, ˜ϕ, σ | y)

.

## 6.8 Polling Model Details 

For full details of the polling model analyzed in section 4, including discussion of modeling choices and parametrization, see Heidemanns et al. (2020). 54 6.9 Almost-Sparse Markov Graphs 

The examples presented in this paper all involve models with a Markov graph MF that is sufficiently sparse to have useful local structure, which can then be visualized using our proposed trees. Many commonly encountered models lack this pleasant property, which limits the usefulness of our visualization technique as defined in the main text. In this section, we describe this problem in greater detail and introduce a simple extension of our method which can be fruitfully applied to “almost” sparse graphs (to be adequately defined below). For our purposes, a Markov graph is sparse if every vertex (corresponding to a single, scalar unknown quantity ψi) is connected to only a few other vertices. In a non-sparse graph, there will be at least one vertex that is connected to a large fraction of the other vertices – potentially all other vertices. The existence of such highly-connected vertices tends to shorten the shortest paths between any two vertices. It follows from our minimal separator construction 26 that the length of a branch in a distributary tree generated by Algorithm 2 is upper bounded by the length of any shortest path connecting the root vertex to a vertex in the leaf node. Consequently, when applied to non-sparse Markov graphs, Algorithm 2 will tend to generate shallow trees with fewer nodes, limiting our ability to discern relationships among the quantity of interest and other unknown quantities. In some cases, this problem is unavoidable. However, in many cases, a Markov graph may be “almost” sparse in the sense that a majority of the vertices have few neighbors, while a few vertices have many neighbors. We refer to the former as local vertices and the latter as global vertices. In this case, the global vertices may block our ability to discern useful local connections among the local vertices. A natural solution in cases like this is to condition away the global vertices. 55 Specifically, suppose that we have divided all unknowns into local parameters ψL = ψIL

and global parameters ψG = ψIG , with 

IL ∪ IG = I∗, I L ∩ IG = ∅, and |IG| ≪ | I∗|.

Then our posterior distribution is p(ψL, ψ G) (suppressing dependence on the data for notational simplicity). If p(ψL, ψ G) factorizes over F, then it is easy to see that p(ψL | ψG)

factorizes over 

F′ =

> {

f ∩ IL 

> ∣∣∣∣

f ∈ F 

> }

,

and that the corresponding Markov graph is MF′ = MF − IG (i.e. the graph obtained from 

M by deleting all vertices in IG). Consequently, if IG corresponds to the global vertices in MF , then p(ψL | ψG) will have a sparse Markov graph MF − IG, to which we can then fruitfully apply Algorithm 2. This strategy leads to a simple extension of Algorithm 2. Let PostTree (

M, θ, { ˇψi}li=1 , γ )

denote the procedure defined in Algorithm 2, which outputs a distributary tree T . Let 

νint (T ) denote the internal nodes of T , i.e. all non-root and non-leaf nodes. Algorithm 3 gives the modified procedure. In words, we simply apply Algorithm 2 to M − IG, and then modify the resulting distributary tree by inserting the global indices IG at each internal node in νint (T ). It is easy to see that the resulting tree T is still a separating tree with respect to the original Markov graph M .Algorithm 3 provides a theoretical solution to the problem of finding useful distributary trees in non-sparse models, but two practical problems remain. The first is the problem of how to specify the global parameters ψG. As the statement of Algorithm 3 indicates, this is entirely user specifiable. While the concept of a global parameter was not given a precise definition, in most cases, we expect there to be a fairly clear delineation between the 56 Algorithm 3 Distributary Tree Construction with Global Parameters 

Require: Undirected graph M , root quantity θ, leaf nodes {ˇνi}li=1 , threshold γ ∈ (0 , 1) ,global indices IG.

M ′ ← M − IG;

T ← PostTree (

M ′, θ, {ˇνi}li=1 , γ )

;

for ν ∈ νint (T ) do 

I(ν) ← I(ν) ∪ IG;

output T

parameters with “few” neighbors and the parameters with “many”. In such cases, simple heuristics can identify the candidate global parameters automatically from the graph M .The second problem arises when IG is large. For any internal node ν in T , we will have 

IG ⊂ I(ν), so the size of IG lower bounds the size of the node index sets I(ν). In this case, the size of IG can make the difference between ˆUI(ν) being tractable and intractable to compute. By constructing minimal separating sets at each internal node, Algorithm 2 was designed to suppress the size of the node index sets I(ν). But by injecting IG in each internal node, Algorithm 3 lacks this virtue, and may thus reasonably be seen as only a partial solution to the problem. When IG is large, another possible solution is to re-run the original posterior inference algorithm (e.g. Markov chain Monte Carlo, as used in our examples) conditioning on a particular value of ψG. In this case, we obtain samples directly from p(ψL | ψG), and can thus drop IG from the computation of the ˆUI(ν). The result, however, is only an approximation to the output of Algorithm 3 since, in this case, we only condition on a single value of ψG (rather than taking an expectation over the posterior). 57